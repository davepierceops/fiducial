# Review: specs/directive-tooling.md — cycle 18

Verdict: changes-required
Reviewed: specs/directive-tooling.md @ de0cc68360db29fa727ea98333e1dc34cf09fce7 (blob 26a7c26a)
Reviewer: Spec Reviewer Agent (execution session, **not independent** — this session authored the cycle-18 revision under the same directive and switched roles to gate it; the cycle-16 gate was the third independent one, and a confirmation-scoped independent gate follows this artifact)
Date: 2026-08-24
Scope: Full gate review of the PRD at the commit above — all eight required PRD sections present and substantively answered against `specs/prd-template.md`; internal consistency across sections; traceability of every claim to a parent artifact or to a marked open question; NFR dimension coverage; AC testability; risk tolerance stated explicitly; open questions naming what resolves them. Plus, specifically: whether **B1's generator invariant is stated at a level the document can hold to in both modes**; whether **N1's re-anchoring on `7853525a` makes §7's and AC-DT-13's every-element-governed claims true as written**, and whether anything else moved when the count became governed; whether **N2's single-source property is stated as a constraint on Q9's resolution rather than as a mechanism**; whether **N3's fenced-only fixture is stated at the shape level**; and whether the cycle-18 **descope** is applied consistently — a routed mechanism gated as a deferral, an unrouted one as a defect. Plus the Depth 1 continuity scan that fires on every spec revision. Every governed quotation introduced or touched this cycle was matched against its source by running, and every corpus figure the document states "at this revision" was recomputed.
Cross-checked: `skills/directive-authoring.md` @ `7853525a` and at `origin/main`; `reviews/expedited-log.md` at `origin/main`; `reviews/directive-tooling-cycle-17.md` @ `1e818feb`; `docs/cycles/directive-tooling-spec-18-20260824T042409Z.md` @ `5ffe9ed6`; `docs/cycles/directive-tooling-spec-17-20260824T033019Z.md` @ `7010e8ff`; `docs/packages/package-a-spec.md` §3.6 @ `768bbe3b` and at HEAD; `skills/spec-review-cycle.md`; `bin/cycle-open`; `decisions/log.md` DEC-000160; `specs/prd-template.md`
Not inspected: no TRD and no derived acceptance-criteria artifact exist, so the Depth 1 spine scan ran against the PRD alone — whether a TRD answers every PRD requirement, and whether ACs trace to the Top K journeys from the TRD side, could not be checked. Neither `bin/directive` nor `bin/check-directive` exists, so no criterion in §6 was executed against an implementation; every verdict about generator or lint behaviour here is a reading of the criteria, not an observation of a binary. AC-CO-5 through AC-CO-12 were read only for whether they constrain the emitted skeleton's **content** — B1 below rests on AC-CO-3, which does; the others were not re-derived. `reviews/directive-tooling-cycle-17.md`'s O1–O5 were re-derived only where this cycle's changes could have disturbed them.
Findings: 1 blocking, 2 non-blocking, 6 observations
Prior cycle: reviews/directive-tooling-cycle-17.md
Dave should inspect: (1) **B1 below** — the new invariant is correct and it makes a **pre-existing** conflict unignorable: the cycle mode preserves AC-CO-3's skeleton structure, which has no working-tree disposition region at all, so a cycle-mode skeleton carries **zero** labelled disposition statements and fails M3 at the executor's first act. Three commitments, at most two can hold, and §4's own conflict rule reserves the resolution to a dictated disposition rather than to a reviewer. (2) **N1 below**: the fifth amendment closed the count exposure and, in closing it, opened a **new false negative** — the governed rule now says "exactly one per directive" flat while M3 counts only unfenced statements, and §7's residual set says in as many words that it did not change this cycle. That claim is wrong, and §7 is the section you sign.

## B1 — blocking
Claim: G3's new generated-skeleton invariant binds the generator "in either mode", but the **cycle mode** preserves AC-CO-3's skeleton structure, which carries no working-tree disposition region of any kind — so a conforming cycle-mode skeleton carries **zero** unfenced labelled disposition statements, violates the invariant, and fails M3 at the executor's first act.
Location: specs/directive-tooling.md:831–:845 (G3's generated-skeleton invariant, "in either mode"); :530 (§4, "the same skeleton structure (AC-CO-3)"); :476–:481 (§4, AC-CO-3 restated); :628–:641 (§4, Mode scoping, the conflict rule as widened in cycle 17); :2014–:2044 (AC-DT-03, which asserts the count and names no mode); §4 M3's row (M3 unconditional, "every directive"); :284–:292 (§2, DEC-000160's first act "to every directive class, reviewer-gated cycle directives included").
Evidence: **Verified by running**, and by reading at the cited revisions. `docs/packages/package-a-spec.md` @ `768bbe3b` `:392-397` states AC-CO-3 whole: "The skeleton matches the format in `skills/spec-review-cycle.md`: an `# Cycle <n> Directive — <title>` heading, a `Date:` line, a `Documents in scope:` list of `- <path> @ <full sha>` entries, a `## Decisions` section containing one commented placeholder decision entry with the `Finding: / Resolution: / Dictated wording:` fields, a `## Deferred / out of scope` section, and an `## Execution notes` section" (*observed*). `grep -n "disposition\|working-tree\|worktree" skills/spec-review-cycle.md` returns **nothing** (*observed*, by running), and `bin/cycle-open`'s `render_directive` (`bin/cycle-open:115-133`) emits exactly those six elements and no disposition region (*observed*, by reading the source). This document states at `:530` that the cycle mode satisfies the inherited contract unchanged, naming "the same skeleton structure (AC-CO-3)". G3 states the slot and, as of this revision, the count — and names **no mode**, which §4's own Mode scoping convention says makes it a claim about both (`:597-599`, *observed*).
Consequence: Concrete, and on a live journey. Three of this document's commitments cannot all hold: (a) the cycle mode preserves AC-CO-3's structure whole; (b) a generated skeleton carries exactly one unfenced labelled disposition statement, in either mode; (c) M3 is unconditional over every directive, and DEC-000160 puts `bin/check-directive` inside the first act for **every** directive class, reviewer-gated cycle directives included. Take (a) and (c): every cycle-mode directive fails M3 at the first act, which is `bin/directive`'s output failing `bin/check-directive` — the exact defect class the cycle-16 and cycle-17 gates each filed as blocking, now in the other mode. Take (b) and (c): the cycle-mode skeleton gains a region AC-CO-3's enumeration does not name, which is a change to a preserved criterion this document forbids itself from making by implication (`:643-647`, "no goal or criterion here narrows a preserved AC-CO criterion by implication"). The document does not say which, and an implementer can satisfy §6 either way — two conforming implementations differing in whether the reviewer-gated path works. Note what is **not** wrong: the invariant itself is right, and stating it "in either mode" is the accurate reading of M3's unconditionality plus DEC-000160. The invariant did not create the conflict; it made a latent one unignorable, which is what a sharpened claim is for. What is missing is the disposition.
Fix: **Not a reviewer's call**, and §4's Mode scoping rule says so in its own words: where a goal of this document and a preserved AC-CO criterion cannot both be satisfied, "neither silently wins", the conflict "is a defect in this document and is raised as a blocking finding at the next gate review", resolved "by a dictated disposition that states, in this document's text, which of the two moved and why" (`:620-628`, *observed*). Raising it is this entry. The admissible dispositions, stated so the choice is visible and none taken here: **(a)** scope G3's slot and its invariant to the **general mode**, and state at §7 the residual that cycle-mode directives are outside M3's generated-path guarantee — cheapest, and it concedes the reviewer-gated class to hand-authorship of the disposition; **(b)** dispose that the cycle-mode skeleton gains the slot, which is M8's cycle-5 shape applied to AC-CO-3 — a preserved criterion moves, on the record, and `docs/packages/package-a-spec.md` §3.6 is amended there rather than read differently here; **(c)** dispose that AC-CO-3's enumeration is a **floor** rather than an exhaustive structure, which is a reading of "matches the format" this document would have to state and which the phrase does not settle — the same undecided-phrase shape cycle 8's B2 found in "what the preserved contract can emit".
Related: N1.

## N1 — non-blocking
Claim: The fifth amendment closes the count's G6 exposure and, in the same move, creates a **false negative** §7 does not disclose — the governed rule now states "exactly one per directive" unqualified while M3 counts only **unfenced** statements — and §7 states in as many words that its residual set did not change this cycle.
Location: specs/directive-tooling.md:2695–:2706 (§7, "The set does not change in cycle 18"); :2688–:2692 (§7, "The false-negative member of the set is still residual one and still the only one"); :2585–:2589 (§7, "M3 leaves **two** residuals"); §4 M3's row (the count anchored on `7853525a`, the fence exclusion this document's); :1136–:1150 (§4, "The fence exclusion is still this document's"); :2173–:2184 (AC-DT-06 fixture (vi), which pins the shape as a **pass**).
Evidence: **Verified by reading**, at the reviewed commit, and **by running** for the governed text. `skills/directive-authoring.md` @ `7853525a` `:15-22` reads: "The disposition is stated as its own labelled statement, **exactly one per directive**, mechanically distinguishable from incidental mention of trees or commands elsewhere in the file" (*observed*, matched against the source by running). The governed sentence qualifies the count by **nothing** — not by fencing, not by markdown context. M3 counts labelled statements "outside fenced code blocks" (M3's row, *observed*). So a directive carrying one unfenced labelled disposition statement **and a second of its own inside a fence** violates the governed rule and exits **0** on M3, and AC-DT-06 fixture (vi) pins that exit as correct. Under the previous revision this could not arise as a shortfall: the count was **this document's own**, so the fence-scoped version simply *was* the requirement and there was no governed sentence to fall short of. The direction is a **false negative** — the lint passing a directive that is wrong by governed text — and §7's revised text asserts the opposite twice: that residual one "is still the only one" of that direction, and that the set "does not change in cycle 18 … in both members and both directions".
Consequence: Bounded but real, and it lands in the section Dave signs. §7 asks for acceptance of a set stated **in extension**, which is the discipline three cycles built after the cycle-14 gate found the set restated without its extension moving. At this revision that set is incomplete by one member, in the direction the section itself says costs the most — "a false stop costs one invocation and a false pass costs whatever the wrong tree costs, and the two are not interchangeable in an accepted-risk list" (`:2676-2679`, *observed*). The instance is contrived rather than common — it takes an author who states two of their own dispositions and fences one — but so was residual two's fenced-only shape, which this cycle fixtured. There is also a reading under which nothing is wrong: "exactly one per directive" means one disposition *of that directive's own*, a fenced carriage of someone else's is not one, and M3's fence exclusion is a mechanical approximation of that semantic. That reading is almost certainly the intended one, and it is **not stated anywhere in the document** — which is the defect: the document now cites a governed sentence for its count and applies a narrower count, with no sentence reconciling the two.
Fix: One paragraph, and the choice between two shapes is Dave's; neither is taken here. **(a)** State the reconciling reading at §4's "The count is exactly one" — that the governed count is over a directive's **own** disposition, and the fence exclusion is M3's mechanical approximation of that, which makes fixture (vi)'s exit 0 an application of the governed rule rather than a departure from it — and leave §7's set at two. **(b)** Leave the reading unstated and add the false negative to §7's residual set in extension, with its direction named, correcting both "still the only one" and "does not change in cycle 18". (a) is the stronger document if the reading is right; (b) is the honest one if it is not. What should not stand is the present state, where §7 asserts a set that the newly-governed sentence makes incomplete.
Related: B1.

## N2 — non-blocking
Claim: The invariant B1's disposition adds is asserted at three sites and checkable at one that cannot yet be run, so between this revision and the TRD the document's central new property has no executable statement — the same shape the cycle-17 gate's N2 raised about the label's single source, inherited by the invariant that replaced it.
Location: specs/directive-tooling.md:2014–:2044 (AC-DT-03's count clause and its verifiability); :831–:845 (G3's invariant); :383–:390 (J1); :414–:421 (J2); :2940–:2966 (§8 Q10); :2891–:2926 (§8 Q9).
Evidence: **Verified by reading**, at the reviewed commit. AC-DT-03 now asserts, over the whole emitted file, "that the number of unfenced labelled disposition statements is **one**", and states of that assertion that it "is **specified now** and becomes **executable** once the TRD fixes the label's lexical form (§8 Q9)" (*observed*). That is accurate — a count of labelled statements is not computable without the label — and it is the same footing AC-DT-06's M3 fixtures already sit on. But it means the property J1, J2, and §4's "The two generated cases" each state as the thing the generated path rests on is, at this revision, asserted three times and executable **zero** times. The cycle-17 gate filed exactly this shape against the single-source property; this cycle folded that into Q9 as a constraint on the resolution (N2's disposition, executed at `:2907-2926`, *observed*) and added a new property with the same scheduling.
Consequence: Modest, and it is a property of the descope rather than a mistake in executing it. Nothing is claimed falsely: the invariant follows, the criterion states it, and Q9 and Q10 name what makes it executable. What Dave should see is that the count of PRD-level properties whose only check waits on one TRD decision is now **three** — M3's fixture set, the single-source property, and the skeleton invariant — all gated on Q9's lexical form. A TRD stage that resolves Q9 discharges all three at once; one that defers it again leaves §6 with a growing set of criteria an implementer must read as amber rather than red, on AC-DT-16's model. That is a scheduling risk in the release gate, not a defect in the text.
Fix: None required, and none proposed as a change to the criteria — the deferral is what the descope directs and the questions name their resolver. **Clearly labelled as a proposal**: if the amber set grows again, one sentence at §6's preamble naming which criteria are gated on Q9, in one place, would make the release gate readable without tracing each criterion to its question. Not taken here, and not a finding against the text as written.

## O1 — observation
Claim: Every corpus figure the document states "at this revision" reproduces at the reviewed commit, and the interval's rise of one is correctly attributed.
Location: specs/directive-tooling.md §1; §4, "The corpus recount against the three-pattern check"; §5, third outcome.
Evidence: **Verified by running**, at `de0cc683`. `git ls-tree --name-only HEAD:docs/cycles` returns 111 entries, **110** `.md` and one non-`.md` (`comfy-archive-and-generalize-20260822T195900-check.txt`, excluded as §4 states). Classifying the 110 by §4's three patterns, first match wins, yields **74 / 7 / 26 / 3**, and the three matching none are exactly the files §4 names — `metadata-scope-fix-20260823.md`, `doc-review-2026-08-02-questions.md`, `friction-refactor-2026-08-09-decisions.md`. The one file added since `6d29a4ab` is `directive-tooling-spec-18-20260824T042409Z.md`, pattern 1, which is the +1 §4 claims; the rise is one rather than two because cycle 17's review was a **self-gate** and landed no directive of its own, and §4 now states that explicitly. 110 − 91 = **19**, matching §1's "grown by nineteen files". `git ls-tree` returns blobs only, so the single-component boundary stays inert over the corpus as §4 claims.
Consequence: None adverse. §5's "1 non-conforming directive file of 110" holds with its stated derivation.

## O2 — observation
Claim: Both landing gates were run at the reviewed revision; the frontmatter gate is clean and the test suite carries only the accepted pre-existing failure pair.
Location: `bin/check-frontmatter`; `bin/tests/run`; specs/directive-tooling.md §6 AC-DT-15.
Evidence: **Verified by running**, in the assigned worktree. `bin/check-frontmatter --all` exits **0**, reporting 53 in-scope files from 12 configured globs. `bin/tests/run` exits **1**: **399 tests, 2 failures**, both `tests.test_bundle.TestAgainstThisRepository` AC-BN-10 cases, each failing with `ERROR: an ENTRY is no longer accepted; use --audience VALUE or --list` — the pair the cycle-18 directive names as pre-existing and accepted. `bin/tests/test_cycle_open.py`'s 31 cases are inside the green 397.
Consequence: None adverse, and no claim about the migration is available: neither `bin/directive` nor `bin/check-directive` exists, so AC-DT-15's actual assertion — that the suite passes **after** the migration — remains untestable and is not tested here.

## O3 — observation
Claim: Every governed quotation this cycle introduced or touched matches its source at the revision cited, including the fifth amendment's sentence and the carry-as-pointer rule the fence exclusion's ground still rests on.
Location: specs/directive-tooling.md §1, the fifth-amendment record; §4, M3's row (Derived-from); §4, "The count is exactly one"; §8 Q4.
Evidence: **Verified by running.** `skills/directive-authoring.md` @ `7853525a` carries the working-tree rule exactly as M3's Derived-from cell quotes it whole and as §1 and §4 quote its labelled-statement clause. `git diff b4a0fa58 7853525a -- skills/directive-authoring.md` shows the amendment inserting exactly "exactly one per directive," and touching nothing else in the body, which is what §1 claims (*observed*). `git diff 7853525a origin/main` on that path shows only the two frontmatter lifecycle fields, so `7853525a` is the content commit as §1 states. `reviews/expedited-log.md` at `origin/main` carries the entry "2026-08-23 — skills/directive-authoring.md @ 7853525aedf831bcc07da3264c3af7a91825b048 — exactly one labelled disposition statement per directive" verbatim; `git merge-base --is-ancestor 7853525a b5fc675c` succeeds, so the merge commit §1 names does carry it; `d4a3158b` is the last commit touching that path on `origin/main`, as §1's citation note now states. The carry-as-pointer rule at `origin/main` `:29-32` is unchanged by the fifth amendment and §4 still quotes it **whole** (*observed*).
Consequence: None adverse. No citation introduced this cycle points at text that is not there or truncates a governed sentence silently, and the five content-commit citations M2's narrowing note enumerates all resolve to commits touching the path.

## O4 — observation
Claim: The cycle-18 dispositions are each executed at the sites they reach, the descope has a stated home, and the four-stood-one-stands disclosure is removed at both sites rather than softened at either.
Location: `docs/cycles/directive-tooling-spec-18-20260824T042409Z.md` @ `5ffe9ed6`; specs/directive-tooling.md as revised.
Evidence: **Verified by reading, and by running for the site sweep.** Descope: §4 gains "Scope split with the TRD" (`:671-696`), naming the three routed classes, the property-plus-route test, and — after the fix O6 records — what it does not claim to enumerate. B1: G3 gains the generated-skeleton invariant, the passes-by-construction clause naming what it rests on, and the TRD routing (`:831-863`); the prompt bullet's categorical claim is restated so nothing rests on it (`:771-780`); J1, J2, §4's "The two generated cases", and §4's "What is retired" each name the invariant; AC-DT-03 binds it and asserts the count (`:2014-2044`); §8 Q10 added. **No PRD text stating the mechanism was found to remove.** At the previous revision `grep -n "prompt" specs/directive-tooling.md | grep -i "fence\|bound"` returns exactly one line — `:920`, "prompt and exactly one unfenced labelled statement whose content is one admitted" — which is §4's statement of the *filled* case's verdict, not a mechanism bounding what the prompt may contain (*observed*, by running). The cycle-17 gate's B1 rests on the **absence** of such a mechanism, so the disposition's "remove any PRD text that states the mechanism" had no referent; none was invented and none was removed. N1: M3's row re-anchored on `7853525a` with its Decision-qualification cell restored to "none"; §4's count section rewritten; §7's "Not accepted" restored to "all four are closed"; AC-DT-13 restored to "Every element M1–M8 satisfies this as written", with its outrun-tally moved to four instances / three amendments / one deletion; §1 gains the fifth-amendment record and G6, Q4, and M2's lastness note carry the new citation. N2: Q9 gains the single-source property as a constraint on the resolution rather than as a mechanism. N3: AC-DT-06 gains fixture **(vii)**, stated as a shape with no literal label text, and §7's second shape now cites it. `grep -n "one stands\|for every requirement it carries but one\|The one exception at this revision"` returns matches only where the text is explicitly historical (*observed*, by running).
Consequence: None adverse for execution. B1 and N1 above are **not** failures to execute a disposition: B1 is a pre-existing conflict the invariant makes visible, and N1 is a consequence of the amendment the disposition anchors on rather than of the anchoring.

## O5 — observation
Claim: No TRD and no derived acceptance-criteria artifact exists, so the Depth 1 spine scan ran against one third of the spine; the PRD's own structural requirements are met.
Location: specs/ at the reviewed commit.
Evidence: **Verified by running** — `specs/` holds `bin-land.md`, `directive-tooling.md`, `prd-template.md`, `trd-template.md`. All eight of `specs/prd-template.md`'s required sections are present as `## ` headings in order and each is substantively answered; **nineteen** AC-DT criteria are defined, AC-DT-01 through AC-DT-19 with no gap; all nine NFR dimensions are addressed, Scalability and Compliance marked N/A with stated reasons; §8 now states **ten** questions, Q1 through Q10, of which three are closed and seven name a resolver in a "Resolved by:" line (*observed*).
Consequence: The scan checked internal consistency and upward traceability to governed parents, which is where all three findings came from, but cannot check that a TRD answers every PRD requirement or that ACs trace to the Top K journeys. Unchanged from cycles 7 through 17. The TRD-stage queue is now explicit and has grown by one: Q9 (the label's lexical form and its single source) and Q10 (the mechanism securing G3's invariant), with Q10 named as resolvable only together with Q1.

## O6 — observation
Claim: Two defects in text this cycle introduced were found during the gate and corrected **before** the reviewed commit, and they are recorded here rather than left to a diff.
Location: specs/directive-tooling.md:671–:696 (§4, "Scope split with the TRD"); :2940–:2966 (§8 Q10).
Evidence: **Verified by reading**, both in the draft and at the reviewed commit. First, the scope-split section's opening said the three routed classes are "each … routed to the TRD stage by a **named question in §8**" and then routed the third at M3's row, contradicting itself in the same sentence; and its review test said a routed mechanism carries "a §8 question", which is false of G11's marker syntax, routed at its own site. Both were corrected before `de0cc683`: the section now says a route is "either a §8 question or a routing stated at the site", and a paragraph states that the section does not enumerate every TRD-stage decision and names the class it governs. Second, Q10's third candidate — the generator fencing sourced text as it emits it — would put fence markers in the emitted prompt region that the committed source does not carry, which contradicts AC-DT-03's assertion that the prompt region's text **matches** that source; Q10 now states that cost as a constraint on the answer rather than leaving a TRD to discover it. This is the same handling the cycle-17 gate's O3 recorded for a truncated governed quotation.
Consequence: None adverse at the reviewed commit. It is recorded because a self-gate that silently repairs its own drafting and then reports a clean sweep is indistinguishable from one that did not look, and the confirmation-scoped gate that follows should be able to tell the two apart.
