# Review: branch rule-divergence-rulings — cycle 2

Verdict: ready-with-findings
Reviewed: LEXICON.md, operating-model.md, context-sets/testing-and-verification.md, docs/global-context/core.md, docs/global-context/decision-layer.md, docs/global-context/review-rubric.md, policies/source-of-truth-policy.md, roles/coder-agent.md, roles/test-designer-agent.md, skills/test-plan-review.md @ 3e064f6
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-26
Scope: `git diff d1086a5..3e064f6 -- docs/global-context context-sets policies roles skills LEXICON.md operating-model.md CLAUDE.md` — eleven files: the ten governed documents cited above plus CLAUDE.md, an adapter outside the frontmatter in-scope set and so not a governed document, reviewed with them because the cycle-2 edits reach it. All twelve rubric criteria applied to the edited text; criterion 12 applied across the governed corpus for each rule the cycle-2 edits touched — the behavioural red-gate, the canonical/derived conflict's scope, and changed-fact propagation. Gating whether the edits carry out the decisions recorded in docs/cycles/cycle-2-directive-rule-divergence-rulings.md @ c778d6b, and whether they introduced anything new; not whether those decisions are right. All eleven cycle-1 findings are confirmed resolved as decided: B1 and B2 by Core rule 9's replacement sentence, which is the directive's verbatim text and drops both the whole-session stop and "waits for Dave"; B3 at operating-model.md:209; N1 at roles/coder-agent.md:21; N2 by the cut at policies/source-of-truth-policy.md, after which Core rule 13's session-kind split is stated in one place corpus-wide; N3 at roles/test-designer-agent.md:26 in the reviewer's wording; N4 at specs/directive-tooling.md:155, outside this gate; N5 at CLAUDE.md:31; N6 at core.md:32; O1 and O2 were observations and needed no act.
Cross-checked: docs/global-context/core.md rules 9, 11, 13 and 15 and its Vocabulary and precedence header, against policies/source-of-truth-policy.md "Conflicts are a hard stop" and "Keeping derived artifacts honest" and operating-model.md:38; a corpus-wide grep sweep over the frontmatter in-scope set plus CLAUDE.md and prose-criteria.md for every statement of the red-gate (operating-model.md:20, 137, 143, 209; context-sets/spec-and-change-discipline.md:24–35; roles/test-designer-agent.md:26; roles/coder-agent.md:21; skills/test-plan-review.md:37; engagements/sre/README.md:29; CLAUDE.md:31), for changed-fact propagation, and for the canonical/derived conflict; the release-impact-label enumeration in LEXICON.md:64–79 against policies/release-readiness-policy.md:14, policies/verification-boundary-policy.md:102–103 and context-sets/testing-and-verification.md:35; roles/chief-of-staff.md:85–90 for a competing staleness rule; the review-artifact schema; the Context Quality Reviewer role's scope and cession; the document metadata policy's revision lifecycle; `bin/check-frontmatter --all`, run, exit 0 over 54 in-scope files; `git diff --stat 21b60b3..3e064f6`, run, confirming every edited governed file traces to a cycle-1 finding and nothing else was touched.
Not inspected: the correctness of the cycle-2 decisions themselves and of rulings C001–C008 beneath them, both out of scope by directive; specs/directive-tooling.md, whose agreement is the Spec Reviewer's to gate and not this role's — its diff was inspected only to state whether it is confined to the quotation, and its `status: in-review` is left where the hook set it; the eleven documents' unedited text outside the passages the cycle-1 findings reach; the commit-by-commit history within d1086a5..3e064f6, the tree at 3e064f6 having been reviewed rather than each intermediate state; MANIFEST.md, OPEN-ITEMS.md, retros/, docs/history/, docs/rule-register/, docs/cycles/, and prior review artifacts, all outside the frontmatter in-scope set; whether roles/chief-of-staff.md:88's deliberately unsettled block-or-flag re-check is a criterion-11 defect, it being unedited text stating no rule the cycle-2 edits touched.
Findings: 1 non-blocking, 1 observation
Prior cycle: reviews/rule-divergence-rulings-cycle-1.md @ 1d67b6b
Dave should inspect: N1 — the N2 cut you ordered leaves a policy section whose only remaining sentence is rationale; whether the section keeps a heading at all is yours.

## N1 — non-blocking
Claim: The cut ordered for cycle-1 N2 leaves "Keeping derived artifacts honest" a heading over a single sentence that states a hazard and no act, which rubric criterion 6 inverts and criterion 5 forbids.
Location: policies/source-of-truth-policy.md:62–65
Evidence: Verified by reading at 3e064f6 and by grep sweep over the in-scope set. The section now reads in full: "When a canonical document changes, derived artifacts downstream of it may go stale." The instruction it previously carried is now stated only at docs/global-context/core.md:32, which the policy does not name. Core is `order: 0` with `audience: [all-roles, human]`, identical to the policy's audience, so no bundle carries the policy without Core — verified by reading both frontmatter blocks and by finding Core in the context-quality-reviewer bundle generated by `bin/bundle`, run.
Consequence: No agent loses the rule, because Core always loads alongside. What breaks is the section: a reader reaching a heading that promises how derived artifacts are kept honest is told only that they may go stale, and the act is nowhere in the file. The vacuum is exactly the shape of the sentence N2 removed, so the next editor of this policy fills it by restating Core rule 13 and re-creates the duplication this cycle cut.
Fix: Cut the heading and its sentence, letting Core carry the rule whole; or fold the sentence into the "Proactive drift detection" section below it, which already states an act.

## O1 — observation
Claim: The unexecutable wording cycle-1 B2 removed from Core survives in the source-of-truth policy's hard stop, which Core rule 9 now agrees with on scope but not on what follows.
Location: policies/source-of-truth-policy.md:58 against docs/global-context/core.md:25
Evidence: Verified by reading both at 3e064f6. Core rule 9 reads "A conflict between a canonical document and an artifact derived from it stops work on the conflicted item; surface it." The policy's numbered hard stop reads "1. Stop work on the conflicted item. 2. Surface the conflict to Dave explicitly in the current response... 3. Wait for Dave to resolve it." B1's divergence is gone — both are now item-scoped. Step 3 has no counterpart in Core, and an execution session, defined at core.md:39 as carrying out a directive against a working tree, has no channel to wait on.
Consequence: None as decided. The cycle-2 directive states "The source-of-truth policy's item-scoped statement is now the one Core agrees with; confirm and leave it," so this is the decision's residue, not the executor's error, and Core's precedence header — "a rule in a layer that conflicts with a rule here does not waive it" — together with rules 11 and 15 gives an execution session a defined act. Recorded because B2's argument, that "wait for Dave" is unexecutable in an execution session, applies unchanged to the one place it still appears.

## Prose

The specs/directive-tooling.md diff is confined to the quotation in its body — the single content hunk updates only the two lines quoting operating-model.md line 137, and the quoted string matches that line exactly at 3e064f6 — accompanied by the frontmatter `status: agreed` → `in-review` and `last-reviewed: null` flip that the document metadata policy requires of any edit to an agreed document.
