# Review: engagements/sre/speed-audit.md — cycle 4

Verdict: changes-required
Reviewed: engagements/sre/speed-audit.md @ b6102de
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-23
Scope: the whole file, all eleven criteria of the review rubric @ b6102de, judged against the current foundation. Confirmation pass within the Pass 1 re-gate confirmation cycle 3; the added questions are whether instruction 1 of the part-4 directive closed SA-5 under D10 restated, and what decision D13 — the Cartographer re-measures after a change, and the Implementer does not measure its own change — did to a file whose step 5 carries a re-measure clause with no subject.
Cross-checked: docs/global-context/core.md (Vocabulary — decision session, execution session; Acting 13), docs/global-context/decision-layer.md, LEXICON.md, operating-model.md, engagements/cartographer.md, engagements/assistant.md, engagements/critic.md, engagements/sre/README.md (Artifacts), engagements/sre/system-discovery.md, engagements/sre/baseline-measurement.md, engagements/sre/implementer.md, engagements/sre/engagement-change-package.md, engagements/working-with-dave.md — all @ b6102de; plus `git diff c8e95d1 9759ed3 -- engagements/sre/speed-audit.md`, docs/cycles/pass1-regate-fix-4-20260823T013000.md (D13, D10 restated, instructions 1 and 4), and reviews/sre-speed-audit-cycle-3.md. Bundle membership computed mechanically over every `audience:` value in the 51-file set at b6102de.
Not inspected: whether a one-week sizing is right; the six-step play's ordering as a matter of engagement practice; whether the Cartographer is the right role to re-measure, which D13 settled and this cycle does not re-open; whether the Assistant is the right runner for the play, which no prior cycle has re-opened.
Findings: 1 — 1 blocking
Prior cycle: reviews/sre-speed-audit-cycle-3.md
Dave should inspect: SA-6. D13 forbids the Implementer measuring its own change, and this file's step 5 still reads as instructing exactly that. It is the one place D13 did not land, and the Implementer reads both sentences in one bundle.

## SA-6 — blocking
Claim: Step 5's re-measure clause has no subject and sits third in a list whose first two clauses name the Implementer and the Critic, so it reads as the Implementer's act — which D13 prohibits and which engagements/sre/baseline-measurement.md, in the same bundle, states as a prohibition.
Location: engagements/sre/speed-audit.md:28-29 ("5. **Attack** — the Implementer builds; the Critic reviews where a review is requested; re-measure after each landing, not in batches."), against engagements/sre/baseline-measurement.md:9-12 ("step 6 runs in an execution session by the Cartographer, who re-measures with the baseline's instrument; the Implementer does not measure its own change").
Evidence: Verified by running. `git diff c8e95d1 9759ed3 -- engagements/sre/speed-audit.md` rewrites step 1 only, under instruction 1; the step 5 clause is untouched, and instruction 4 dictated the fix for baseline-measurement.md alone, so step 5 was never in the edit's path. Instruction 4's own closing sentence — "Conform step 6's body if it names a different runner" — reaches baseline-measurement.md's step 6, which names no runner, and does not reach this file. The three clauses of step 5 are joined by semicolons; the first two carry explicit subjects and the third carries none, so the nearest available subject is the Implementer. Bundle membership computed over `audience:` at b6102de: this file carries `audience: [assistant, cartographer, critic, implementer, human]` and engagements/sre/baseline-measurement.md carries `audience: [cartographer, implementer, assistant, human]`, so the `implementer` bundle (24 files) and the `cartographer` bundle (24 files) each hold both sentences at once. This file's own :9-10 — "The Cartographer, Implementer, and Critic read it for the steps assigned to them" — compounds it: the Cartographer reading for its assigned steps finds only step 1, and never learns here that step 5 obliges it. Criterion 11 ("boundaries two roles could both claim are defects") and criterion 1.
Consequence: The Implementer holds two documents in one bundle giving opposite instructions on the same act. One tells it to re-measure after each landing; the other tells it that it does not measure its own change. The concrete failure is the one D13 exists to prevent: an Implementer that re-measures its own change under step 5, producing the delta claim its own change package cites, with no independent measurement anywhere in the evidence chain — and the change package's `delta-verified` label then asserting an independence that did not happen. The Cartographer is affected symmetrically: the document describing the whole play never assigns it the step it now owns.
Fix: Give the clause its subject and match D13: "5. **Attack** — the Implementer builds; the Critic reviews where a review is requested; the Cartographer re-measures after each landing, not in batches." That also puts the Cartographer into a second step of the play, which is what :9-10 promises its readers.
Related: SRE-BM-5, SRE-BM-6, CN-16, BD-11
