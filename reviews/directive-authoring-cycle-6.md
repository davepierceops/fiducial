# Review: skills/directive-authoring.md — cycle 6

Verdict: ready
Reviewed: skills/directive-authoring.md @ 6a30ff19f2394642e4f387c518262486da6f8826 (blob `ab43abc6b435d7c7eea220bc44a591fb43bb5799`)
Reviewer: Context Quality Reviewer (execution session). This session authored no text under review, took no decision consolidated or amended here, and holds no prior position on either document. Its only writes are `docs/cycles/directive-authoring-consolidation-gate-3-20260831T194500Z.md`, this artifact, and `reviews/directive-invariants-cycle-7.md`.
Date: 2026-08-31
Scope: a confirmation gate, not a full pass, and the second one over this document. (1) Confirmation of cycle 5's F-1 against amendment d's E1, in O-1 below. (2) The three cross-checks named on the Cross-checked line, all re-run at the reviewed blob. (3) The text the amendment introduced, read as new, with anything failing a criterion raised as a finding citing it — there is none, and O-1 records what was tested for. No new decision is taken here and nothing else in this document is re-litigated: cycle 4's O-3 (the timestamp form fixes no timezone) and O-4 (the first bullet's trailing rationale and preference), both of which the decision record ruled no action on, persist unchanged at `:53`–`:55` and `:20`–`:21` and are recorded as persisting rather than re-raised, as cycle 5 recorded them. The amendment was reconstructed by diffing this file between cycle 5's reviewed ref `efaad5f6a4db5c6005b81f2af9f1bd53a1063872` and the reviewed ref (*observed*, by running): the diff touches this file in exactly the one passage E1 names — the second bullet, one clause added and the bullet reflowed at 79 characters — and nowhere else; `git diff --stat` over the same range reports three files, this one, the other document under review, and the amendment's own directive file. Frontmatter is untouched, `status: in-review`, `last-reviewed: null`, audience unchanged. `efaad5f` is also the merge-base of the reviewed ref with `7e055d7393d96deabd5c50c6ca01e52047e306e4`, the commit carrying cycle 5's artifacts (*observed*, by running `git merge-base`), so the amendment was made against exactly the tree cycle 5 reviewed.
Cross-checked: three named cross-checks, each with its result, all *observed* by running. **(1) The disposition-prompt fence against this document's first bullet — equal, at 669 bytes each.** The first fenced block of `skills/directive-invariants.md`'s `## Working-tree disposition prompt` section (body `:85`–`:93`) and this document's first bullet (`:13`–`:21`) were each flowed by the method the prior cycles used — whitespace runs collapsed with `re.sub(r"\s+", " ", s).strip()`, lines joined, the bullet's leading `- ` stripped — and compared: `fence bytes: 669`, `bullet bytes: 669`, `equal: True` from `f.encode() == b.encode()`. The expected 669 is met on both sides. Amendment d moved neither copy — E1's target is the second bullet, not the first — so the pair holds at the value cycle 5 established, and this is a verification that the edit stayed clear of it rather than a re-derivation. **(2) The shipped `unfenced_labelled_statements` over both documents — no hits in either**: `[]` over `skills/directive-authoring.md` and `[]` over `skills/directive-invariants.md`, the function imported from `bin/tests/test_directive.py` and called over each file's text with the shipped `DISPOSITION_LABEL` (`'WORKING-TREE DISPOSITION'`). This document's zero is vacuous, as in cycle 5: the bare token does not occur here at all — a scan of every line for the token returns nothing — which also discharges the amendment directive's constraint that the bare label token not appear in this file. The substantive zero is the other document's, and cycle 7's cross-check 2 records it line by line. **(3) Criterion 12 between the amended `## Stop conditions` sentence and the `## Base verification` sentence — they compose; no divergence to file.** The pair is in the other document, at `skills/directive-invariants.md:154-155` and `:110-114`, and cycle 7's cross-check 3 carries the full result; recorded here because the amendment is one unit across two files. The two sentences answer different questions over the same event and never license opposite actions: the Stop conditions sentence is a predicate on the trigger word its own region uses — "A remote operation that exits successfully is not a failed command, whatever a credential helper writes to stderr" — and the Base verification sentence selects which output carries information about the base — "Judge every remote operation — fetch, push, ls-remote — by the refs it reports, not by a credential helper's noise on stderr". Neither says exit status establishes the base, and neither says refs establish failure; their one overlapping claim, that credential-helper stderr is not evidence, they state the same way. On this document's side criterion 12 is unengaged by the amendment: nothing here states either rule, and a scan of `skills/`, `specs/`, `policies/`, `roles/`, `docs/global-context/` and `decisions/` for "failed command", "refs it reports" and "exits successfully" returns only the other document's two sentences (*observed*, by running). Also cross-checked: `skills/directive-invariants.md` @ `6a30ff1`, whole; `docs/global-context/review-rubric.md`; `skills/review-artifact.md`; `roles/context-quality-reviewer.md`; `specs/bin-land.md` G4 and G5 and `specs/bin-land-trd.md` §3.6 B2, both `status: agreed`, which cycle 7's cross-check 3 reads on the exit-status rule; `reviews/directive-authoring-cycle-5.md` and `reviews/directive-invariants-cycle-6.md`, both at `7e055d7393d96deabd5c50c6ca01e52047e306e4` via `git show`; the amendment record `docs/cycles/directive-authoring-consolidation-d-20260831T190000Z.md` @ `cd4135c2dd82a76688590b385d6a8f3c86a01b2c`; and, read and run, `bin/aimeta/mdmask.py` and the `unfenced_labelled_statements` check in `bin/tests/test_directive.py`.
Not inspected: the `bin/` test suite — not run by this directive, and reported as not run, so no claim is made here about whether any test passes; the check named above was imported and called over the two files, which is a claim about that function and not about the tests' status. `bin/directive` and `bin/check-directive` were not invoked and no skeleton was generated. This is a confirmation gate, so it is **not** a full twelve-criterion pass over the whole document: criteria 2, 5, 6, 7, 8, 9 and 10 were not re-examined over passages the amendment did not touch, and the Naming section, the route-line bullet and the ten bullets E1 did not reach were re-read only as far as the three cross-checks above and the structural constraints in O-1 reach. `specs/directive-tooling-trd.md` was not re-consulted this cycle: cycle 5 closed the §3.3 and §3.4 axes and amendment d moved neither, so no claim is made here about the TRD's current text. Also not inspected: `docs/global-context/core.md` and `docs/global-context/decision-layer.md`; `policies/document-metadata-policy.md`; the PRD `specs/directive-tooling.md` and every acceptance-criteria document it and the TRD name; `specs/bin-land.md` and `specs/bin-land-trd.md` other than the passages named above; `decisions/log.md`; `bin/cycle-open`, `bin/check-frontmatter`'s source, and every module under `bin/aimeta/` other than `mdmask.py`; the `docs/cycles/` corpus except the amendment record named above; whether any rule here is correct as engineering judgment, which this role cedes; and every governed file not named on the Cross-checked line.
Findings: none — 1 observation, and no blocking or non-blocking finding.
Prior cycle: reviews/directive-authoring-cycle-5.md

## O-1 — observation
Claim: Cycle 5's F-1 is resolved by amendment d's E1, in the form its Fix line dictated and in the by-title shape cycle 4's O-2 admitted, and the edit introduces nothing that fails a criterion.
Location: `skills/directive-authoring.md:22-23`, against `efaad5f6a4db5c6005b81f2af9f1bd53a1063872`
Evidence: *Observed*, verified by reading the one-file diff between cycle 5's reviewed ref and the reviewed ref and by running where a claim could be tested that way. **Cycle-5 F-1 (the term of art "eligible" imported into a file that neither defines it nor marks it as defined elsewhere) — resolved.** E1 took the first of F-1's two offered shapes, which the Fix named as the smaller and the one that puts the marker on the sentence that needs it, and landed its dictated wording: the bullet now reads "**The disposition label leads the disposition statement and no other eligible line, eligibility as the Directive Invariants document defines it.**" (`:22`–`:23`). The defect F-1 named is closed on its own terms. The term still occurs exactly once — a scan for "eligib" over this file returns `:22` and `:23` and nothing else — but it no longer arrives unmarked: the same sentence that binds the author now says on its face that a definition exists and which document holds it, which is the marker F-1 said was absent, and it is the gloss the other document already attaches every time it binds an editor with the same term (`skills/directive-invariants.md:25-26`). The pointer resolves to a real definition: `skills/directive-invariants.md:223-231` states eligibility — fenced blocks, blockquote lines and HTML comments mask, indented code blocks do not — and `bin/aimeta/mdmask.py:36-71` implements exactly those three masks and no fourth. Both files carry `audience: [chief-of-staff, human]` (*observed*, by reading both frontmatter blocks), so the dependency is co-bundled, which is the shape cycle 4's O-2 admitted and criterion 1 tolerates; criterion 3 is not engaged, because the reference is by title and nothing here is path-shaped. The by-title form also matches the file's own established way of naming that document: the preceding bullet reads "...are stated in the Directive Invariants document, which is their one definition" (`:19`–`:20`), so the file names the holder one way and not two. The failure F-1's Consequence described — an author reading "no other eligible line" as ordinary English, then avoiding a blockquoted disposition that `bin/aimeta/mdmask.py` masks and that the design names as the common case — no longer has a foothold, because the sentence is now self-marking: a reader who does not hold the definition is told there is one rather than left to read a term of art as plain English. What the edit does not do, and was not asked to do, is state the definition here; that remains a co-bundled dependency, which cycle 4's O-2 settled as admitted and this entry does not reopen. **Nothing new fails a criterion.** The amendment's own constraints were verified rather than assumed (*observed*, by running): no `##` section is added, removed, renamed or reordered — the heading list is identical to `efaad5f`'s; the file carries twelve top-level `- **` bullets before and after, in the same order, so no bullet moved; the first bullet is byte-untouched, which cross-check 1 above confirms from the fence's side at 669 and 669; frontmatter is unchanged, `status: in-review`, `last-reviewed: null`. The reflow holds the file's 79-character prose wrap: `:22` is 79 characters and `:23` is 70, counted as characters rather than bytes, and the single line in this file over 79 (`:30`, at 80) is byte-identical to `efaad5f`'s and lies outside the amended passage, so the amendment introduced no over-long line. Criterion 6 was considered against the added clause and is not failed: the gloss is a pointer to a definition, not a justification for the rule, and it is the kind of clause criterion 6's "trailing justifications are cut" does not reach.
Fix: None. The confirmation is clean and nothing on this side is left open.
