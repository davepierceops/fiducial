# Review: specs/directive-tooling.md — cycle 20

Verdict: ready-with-findings
Reviewed: specs/directive-tooling.md @ 255eb65b95705551b1463ddf6a05be275c4323f8 (blob 62a9d3b1)
Reviewer: Spec Reviewer Agent (execution session, **independent** — this session authored nothing under review: it did not write the cycle-19 revision, the cycle-19 fix revision, or any prior artifact in this series, and it creates exactly two files, its own directive and this artifact, modifying none. This is the **fourth** independent gate on this document; the cycle-8, cycle-13, and cycle-16 gates were the first three)
Date: 2026-08-24
Scope: **Confirmation-scoped**, not a fresh full-depth read — the scope is bound by `docs/cycles/directive-tooling-spec-gate4-20260824T105813Z.md` @ `bf42ade6` to exactly three confirmations, each run against the artifacts where they permit running. **(1)** That the cycle-18 gate's B1 is resolved as Dave ruled: the cycle mode emits the labelled disposition statement as an **extension** of the emitted structure, AC-CO-1 through AC-CO-12 untouched, G3's invariant uniform in both modes, the ruling recorded with its ground. **(2)** That the cycle-19 findings are resolved as ruled: N1 — M3's Derived-from quotation runs to the end of the governed bullet, matched against `skills/directive-authoring.md` at `origin/main` **by running**, and "quoted whole" true as written; N2 — the reading scoped to **carried** wording, the fenced-only shape restored to §7's residual set as an accepted false positive, and the two consequential-update sites the fix session disclosed (M3's contract and qualification cells; AC-DT-13's cycle-19 sentence) consistent with that scoping. **(3)** That no new contradiction stands between the document and the governed text it cites, over the text the cycle-19 and cycle-19-fix revisions touched — the citation sweep re-run **by running** over the quotations those two revisions introduced or changed, against the cited sources at the cited revisions. The corpus figures those revisions restated were recomputed. Nothing outside these three was re-derived.
Cross-checked: `skills/directive-authoring.md` at `origin/main` and @ `7853525a` (bodies diffed); `docs/packages/package-a-spec.md` §3.6 AC-CO-1 through AC-CO-11 and §8.2 AC-CO-12, each read whole @ `768bbe3b`, and that path diffed `768bbe3b`→`origin/main`; `skills/spec-review-cycle.md`; `bin/tests/test_cycle_open.py`; `bin/cycle-open`; `decisions/log.md` DEC-000180; `reviews/expedited-log.md` at `origin/main`; `reviews/directive-tooling-cycle-18.md` @ `c5090995`; `reviews/directive-tooling-cycle-19.md` @ `616e382b`; `docs/cycles/directive-tooling-spec-19-20260824T061718Z.md` @ `c05efadd`; `docs/cycles/directive-tooling-spec-19-fix-20260824T081022Z.md` @ `5ea201e6`; the seven `docs/cycles/cycle-<N>-directive.md` files; the cycle-15, -17, -18, -19 and cycle-19-fix directive files
Not inspected: this is a **bound confirmation gate and not a gate review of the whole document** — the eight PRD sections, the nineteen AC-DT criteria, the nine NFR dimensions, and the ten §8 questions were **not** re-derived, and no verdict here should be read as a fresh finding that they hold; the cycle-16, cycle-18, and cycle-19 gates are where those were last checked. The Depth 1 continuity scan was **not** re-run at full depth; only the three bound confirmations and the citation sweep inside confirmation 3 were run. Residual non-blocking findings from prior cycles were **not** re-litigated, per Dave's standing ruling that they ride to the TRD stage. Still true and unchanged since cycle 7: no TRD and no derived acceptance-criteria artifact exists, so no spine check from the TRD side was possible; and neither `bin/directive` nor `bin/check-directive` exists, so **no criterion in §6 was executed against an implementation** — every verdict about generator or lint behaviour here is a reading of the criteria, not an observation of a binary, and no cycle-mode skeleton carrying the disposition slot was generated because nothing emits one. The prior gates' own evidence lines were re-run only where a bound confirmation depended on them.
Findings: 1 non-blocking, 5 observations
Prior cycle: reviews/directive-tooling-cycle-19.md
Dave should inspect: **N1 below**, and nothing else. The cycle-19-fix scoping does what you ruled, at every site the fix session disclosed — but scoping the reading to *carried* wording reopens, for one narrower shape, the false negative the unscoped reading closed: an author who states **two** dispositions of their own and fences one of them, where the fenced one is an **original** rather than a copy or a duplicate. That is the shape the cycle-18 gate's N1 literally described ("an author who states two of their own dispositions and fences one"); the cycle-19 revision restated it as "a fenced copy of one", and the scoping closes only the copy case. §7 still says residual one is the **only** false-negative member. Nothing executable moves — no fixture, no criterion, no verdict — and the fix is one clause, but §7 is the section you sign and it states its set in extension.

## N1 — non-blocking
Claim: Scoping the reading to **carried** wording reopens the cycle-18 gate's false negative for the sub-case the cycle-19 revision's restatement dropped — an author's **second, original** labelled disposition statement placed inside a fence — and §7 asserts, in text this revision wrote, that residual one is the only false-negative member of the set.
Location: specs/directive-tooling.md:3103 (§7, "Residual one is therefore still the **only** false-negative member of this set, and the claims above are true as written"); :1374–:1383 (§4, the scoping paragraph — "A fenced **copy of another directive's statement**, and a fenced **duplicate** of one this file states unfenced, are **mentions**" / "An author's **own** fenced statement — the only labelled instance in the file, carried from nowhere — is **a statement they formatted badly**"); :1362 (§4, the restatement of the cycle-18 shape as "and a fenced copy of one"); :2716 (AC-DT-13, "no **false negative** hides behind the exclusion"); :1215 (M3's contract and qualification cells, which enumerate carriage and the own-**sole**-statement case and no third).
Evidence: **Verified by running** for the governed text and for the provenance of the restatement; **by reading** for the entailment. The governed sentence reproduces at `origin/main` and @ `7853525a`, bodies identical but for two frontmatter lifecycle fields (*observed*, `git diff 7853525a origin/main -- skills/directive-authoring.md` returns only `status:` and `last-reviewed:`): "The disposition is stated as its own labelled statement, **exactly one per directive**, mechanically distinguishable from incidental mention of trees or commands elsewhere in the file". The cycle-18 gate's N1 states its shape as one unfenced labelled statement "**and a second of its own** inside a fence", and says in as many words that "it takes an author who states **two of their own dispositions** and fences one" (*observed*, `reviews/directive-tooling-cycle-18.md` @ `c5090995`). The cycle-19 revision **introduced** the narrower restatement — `git show de0cc683:specs/directive-tooling.md | grep 'fenced copy of one'` returns **nothing**, and the same grep at `a304212c` and at `255eb65` returns the line (*observed*, by running) — so the text that narrows the gate's shape from *a second of its own* to *a copy* is text these two revisions introduced, which is inside this confirmation's sweep. Under the scoping as written, a fenced **original** of the author's own is neither of the two mention classes §4:1374–:1379 names; §4:1381 says the reading "says nothing about it". So a directive carrying one unfenced labelled statement and a second, distinct, original one inside a fence has **stated two dispositions**, violating the governed count, while M3 counts **one** unfenced statement carrying an admitted form and exits **0** (M3's row, :1215). That is a false negative, and it is in no residual: residual one is content-falsity, residual two is a false positive in both its shapes (:2955–:3020).
Consequence: Concrete, and it lands in the section Dave signs. §7 states its residual set **in extension** — the discipline three cycles built after the cycle-14 gate found the set restated without its extension moving — and at this revision that set is incomplete by one member, in the direction §7 itself says costs the most: a false stop costs one invocation, a false pass costs whatever the wrong tree costs. Two subsidiary claims go with it: AC-DT-13's "no **false negative** hides behind the exclusion" (:2716) reads as universal in a sentence whose next clause treats carriage and the own-**sole**-statement case as the whole space, and M3's contract cell enumerates the same two cases and no third. Bounded, and the bounds are worth stating: the shape is **contrived** — it takes an author who states two dispositions for their own session and fences one, and it is not the origin-exception shape AC-DT-06 fixture (vi) pins, which is carriage and stays a mention under the scoping (*observed*, :2504–:2514). **Nothing executable moves**: fixtures (vi) and (vii) keep their shapes and their verdicts, which this gate verified are byte-identical at `a304212c` and `255eb65`, and no §6 criterion's verdict changes in either direction. This is why it is filed non-blocking rather than blocking: it is the same defect class, at the same severity, that the cycle-18 gate filed non-blocking for the wider shape.
Fix: Dave's call between two, and neither is taken here. **(a)** Extend the scoping's non-mention class explicitly and disclose what follows: state at §4:1374 that an author's own fenced statement is a badly formatted statement **whether or not it is the file's only labelled instance**, and add the resulting false negative to §7's set in extension with its direction named, correcting ":3103" and softening AC-DT-13's clause to the carriage scope its next sentence already implies. **(b)** Narrow the mention class the other way — state that a fenced instance is a mention **unless it is the file's only labelled instance**, which keeps the fenced-only shape a false positive exactly as ruled and returns every other fenced instance, original or copy, to mention-hood — in which case §7's "only false-negative member" stands as written and no residual is added. (a) is the honest one if the scoping is meant as literally as it reads; (b) is the one that preserves the ruled outcome at §7 with one clause and no new residual. What should not stand is the present state, where the reading's own words leave a shape the governed sentence forbids and M3 exits 0 on, and §7 says there is no such shape.

## O1 — observation
Claim: Confirmation 1 holds — the cycle-18 B1 ruling is executed as an extension, the precondition check against all twelve preserved criteria reproduces, G3's invariant is uniform in both modes, and the ruling's ground is recorded and reproduces by running.
Location: specs/directive-tooling.md:556–:700 (§4, "The disposition of that contract"; "The cycle mode emits the disposition statement too"; "The conflict this resolves"; "The ground of the ruling"; "Which of the two moved"); :1019–:1044 (G3's invariant and "'In either mode' now has a ground"); :2323–:2334 (AC-DT-03's mode clause); :2338–:2348 (AC-DT-03's whole-skeleton clause).
Evidence: **Verified by running.** AC-CO-1 through AC-CO-11 were read whole at `docs/packages/package-a-spec.md` @ `768bbe3b` §3.6 (`:388-417`) and AC-CO-12 at §8.2 (`:818-828`); `git diff 768bbe3b origin/main -- docs/packages/package-a-spec.md` returns **empty**, so the criteria this document cites are the criteria standing today (*observed*). **None forbids an addition to the skeleton**, and the document's characterization of each is accurate: AC-CO-1, -2, -5, -6 govern the filename and the refusals; AC-CO-7, -8, -9, -12 govern the bundle and `--out`; AC-CO-11 states "The tool writes only the directive and the bundle directory", which bounds **files** and not directive content; AC-CO-4 and AC-CO-10 fix the **values** of two elements AC-CO-3 already enumerates; AC-CO-3 alone reaches structure, and it carries **no exhaustiveness clause** — `grep -c 'and nothing else'` over the criterion returns 0 (*observed*). The skill AC-CO-3 defers to states **no skeleton format**: `grep -i` over `skills/spec-review-cycle.md` for `disposition|worktree|working tree|skeleton` returns exactly one line, `:66`, about the executor verifying the working tree against reviewed SHAs — not a format, not a region (*observed*, by running; see O5). The executable statement agrees: the three AC-CO-3 cases in `bin/tests/test_cycle_open.py` (`:109`, `:119`, `:127`) are `assertIn`/`assertRegex` **presence** assertions with no whole-skeleton equality, and the file's only two `assertNotIn` calls (`:207`, `:242`) are both inside AC-CO-7 bundle tests (*observed*, by reading at the reviewed revision). The ruling's **ground** reproduces: the cycle-15, -17, -18, -19 and cycle-19-fix directives each carry a `WORKING TREE — exclusive assignment` block, and the cycle-15 and cycle-19 ones carry `git worktree add` (*observed*, by running); of the **seven** `cycle-<N>-directive.md` files actually in `docs/cycles/` — cycle-1, -2, -3, -5, -6, -7, -8 — exactly **two** (cycle-5, cycle-6) mention a tree at all and none carries a disposition (*observed*, by running), which is the "two of them" the document claims. `git log -S 'AC-DT-14'` resolves to `dcfd966`, the cycle-2 revision, so the DEC-000180 corroboration's date is right, and `decisions/log.md:390` carries "emits Route and Model and no Track" verbatim (*observed*).
Consequence: None adverse. The one thing not available, and stated so it is not over-read: **no cycle-mode skeleton carrying the slot was generated**, because no binary emits one — the confirmation is against the criteria, the committed AC-CO-3 test cases, and the governed sources, not against an artifact.

## O2 — observation
Claim: Confirmation 2 holds at both halves — N1's quotation is whole against `origin/main` by mechanical match, and N2's scoping reaches §4, §7, and both disclosed consequential-update sites consistently, with fixture (vii) byte-identical across the movement.
Location: specs/directive-tooling.md:1215 (M3's row, all three cells); :1352–:1397 (§4, "The governed count and M3's count agree" and the scoping paragraph); :1407–:1437 (§4, the withdrawal of the previous revision's §7 consequence); :2519–:2527 (AC-DT-06 fixture (vii)); :2706–:2719 (AC-DT-13's cycle-19 sentence); :3001–:3020 and :3090–:3116 (§7's second shape and "The set does not change in cycle 19").
Evidence: **Verified by running.** The Derived-from cell's quotation was extracted programmatically and compared, whitespace- and emphasis-normalized, against the first bullet of `git show origin/main:skills/directive-authoring.md` taken end to end: **exact match**, 557 characters, running through "Two sessions sharing a tree mutate each other's preconditions; prefer not splitting work across trees." So "quoted whole at its current form" is **true as written**, and the cell's own parenthetical — that `origin/main`'s body differs from `7853525a`'s in nothing but the frontmatter lifecycle fields — reproduces by diff (*observed*). N2: the scoping is stated where the reading is stated (:1374), the fenced-only shape is back in §7's set as an accepted **false positive** with its removal and restoration both labelled Core rule 13 updates at both ends (:3001–:3020, :3090–:3116), and §7's arithmetic is consistent — residual count two, residual two carrying two shapes, direction mix unchanged. Both disclosed consequential-update sites are consistent with the scoping: M3's contract cell now says the counts agree "over the shapes carriage produces" and names the own-**sole**-statement difference as running in the **false-positive** direction, its qualification cell says the same, and AC-DT-13 carries the matching sentence. Fixture (vii)'s text at `a304212c` and at `255eb65` is **identical**, shape and non-zero verdict alike (*observed*, by diff of the fixture block). A sweep for leftovers from the withdrawn reclassification — `no longer a residual`, `one shape leaves`, `set changes in cycle 19`, `correct behaviour` — returns only historical narration of the previous revision, correctly framed (*observed*, by running).
Consequence: None adverse for execution. N1 above is **not** a failure to execute either disposition: both were executed as ruled, and the finding is a consequence of the ruled scoping reaching a shape the fix directive did not name — the same relation the cycle-19 gate's own N2 bore to the cycle-19 directive.

## O3 — observation
Claim: Confirmation 3's citation sweep is otherwise clean — every governed quotation the cycle-19 and cycle-19-fix revisions introduced or changed matches its source at the revision cited, and every newly cited SHA resolves to a commit touching the path it is cited for.
Location: specs/directive-tooling.md §1 (the fifth-amendment record and the cycle-19-fix origin paragraph); §4 M3's row (all three cells); §4, "The count is exactly one"; §4, "The cycle mode emits the disposition statement too"; §6 AC-DT-03, AC-DT-06 fixtures (vi) and (vii), AC-DT-13; §7.
Evidence: **Verified by running**, quotation by quotation, over the added lines of `git diff de0cc683 255eb65 -- specs/directive-tooling.md`. Governed quotations introduced or changed: the whole first bullet of `skills/directive-authoring.md` (O2 above); "**every** directive states its working-tree disposition"; "the label's fixed form is a tooling concern, not this document's"; the carry-as-pointer clause "carries it inline and downstream artifacts point at it", matched at `origin/main` `:29-32`; AC-CO-11's "only the directive and the bundle directory"; AC-CO-3's "matches the format in `skills/spec-review-cycle.md`"; DEC-000180's "emits Route and Model and no Track" at `decisions/log.md:390`; and the expedited-log entry naming `7853525a`, carried verbatim at `origin/main` `reviews/expedited-log.md:57`. All match. The claim that AC-CO-3 carries no "and nothing else" was checked as an **absence** and holds. Newly cited SHAs each resolve to a commit **touching** the cited path — `c05efadd` and `5ea201e6` to their own directive files, `768bbe3b` to `docs/packages/package-a-spec.md`, `7853525a` to `skills/directive-authoring.md` (*observed*, by `git show --name-only`).
Consequence: None adverse. The sweep found no quotation pointing at text that is not there and no truncation claiming wholeness; N1 above is a **consistency** defect between the document's own claims, not a misquotation.

## O4 — observation
Claim: Every corpus figure the cycle-19-fix revision restated reproduces at the reviewed commit, and the interval's rise of one is correctly attributed.
Location: specs/directive-tooling.md §1 ("grown by twenty-one files", 112); §4, "The corpus recount against the three-pattern check" (112 / 76 / 7 / 26 / 3); §5, third outcome ("1 non-conforming directive file of 112").
Evidence: **Verified by running**, at `255eb65`. `git ls-tree --name-only 255eb65:docs/cycles` returns **113** entries, **112** `.md` and one non-`.md` (`comfy-archive-and-generalize-20260822T195900-check.txt`, excluded as §4 states); `git ls-tree` returns **blobs only**, so the single-component boundary stays inert as §4 claims. Classifying the 112 against M8's three patterns, first match wins, yields **76 / 7 / 26 / 3**, and the three matching none are exactly the files §4 names — `metadata-scope-fix-20260823.md`, `doc-review-2026-08-02-questions.md`, `friction-refactor-2026-08-09-decisions.md`. 112 − 91 = **21**, matching §1. The one file this interval added is `directive-tooling-spec-19-fix-20260824T081022Z.md`, pattern 1, which is the +1 §4 claims, and the rise is one rather than two because the cycle-19 review was a self-gate that landed no directive of its own.
Consequence: None adverse. §5's derivation holds at the new size.

## O5 — observation
Claim: Two evidence-level imprecisions were found beside the confirmations — one a pre-existing unmarked internal elision in M3's contract cell, one an overstated grep in the cycle-19 artifact — and both are recorded rather than filed, neither being inside the bound scope nor a contradiction.
Location: specs/directive-tooling.md:1215 (M3's contract cell, "the rule requiring the disposition to be stated \"as its own labelled statement, mechanically distinguishable from incidental mention of trees or commands elsewhere in the file\""); `reviews/directive-tooling-cycle-19.md` @ `616e382b`, O7's evidence line.
Evidence: **Verified by running.** The governed sentence reads "as its own labelled statement, **exactly one per directive**, mechanically distinguishable from …"; the contract cell's quotation skips "exactly one per directive," mid-sentence with no ellipsis. It is **pre-existing** — the same string is present at `de0cc683`, before either revision under confirmation (*observed*, by grep at both commits) — so it is outside confirmation 3's sweep, and it is not a contradiction: the elided clause is asserted as governed text twice elsewhere in the same row, and the cell claims no wholeness. Separately, the cycle-19 artifact's O7 states that `grep -n 'disposition\|worktree\|working tree'` over `skills/spec-review-cycle.md` "returns nothing"; run as written it returns `:66`, an executor instruction about verifying the working tree (*observed*). The cycle-18 gate's version of the same check used `working-tree` hyphenated and did return nothing, which is where the drift came from. The **document's** claim — that the skill carries no skeleton format — is unaffected and true.
Consequence: None adverse to either confirmation, and neither is filed as a finding. The elision is worth a later reader's attention because M2 is the element this document has to police most carefully, and the cycle-19 N1 that Dave has just ruled on was a defect of exactly this family one cell away. The overstated grep is recorded because a confirmation gate is supposed to be able to tell a prior gate's evidence apart from a prior gate's assertion.
