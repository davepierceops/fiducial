# Review: skills/spec-review-cycle.md — cycle 11

Verdict: ready-with-findings
Reviewed: skills/spec-review-cycle.md @ 7600590f900fd195eeb0763e87f36bbf4ec1f092
Reviewer: Context Quality Reviewer (execution session), independent — this session authored nothing under review and did not author the cycle-11 Editor revision.
Date: 2026-09-02
Scope: full-depth gate. The document read whole at 7600590f900fd195eeb0763e87f36bbf4ec1f092 against all twelve criteria of docs/global-context/review-rubric.md @ fda7970ece0f0cc4d8f0fdadf2185194444f677d and against LEXICON.md @ 17960bb7570e1a0abe6ca0492e35f95a15d627cf under the touch rule, from the assigned worktree at 970f130 (main 759b6217618e06ff7a1cd32c550c6f4df7289f25 plus the directive file), inside the sandbox. Realization of the five ruled changes in docs/cycles/spec-review-cycle-cycle-11-editor-directive.md @ 9a2e82b3388c71eb30b65da8d7e2202fbf65b9e4, each observed against the document and the Editor's diff (git diff 9d5456c 7600590 -- skills/spec-review-cycle.md, 100 lines): SR-1 at lines 96-99 — a re-gate disposes the findings of the cycle it closes, takes no new decision, and a new decision opens its own cycle at step 1; SR-2 at lines 43-54 (the Loop start section: agreement bar as ready or ready-with-findings per the document metadata policy, and gate cadence, both stated by Dave and recorded by the opening directive) and lines 100-105 (the confirmation-scoped re-gate admitted as a named form, stated in its directive and its artifact); SR-3 at lines 68-73 — a finding below the reviewed document's stage routes to the next stage's question list, is not filed as blocking, and does not bar the flip; SR-4 at lines 64-67 — a named defect class takes one ruling before its instances; SR-5 at lines 110-137, its own section, carrying all six ruled elements — the spec stays open while the Test Designer writes tests (116), neither final until they cohere (117-120), findings flow both ways to triage (121-124), the decision session mediates between the two execution sessions (125-129), dispositions are intent with verification against the counterparty's artifact and disclosure of deviation (130-134), both flip agreed together on one ruling (135-137). All five realized as ruled — observed. The frontmatter flip to status in-review and last-reviewed null landed in the same commit as the content edit — observed in the diff. Cross-check one, the Convergence section against context-sets/spec-and-change-discipline.md's red-gate rule (lines 24-36) and open-spec-delta rules (lines 42-66): no contradiction. The section does not restate what a red-gate proves; its rule that neither execution session edits the other's artifact on its own judgment agrees with the discipline's "the licence is the owner's"; the discipline sourcing the interface contract from the architecture summary while the section makes a contract the spec does not state a spec finding is compatible — the ruled intent pushes such a contract into the spec — and is not filed. Where the section's use of "red-gate evidence" leaves a gap, it is N-3. Cross-check two, the re-gate forms against the two directives that ran a confirmation-scoped re-gate and their artifacts: the skill describes what was practised on the form's name, its statement in both directive and artifact, the no-new-decision rule, the out-of-scope filing rule, and the role that gated running the re-gate; it does not describe the third confirmation both directives named — that is N-5. Rubric criteria observed passing without a finding: 3 (grep for path-shaped references in the document — none; the three governed documents it names by name each carry audience all-roles and land in every bundle this document lands in, observed by running bin/bundle for its five audience values); 4 (docs/global-context/core.md and docs/global-context/decision-layer.md grepped for the new rules' subjects — re-gate, cadence, bar, defect class, mediating, convergence, disclosure, routing forward — none stated there; Decision Layer rule 10 is consistent with SR-1's rule, not a restatement of it); 5 and 6 (the new text is instruction; the one trailing consequence clause at 97-98 mirrors the pre-existing line 79 that prior cycles passed); 7 (session kind stated at lines 9-10, and the Convergence section names the session kind of each party); 8 (no model names; grep for track, prompt, dispatch, sync block — none); 9 (no filenames prescribed); 10 (lands in five bundles). Pre-existing text — Purpose, Hard constraints, Inputs, the Procedure as it stood at 9d5456c, Reconciliation — read again whole; nothing new filed against it, and the Reconciliation section's trailing rationale at 153-156, passed by cycle 10 and by pass-2b, is not re-derived.
Cross-checked: docs/global-context/review-rubric.md @ fda7970ece0f0cc4d8f0fdadf2185194444f677d; LEXICON.md @ 17960bb7570e1a0abe6ca0492e35f95a15d627cf; context-sets/spec-and-change-discipline.md @ cac23b8c9e6f3335e930acb7ceb024bd4959c8a9; policies/document-metadata-policy.md @ 1d6213baf82bd2a9eeb4c10e9dc9b8fb78025390; docs/cycles/spec-review-cycle-cycle-11-editor-directive.md @ 9a2e82b3388c71eb30b65da8d7e2202fbf65b9e4; docs/cycles/conversation-retro-cycle-4-regate-directive.md @ c0c20b68296e7db03d45f5101559c1d91241945b; docs/cycles/bundle-system-cycle-2-regate-directive.md @ 34dd22a7944e06b9e4772ddffd905c4088406743; roles/context-quality-reviewer.md @ d202b83412d8da512b025eb7f39de4dd8a3f2e40; skills/review-artifact.md @ 6b210cb0a749bcf40227a3f7bc7da8f6d0306a3d; and at 759b6217618e06ff7a1cd32c550c6f4df7289f25: reviews/conversation-retro-cycle-4.md and reviews/bundle-system-cycle-2.md (the practised re-gates' artifacts), docs/global-context/core.md, docs/global-context/decision-layer.md, operating-model.md (Change flow, Change package), roles/test-designer-agent.md, policies/commit-and-change-control-policy.md (Spec branches and the reconciliation pull request), policies/source-of-truth-policy.md (the architecture summary), specs/trd-template.md and specs/prd-template.md (their open-questions sections), and bin/bundle output for the audience values spec-reviewer-agent, context-quality-reviewer, architect-agent, chief-of-staff, human, and test-designer-agent.
Not inspected: whether any rule is correct as engineering or product judgment, which this role cedes; the practice SR-5 canonizes — reviews/directive-tooling-trd-cycle-1.md through cycle-3.md and the docs/cycles/directive-tooling-trd-* chain — was not re-read, and SR-5's fidelity was judged against the resolution text in the Editor directive; OPEN-ITEMS.md and retros/retro-synthesis-20260831T163000.md, the rulings' sources — the Editor directive's Decisions entries were taken as the ruled intent (told); the commit-and-change-control policy beyond its spec-branch section; docs/history/, docs/batons/, and every other review artifact; the Editor's execution report, which was chat-held and not available — realization was confirmed against the diff and the document instead; any TRD under convergence, none being in this repository.
Findings: 7 — 0 blocking, 6 non-blocking, 1 observation
Prior cycle: reviews/pass-2b-rulings-cycle-1.md — the most recent artifact over this document, cited by its last-reviewed at 9d5456c; the last artifact in this stem's own chain is reviews/spec-review-cycle-cycle-10.md.
Dave should inspect: N-1 — which file yields, this section or the operating model's change flow and the Test Designer role; N-3 — the unit and the record of the joint flip; N-6 — the directive under which this review ran stated the bar and cadence in the decision session's voice, which is the gap N-6 names.

## N-1 — non-blocking
Claim: Convergence step 1 has the Test Designer write tests against a spec that is still in review and states no condition on doing so, where the document metadata policy admits testing against an in-review spec only with explicit human confirmation per task, the operating model's change flow places the test plan after specs agreed, and the Test Designer role derives test cases from acceptance criteria where the section derives them from the spec's testable claims. Criteria 12 and 11.
Location: skills/spec-review-cycle.md:116-120, against policies/document-metadata-policy.md:273-282, operating-model.md:119-124, and roles/test-designer-agent.md:19.
Evidence: inferred by reading all four at 759b621. The policy's definition of "build against" names test code explicitly. Verified by running bin/bundle --audience test-designer-agent: the policy and the operating model are in the Test Designer's bundle and this skill is not (N-2).
Consequence: a Test Designer session reads, in its standing context, that tests follow an agreed spec and that testing against an in-review spec needs confirmation; the directive that puts it to work under this section says neither, and whether Dave's ruling that opens a convergence is the confirmation the policy requires is left to inference. The two rules meet only in the decision session. Which of the operating model's step order and this section yields is not this document's to decide.
Fix: one sentence in the section stating that the ruling opening a convergence is the per-task confirmation the document metadata policy requires for testing against an in-review spec, and that in this shape the tests derive from the spec's testable claims as well as the acceptance criteria; or, if Dave prefers, amend the operating model's change flow and the Test Designer role instead. Surfaced, not resolved.
Related: N-2, N-3

## N-2 — non-blocking
Claim: the Convergence section instructs the test-writing execution session — its report files findings against the spec (step 2), and when it revises the tests it verifies against the spec and discloses deviation (step 4) — but the document's audience does not select test-designer-agent, so the Test Designer's bundle does not carry it. Criteria 2 and 1.
Location: skills/spec-review-cycle.md:4 and 121-134.
Evidence: verified by running bin/bundle --audience test-designer-agent at 759b621 in the assigned worktree: 17 members, skills/spec-review-cycle.md not among them; its one occurrence in that bundle's text is the metadata policy's ineligible list. Verified by reading the frontmatter: five audience values, none test-designer-agent.
Consequence: the two-way findings rule and the verify-and-disclose rule reach the Test Designer only when a directive restates them; a Test Designer session reading its bundle sees a role that runs the red-gate and hands off to the Coder, and never the shape this section describes.
Fix: add test-designer-agent to audience, carried in the fix commit; or, if the section is meant for the decision session alone, restate steps 2 and 4 as instructions to the decision session's directive rather than to the executor.
Related: N-1

## N-3 — non-blocking
Claim: step 5 says the spec's agreement flip and "the acceptance of the tests as its red-gate evidence land together" without saying what the acceptance lands as, where it is recorded, or whether the red-gate has run. Criteria 11 and 12.
Location: skills/spec-review-cycle.md:135-137, against policies/document-metadata-policy.md:123-127, context-sets/spec-and-change-discipline.md:24-36, operating-model.md:123 and 130-131, and roles/test-designer-agent.md:26.
Evidence: inferred by reading. The metadata policy fixes the flip to a commit containing nothing but the frontmatter transition; the discipline, the operating model, and the Test Designer role define the red-gate as the behavioral failure demonstration run before handing off to the Coder; the tests live in the project repository, and no governed file names a record of their acceptance.
Consequence: an executor of the joint flip cannot tell whether "land together" is one commit — which the policy forbids if the acceptance is recorded in any file — or one ruling with two records, and cannot tell whether tests that have not yet been shown to fail on bad logic are red-gate evidence at the flip. Two readings, both plausible, one of which breaks a policy.
Fix: state the unit — one ruling by Dave; the flip a frontmatter-only status-transition commit as step 10 has it; the tests' acceptance recorded in a named place — and state whether acceptance requires the red-gate to have been run or only the coherence step 1 defines.
Related: N-1

## N-4 — non-blocking
Claim: the stage-routing rule sends a spec gate's implementation questions to "the implementation package's open questions", an artifact and a list no governed file defines. Criterion 11.
Location: skills/spec-review-cycle.md:68-73.
Evidence: verified by running grep -rn -i "implementation package" over policies, roles, skills, context-sets, docs/global-context, operating-model.md, and LEXICON.md at 759b621: the only occurrence is this line. Verified by reading operating-model.md:152-168: the change package — the lexicon's term — has twelve parts and no open-questions list. The sentence's first destination resolves: specs/trd-template.md:125 and 150, "Open technical questions". Verified by reading context-sets/spec-and-change-discipline.md:75-76, which already names the loose-end tracker as where Spec Reviewer findings not immediately resolved land before a spec is agreed.
Consequence: a finding routed forward from a spec gate has no defined place to land, so the rule that keeps it off the reviewed document also loses it; the next stage's executor has no list to read.
Fix: name a destination a governed file defines — the change package's Known gaps, or the loose-end tracker as the discipline has it — and use the term change package.

## N-5 — non-blocking
Claim: the confirmation-scoped form has the gate confirm that the named resolutions landed "and reads nothing else", yet files a finding outside that scope when it is a new blocking contradiction the revision introduced — which cannot be found without reading the revision's diff against the governed text it cites, a read both practised re-gates named as a confirmation of its own. Criterion 11; the practice cross-check.
Location: skills/spec-review-cycle.md:100-105, against docs/cycles/conversation-retro-cycle-4-regate-directive.md:56-77 and docs/cycles/bundle-system-cycle-2-regate-directive.md:53-74.
Evidence: verified by reading both directives and both artifacts at 759b621. Each directive names three confirmations: the dispositions landed as ruled; the Editor's disclosed judgment calls hold; no new contradiction between the revision's diff and the governed text it cites. Each artifact's Scope line states the form and reports the diff and the cited text read for the third. The skill's form covers the first, states the third only as a filing exception, and rests the second on the executor disclosing its judgment calls — a rule this document states only inside the Convergence section (step 4), not in the Procedure's step 7.
Consequence: a reviewer following the form as written either reads the diff and breaches "reads nothing else", or does not and cannot file the one out-of-scope finding the form permits. A revision's touch-rule edits — lexicon conformance the Editor makes beyond any named resolution — are the text most likely to carry a new contradiction, and they lie outside every named resolution.
Fix: state the confirmation-scoped read as the named resolutions plus the revision's diff against the governed text it cites; and state in step 7 that the executor discloses deviation from a disposition, which makes the second practised confirmation part of "landed as ruled".

## N-6 — non-blocking
Claim: the loop-start statements "stand for every cycle in the loop unless a later cycle directive restates them", and the restatement's author is not named; the section's first sentence gives the statements to Dave, and the re-gate rule calls a change of bar a new decision recorded at step 1, but neither says the restatement is his. Criterion 11.
Location: skills/spec-review-cycle.md:46-48, against 96-99 and 60-62.
Evidence: verified by reading. Observed in practice: docs/cycles/spec-review-cycle-cycle-11-gate-directive.md — the directive this review ran under — states the bar and cadence as "the decision session's statement, Dave's to override", a form the restatement sentence admits and the first sentence does not.
Consequence: a document's agreement bar can be set by a decision session and stand unless Dave notices and overrides it.
Fix: "unless Dave restates them, and the cycle directive records the restatement as the opening directive recorded the originals."

## O-1 — observation
Claim: "open" in the Convergence section means not yet agreed — "the spec stays open", "neither is agreed while the other is open" — while the lexicon's defined term open spec delta, which this document uses in its Reconciliation heading, means the interval a spec branch carries edits the default branch does not.
Location: skills/spec-review-cycle.md:116 and 137, against 139 and LEXICON.md:26-29.
Evidence: verified by reading. No lexicon term is misused and no criterion fails; recorded so a later cycle does not re-derive it, and because a TRD in convergence may also be claimed by an open delta, where the two senses meet in one sentence.
Consequence: none concrete — hence an observation.
Fix: none required; "not yet agreed" in place of "open" at 116 and 137 removes the overlap if wanted.
