# Review: specs/bin-land-trd.md — cycle 8

Verdict: changes-required
Reviewed: specs/bin-land-trd.md @ `49a71eb6` — blob `5baf6d0df0e9db398c64d90a8b36e492daf23722`, byte-identical to the blob at `d8506d2a018414731bb1cb74ccc7793230b634f5` (*observed*, `git show d8506d2:specs/bin-land-trd.md | git hash-object --stdin` returns the SHA `git rev-parse HEAD:specs/bin-land-trd.md` returns at this base). `d8506d2` — "bin-land-trd-7: narrow branch_head, floor becomes ceiling, guard short-circuits" — is the most recent commit touching this file (*observed*, `git log --oneline -- specs/bin-land-trd.md`). Frontmatter is unchanged: `status: draft`, `last-reviewed: null` (*observed*), as the cycle-7 directive required.
Reviewer: Spec Reviewer Agent (execution session)
Date: 2026-08-24
Scope: Re-gate over the cycle-7 revision, run as five things together. First, a discharge check of D7-N1, D7-B1, D7-N2 and D7-N3 against what `docs/cycles/bin-land-trd-7-20260824T015000Z.md` @ `a6c0f323` decided, plus the two cycle-6 observations that directive told the executor to dispose of unnamed; nothing was rejected that cycle and the absence of a rejection was re-verified against the directive text. Second — this cycle's primary question — an independent whole-document sweep for any passage surviving from the floor era: any sentence that permits, assumes, or tolerates a key appearing outside the paths §5.3's table names, reported in full below including the passages the cycle-7 executor did not name. Third, a deliberate ruling on §3.2 step 5's promise that the report says which of the two checks refused, which the executor named and did not close. Fourth, independent re-derivation — not acceptance — of the two claims the executor labelled *inferred by reading* and on which the ceiling's correctness rests: §5.3's four success-path column entries, and the agreement of §6's eleven cells with the twelve table rows in both directions. Fifth, an assessment of the four deltas the executor introduced beyond a decision's stated scope. Plus the full gate from `roles/spec-reviewer-agent.md` re-run over the whole document — nine template sections present and substantively answered, internal consistency section against section, traceability, NFRs instantiating the PRD's dimensions, journeys tracing to the PRD's Top K, SLOs per journey or explicitly unverified, no section overstating confidence, open questions naming what would resolve them, plus the TRD-specific four — and the Depth 1 continuity scan across the PRD → TRD → acceptance-criteria spine. Artifact shape per `skills/review-artifact.md`. `bin/check-frontmatter --all` exits 0 in the review worktree (*observed*, "53 file(s) matched, from 12 configured glob(s)", `EXIT=0`).
Cross-checked: specs/bin-land.md @ `49a71eb6` (the PRD, `status: agreed`, `last-reviewed: reviews/bin-land-cycle-7.md @ 3716ae6` — G1–G7, §4's nine NFR dimensions and its non-goals, §7's risk posture, and AC-LAND-01a through -10 read directly), specs/trd-template.md @ `49a71eb6` (its nine required sections enumerated and matched against this document's headings), reviews/bin-land-trd-cycle-6.md @ `49a71eb6` (the prior cycle; B1, N1, N2, N3, O1 and O2 read in full, together with its section-3 sweep and its ruling on `branch_head`), docs/cycles/bin-land-trd-7-20260824T015000Z.md @ `a6c0f323` (D7-N1, D7-B1, D7-N2, D7-N3, the three-part sweep instruction, and the Do-not block), docs/global-context/core.md @ `49a71eb6` (rule 6's four provenance classes, rules 8, 10 and 13), roles/spec-reviewer-agent.md and skills/review-artifact.md @ `49a71eb6`, and the `9c776bc..d8506d2` diff for this file read in full to establish exactly what cycle 7 changed and what is inherited.
Not inspected: No implementation of `bin/land` exists (*observed*, `ls bin/land` reports no such file at this base; `bin/` holds seven executables and two directories), so nothing this document specifies was executed as the tool; every claim below about what the tool would emit is *inferred by reading*, and the git behaviour those derivations rest on is separated out and marked where I ran it. `bin/tests/run` was not run: this is a document gate, no code changed, and the directive puts the two pre-existing AC-BN-10 bundle failures out of scope. `policies/remote-write-verification-policy.md`, `policies/document-metadata-policy.md` and `policies/verification-boundary-policy.md` were not re-read — this was a Depth 1 scan and they are outside its scope; the TRD's citations of them are carried as *told*. reviews/bin-land-trd-cycle-1.md, -cycle-2.md, -cycle-3.md and -cycle-4.md were not re-read; their dispositions were confirmed discharged at cycles 2, 3, 4 and 6 respectively and are carried as *told*. There is no reviews/bin-land-trd-cycle-5.md and no reviews/bin-land-trd-cycle-7.md — cycles 5 and 7 were revision cycles that produced no review artifact (*observed*, `ls reviews/` lists cycles 1, 2, 3, 4 and 6 only) — which is why the prior cycle named below is 6. reviews/bin-land-cycle-4.md, -cycle-5.md and -cycle-7.md were not re-read; the TRD's citations of their O1/O2/O3/F1 items are carried as *told*. The cycle-7 executor's own execution report was not read: it is not in the repository (*observed*, only the directive at `a6c0f323` and the revision commit `d8506d2` are), so the four deltas assessed below were taken from this cycle's directive enumeration and from the `9c776bc..d8506d2` diff, not from the report's text. Three probes were run this session against `git` 2.55.0 over throwaway local repositories, one of them over a `file://` bare remote; they establish `git`'s behaviour and nothing about GitHub, and no probe was run against a real remote. Every other `*observed*` git claim this document makes — `ls-remote`'s tail-matching, `cat-file -e` under a narrowed refspec, `checkout -B`'s index carry, the step-5 orphaning probe, FM-4's locally-modified-file probe, FM-11's exit-0-and-no-output premise — is carried from the cycles that ran it, as *told*, not re-verified here; this was judged safe because the `9c776bc..d8506d2` diff adds no new `*observed*` provenance claim (*observed*, its single added line carrying the token names the two classes the report may emit rather than labelling an assertion).
Findings: 1 blocking, 3 non-blocking, 2 observations
Prior cycle: reviews/bin-land-trd-cycle-6.md
Dave should inspect: (1) **B1 is the answer to this cycle's primary question, and the answer is yes — one floor-era passage survived.** It is §5.3's own gloss paragraph, six lines below the emission rule, which says `detail.prior_branch` appears on a landing that succeeded. The table row eleven lines above it says the key is established on the success path *only where* step 6 found HEAD on some branch other than `<branch>`. Under the floor the gloss was permissible; under the ceiling it is false, and it is false on the **J2 success path** — the second invocation in a session, which §3.3 explicitly designs the tool around and which is the modal landing this repository performs. I verified by probe that on that path there is no prior branch to name. (2) **The systemic-defect question you asked in §8 has a specific answer, and it is one sentence wide.** §5.3 states its normative content twice: as a table, and as prose glossing the table. The table has now been mechanically verified twice — cycle 6 in one direction, this cycle in both, against §6's eleven cells. The prose has never been, because prose cannot be diffed against a table. Every one of the last three blocking findings has lived in §5.3's prose or in a column read against it, and §5.3's own tie-break clause names four *other* sections and does not reach the prose of §5.3 itself. That is the hole. Extending that clause is the second of the two edits in the shortest path below, and it closes the class rather than this instance. (3) **The four cycle-7 dispositions and both cycle-6 observations are discharged as decided, and all four unscoped deltas are correct** — two of them, the populated §5.2 shape example and the §3.3/§3.4/OQ-2/OQ-5 rewordings, are not merely permitted by their decisions but *required* by them; the executor was right to make them and right to flag them. (4) **On the seam the executor declined to close (§3.2 step 5's promise): the executor's derivation is sound.** I verified the biconditional holds on every FM-3 path, by probe. My ruling is nonetheless that the correspondence should be stated — N3 — because it takes three rules composed across two sections to reach, and the section that makes the promise characterises what it defers without saying how the deferral pays it. That is a one-sentence non-blocking fix, not a design question. (5) **Nothing here reaches the PRD.** AC-LAND-06, -07 and -09 are all satisfied as floors under the ceiling, and I confirm the executor's position that the ceiling **strengthens** AC-LAND-09 rather than threatening it — under the floor a `detail` key's absence was explicitly not a claim, so `detail` served only the "what was established" half; under the ceiling it serves both. Every fix below is inside this document's gift.

## B1 — blocking
Claim: §5.3's gloss paragraph states without condition that `detail.prior_branch` appears on a landing that succeeded, which the same section's table row establishes on the success path only where step 6 found HEAD on some branch other than `<branch>`; under the ceiling both statements are claims, they contradict on the ordinary J2 success path, and §5.3's tie-break clause does not reach a passage inside §5.3.
Location: specs/bin-land-trd.md §5.3, lines 890–896 ("The other four `detail` keys do: … and `prior_branch` because it records a mutation of the session's own working tree that **happens on the success path**"), against line 856 (the `detail.prior_branch` row: "FM-5 through FM-9, and the success path … **and there only where it found HEAD on some branch other than `<branch>`**") and lines 860–870 (the one emission rule); with §3.3's J1→J2 paragraph at lines 332–337 and the tie-break clause at lines 837–839.
Evidence: The contradiction is *inferred by reading* at `49a71eb6`; the reachability of the state on which it bites is *verified by running*. Four steps. (i) Line 856's column is conditional, and correctly so — where HEAD was already on `<branch>`, step 6 moves it off nothing and there is no prior branch to record. (ii) Lines 893–896 assert the opposite without the condition, and supply a reason that is itself false in that state: the mutation it says "happens on the success path" does not happen there. (iii) Under the cycle-7 ceiling this is no longer a harmless gloss. Lines 864–867 make a `detail` key's absence an assertable claim — "Both of those are claims, and both are assertable" — so the table now says `prior_branch` is *absent* on that success path, in a form a test may assert, while the gloss says it is present. (iv) There is no outer rule to break the tie. Lines 837–839 read "Nothing else in this document states any of that, and no other passage may: §3.2 states what each step does, §3.7 states that `build` assembles the report from the accumulated facts, §5.2 states the shape and the value domains, and §6's cells are derived from this table … Where any of those and this table disagree, this table is normative and the other passage is wrong." The clause enumerates §3.2, §3.7, §5.2 and §6. The offending paragraph is in §5.3, so it is not among "any of those", and the clause does not dispose of it. The state is reachable and ordinary: I ran the full J1→J2 sequence of §3.2 over a `file://` bare remote against `git` 2.55.0 in a throwaway repository — J1 from a worktree on `main` reports "Switched to a new branch 'topic'" and establishes `prior_branch` = `main`; J2, with the session doing nothing in between exactly as §3.3 line 332 promises, finds HEAD already on `topic`, both guard checks pass, `git checkout -B topic <base>` reports "**Reset branch 'topic'**" rather than switching, and there is no branch HEAD was moved off (*observed*). Both invocations reach the success path. Checked against the other three keys the paragraph groups with `prior_branch`: `base`, `local_head` and `remote_head` are unconditional on the success path in line 853, 854 and 858, so the paragraph is true of them and the defect is unique to the fourth. Checked against the first half of the same sentence: `stage`, `branch_head` and `git_status` are correctly excluded from the success path by lines 852, 855 and 857, so that half is true.
Consequence: Concretely, on the second landing of any session that used `bin/land` for its first — the J1-then-J2 pattern this repository runs on every directive, and one of the PRD's three Top K journeys — an implementer building `Report.build` from §5.3's prose emits `detail.prior_branch` with `class: "observed"` on a path where the table says the key is absent. That is the over-claiming direction: a provenance-labelled report asserting the tool observed a branch it was moved off when it was moved off nothing. It is the precise direction D7-N2 reversed the rule to make catchable, and the ceiling did make it catchable — §5.4's AC-LAND-T01 now asserts that "the keys of `detail` are exactly the `detail` keys the table establishes there, with none beyond them". But the test is written from §5.3, and §5.3 says both things. A test author who writes the success case from the gloss encodes the wrong expectation, the implementation written from the same gloss satisfies it, and the two agree with each other and disagree with the table. That is worse than an implementation defect a test catches: it is a defect the test ratifies. The second cost is that the property this cycle was run to establish — that no floor-era passage survives — is false, and it is false in the one section the whole cycle-5 collapse made the single home.
Fix: One clause, in line 894–896, conditioning `prior_branch` to match line 856 rather than asserting it: "…and `prior_branch` on the success paths where step 6 found HEAD on some branch other than `<branch>`, because it records a mutation of the session's own working tree that §3.3 decides the tool is permitted to make." Then extend line 838–839's tie-break to cover this section's own prose — "Where any of those, **or any prose in this section**, and this table disagree, this table is normative and the other passage is wrong" — which costs nine words and closes the class rather than the instance. The two are one edit. This touches no PRD goal or criterion and is entirely this document's to take.
Related: N1, N2.

## N1 — non-blocking
Claim: §3.7 states `build`'s emission rule in the floor's terms — emit what `facts` carries, and consult §5.3 only for what happens where `facts` does *not* hold a key — which leaves no case for the one the ceiling creates and explicitly contemplates: `facts` holding a fact on a path the table does not name, where the key must not be emitted.
Location: specs/bin-land-trd.md §3.7, lines 480–488 ("for each field and key that table names, `build` emits what `facts` carries" … "What a field or key carries where `facts` does not hold it — a contract field still present but `unknown`, a `detail` key absent — is §5.3's to state"), against §5.3 lines 862–866 ("Off them it does not, **whatever the accumulated `facts` happen to hold**").
Evidence: *Inferred by reading* at `49a71eb6`; no git behaviour is at issue. §3.7's account is exhaustive over two cases — `facts` holds the key, so emit it; `facts` does not, so §5.3 says what stands in its place — and the discriminant in both is `facts` membership. That was exactly right under the floor, whose rule was that a fact `facts` carries on an unnamed path is "emitted there, not dropped". Under the ceiling the discriminant is the path, not `facts`, and the two now come apart: line 864 states outright that the report does not carry the fact on an unnamed path whatever `facts` holds, which presupposes a third case §3.7 has no room for. That the third case is reachable is not hypothetical — B1's `prior_branch` on the J2 success path is an instance, where `facts` can perfectly well carry the branch HEAD stood on (`<branch>` itself) and the table forbids emitting it. The cycle-7 sweep instruction 7a named §5.4, §5.2, §5.3's success-path entries and §6's cells; §3.7 is not on that list, which is *observed* from `docs/cycles/bin-land-trd-7-20260824T015000Z.md` @ `a6c0f323`, and the `9c776bc..d8506d2` diff leaves lines 480–488's `facts`-keyed framing untouched apart from D7-N3's `git_status` edit four lines above (*observed*).
Consequence: §3.7 exists because `land.py` and `report.py` are written by separate agents under this repository's separation of test authorship from implementation, so it is the only text the `report.py` author reads for `build`'s rule. That author implements the floor. Unlike B1 this is caught rather than ratified — AC-LAND-T01's exact-set assertion is written from §5.3, not from §3.7, so the test fails and the seam is repaired at implementation time. The cost is a round trip and a contradiction standing in the document between the section that states the seam and the section declared normative over it.
Fix: Reword line 481 to key on the path rather than on `facts`: "for each field and key that table names, on the paths that table names it, `build` emits what `facts` carries"; and line 485's clause to "What a field or key carries where the table does not establish it on that path — a contract field still present but `unknown`, a `detail` key absent — is §5.3's to state". Two phrases; it adds no parallel account of scope, because it still names no path.
Related: B1.

## N2 — non-blocking
Claim: AC-LAND-T01's enumeration is one case per failure mode, but the ceiling made the exact `detail` key set assertable and two table rows are conditional *within* a single failure mode, so for six of the criterion's ten cases the exact set the criterion demands is not determined by the case the criterion names.
Location: specs/bin-land-trd.md §5.4, lines 942–967 (AC-LAND-T01: "The enumeration is the success path and each failure mode in §6 **except FM-10**" … "the keys of `detail` are exactly the `detail` keys the table establishes there, with none beyond them"), against §5.3's conditional rows at line 855 (`detail.branch_head`, "FM-3, and there only where the local `<branch>` is the ref whose check refused") and line 856 (`detail.prior_branch`, "FM-5 through FM-9, and the success path … and there only where it found HEAD on some branch other than `<branch>`").
Evidence: *Inferred by reading* at `49a71eb6`, plus the two probes recorded under B1 and N3. T01's enumeration has ten members: the success path and FM-1 through FM-9 and FM-11, less FM-10. Of those, FM-3 carries a conditional `branch_head`, and FM-5, FM-6, FM-7, FM-8, FM-9 and the success path each carry a conditional `prior_branch` — six of the ten, once FM-3 is counted. On each, the table establishes the key on one sub-case of the mode and not on the other, and I confirmed by probe that both sub-cases of FM-3 are reachable and disjoint: with local HEAD diverged the first check exits 1 and the guard short-circuits before `<branch>`'s SHA is read; with HEAD at the base and `<branch>` diverged the first check exits 0 and the second exits 1 (*observed*, `git` 2.55.0, throwaway repositories). Under the floor this cost nothing, because line 915 of the prior text forbade asserting absence at all and the criterion asserted presence only. Under the ceiling the criterion asserts an exact set, and "the `detail` keys the table establishes **there**" has no single referent where "there" is a mode rather than a path.
Consequence: A test author writing T01's FM-3 case has to choose an expected key set and neither choice is right for the mode: including `branch_head` fails the first-check refusal, excluding it fails the second. The likely resolutions are both bad — pick one sub-case and leave the other with no coverage at all, or weaken the assertion back to a minimum and give up exactly the property D7-N2 was taken to buy. The same choice recurs on five more cases for `prior_branch`, including the success path, which is where B1 lives.
Fix: One sentence in AC-LAND-T01, after the enumeration: state that where §5.3's table conditions a key within a failure mode, the mode contributes one case per condition — FM-3 splitting on which check refused, and FM-5 through FM-9 and the success path splitting on whether step 6 found HEAD on another branch — each asserting its own exact set. This adds no AC and amends the existing one, as the cycle-7 directive's 7a required of this section. AC-LAND-T03 already stands as the worked instance of FM-3's second-check sub-case.
Related: B1, N3.

## N3 — non-blocking
Claim: §3.2 step 5 promises the refusal report says which of the two checks refused, and the promise is now met — but only by a derivation the document never states, composed from three rules in two sections, so a reader of §3.2 step 5 following its pointer is not told what in §5.3 discharges the promise.
Location: specs/bin-land-trd.md §3.2 step 5, line 226 ("What that refusal's report carries, including which of the two checks refused, is §5.3's table"), against §5.3 line 855 (the `detail.branch_head` row), lines 864–867 (absence is a claim and is assertable), and §3.2 step 5 lines 213–223 (the short-circuit paragraph) and line 230 (the second check skipped where `<branch>` is absent locally).
Evidence: The soundness half is *verified by running*; the drafting half is *inferred by reading*. The cycle-7 executor's position is that no edit is needed because `detail.branch_head` present implies the second check refused and absent implies the first did. I re-derived it rather than accepting it, and it is sound on every path. FM-3 is reachable in exactly two ways. Where local HEAD carries a commit the base does not, the first check refuses, the guard short-circuits, and `<branch>`'s SHA is never read — including where `<branch>` has diverged as well (*observed*: with `topic` two commits ahead and `main` one commit ahead of the base, `git merge-base --is-ancestor HEAD <base>` exits 1). Where local HEAD is at or behind the base and the local `<branch>` diverges, the first exits 0 and the second exits 1 (*observed*, same substrate). The third state — HEAD at or behind the base and `<branch>` absent locally — is not an FM-3 path at all: the second check is skipped and the guard passes (*observed*, `git rev-parse --verify --quiet topic` exits 1 and no check refuses). So on every path that reaches FM-3, `branch_head` present ⟺ the second check refused, and under the ceiling its absence is a claim a session and a test may both rely on. The executor is right on the merits. What the executor's position does not supply is that a reader arrives at it: the derivation needs the short-circuit rule from §3.2 step 5, the skip rule from §3.2 step 5, and the assertable-absence rule from §5.3 lines 864–867 four hundred lines away, and §5.3's `branch_head` row states a *condition on emission*, never a *correspondence to a cause*. Cycle 6's N1 anticipated this route and called it "the cheapest if those are taken"; cycle 7 took both, and closing the promise is what remains of it.
Consequence: A session handed an FM-3 report with no `detail.branch_head` has to reconstruct three rules to learn what the tool told it — and what it learns decides whether the divergence it must now resolve is on its own HEAD or on a branch it is not standing on, which are different repairs. An implementer reading §3.2 step 5 for what the report must carry is pointed at a table whose relevant row does not visibly answer the question asked, and may reasonably conclude the promise is unmet and add a key or a stage token to meet it — a change to a closed value set, which is the larger decision cycle-6 N1 warned against.
Fix: One sentence, in §3.2 step 5 after line 226: state the correspondence rather than leaving it derivable — "Because the guard short-circuits, the two refusals are distinguished by `detail.branch_head`: present, the second check refused; absent, the first did, the second never having read the ref. Under §5.3's emission rule that absence is a claim, not a silence." No new key, no new token, no change to §5.3's table. N2's split of T01's FM-3 case is what makes the absent half testable.
Related: N2.

## O1 — observation
The cycle-7 rename of "the one absence rule" to "the one emission rule" is complete and correct within the document — no occurrence of the old phrase survives, and the new phrase appears at lines 842 and 860 with §6 line 1018 referring to the concept rather than the name (*observed*, grep at `49a71eb6`). The name had to change, because the rule reversed direction and "absence" no longer describes what it governs. What the rename leaves is that the phrase is still live in the record: `docs/cycles/bin-land-trd-5-20260824T005500Z.md`, `docs/cycles/bin-land-trd-7-20260824T015000Z.md` and `reviews/bin-land-trd-cycle-6.md` all cite "the one absence rule" by that name (*observed*, repo-wide grep), and a reader arriving from cycle-6's B1 or N2 finds no such phrase in the document. This is the correct outcome under `skills/review-artifact.md`'s rule that existing artifacts are not retrofitted, and the document should not carry a changelog, so nothing needs fixing. It is recorded only so that the next reader who follows a cycle-5 or cycle-6 citation and finds nothing knows why.

## O2 — observation
The success path is the only path in this document that no §6 cell describes, and it is where the surviving floor-era passage was. §6 enumerates failure modes; its cell-derivation rule at lines 1010–1018 gives every failure path a second, independently-readable statement of its exact key set, which is what let me check §5.3's table against §6 in both directions and find them exact. The success path has §5.3's twelve column entries, §5.2's one worked example, and the gloss paragraph — and no derived cell to disagree with. That is a structural reason B1 survived the cycle-7 executor's 7a sweep despite that sweep being run in good faith and hitting every target its directive named: the sweep checked §6's cells, §5.4, §5.2's always-present bullet and §5.3's success-path column entries, all of which were correct, and the one statement about the success path that was not correct sits in prose that none of those cross-checks reach. Nothing follows for this cycle beyond B1's fix. It is worth carrying forward that a future edit to the emission rule should be checked against the success path by hand, because the document has no mechanism that checks it.

## The section-3 sweep — did a floor-era passage survive?
Stated explicitly, as the directive requires, and reported whether or not anything was found.

**Result: one survived.** It is B1 — §5.3's gloss paragraph at lines 890–896, on `detail.prior_branch` and the success path. A second passage, §3.7's `build` rule at lines 480–488, states the floor's *discriminant* without stating a floor-era permission, and is entered as N1 rather than as part of B1 because it names no path and so contradicts no column; it describes the mechanism in terms the reversal invalidated.

The sweep was independent of the cycle-7 executor's, run over the whole document rather than over the four targets the cycle-7 directive's 7a named. Method: every passage that mentions emission, presence, absence, the report's key set, or a named path was read against the ceiling and asked the directive's question — is this true under a rule that forbids emitting a fact on a path the table does not name, and not merely under a rule that forbids dropping one it does. Located by reading §5.2, §5.3, §5.4 and §6 whole and by grep over `floor`, `ceiling`, `minimum`, `always present`, `appears on`, `never appears`, `emitted`, `emits`, `absent`, `absence`, `success path`, `landing that succeeded`, `on success`, and `beyond it` (*observed*).

Passages checked and found correct under the ceiling:

- **§5.2's always-present bullet** (lines 754–758). Now says it fixes "the top-level key set and nothing more", and defers both what each key carries and which keys `detail` carries to §5.3. Correct; this was a cycle-7 edit.
- **§5.2's worked shape example** (lines 727–750). Now populated, labelled as one path's, and — checked key by key against the table — **exact** for the path it names: a first landing that created `<branch>` from a worktree on `main` establishes `base`, `local_head`, `prior_branch` and `remote_head` and no other `detail` key, which is what the example shows. Under the ceiling the previous `"detail": {}` would have been a false report, not merely an incomplete illustration.
- **§5.2's three value-domain rules and their reconciliation paragraph** (lines 764–784). Each admits `null` only under `class: "unknown"`, which is what the ceiling requires of a contract field off its named paths. Correct.
- **§5.2's two `files` paragraphs** (lines 787–806). Both defer which paths carry entries, and of which shape, to §5.3. Correct.
- **§5.3's emission rule and its "why exact" paragraph** (lines 860–888). This is the ceiling itself. Correct.
- **§5.3's twelve table rows** (lines 847–858). Re-derived independently; see the section below. Exact in both directions against §6.
- **§5.3's AC-LAND-09 paragraph** (line 928 area, "served twice over"). Under the floor this over-claimed, which cycle-6 O1 recorded; under the ceiling `detail` key absence is a claim, so `detail` serves both halves and the sentence is now true as written. Correct, and discharged by the D7-N2 edit without a separate edit — which is what cycle-6 O1 predicted.
- **§5.4's AC-LAND-T01** (lines 942–967). Rewritten to assert the exact set. Correct in direction; N2 is about the granularity of its enumeration, not about a floor-era permission.
- **§5.4's AC-LAND-T01a and T02** (lines 969–979). Untouched by the reversal and correct.
- **§5.4's AC-LAND-T03** (lines 981–995). Its given is HEAD at or behind the base with the local `<branch>` diverged, which is the second-check refusal, so `detail.branch_head` present is right; it names no `prior_branch`, which is right because step 6 never ran. Correct, and the cycle-7 sweep's 7c target.
- **§6's cell-derivation rule** (lines 1010–1018). Now states the derived set is exact "and no others". Correct; this was a cycle-7 edit.
- **§6's eleven cells** (lines 1028–1038). Re-derived independently; see below. Correct.
- **§3.2 step 11** (lines 272–289). Concerns which terminal paths emit a report at all, not which keys; its claim that `branch` is given "every terminal path the sequence does reach" matches the table's `branch` row. Correct.
- **§3.3 and OQ-5** (lines 355–359, 1320–1324). Both say the report names the branch HEAD was moved off "on the paths §5.3's table states", which defers rather than asserting. Correct — and notably these are the two passages that come closest to B1's subject and get it right by deferring, which is what B1's paragraph fails to do.
- **§3.4 and OQ-2** (lines 383–397, 1284–1295). Concern the guard's routes, not emission. Correct, and correct under the short-circuit; see the deltas section.
- **§7 and §8** (lines 1097–1266). No statement of emission scope anywhere. Clean.

That is the whole of it. One blocking survivor, one mechanism description entered as N1, nothing else.

## The ruling on §3.2 step 5's promise
The directive asks for a deliberate ruling on three questions. Taking them in order.

**Is the derivation sound on every path?** Yes, and I verified it by probe rather than accepting it — the three FM-3-adjacent states are enumerated and the git exit statuses recorded under N3's Evidence. On every path that reaches FM-3, `detail.branch_head` is present exactly when the second check refused. The executor's claim class for this was *inferred by reading*; mine is *observed* for the git behaviour and *inferred* for the composition, which is the strongest class this question admits while no implementation exists.

**Would an implementer reading §3.2 step 5 arrive at it unaided?** No. §3.2 step 5 makes the promise and points at §5.3's table. The row it points at states a condition on emission — "there only where the local `<branch>` is the ref whose check refused" — which tells the implementer when to emit the key, not what a reader may conclude from its absence. To convert one into the other the implementer needs the short-circuit rule and the skip rule from §3.2 step 5 itself, and the assertable-absence rule from §5.3's emission paragraph four hundred lines later, and must additionally notice that the branch-absent-locally state never reaches FM-3 at all. Three of those four are stated; none of them is stated as answering the promise.

**Should the document state the correspondence rather than leave it derivable?** Yes, and this is the ruling. A promise satisfied only by inference *is* satisfied — the report does carry the discrimination, on every path, and under the ceiling it carries it assertably, so I do not find the document wrong and I do not raise this as blocking. But §3.2 step 5 does not merely defer; it characterises what it defers, in the words "including which of the two checks refused". A section that says what its pointer will deliver owes the reader a pointer that delivers it visibly. The fix is one sentence and is entered as N3. The alternative the executor implicitly chose — leave it, because it is true — is defensible and would leave the document correct; my judgment is that this particular seam has now produced a finding in three consecutive cycles (cycle 4's B1 sub-point, cycle 6's N1, and this one) precisely because it keeps being left true-but-unstated, and one sentence retires it.

## The two ceiling-critical claims, independently re-derived
The directive asks me to verify rather than accept, and to label the result.

**(a) §5.3's four success-path column entries.** Re-derived from §3.2's step definitions rather than from the table. *Inferred by reading*, with §3.2's step semantics as the input.

- `detail.base` (line 853), "Every path on which step 3 resolved one: FM-2 through FM-9, and the success path". Step 3 resolves the base before step 4; the success path passes through both. Correct as a normative requirement.
- `detail.local_head` (line 854), "Every path on which step 5 ran: FM-3 through FM-9, and the success path". Step 5 runs on the success path and the table's middle column defines the fact as the SHA the guard read. Correct. One implementation consequence worth naming, which is not a defect: `merge-base --is-ancestor` does not return HEAD's SHA, so an implementation must resolve it separately to satisfy this row. §3.7 line 449 makes §5.3's table the authority on which fact each step establishes, so the table imposing it is legitimate and §3.2 step 5 not mentioning it is not an omission.
- `detail.remote_head` (line 858), "FM-8, FM-9, and the success path; on the latter two it equals `head`, and is redundant rather than wrong". Step 10's `ls-remote` runs on the success path. The redundancy is acknowledged in the row itself. Correct.
- `detail.prior_branch` (line 856), "FM-5 through FM-9, and the success path — the paths on which step 6 completed — and there only where it found HEAD on some branch other than `<branch>`". Correct **as a column entry**, including its condition, which is exactly right: I confirmed by probe that the J2 success path does not satisfy it. The defect B1 records is not in this entry; it is in the prose that glosses it.

So all four column entries are correct as normative requirements now that the column is exact. The executor's claim stands, and the failure is one level away from where the directive expected it.

**(b) §6's eleven cells against the twelve table rows, in both directions.** Re-derived cell by cell and row by row rather than accepted. *Inferred by reading* at `49a71eb6`; the derivation is mechanical over the two texts and no git behaviour enters it.

Forward — for each of the eleven §6 cells, the exact set the table establishes on that path, checked against what the cell names:

| Cell | Set derived from §5.3 | Cell agrees |
| --- | --- | --- |
| FM-1 | `branch`; `detail.git_status` | yes |
| FM-2 | `branch`, `prior_head`; `detail.base`, `detail.git_status` | yes |
| FM-3 | `branch`, `prior_head`; `detail.base`, `detail.local_head`, `detail.git_status`, `detail.branch_head` conditionally | yes |
| FM-4 | `branch`, `prior_head`; `detail.base`, `detail.local_head`, `detail.git_status` | yes |
| FM-5 | `branch`, `prior_head`; `detail.base`, `detail.local_head`, `detail.git_status`, `detail.prior_branch` conditionally | yes |
| FM-6 | as FM-5 | yes |
| FM-7 | `branch`, `prior_head`, `head`, `verification` (`incomplete`), `files` (unknown entries); `detail.base`, `detail.local_head`, `detail.git_status`, `detail.prior_branch` conditionally | yes |
| FM-8 | `branch`, `prior_head`, `head`, `verification` (`incomplete`), `files` (unknown entries); `detail.base`, `detail.local_head`, `detail.remote_head`, `detail.prior_branch` conditionally | yes |
| FM-9 | `branch`, `prior_head`, `head`, `verification` (`incomplete`), `files` (observed entries); `detail.base`, `detail.local_head`, `detail.remote_head`, `detail.prior_branch` conditionally | yes |
| FM-10 | nothing — no report is emitted, and the `branch` row scopes itself to "every path on which a report is emitted" | yes |
| FM-11 | `branch`, `prior_head` (`created`) | yes |

No cell names a key the table does not establish on that path. In particular FM-8, FM-9 and FM-11 correctly omit `detail.git_status`, whose row stops at FM-7 because their stops are a comparison and an exit-0 read respectively; FM-3 and FM-4 correctly omit `detail.prior_branch`, whose row begins at FM-5 because step 6 has not completed; FM-4 correctly omits it for the same reason though step 6 is the step that failed; and FM-11 correctly omits `detail.base`, no base having been resolved.

Backward — for each of the twelve rows, every path the row names carries the key in that path's cell: `branch` on all ten reporting modes, yes; `head` on FM-7, FM-8, FM-9, yes; `prior_head` on FM-2 through FM-9 and FM-11, yes; `files` on FM-7 and FM-8 as unknown entries and FM-9 as observed entries, yes, and named by no other cell; `verification` on FM-7, FM-8, FM-9, yes, and by no other; `detail.stage` on FM-1 through FM-9 and FM-11 via its own column, yes, with FM-10's dash correct; `detail.base` on FM-2 through FM-9, yes; `detail.local_head` on FM-3 through FM-9, yes; `detail.branch_head` on FM-3 only, yes, and by no other cell; `detail.prior_branch` on FM-5 through FM-9, yes, and by no other; `detail.git_status` on FM-1 through FM-7, yes, and by no other; `detail.remote_head` on FM-8 and FM-9, yes, and by no other.

**The two agree exactly, in both directions, over all eleven cells and all twelve rows.** The executor's claim is confirmed. The one thing this check cannot reach is the success path, which has no cell — see O2, and B1.

## The four cycle-7 dispositions and the two cycle-6 observations, checked one by one
- **D7-N1 — the guard short-circuits.** Discharged. §3.2 step 5 gains the "Order is part of the guard" paragraph at lines 213–223, stating that the checks run in the order written, that the second is not evaluated where the first refuses, that `<branch>`'s local SHA is never read in that case, and that at most one check refuses on any path reaching FM-3. The rationale is in the document's own words as the decision required — the refusal is cheap to retry and a report naming both causes would describe a state stopping makes impossible — and is not a changelog. Verified by probe that the behaviour it specifies is the behaviour git produces (N3's Evidence).
- **D7-B1 — narrow `detail.branch_head`'s definition.** Discharged as decided. Line 855's middle column now reads "read by step 5's second check, where that check is the one that refused, before step 6 would rewrite that ref". The right column is unchanged, as the decision required — the executor did not widen it. Under D7-N1 the condition is well-formed on every path, which I confirmed by probe.
- **D7-N2 — floor becomes ceiling.** Discharged as decided, and this is the substantial edit. Lines 860–888 replace the absence rule with the emission rule, keeping D5-N1's structure — one rule, stated once, governing contract fields and `detail` keys alike — and reversing only its direction. The reversal's rationale is stated in the document's own words rather than as a changelog, and the cost is correctly relocated onto the table as a drafting constraint ("no row's column may under-name the paths on which its fact is established"). The consequential edits 7a called for are present: §5.4's AC-LAND-T01, §5.2's always-present bullet, and §6's derivation rule. The one target 7a named that was not fully carried is §5.3's success-path treatment — the column entries were checked and are correct, the prose glossing them was not, which is B1.
- **D7-N3 — §3.7's `git_status` clause.** Discharged. Line 470–472 now reads "the signature admits `None` because not every stop is a subprocess, and §5.3's row for that key names which" — reduced to a pointer, which is one of the two routes the decision permitted. It no longer states a scope condition, so the invariant D5-B1 established is now literally true of §3.7 for `git_status`. Note that N1 above is a *different* defect in the same section and is not a failure of this disposition: D7-N3 was about a scope condition for one key, and N1 is about the discriminant `build` is told to use for all of them.
- **Cycle-6 O1 — "served twice over".** Discharged without a separate edit, and correctly so: O1 itself recorded that "Under N2's ceiling it would become true as written". It is. Under the ceiling a `detail` key's absence is a claim, so `detail` names both what was established and what was not, and the sentence no longer over-claims. The executor was right to leave the sentence alone.
- **Cycle-6 O2 — the `prior_head` citation.** Discharged exactly as O2 proposed. Lines 768–769 now read "the literal string `"created"` (PRD G1, first arm), or `null`; `null` only with `class: "unknown"`", moving the citation from the `null` clause to the `"created"` clause it belongs to.

Nothing was rejected at cycle 7, which I re-verified against the directive text at `a6c0f323` rather than assuming it (*observed*, all four decisions are headed "accept").

## The four deltas beyond a decision's stated scope
Entered as the directive requires — each a finding, an observation, or a stated clean check.

**(a) §5.2's shape example populated for one named path, with a template disclaimer. Clean check, and required rather than merely permitted.** The example previously showed `"detail": {}` on a success path. Under the floor that was an under-illustration. Under the ceiling it is a false report: the table establishes four `detail` keys on that path, and the ceiling makes their absence a claim the report would be making wrongly. Populating it was therefore required by D7-N2 and not an unscoped addition, and the disclaimer at lines 748–750 correctly prevents the populated example from becoming a second home for scope. I checked the populated example key by key against the table for the path it names and it is exact.

**(b) The cycle-6 O2 citation move. Clean check, and within scope rather than beyond it.** The cycle-7 directive told the executor that the cycle-6 artifact "carries two observations this directive does not name — read and dispose of both". This is one of them. The executor flagged it as outside a *decision's* stated scope, which is accurate, but it was squarely inside the directive's. The edit is exactly what O2 proposed and it is correct.

**(c) "The one absence rule" renamed "the one emission rule" in two places. Clean check.** The rename was required by the reversal — the rule no longer governs absences, it governs emission — and it is complete within the document. See O1 for the one residual, which needs no fix.

**(d) The §3.3, §3.4, OQ-2 and OQ-5 rewordings. Clean check, and required by D7-N1.** The executor flagged these because two of them touch an open question's argument text, which is the right instinct. Checked on the merits: the pre-cycle-7 wording of §3.3 said the no-orphan claim "holds only because step 5's **second check** refuses in that state", and §3.4 said the refusal falls out of the guard "on both routes". Under D7-N1's short-circuit both are false where local HEAD has diverged as well, because there the *first* check refuses and the second never runs. So these four passages were made stale by D7-N1 and the rewordings repair them; leaving them would have been a core rule 13 defect. I checked each of the four against the probe results and each is now correct, including the three-way enumeration §3.4 and OQ-2 now carry.

On the open-question question specifically: the rewording strengthens OQ-2's argument for why the *tool's* behaviour is settled without changing what OQ-2 asks or what would resolve it — it still asks whether the PRD should govern the branch-absent diverged case, and still names Dave's disposition of cycle-5 O1 as the resolver. OQ-5's rewording likewise changes an attribution ("step 5's second check" to "step 5's guard") inside a retired question's rationale without disturbing its answer. Neither touches the PRD, and neither softens or restates OQ-6 or §4.3, which the cycle-7 directive protected (*observed*, the `9c776bc..d8506d2` diff touches neither).

## The full gate re-run
Per `roles/spec-reviewer-agent.md`, over the whole document rather than over the diff.

- **All nine required sections present and substantively answered.** Matched heading by heading against `specs/trd-template.md`'s required set: System overview, User journeys and SLOs, Architecture and boundaries, Verification boundaries, Data and state, Failure modes and recovery, Operational concerns, Constraints/NFRs/non-goals, Open technical questions — all nine, in order, with the template's titles (*observed*).
- **Internal consistency, section against section.** One defect, B1, plus N1's mechanism description. Everything else checked in the sweep above agrees.
- **Traceability.** Every substantive claim traces to a PRD goal or criterion, to a cited repository observation, or to a numbered open question. Counts re-checked and all correct (*observed*): "eight open, and two retired" against OQ-1, -2, -3, -4, -6, -7, -9, -10 open and OQ-5, -8 retired; "nine tokens and no others" against the nine-row token table; "eleven cells" against FM-1 through FM-11; twelve table rows. §1's "eighth executable in `bin/`" and "the other seven" are correct — `bin/` holds seven files and two directories at this base — and its claim that no production file under `bin/` performs a remote operation still holds (*observed*, grep for `ls-remote`, `git push` and the bare token over `bin/` excluding `bin/tests/` returns nothing).
- **NFRs instantiate the PRD's dimensions.** All nine of PRD §4's dimensions — Performance, Reliability, Scalability, Security, Maintainability, Usability, Observability, Portability/Compatibility, Compliance — are addressed in §8 in that order, each either instantiated concretely or marked N/A with a reason (*observed*).
- **Journeys trace to the PRD's Top K.** J1, J2, J3 as the PRD declares them, each treated in §2 (*observed*).
- **SLOs per journey, or explicitly marked unverified.** All three explicitly have none, with the structural reason given and recorded as OQ-1. This is the template's required form.
- **No section overstates confidence.** Every assertion about this repository or about git carries a provenance class from core rule 6's four, and the two the report itself may emit are correctly stated as a subset of the four rather than a redefinition. §4.3 is the strongest instance in the document of the opposite of overstatement — it states plainly that AC-LAND-06's comparison is arithmetically redundant and that G5's stated purpose is therefore not achieved as written, and ranks that as OQ-6 rather than softening it. §4.2's B5 declares its own weakest evidence class rather than inheriting it.
- **Open questions name what would resolve them.** All eight do. Both retirements state their kind — OQ-8 retired because the question had no closing move, OQ-5 because it was answered — and both keep the identifier rather than reusing it.
- **TRD-specific four.** Every component has a named responsibility and its interfaces are listed (§3.1, §3.7). Every external dependency is captured as a boundary carrying an evidence class (§3.5, §3.6, §4.2's B1–B5, each with a class and a "does not prove"). Failure modes and recovery are addressed at the system level (§6, with recovery stated once and uniformly as none-inside-the-tool). Technical non-goals are explicit (§8's list of ten).

## Depth 1 continuity scan — PRD → TRD → acceptance criteria
Run per the role document, on the spine only. The directive asks specifically about AC-LAND-06, -07 and -09 as floors under the ceiling.

- **AC-LAND-06** — satisfied, and untouched by the reversal. §3.2 step 10 performs the per-file blob comparison, FM-9 exits 4 and names the mismatching path via `files` carrying `match: false`. §4.3 states honestly that the comparison is redundant with the `ls-remote` check under git transport and ranks the consequence as OQ-6 rather than claiming evidence it does not have. The ceiling neither strengthens nor weakens this.
- **AC-LAND-07** — satisfied as a floor under the ceiling. The criterion tests presence and labelling of the five G6 fields. §5.2's always-present rule keeps all five present on every path a report is emitted, and the ceiling changes only what they *carry*, never whether they are there: off its named paths a contract field is still present, with `value: null` and `class: "unknown"`. So the ceiling cannot reduce presence, which is the only thing this criterion tests. Its deferral of mechanical parseability to this document is discharged by §5.4's AC-LAND-T01.
- **AC-LAND-09** — satisfied, and **the ceiling strengthens it**, which the directive asked me to verify rather than accept. The criterion requires that on any failure the output names what was established and what was not, as separate statements. Under the floor, §5.3 said in terms that a `detail` key's absence "is not a claim" and that no test written to the document may assert one — so `detail` named what was established and named nothing at all about what was not, and the "what was not" half rested entirely on the `unknown` class on the contract fields. That is what cycle-6 O1 recorded. Under the ceiling, lines 864–867 make the absence a claim and make it assertable, so `detail` now serves both halves and the criterion is met twice over rather than once. The strengthening is real and the executor's position is confirmed. The criterion's clause "including the prior head, where the branch existed at the remote" is met on every path where step 3's read succeeded — FM-2 through FM-9, FM-11 and the success path — and on FM-1, where the read is what failed, `prior_head` is present as `unknown`, which is the criterion's own "names what was not" rather than a violation; PRD AC-LAND-01b independently scopes its prior-head requirement to "a step that fails **after the branch was resolved**", which excludes FM-1 (*observed*). The criterion's second clause, "no failure path issues a second write of any kind", is instantiated by §8's argv-recording test, which asserts `push` appears at most once on every failure path.
- **No contradiction found between the PRD and the TRD.** The three places the TRD declines to follow the PRD are all declared as such rather than silently diverged: §4.3 and OQ-6 on G5's stated purpose, §3.4 and OQ-2 on the branch-absent diverged case, and §7's flag-mechanism departure from `specs/trd-template.md` §7, which records Dave's cycle-1 acceptance with a citation.
- **Acceptance criteria.** No derived acceptance-criteria artifact exists for this spec yet (*observed*), so the AC leg of the spine is the PRD's own §6 list plus §5.4's four proposed TRD-stage criteria, which are correctly marked as proposals for the derived artifact and not as amendments to PRD §6.

## Readiness
**This document is not ready for Dave's agreement. The shortest path to it is two edits in §5.3 — B1's fix — after which it is.**

B1 is one clause plus nine words, both inside §5.3, both this document's gift, neither touching a PRD goal or criterion or any closed value set. N1, N2 and N3 are each one or two sentences and are residue of the same reversal; they are worth taking in the same pass because they are cheap and because leaving N3 is what has kept that seam producing findings, but none of them holds agreement.

**On whether the trend is real or the document has a systemic defect: the trend is real, and there is also a systemic defect, and it is small enough to name and close in the same edit.**

The trend first. The blast radius of the blocking finding has shrunk monotonically. Cycle 4's B1 was emission scope stated in three places that disagreed — §3.7, §6's cells, and §5.3. Cycle 5 collapsed it to one home. Cycle 6's B1 was intra-§5.3, one row's two columns disagreeing, with the floor clause resolving it the wrong way. Cycle 7 narrowed the row and reversed the rule. This cycle's B1 is intra-§5.3 still, but now it is one paragraph of prose disagreeing with a table that is itself, as of this cycle, verified exact in both directions against §6's eleven cells and correct on all four of its success-path entries. That is not four cycles of the same defect. It is one defect being driven from three sections into one section into one paragraph, and the current instance is a sentence that was true when written and was made false by a decision taken two cycles later. Four blocking findings in four cycles reads worse than it is; the fourth is not evidence the first three failed.

The systemic defect second, because the trend does not excuse it. §5.3 states its normative content twice — as a table and as prose glossing the table — and the prose is where the last two blocking findings have lived. The table is now verified by two independent mechanical checks; the prose is verified by nobody, because prose cannot be diffed against a table. And the document's own defence against exactly this failure, the tie-break clause at lines 837–839, enumerates §3.2, §3.7, §5.2 and §6 and does not name §5.3's own prose. So when a sentence in §5.3 contradicts the table in §5.3, the document has no rule that says which wins. That gap is what made B1 blocking rather than a drafting nit, and it is what would make the next one blocking too. Closing it costs nine words and is the second half of B1's fix.

With those two edits I would expect this document to be ready, and I would not expect a fifth blocking cycle. What I would still want stated in the change package, and what no edit to this document can supply, is that §4.2's B5 ships **assumed** and §4.3 leaves OQ-6 open by Dave's standing decision — both are declared honestly and neither is a defect, but both are the kind of thing an agreement should be knowing rather than incidental.
