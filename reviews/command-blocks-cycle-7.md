# Review: skills/command-blocks.md — cycle 7

Verdict: ready
Reviewed: skills/command-blocks.md @ 1c86595f0bcd89c6ddb6ae38ed637f1e5b180d8c
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-09-03
Scope: skills/command-blocks.md, read whole at 1c86595f0bcd89c6ddb6ae38ed637f1e5b180d8c from the assigned worktree, against all twelve criteria of docs/global-context/review-rubric.md @ fda7970ece0f0cc4d8f0fdadf2185194444f677d and against LEXICON.md @ e4e62cc6375934c34e13f8ff15545f6f42185b41 (retired terms, provenance-class vocabulary, the execute/run distinction). Full-depth gate, per the cycle's loop-start cadence. This artifact reviewed skills/command-blocks.md at 1c86595f0bcd89c6ddb6ae38ed637f1e5b180d8c and no other document. Also inspected: whether the three changes recorded in docs/cycles/command-blocks-cycle-7-editor-directive.md @ d9bde65e7974d38d6eee735b9e8b82a681383755 (CB-1, CB-2, CB-3) are realized as ruled, read against the content diff f9e517b5331b62a3c0f6fe04af619f8ad56d0583..1c86595f0bcd89c6ddb6ae38ed637f1e5b180d8c (observed: one content commit, one file; CB-1 lands at lines 64-68 and criterion 6 of eleven at lines 112-113; CB-2 amends lines 91-95 and criterion 11 at lines 122-124 with no second criterion added; CB-3 lands at lines 48-51 and criterion 9 at lines 119-120; the count word reads "all eleven" at line 99 and the list holds eleven bullets, counted by script — they agree); the conformance list against the body, observed as stated under CB7-2; the two Editor choices the directive names, ruled under "Rulings" below; frontmatter (observed: status in-review, last-reviewed null, audience [all-roles, human] — the state the document metadata policy's revision lifecycle requires after a content edit to an agreed document). Criterion 6 was applied as the cycle 6 gate applied it to this document: the one failure-mode sentence that accompanies each bold rule is read as part of the rule's statement, not as a trailing justification; the three new rules follow that established shape and add no more.
Cross-checked: policies/commit-and-change-control-policy.md @ 6bb7189ab8351ae6d6526a28eac9b72de0c705d6, sections Push mechanics, Branch protection is the structural gate, Agents may open and merge pull requests, and Spec branches and the reconciliation pull request, against the never-push rule (lines 48-51) — no contradiction: the policy's "plain git push is allowed for agents" governs approval, not the target ref, and its "nothing reaches [the default branch] except through a pull request" is the fact the skill's rule rests on; the skill's clause "a change lands through a pull request, and branch protection on the default branch rejects a direct push" is a one-clause restatement of that policy content, which no rubric criterion reaches (criterion 4 covers Core and the Decision Layer only) and which criterion 1 needs, since a bundle reader cannot open the policy; the one narrowing the skill adds — that the decision session merges — is recorded as CB7-1. docs/global-context/core.md @ 941d7f2482fa260f42147ab52647d813bac17e16 rule 6 against the amended expected-output rule (lines 91-95, criterion 11) — consistent: the skill's "observed" is Core's provenance class (you saw it), and the rule's exclusion of a count measured in another environment is what Core 6 would label inferred at best; and Core's Vocabulary (paste block, command block, execution block) against the fence rule (lines 64-68) — consistent: Core defines a paste block as a fenced block copied whole and pasted whole, and the skill's rule protects exactly that property; Core's vocabulary entries are unchanged since the cycle 6 gate (observed by diff cd7db71..941d7f2), so the document's opening definitions stand as that gate ruled them. docs/global-context/decision-layer.md @ 0129260877703b3b0b13045de1726c20040c8ec9 — the directive names its command-block rules 16 and 17; at this revision the file has sixteen rules, its rule 16 is "a value he will type is its own paste block", and the former rule 16 ("command blocks conform to the command-blocks skill") was deleted at 3e89a21; rules 8, 14 and 16 were read against the expected-output rule and the fence rule — no rule of the Decision Layer at this revision speaks to expected output or fences, so no contradiction is possible there. policies/remote-write-verification-policy.md @ 2a14bcc1b7f5092d2c991abc9e044a3b07298912 rule 1 against the sync rule's exit-status requirement — consistent, unchanged from cycle 6. policies/document-metadata-policy.md @ dda60a262c6eb775632ae5fefcf18fbe02d9add5, Scope and Revision lifecycle, for frontmatter state. reviews/command-blocks-cycle-6.md @ 8e3b95ad622e256e6abc80f5bbeb370ed14616f1 for the prior gate's rulings.
Not inspected: bin/ behaviour beyond running bin/check-frontmatter --all in the assigned worktree (exit 0, sandboxed run); bundle rendering — no bundler was run, and the all-roles membership stated here is computed from the audience: value; the rendering behaviour of any delivery surface — the fence-splitting and copy-control claims the document makes were not reproduced in a surface, and are taken as told from the retros the Editor directive cites; the Editor's report itself, which is not a companion of this directive — the two Editor choices reached this gate as told, through the directive; the retros CB-1 through CB-3 cite, OPEN-ITEMS.md, and the retro synthesis, which are not companions of this directive; whether any rule is correct as engineering or product judgment, which this role cedes; docs/history/, docs/batons/, and every docs/cycles/ file other than the Editor directive; skills/directive-authoring.md and skills/directive-invariants.md, whose execution-block fence form the directive places out of scope.
Findings: none
Prior cycle: reviews/command-blocks-cycle-6.md
Dave should inspect: CB7-1 — whether "the decision session merges" (line 49) is the boundary he means, given the Commit and Change Control policy grants the routine-class open-and-merge to agents without naming a session kind.

## CB7-1 — observation
Claim: The never-push rule's body assigns the merge to the decision session, a narrowing the governing policy does not state and the matching criterion does not carry.
Location: skills/command-blocks.md:48-51, against skills/command-blocks.md:119-120
Evidence: Inferred by reading. Line 49 reads "the decision session merges that branch through a pull request"; the Commit and Change Control policy at 6bb7189ab8351ae6d6526a28eac9b72de0c705d6, section "Agents may open and merge pull requests", reads "agents open a pull request and merge it" for the routine class and names no session kind; criterion 9 (lines 119-120) reads "the branch reaches the default branch through a pull request" and names no session. The wording follows the ruled intent — the CB-3 resolution in docs/cycles/command-blocks-cycle-7-editor-directive.md @ d9bde65e7974d38d6eee735b9e8b82a681383755 reads "it pushes a branch, and the decision session merges through a pull request" — so this is not an Editor deviation. A decision session is an agent session (Core's Vocabulary), so the sentence is a subset of the policy's grant, not a contradiction of it; criterion 12 is not broken.
Consequence: None demonstrable. An execution session reading the skill in the all-roles bundle learns that merging is not its act; the policy would permit it for the routine class. Repository practice — the sandbox cannot reach the GitHub API, and every cycle directive has the decision session open the pull request — matches the skill's sentence, so nothing observed goes wrong.
Fix: None required for agreement. If body and criterion are to say the same thing, either drop "the decision session" from line 49 (leaving "and that branch reaches the default branch through a pull request") or add the session to criterion 9; a change here is a new decision for a cycle directive, not this gate's.

## CB7-2 — observation
Claim: The body and the conformance list do not correspond one-to-one, and the non-correspondence predates this cycle.
Location: skills/command-blocks.md:70-75 (body rule with no criterion); skills/command-blocks.md:102 and 104-108 (criteria 1 and 3 with no bold body rule)
Evidence: Verified by running — the ten bold rule sentences and the eleven criteria were listed by script from the file in the worktree. The three cycle-7 changes correspond exactly: the fence rule (lines 64-68) to criterion 6, the never-push rule (lines 48-51) to criterion 9, the amended expected-output rule (lines 91-95) to criterion 11. Outside them: "Send one block per turn when a human must relay output between blocks" (line 70) has no criterion; criterion 1 (valid and non-harmful) and criterion 3 (safe to re-run) have no body paragraph. The content diff f9e517b..1c86595 touches none of these lines (observed), and the cycle 6 gate passed the document at cd7db71 with this shape and no findings.
Consequence: None demonstrable. The one-block-per-turn rule is a property of a turn, not of a block, so a per-block criterion cannot carry it and a reviewer running down the list finds nothing missing from any block. Criteria 1 and 3 are self-contained as stated. The count word and the list agree.
Fix: None required for agreement. If a later cycle wants the two lists to mirror, criteria 1 and 3 would gain body paragraphs and the turn rule would be marked as a turn rule, not a block criterion; that is a new decision, outside this gate.

## Rulings on the Editor's choices

Both choices the directive asked this gate to rule on are non-findings.

The triple-backtick literal is written as a double-backtick inline-code span (lines 64, 65 and 112). That is the CommonMark form for a code span whose content is a backtick run, and it is the only form that renders the literal without a renderer reading it as a fence; a bare run at line start would open a fence in the document itself. No rubric criterion and no lexicon term reaches the choice, and the raw text is legible to a bundle reader as a code span containing three backticks (observed: the file renders as one document and the frontmatter check passes). Non-finding.

The CB-3 grounding cites the Commit and Change Control policy by title (line 50), not by path. Criterion 3 makes a path-shaped reference a defect, so the by-title form is the only conformant way to cite, and it is the form the governed policies themselves use (the Commit and Change Control policy cites "the Project Setup Requirements policy" by title). The Editor directive's instruction was to ground by citation, not by restating the policy; the one clause that follows the title is the citation's referent, which criterion 1 requires for a reader who cannot open the policy. Non-finding.
