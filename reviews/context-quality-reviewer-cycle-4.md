# Review: roles/context-quality-reviewer.md — cycle 4

Verdict: changes-required
Reviewed: roles/context-quality-reviewer.md @ 3e0b9f48fab2af32608f4b0254604a654cedee7d
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-23
Scope: the whole file, all 38 lines, read whole against all eleven criteria of docs/global-context/review-rubric.md @ 3e0b9f4. Two passes. (a) The change since 12ecaeb, verified by running `git log --oneline 12ecaeb..HEAD -- roles/context-quality-reviewer.md` (one commit, 6211312) and `git diff 12ecaeb HEAD -- roles/context-quality-reviewer.md`: `session: execution` added to the frontmatter, and the Scope section rewritten from a four-part base ("the frontmatter in-scope set, the global-context documents, the engagement documents, and the Public Prose Criteria") to a single base ("the frontmatter in-scope set"), with the eight-item exclusion tail carried over unchanged. No other line changed. (b) All eleven criteria re-applied, with the Scope rule resolved mechanically against the in-scope set. The file is byte-identical to its state at cf8b161.
Cross-checked: policies/document-metadata-policy.md (:24-37 the twelve in-scope globs, :39-57 the out-of-scope set, :84-96 `last-reviewed`, :105-108 `session:`, :136-188 the expedited path, :217-267 the doc-only cycle), skills/review-artifact.md (:4, :42-54, :63, :68, :80-81), docs/global-context/review-rubric.md (:4, :9-11, :20-22, :48-50), docs/global-context/core.md (:15 Standing 2, :17 Standing 4, :55 Vocabulary), docs/global-context/decision-layer.md:32, roles/chief-of-staff.md (frontmatter), roles/spec-reviewer-agent.md — all @ 3e0b9f4. Bundle membership computed from `audience:` over all 51 in-scope files. Every in-scope file's `status:` and `last-reviewed:` was read and each pointer resolved to its artifact's `Verdict:` line. Baseline: `bin/check-frontmatter --all` exits 0 over 51 files from 12 globs; `bin/tests/run` exits 1 with 2 failures of 399, both the accepted AC-BN-10 pair.
Not inspected: whether the excluded classes in the Scope rule are the right exclusions — that was dictated and is Dave's; CQR-5 reports that one of them contradicts the set it subtracts from, not that it is the wrong exclusion. Also not inspected: whether the rubric's eleven criteria are the right eleven — this file cites the rubric, it does not define it; the other 50 in-scope files, except where one was read as the other side of a cross-check row; `bin/` behaviour beyond the two baseline commands; the content of the 47 review artifacts cited by `last-reviewed` beyond each one's `Verdict:` line; whether this role's cession to the Spec Reviewer at :34-35 is correctly drawn, which no change since 12ecaeb touched.
Findings: 4 — 2 blocking, 2 non-blocking
Prior cycle: reviews/context-quality-reviewer-cycle-3.md
Dave should inspect: CQR-5 — the Scope rule and the metadata policy's in-scope set were made to agree by the metadata-scope cycle and still differ by `vendors/**`, two files this role has already reviewed twice; dropping three words from this file or adding a glob to the policy are different decisions. CQR-6 — the sentence CQR-4's fix landed makes `ready` the precondition for `agreed`, and 45 of the 47 agreed documents in the repository cite an artifact whose verdict is `changes-required`; which yields is yours.

## Closure of the cycle-3 findings

CQR-4 (the agreement sentence states an unconditional precondition the metadata policy's two lighter paths contradict) — **closed**, verified by reading. :27-30 now reads "...except on the expedited path and the doc-only cycle the document metadata policy defines, which reach `agreed` without a reviewer," which is CQR-4's fix in this file's own words. The carve-out matches policies/document-metadata-policy.md:136-139 and :217-224. CQR-6 below is a different residue of the same sentence and is not a re-opening of CQR-4.

CQR-1 and CQR-2 (carried from cycle 2 into cycle 3's scope) — the Scope rewrite at 6211312 is the second half of the fix RR-2 named: this file now states one base rather than four. **Closed as to the enumeration**, open as to the result — see CQR-5.

## CQR-5 — blocking
Claim: The Scope rule subtracts `vendors/**` from the frontmatter in-scope set it is defined as, so the reviewable set and the enforced set differ by two files this role has already reviewed; the other seven exclusions subtract nothing.
Location: roles/context-quality-reviewer.md:15-17 ("Every governed non-code file: the frontmatter in-scope set; excluding history, batons, cycles, reviews, retros, trackers, the decision log, and vendor-tooling files."), against policies/document-metadata-policy.md:32 (`vendors/**`).
Evidence: Verified by running. The policy's twelve globs at :26-37 enumerate 51 markdown files, confirmed by `bin/check-frontmatter --all` reporting "51 file(s) matched, from 12 configured glob(s)". Resolving each of the eight exclusions against that set: history (`docs/history/**`), batons (`docs/batons/`), cycles (`docs/cycles/`), reviews (`reviews/**`), retros (`retros/`), trackers (`MANIFEST.md`, `OPEN-ITEMS.md`, `COLLAB-STATE.md`, `BACKLOG-v2.md`) and the decision log (`decisions/log.md`) match no in-scope glob — the globs anchor at the repository root, as the policy states at :42-44 — and each subtracts zero files. "Vendor-tooling files" subtracts two: `vendors/README.md` and `vendors/claude-code/environment-config.md`. The exclusion tail is unchanged from before the rewrite — `git diff 12ecaeb HEAD -- roles/context-quality-reviewer.md` shows the base changed and the tail carried over — and it was written against a four-part base in which the seven inert exclusions did have something to exclude. This role has in fact reviewed both vendor files: reviews/README-cycle-4.md opens "# Review: vendors/README.md — cycle 4" and reviews/environment-config-cycle-1.md opens "# Review: vendors/claude-code/environment-config.md — cycle 1", both signed "Reviewer: Context Quality Reviewer (execution session)". Criterion 11 — an underspecified boundary the agent must resolve by inference, on the sentence that defines what this role gates.
Consequence: Two governed files sit inside the enforced set and outside the stated reviewable set. Both are `status: agreed` at HEAD. Under :27-28 a governed context document reaches `agreed` only after this role's verdict is `ready`, and this role's Scope says it does not review these two — so either the two agreements are unaccounted for, or the Scope rule is wrong and the reviewer has been working outside its stated scope for two cycles. Concretely, a reviewer commissioned "the in-scope set" reviews 51 files and a reviewer commissioned "your Scope rule" reviews 49, and the directive that commissions the cycle decides which, exactly as before the rewrite. The rewrite was meant to end that.
Fix: If `vendors/**` is in scope for review, delete "and vendor-tooling files" and the sentence resolves mechanically to the 51. If it is not, add a `vendors/**` exclusion to policies/document-metadata-policy.md's out-of-scope set and remove the glob from :32, so one document states the set. Either way, the seven inert exclusions should go with it — as written they read as substantive and are not, and one of them ("batons") presupposes that batons are files, which docs/global-context/decision-layer.md:32 now denies.
Related: DMP-9, DL-1

## CQR-6 — blocking
Claim: "A governed context document reaches `agreed` only after this role's verdict is `ready`" excludes a verdict the artifact schema defines and this role returns, and is contradicted by the state of 45 of the 47 agreed documents in the repository.
Location: roles/context-quality-reviewer.md:27-28, against skills/review-artifact.md:63 ("Verdict: ready | ready-with-findings | changes-required") and :80-81.
Evidence: Verified by running. Reading `status:` and `last-reviewed:` from all 51 in-scope files and resolving each pointer to its artifact's `Verdict:` line: 47 are `agreed`, of which 45 cite one of reviews/corpus-regate-cycle-1.md through -cycle-4.md, each stating `Verdict: changes-required`, and 2 cite reviews/corpus-regate-cycle-6.md, stating `Verdict: ready`. The remaining 4 are `draft` — the four files this cycle reviews. No agreed document cites an artifact whose verdict is `ready-with-findings`, though the schema defines it and this role has returned it: reviews/review-rubric-cycle-1.md and reviews/document-metadata-policy-cycle-16.md both do. The four artifacts the 45 cite state a file count on their `Reviewed:` line — 52, 30, 14 and 7 files, each followed by "(enumerated below)" — so the document is covered in the body while the verdict on the line is the sweep's; reviews/corpus-regate-cycle-6.md, which the 2 cite, names both of its documents on that line. Neither this file nor the schema nor the metadata policy states how a per-file result is read out of a sweep's verdict. Criterion 11 — "No wording lets the agent decide what is Dave's," on the sentence that says what Dave's agreement is conditioned on.
Consequence: Read literally, 45 of 47 agreed documents are agreed in violation of the role that gates them, and any document whose review ends `ready-with-findings` can never reach `agreed` without a further cycle whose only purpose is to change the word — which is what the three-verdict vocabulary exists to avoid. Read charitably, the agent must supply two inferences the text does not license: that a corpus sweep's verdict does not bind its members, and that `ready-with-findings` counts as `ready`. Both readings are available to a session holding this file and the schema in the same bundle, and they yield opposite answers on whether a given document may be flipped.
Fix: State which verdicts satisfy the gate and how a per-file result is established: "...only after this role's verdict for that document is `ready` or `ready-with-findings`, in an artifact whose scope states it reviewed the document at the cited SHA." Whether `ready-with-findings` should satisfy it is Dave's; that it currently satisfies nothing while 45 documents rest on something weaker is the defect.
Related: DMP-6

## CQR-7 — non-blocking
Claim: The file names the review rubric to a bundle that does not carry it, which criterion 1 forbids.
Location: roles/context-quality-reviewer.md:20-21 ("Each document in scope, whole, against every criterion of the review rubric."), against :4 (`audience: [context-quality-reviewer, chief-of-staff, human]`) and docs/global-context/review-rubric.md:4 (`audience: [context-quality-reviewer, human]`).
Evidence: Verified by reading both `audience:` values at 3e0b9f4. `chief-of-staff` is a valid audience slug — `roles/chief-of-staff.md` opens `# Role:` — and it appears in this file's audience and not in the rubric's, which carries neither `chief-of-staff` nor `all-roles`. So a `chief-of-staff` bundle carries this file and not the rubric. The other three documents this file names all resolve in that bundle: skills/review-artifact.md carries `chief-of-staff` at :4, and policies/document-metadata-policy.md and docs/global-context/core.md both carry `all-roles`. The rubric is the single exception. Criterion 1 — "Nothing in it assumes the reader can open another file."
Consequence: A Chief of Staff session reads that this role inspects each document "against every criterion of the review rubric" and holds no text stating what the criteria are. That session is the one that triages returned artifacts and proposes documents for the expedited path, so it is asked to judge whether a verdict covered the criteria while holding none of them.
Fix: Add `chief-of-staff` to docs/global-context/review-rubric.md's `audience:`. One value, and it is the cheaper of the two — dropping `chief-of-staff` from this file would take the role document out of the bundle that consumes it.

## CQR-8 — non-blocking
Claim: The clause "agreement is Dave's" restates a rule Core states, in bundles that carry both.
Location: roles/context-quality-reviewer.md:26-27 ("It never agrees a document; agreement is Dave's."), against docs/global-context/core.md:15 (Standing 2, "Dave decides. You propose. Agreement, release, prioritization, and publication are his.")
Evidence: Inferred by reading, both texts at 3e0b9f4. core.md carries `audience: [all-roles, human]` and `order: 0`, so it loads first in every bundle that carries this file. The first half of the sentence is role-specific and states something Core does not — that this particular role never agrees a document, as distinct from never being the one who does. The second half is Core Standing 2 with the other three nouns dropped. Criterion 4.
Consequence: Minor: one duplicated rule in a bundle, and the drift exposure criterion 4 exists to prevent — if Standing 2's list of what is Dave's is ever revised, this clause states the old version and nothing points at it. The sentence that follows, the `agreed` precondition and its carve-out, is what this file must state and Core does not.
Fix: Cut four words: "It never agrees a document."
