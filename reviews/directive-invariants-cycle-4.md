# Review: skills/directive-invariants.md — cycle 4

Verdict: ready-with-findings
Reviewed: skills/directive-invariants.md @ 3f0a96e4f97015ed3091e3d666b64fbc22895eec
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-30
Scope: two parts. (1) Confirmation of cycle 3's F-1, F-2, O-2, and O-1's undefined-token item against the cycle-3 baseline fd35ed20f55689c28cf7714379097537b6d4accd, entry by entry in O-1 below. Cycle 3's own O-1 and O-2 are otherwise closed by the revision or tracked in the decision session and are not re-raised. (2) A full pass over the whole document at the reviewed SHA against all twelve criteria of the review rubric, with anything the revision introduced raised as a finding citing its criterion. Confirmation was done against the baseline-to-revision diff of the one file, read whole; the diff touches that file in exactly the four passages the four decisions name and nowhere else, and the commit's only other file is the revision's directive file.
Cross-checked: three named cross-checks, each with its result. **(1) The disposition-prompt fence against `skills/directive-authoring.md`'s first bullet — pass.** Verified by running: the first fence of `§Working-tree disposition prompt` and the bullet at lines 13–20 of the authoring skill were each flowed (whitespace runs collapsed to one space, lines joined) and the bullet's `- ` marker stripped; both are 563 bytes and compare equal, `f.encode() == b.encode()` returning True. **(2) The per-region placeholder list against the actual `{{…}}` tokens in each region body — pass.** Verified by running: the file was parsed with the shipped `invariants.parse_sections`, which returns twenty-one sections (seventeen region, four lint), and each of the seventeen region bodies was scanned for `{{…}}`; declared and actual agree for all seventeen, including the nine declared "none". The escape token `{{{{` occurs once in the file, in the preamble at :27, and in no region body. **(3) The disposition label's five parts against the TRD §3.4 — pass.** All five are in the one `## Disposition label` section and agree with §3.4: the emitted literal at column 0 (:208–212), which `Document.label_literal` resolves to `WORKING-TREE DISPOSITION:`; the match rule (:214–217), which agrees with the shipped `Document.matches_label` — verified by running, `WORKING-TREE DISPOSITION: x` and `WORKING-TREE DISPOSITION (exclusive assignment): y` match, `the WORKING-TREE DISPOSITION: token` and a colonless line do not; the statement's extent with zero-fails/both-fail (:236–238); the exclusive-assignment form test with its narrowing disclosure (:240–243); and the canonical sole-tree sentence (:245–249), which `Document.sole_tree_sentence` resolves from the section's last fence and which the prompt's worked fence carries verbatim at :103. The sole-tree membership test at :251–253 agrees with §3.4 too. One looseness is the TRD's own and is carried forward rather than resolved here: §3.4's decision sentence calls the label the bare token while its match-rule bullet says "exactly the literal", whose preceding bullet defines the literal with its colon; the reviewed document resolves this in the bare-token direction, which is what `matches_label` implements. Also cross-checked: `docs/global-context/core.md`; `docs/global-context/review-rubric.md`; `skills/review-artifact.md`; `roles/context-quality-reviewer.md`; `specs/directive-tooling-trd.md` §3.2, §3.3, §3.4 and §3.5; and, read and run to distinguish what the document states from what ships, `bin/aimeta/invariants.py`, `bin/aimeta/mdmask.py` and `bin/aimeta/elements.py`, plus `bin/tests/test_directive.py`, `bin/tests/test_directive_trd.py` and `bin/tests/helpers.py` read only.
Not inspected: the `bin/` test suite — not run by this directive, and reported as not run, so no claim is made about whether any test passes; the shipped check named under F-1's confirmation was imported from `bin/tests/test_directive.py` and called over the file and over constructed text, which is a claim about that function and not about the tests' status. `bin/directive` and `bin/check-directive` were not invoked as binaries and no skeleton was generated; every claim about shipped behaviour below comes from importing and calling `aimeta.invariants`, `aimeta.mdmask` and `aimeta.elements` directly. Also not inspected: the PRD; the acceptance-criteria documents (AC-DT-*, AC-CO-*, AC-X-*) named throughout the TRD; `decisions/log.md`; `policies/document-metadata-policy.md`; `specs/directive-tooling-trd.md` §§1–3.1, §3.6 and §§3.7–9 and 4–9; `bin/cycle-open`, `bin/check-frontmatter`'s source, and every other module under `bin/aimeta/`; every governed file other than those named on the Cross-checked line.
Findings: 1 non-blocking, 3 observations
Prior cycle: reviews/directive-invariants-cycle-3.md
Dave should inspect: F-1, whether the property the revision now states correctly should also be stated as a constraint, and stated inside the paragraph that tells the editor which rules bind them. O-2, whether the fence-ordering rule the revision adds should be scoped to the one section it names, given that the document carries a second positional prose dependency no rule covers and no test would catch.

## F-1 — non-blocking
Claim: The revised paragraph states the fence property as a description of the file rather than as an instruction to the editor bound by it, and sits outside the paragraph that tells the editor which rules bind them.
Location: skills/directive-invariants.md:44–49, against :18–25
Evidence: Verified by reading, with the property itself verified by running. Cycle 3's F-1 asked that the bound be the match rule rather than containment of the emitted literal; the revision states the match rule, and the statement is true of the file — the shipped `unfenced_labelled_statements` over the file as committed returns `[]`, the colon-terminated literal occurs at :103 and :211 and both are masked, and the one eligible occurrence of the bare token is at :215, inside an inline code span that does not lead the line after stripping. What the paragraph does not do is bind anyone. Its three clauses are declarative and permissive: "no eligible line of this document satisfies", "appears only inside fenced blocks", "the bare token may appear in prose where it does not lead the line". The paragraph that does bind is :18–25, which opens "These format rules bind whoever edits this file" and, in the same revision, states its own new rule imperatively — "so no fence is added, removed, or reordered there" (:25). The contrast is inside one revision: the O-2 fix is written as a constraint, the F-1 fix as a report of state, and only the second is the property the whole design rests on.
Consequence: An editor adding a worked labelled example unfenced — the plausible case, since the document's purpose is to show authors both admitted forms — reads :44–49 as a description of how the file currently stands, not as a prohibition, and finds nothing in :18–25, the paragraph they were told binds them, that forbids it. The residual is narrower than cycle 3's, and should be triaged as such: the third clause's "where it does not lead the line" does exclude the bad line, so a careful reader is not misled, and the failure is loud — the two tests that apply this check to the committed document go red. What remains is that the file's one load-bearing editing constraint is stated in the register of an observation and in the wrong paragraph, so the editor most likely to break it is the one least likely to read it as addressed to them.
Fix: Move the property into the format-rules paragraph at :18–25 and state it as a constraint — no line of this document outside a fenced block leads with the bare label token and carries a colon later on the same line — keeping the permissive clause as the tolerance it is. No fence, section, or marker line moves. Criterion 5.

## O-1 — observation
Claim: All four cycle-3 items confirmed are resolved by the revision; one landed a single character short of its dictated wording, consistent with the list it joins.
Location: skills/directive-invariants.md, whole, against fd35ed20f55689c28cf7714379097537b6d4accd
Evidence: Verified by reading the one-file diff between the baseline and the reviewed SHA, and by running where a claim could be tested that way. The diff touches `skills/directive-invariants.md` in four passages and nothing else; the commit's other file is the revision's directive file. Entry by entry. **F-1 (which string the fence-only sentence bounds) — resolved.** Lines 44–49 replace the containment sentence with one stating the match rule over eligible lines. Flowed, the landed paragraph is 430 bytes and compares equal to the decision's dictated wording. Verified by running: `unfenced_labelled_statements` over the file as committed, including the new paragraph, returns `[]`; the paragraph is true of the file clause by clause — no eligible line leads with the bare token and carries a colon (the only eligible occurrence, :215, does not lead), and both occurrences of the colon-terminated literal, at :103 and :211, are masked. The bound the sentence now states is the bound the check applies. F-1 above raises what the resolution leaves open, which is register rather than bound. **F-2 (the unconditional worktree-removal report item) — resolved.** Lines 163–164 read "- the worktree-removal status — or, under the sole-tree form, that no worktree existed", answerable under either admitted form. The marker line `REPORT` and the other four bullets are unchanged. The dictated wording ended in a full stop and the landed bullet does not; the list's other four bullets carry no terminal punctuation either, so the drop is consistent with the list it joins. Surfaced rather than resolved, per the rule on two sources disagreeing. **O-2 (fence order in the Disposition label section is load-bearing and unstated) — resolved.** Lines 23–25 append the dictated sentence to the format-rules paragraph, byte-equal after flowing at 209 bytes. Its claim is true of the shipped code: verified by running, the section's fences are, in order, the emitted literal and the canonical sole-tree sentence, and `Document.label_literal` reads the first while `Document.sole_tree_sentence` reads the last. O-2 below records what the sentence's scope leaves uncovered. **O-1's undefined-token item (`<branch>` in the worked example) — resolved.** Line 101 reads `<name> origin/main`; verified by running, the worked fence contains `"wt/<name>"` exactly twice, `<branch>` zero times, and `<name>` three times, so the example's one placeholder names the directory and the branch together. Both worked forms still pass the shipped form test: `elements.m3` returns `('pass', None, None)` over each of the fence's two statements, and `('fail', 'disposition-form-absent', …)` over a sole-tree paraphrase and over a blank slot.
Fix: None for this entry; F-1 above carries the one thing the confirmation leaves open.

## O-2 — observation
Claim: The fence-ordering rule the revision adds is scoped to the one section it names, while the document carries a second positional prose dependency that no rule covers and no check would catch.
Location: skills/directive-invariants.md:23–25, against :272–273
Evidence: Verified by running. The new sentence binds fence order in the `Disposition label` section, which is where the tooling reads positionally — `label_literal` takes the first fence, `sole_tree_sentence` the last. The `Preamble markers` section's prose at :272–273 takes the same shape of dependency on its fence's two lines: "The first entry stands for whatever heading line the mode emits and is not matched as a literal; the second is matched as a literal." The shipped code does not read those two positionally. `elements._preamble_sets` classifies each entry by whether it is written `<…>`, so with the two lines as committed it returns `({'ROUTE AND MODEL'}, True)`, and with them swapped it returns `({'ROUTE AND MODEL'}, True)` unchanged, while `unfenced_labelled_statements` over the swapped file still returns `[]`. The document's sentence is then false and nothing says so.
Fix: Not raised as a finding: no rubric criterion is failed by a rule's absence, the document as committed is correct, and the divergence is between the document and `bin/`, which is not a governed file criterion 12 reaches. Recorded because the revision's new rule names one section as positional and thereby implies the others are not, and because the `Preamble markers` case fails in the opposite direction to the one the new rule guards — silently, with the suite green and the prose wrong, rather than loudly.

## O-3 — observation
Claim: The new paragraph's second clause binds unfenced lines where the rule it restates binds eligible lines, which is strictly stronger than the property the tooling tests.
Location: skills/directive-invariants.md:44–46, against :219–227
Evidence: Verified by running. The paragraph's first clause is stated over "eligible line", which is the document's own term, defined at :219–227 as a line that is not masked — fences, blockquotes and HTML comments all mask. Its second clause is stated over "unfenced line", a wider set: a labelled line inside a blockquote or an HTML comment is unfenced and not eligible. Over the file as committed the difference is empty — the bare token occurs at :99, :103, :211 and :215, the first three masked by fences and the fourth eligible but not leading — so both clauses are true and the shipped check returns `[]`. As a rule read forward by an editor, the second clause forbids a blockquoted or commented labelled example that the check would accept.
Fix: Not raised as a finding: the divergence over-constrains rather than under-constrains, so nothing the tooling requires is left unstated, and the document is correct as committed. Recorded because it travels with F-1 — if the paragraph is restated as a constraint, "eligible" is the term that makes the constraint say what the check tests.
