# Review: specs/directive-tooling.md — cycle 19

Verdict: ready-with-findings
Reviewed: specs/directive-tooling.md @ a304212c7be0a748351eaefe5170e7a4ec89bd5c (blob dc9a55d3)
Reviewer: Spec Reviewer Agent (execution session, **not independent** — this session authored the cycle-19 revision under the same directive and switched roles to gate it; the cycle-16 gate was the third independent one, and a confirmation-scoped independent gate follows this artifact)
Date: 2026-08-24
Scope: Full gate review of the PRD at the commit above — all eight required PRD sections present and substantively answered against `specs/prd-template.md`; internal consistency across sections; traceability of every claim to a parent artifact or to a marked open question; NFR dimension coverage; AC testability; risk tolerance stated explicitly; open questions naming what resolves them. Plus, specifically: whether **B1's ruling is executed as an extension rather than as an amendment of a preserved criterion**, and whether the verification the cycle-19 directive requires against AC-CO-1 through AC-CO-12 was actually run and is recorded in extension rather than asserted; whether the **cycle mode's new obligation reds `bin/tests/test_cycle_open.py`**, which AC-DT-15 names; whether **N1's reading makes §7's residual-set claims true as written**, and where else in the document that reading reaches; whether **N2 is recorded as the descope's design rather than acted on**; and whether the **descope stated in §4 is applied consistently** — a mechanism routed to the TRD by a §8 question gated as a deferral, an unrouted one as a defect. Plus the Depth 1 continuity scan that fires on every spec revision. Every governed quotation introduced or touched this cycle was matched against its source by running, and every corpus figure the document states "at this revision" was recomputed.
Cross-checked: `skills/directive-authoring.md` @ `7853525a` and at `origin/main`; `docs/packages/package-a-spec.md` §3.6 and §8.2 @ `768bbe3b` and at HEAD, AC-CO-1 through AC-CO-12 each read whole; `bin/tests/test_cycle_open.py` at HEAD, run; `bin/cycle-open` `render_directive`; `skills/spec-review-cycle.md`; `decisions/log.md` DEC-000160 and DEC-000180; `reviews/directive-tooling-cycle-18.md`; `docs/cycles/directive-tooling-spec-19-20260824T061718Z.md` @ `c05efadd`; `docs/cycles/directive-tooling-spec-18-20260824T042409Z.md` @ `5ffe9ed6`; the seven `docs/cycles/cycle-<N>-directive.md` files; `specs/prd-template.md`
Not inspected: no TRD and no derived acceptance-criteria artifact exist, so the Depth 1 spine scan ran against the PRD alone — whether a TRD answers every PRD requirement, and whether ACs trace to the Top K journeys from the TRD side, could not be checked. Neither `bin/directive` nor `bin/check-directive` exists, so no criterion in §6 was executed against an implementation; every verdict about generator or lint behaviour here is a reading of the criteria, not an observation of a binary. In particular, **no cycle-mode skeleton carrying the disposition slot was generated**, because no binary emits one — B1's execution is verified against the criteria and against the committed AC-CO-3 test cases, not against an artifact. `reviews/directive-tooling-cycle-18.md`'s O1–O6 were re-derived only where this cycle's changes could have disturbed them; O3's citation sweep was re-run only over quotations this cycle introduced or touched, which is how N1 below was found in text the cycle-18 gate had passed.
Findings: 0 blocking, 2 non-blocking, 7 observations
Prior cycle: reviews/directive-tooling-cycle-18.md
Dave should inspect: (1) **N2 below** — §7 is the section you sign, and one member of its accepted-risk set **leaves** it this cycle on a consequence the cycle-19 directive did not dictate. The directive dictated a *reading*; the removal follows from it under Core rule 13 and is labelled as such at both sites, but it is not itself a dictated disposition and it is stated so you can reverse it in one line. No test moves either way. (2) **N1 below** — M3's Derived-from cell says it quotes the governed rule "whole" and omits that rule's final sentence. The omitted sentence states no requirement M3 checks, so nothing downstream is wrong; what is wrong is a wholeness claim in the row that anchors the element M2 exists to police.

## N1 — non-blocking
Claim: M3's Derived-from cell cites `skills/directive-authoring.md`'s first rule "quoted whole at its current form" and then quotes three of that rule's four sentences, dropping the fourth.
Location: specs/directive-tooling.md, §4, the M3 row's **Derived-from** cell (the mechanically-checkable table); cf. §4, "AC-CO-12, quoted whole", which handles the same problem correctly.
Evidence: **Verified by running.** `git show 7853525a:skills/directive-authoring.md` and `git show origin/main:skills/directive-authoring.md` both carry the rule as **one bullet** ending: "… the label's fixed form is a tooling concern, not this document's. **Two sessions sharing a tree mutate each other's preconditions; prefer not splitting work across trees.**" (*observed*). M3's Derived-from cell quotes the bullet through "not this document's." and stops, while claiming the rule is "quoted whole at its current form". The document's own convention is bullet-whole and it is applied elsewhere: §4's carry-as-pointer quotation reproduces that bullet end to end, verified by running against `origin/main` `:29-32`, and §4's AC-CO-12 quotation stops where it stops and **says so** ("The two parenthetical notes that follow it in §8.2 record the defect history"). Only M3's row claims wholeness it does not have. The cycle-18 gate's O3 asserted the opposite — "carries the working-tree rule exactly as M3's Derived-from cell quotes it whole" — which is where this went unfound.
Consequence: Bounded, and no downstream claim is wrong. The omitted sentence states a **rationale and a preference** ("prefer not splitting work across trees"), not a requirement, so nothing M3 enforces, nothing G6 tests, and nothing AC-DT-13 asserts changes if it is restored. What is concretely wrong is the wholeness claim itself, in the one row that anchors the check, in a document whose M2 element exists to police exactly this discipline and whose §1 records two prior citation defects at length precisely because a reader must be able to trust a citation without opening the source. A reader auditing M3 against its source finds text in the source that the "quoted whole" cell does not carry, and has no way to tell from the row whether the omission is deliberate scoping or drift.
Fix: One of two, and neither is a reviewer's call to pick. **(a)** Extend the quotation to the end of the bullet, which is what "whole" claims and what the carry-as-pointer citation already does. **(b)** Keep the quotation where it ends and replace "quoted whole at its current form" with a scoped claim naming what is omitted and why — the AC-CO-12 model, which this document already uses one section away. (b) is cheaper and arguably more useful, the trailing sentence being rationale that M3 does not derive from; (a) is the one that needs no judgment from a later reader.

## N2 — non-blocking
Claim: One shape leaves §7's accepted-risk set this cycle on a consequence the cycle-19 directive did not dictate — it dictated the reading the consequence follows from — and §7 is the section Dave signs.
Location: specs/directive-tooling.md, §7, "A second shape ran the same direction until this cycle and no longer does"; §7, "The set changes in cycle 19"; §4, "One consequence of the reading reaches §7"; §6, AC-DT-06 fixture **(vii)**.
Evidence: **Verified by reading**, at the reviewed commit and against the directive. The cycle-19 directive's N1 dictates a **reading** — "A fenced copy of a labelled statement is a mention, not a statement" — and dictates two things done with it: state it where M3's count is grounded, and "restore §7's residual-set claims to true as written under that reading". Both are executed (§4, "The governed count and M3's count agree"; §7's cycle-19 paragraph). The directive says nothing about residual two's **second shape** — the author whose only labelled instance is fenced, which the previous revision accepted as a **false positive**. That shape does not survive the reading: if a fenced instance is a mention, a directive whose only labelled instance is fenced has stated **no** disposition, so M3's non-zero exit is the governed rule applied and not a false stop on a well-formed directive. The revision follows that through and reclassifies it, labelling the change a **consequential update under Core rule 13** at both sites and naming it as not a dictated disposition (*observed*, at all four locations).
Consequence: Concrete, and it lands in the section that carries Dave's signature. §7 states its residual set **in extension**, a discipline three cycles built after the cycle-14 gate found the set restated without its extension moving, and a member leaving that set is exactly the event the discipline exists to make visible. Two outcomes are possible and the document should not be read as having settled between them: if the reading is right, the set is correctly one shape smaller and the previous revision over-accepted; if the reading is wrong — if a fence is formatting rather than mention-marking, and an author who fences their sole disposition has stated it — the shape returns as a false positive and §7 is currently understating what it accepts. Nothing downstream moves either way: AC-DT-06 fixture (vii)'s verdict is **non-zero under both readings**, so no test, no criterion, and no fixture is at stake — only what §7 calls the exit.
Fix: Dave's call, in one line, and the revision is written so either answer costs nothing. **(a)** Confirm the reading's reach, and the reclassification stands as written. **(b)** Scope the reading to **carried** wording only — a fenced copy of another directive's statement is a mention; an author's own fenced statement is a statement they formatted badly — in which case the fenced-only shape returns to §7 as a false positive, N1's closure is unaffected (the shape N1 turns on is a **duplicate**, which is carriage under either scoping), and fixture (vii) is untouched. **Clearly labelled as a proposal, not taken here**: (b) is the narrower reading and it is what the directive's word "copy" most literally says; (a) is the more consistent one, since a rule that makes fencing mention-marking for one class of text and not another has to say which class, and the document currently does not.
Related: N1.

## O1 — observation
Claim: Every corpus figure the document states "at this revision" reproduces at the reviewed commit, the interval's rise of one is correctly attributed, and one figure that had gone stale at the previous revision is corrected here.
Location: specs/directive-tooling.md §1; §4, "The corpus recount against the three-pattern check"; §5, third outcome.
Evidence: **Verified by running**, at `a304212c`. `git ls-tree --name-only HEAD:docs/cycles` returns 112 entries, **111** `.md` and one non-`.md` (`comfy-archive-and-generalize-20260822T195900-check.txt`, excluded as §4 states), and `git ls-tree` returns **blobs only**, so the single-component boundary stays inert over the corpus as §4 claims. Classifying the 111 by §4's three patterns, first match wins, yields **75 / 7 / 26 / 3**, and the three matching none are exactly the files §4 names. `git diff --name-status de0cc683 HEAD -- docs/cycles` returns exactly one addition, `directive-tooling-spec-19-20260824T061718Z.md`, pattern 1, which is the +1 §4 claims; the rise is one rather than two because cycle 18's review was a **self-gate** and landed no directive of its own, and §4 now states that of both of the last two intervals. 111 − 91 = **20**, matching §1's "grown by twenty files". Running the classification with cycle 6's deleted character class and without it agrees on every one of the 111 files, so §4's inertness claim reproduces at the new size. **One correction**: the previous revision's "the +2 the paragraph above accounts for" contradicted its own "each rise by **one**" three sentences earlier — right at cycle 17 where the rise was two, stale at cycle 18 (*observed*, by diff against `6d29a4ab` and `de0cc683`). It reads "+1" here, and the paragraph records the staleness rather than silently repairing it.
Consequence: None adverse. §5's "1 non-conforming directive file of 111" holds with its stated derivation. The stale figure is recorded because the paragraph carrying it labels itself a Core rule 13 update, so a Core rule 13 miss inside it is worth a reader knowing about.

## O2 — observation
Claim: Both landing gates were run at the reviewed revision; the frontmatter gate is clean, the test suite carries only the accepted pre-existing failure pair, and the suite AC-DT-15 names is green in full.
Location: `bin/check-frontmatter`; `bin/tests/run`; `bin/tests/test_cycle_open.py`; specs/directive-tooling.md §6 AC-DT-15.
Evidence: **Verified by running**, in the assigned worktree. `bin/check-frontmatter --all` exits **0**, reporting 53 in-scope files from 12 configured globs. `bin/tests/run` exits **1**: **399 tests, 2 failures**, both `tests.test_bundle.TestAgainstThisRepository` AC-BN-10 cases, each failing with `ERROR: an ENTRY is no longer accepted; use --audience VALUE or --list` — the pair the cycle-19 directive names as pre-existing and accepted. Run alone, `python3 -m unittest discover -s bin/tests -t bin -p 'test_cycle_open.py'` reports **31 tests, OK**.
Consequence: None adverse, and one claim this cycle needed is available: the 31 green cases are the executable statement of AC-CO-1 through AC-CO-11, and B1's extension is checked against them rather than only against the criteria's prose. No claim about the migration is available: neither `bin/directive` nor `bin/check-directive` exists, so AC-DT-15's actual assertion — that the suite passes **after** the migration — remains untestable and is not tested here.

## O3 — observation
Claim: B1's disposition is executed as an extension rather than as an amendment, its precondition check was actually run against all twelve preserved criteria, and the verification is recorded in extension rather than asserted.
Location: specs/directive-tooling.md §4, "The cycle mode emits the disposition statement too"; §4, "The disposition of that contract"; §4, "Mode scoping", the Cycle mode bullet and the dispositioned-conflict enumeration; §4 G3, "'In either mode' now has a ground"; §6 AC-DT-03; §6 AC-DT-15; §2; §4, "The two generated cases".
Evidence: **Verified by running for the sources, by reading for the execution.** AC-CO-1 through AC-CO-12 were read whole at `docs/packages/package-a-spec.md` @ `768bbe3b` and at HEAD. **None forbids additions to the skeleton**: AC-CO-1, -2, -5, -6 govern the filename and the refusals; AC-CO-7, -8, -9, -12 govern the bundle and `--out`; AC-CO-11 bounds which **files** are written, not what the directive contains; AC-CO-4 and AC-CO-10 fix the **value** of two elements AC-CO-3 already enumerates; and AC-CO-3 alone reaches skeleton structure, stating that the skeleton "matches the format in `skills/spec-review-cycle.md`" plus an enumeration, with **no exhaustiveness clause** (*observed*). So the directive's stop condition did not fire, and the revision records that it did not. The revision's own corroborations check out: `grep -n 'AC-CO-3' bin/tests/test_cycle_open.py` returns three cases, and reading them shows `assertIn`/`assertRegex` presence assertions only, with the file's two `assertNotIn` calls both in AC-CO-7 bundle tests and neither reaching skeleton structure (*observed*, by running and reading); `render_directive` (`bin/cycle-open:115-133`) emits six elements and neither `Route` nor `Model`, while `decisions/log.md` DEC-000180 states as its tooling consequence that the cycle skeleton "emits Route and Model and no Track" and AC-DT-14 carries that onto the cycle mode — so an AC-CO-3-plus-additions cycle skeleton has been this document's requirement since `dcfd966`, the cycle-2 revision (*observed*, by `git log -S`). The disposition reaches every site the ruling touches: §2's cycle-mode sentence, §4's preserved-contract paragraph, the Cycle mode bullet, the conflict enumeration (now six, scoped and with the sixth named as the first resolved with neither side moving), G3's invariant, §4's "The two generated cases", AC-DT-03, and AC-DT-15.
Consequence: None adverse. The one thing a reader should not over-read is the DEC-000180 corroboration: it shows the floor reading already in use at the same criterion, which is evidence and not proof, and the revision says so in its own words.

## O4 — observation
Claim: N1's reading is stated where the count is grounded, §7's residual-set claims are true as written under it, and N2 is recorded as the descope's design with no criterion amended.
Location: specs/directive-tooling.md §4, "The governed count and M3's count agree"; §4, "What the reading changes about the fence exclusion"; §4, M3's row (both cells); §1's fifth-amendment record; §6 AC-DT-06 fixtures (vi) and (vii); §6 AC-DT-13; §7's cycle-19 paragraph; §8 Q9.
Evidence: **Verified by reading**, at the reviewed commit, and **by running** for the governed text. The two grounds the reading rests on both reproduce: `skills/directive-authoring.md` at `origin/main` carries "mechanically distinguishable from incidental mention of trees or commands elsewhere in the file" and the carry-as-pointer rule's "carries it inline and downstream artifacts point at it", each matched by running. Under the reading the cycle-18 gate's shape — one unfenced labelled statement plus a fenced duplicate — carries exactly **one** statement, so no false negative arises, and §7's "residual one … still the only one" is true as written. The **provenance** of the fence exclusion is unmoved: §4, M3's row, §7, and AC-DT-13 all still state it as this document's rather than as governed text, so no new G6 exposure is created and none is recorded — which is the half of this that a reader could most easily get wrong, the reading supplying a *ground* and not a *source*. N2 is recorded at §8 Q9, naming the three Q9-gated properties in one place, stating the queue as a **scheduling** item for the release gate rather than a defect, and amending no criterion; the §6 preamble sentence the cycle-18 gate proposed is **not** added, which is what "no action, recorded" directs.
Consequence: None adverse for execution. N2 above is not a failure to execute N1's disposition — it is a consequence of the dictated reading reaching a site the directive did not name.

## O5 — observation
Claim: No TRD and no derived acceptance-criteria artifact exists, so the Depth 1 spine scan ran against one third of the spine; the PRD's own structural requirements are met.
Location: specs/ at the reviewed commit.
Evidence: **Verified by running** — `specs/` holds `bin-land.md`, `directive-tooling.md`, `prd-template.md`, `trd-template.md`. All eight of `specs/prd-template.md`'s required sections are present as `## ` headings in order and each is substantively answered; **nineteen** AC-DT criteria are defined, AC-DT-01 through AC-DT-19 with no gap; all nine NFR dimensions are addressed, Scalability and Compliance marked N/A with stated reasons; §8 states **ten** questions, Q1 through Q10, of which three are closed (Q3, Q7, Q8) and seven name a resolver in a "Resolved by:" line (*observed*).
Consequence: The scan checked internal consistency and upward traceability to governed parents, which is where both findings came from, but cannot check that a TRD answers every PRD requirement or that ACs trace to the Top K journeys. Unchanged from cycles 7 through 18. The TRD-stage queue did not grow this cycle: Q9 and Q10 stand as the cycle-18 revision left them, Q9 gaining only the enumeration of what waits on it.

## O6 — observation
Claim: Four defects in text this cycle introduced were found during the gate and corrected **before** the reviewed commit, and they are recorded here rather than left to a diff.
Location: specs/directive-tooling.md §4, "The cycle mode emits the disposition statement too"; §4, "The governed count and M3's count agree"; §4, "The ground of the ruling".
Evidence: **Verified by reading**, in the draft and at the reviewed commit. First, the AC-CO enumeration originally said AC-CO-3 is "the only one that reaches skeleton content", which is false of AC-CO-4 and AC-CO-10 — both fix the **value** of an element AC-CO-3 enumerates — and it now distinguishes content from membership and names those two. Second, the same section said the skill AC-CO-3 defers to "states no such clause either", which implies that skill states a format; it states **no skeleton format at all**, so AC-CO-3's enumeration is the whole of the stated format, and the section now says that and notes the dangling deferral is a defect in `docs/packages/package-a-spec.md` rather than one this reading repairs. Third, the N1 section said the reading leaves §7's set "complete in extension" in a paragraph two below one that **removes** a shape from it; it now says the set gains no member on that account and points forward to the removal. Fourth, the DEC-000180 corroboration originally claimed "seventeen cycles" and "nineteen gate reviews — four of them independent"; the independent count is **three**, and both figures are now replaced by the commit `git log -S` resolves to and by a range naming the gates.
Consequence: None adverse at the reviewed commit. It is recorded because a self-gate that silently repairs its own drafting and then reports a clean sweep is indistinguishable from one that did not look, and the confirmation-scoped gate that follows should be able to tell the two apart. This is the fourth cycle to record it, and the pattern is now that the largest defect count is in the section this cycle wrote from scratch.

## O7 — observation
Claim: AC-CO-3 defers the skeleton format to `skills/spec-review-cycle.md`, and that skill states no skeleton format — so the deferral resolves to nothing, and the criterion's own enumeration is load-bearing in a way its wording does not admit.
Location: `docs/packages/package-a-spec.md` §3.6 AC-CO-3 @ `768bbe3b`; `skills/spec-review-cycle.md`; specs/directive-tooling.md §4, "The cycle mode emits the disposition statement too".
Evidence: **Verified by running.** AC-CO-3 reads "The skeleton matches the format in `skills/spec-review-cycle.md`:" followed by its enumeration (*observed*). `skills/spec-review-cycle.md` carries Purpose, Use when, Hard constraints, Inputs, Procedure, and a Reconciliation section, and **no skeleton template, format block, or element list** of any kind; `grep -n 'disposition\|worktree\|working tree'` over it returns nothing, as the cycle-18 gate also found (*observed*).
Consequence: None for this PRD, and it is recorded rather than filed against it because **the defect is not here**: this document cites AC-CO-3 accurately, quotes it whole, and its floor reading does not depend on the deferral resolving — if anything the dangling reference makes the enumeration the only stated format, which is the reading B1 takes. What a later reader should know is that "matches the format in `skills/spec-review-cycle.md`" cannot be checked against that skill, so anyone re-deriving AC-CO-3's exhaustiveness from the skill will find nothing there in either direction. Closing it is a change to `docs/packages/package-a-spec.md` §3.6, made there, exactly as §4's conflict rule requires — not a reading taken here.
