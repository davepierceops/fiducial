# Review: specs/directive-tooling.md — cycle 23

Verdict: ready-with-findings
Reviewed: specs/directive-tooling.md @ d3ab472517068a4c13796bd2b7de964755995b2f (branch `directive-tooling-prd-rewrite`, PR #225, base main @ ed46f40429e478189b1e6cabf5528b99df70d3a0)
Reviewer: Spec Reviewer Agent (execution session, **independent** — this session authored nothing under review: not the rewrite, not the cycle-21 artifact, not the cycle-22 fixes; it creates exactly two files, its own directive and this artifact, and modifies none)
Date: 2026-08-28
Scope: **Confirmation gate, not a full re-review.** Confirmed that each cycle-21 finding was closed as dispositioned in `docs/cycles/directive-tooling-prd-rewrite-fix-20260828T0940.md` — B1, B2, B3, B4, N1, N2, N3, N4, N5, O2, O4 accepted and applied; O1 no-edit; O3 accept-in-part, whose withheld half (the quote-AC-CO-criteria-whole obligation) is deliberately absent and is not reported here. Confirmed the frontmatter is byte-identical to `0b5a6d4e` and reads exactly `status: draft`, `last-reviewed: null`, `audience: [human]` (*observed*, by running `diff`; `bin/check-frontmatter --all` exits 0). Ran the register check over `git diff 80e5337..d3ab472`: no provenance tag, cycle-by-cycle changelog prose, closed question, or per-citation SHA is reintroduced on any added line (*observed*, by running). Checked internal consistency of the edited sections only — §1, §4 (conflict rule, G10, Non-goals), §5, §6 preamble, AC-DT-06, AC-DT-13, §7, Q9 — including the three disclosed executor choices: the nested pairing of M3's two fence residuals under one §7 bullet, the §7 Accepted ordering, and the literal fixture paths in AC-DT-06's M8 set. Re-verified by running every restored factual claim whose source is in this repository: the §1 corpus figure against `docs/research/gh-write-friction-20260823T184149Z.md` §3.1; G10's restored address against `specs/bin-land.md` §4 G6; AC-DT-13's fence-exclusion claim and §7's carriage clause against `skills/directive-authoring.md`; M8's date-and-time requirement against the same file's Naming rule; §7's write bullet against AC-DT-12 and against `docs/packages/package-a-spec.md` AC-CO-1, AC-CO-7, AC-CO-11; AC-DT-06's two literal names against AC-CO-1; and §6's preamble exception pair against AC-DT-15 and AC-DT-16.
Cross-checked: `reviews/directive-tooling-cycle-21.md`; `docs/cycles/directive-tooling-prd-rewrite-fix-20260828T0940.md`; `skills/directive-authoring.md`; `specs/bin-land.md` §4 G6; `docs/packages/package-a-spec.md` §3.6 AC-CO-1, AC-CO-3, AC-CO-7 and §8.2 AC-CO-11; `docs/research/gh-write-friction-20260823T184149Z.md` §3.1; `policies/document-metadata-policy.md`; `skills/review-artifact.md`; `specs/directive-tooling.md` @ `3e064f6` and @ `0b5a6d4e`
Not inspected: sections the cycle-22 edits did not touch were **not** re-reviewed — §2, §3, §4's element sets and remaining goals, §6's untouched criteria, and §8's other questions were read only where an edited section pointed into them. The loss check against `3e064f6` was **not** re-run in full; only the eleven restorations cycle 21 named were traced back to it. No spine check from the TRD side was possible — no TRD and no derived acceptance-criteria artifact exists for this document, the standing condition every gate in this series since cycle 7 has recorded. Neither `bin/directive` nor `bin/check-directive` exists, so **no criterion in §6 was executed against an implementation**: every verdict here about generator or lint behaviour is a reading of the criteria. `audience: [human]` was confirmed unchanged and **not** re-litigated, per Dave's recorded ruling. Prose quality was not assessed; that is the Context Quality Reviewer's rubric.
Findings: 1 non-blocking
Prior cycle: reviews/directive-tooling-cycle-21.md (cycle 22 was the fix cycle and produced no review artifact)
Dave should inspect: **N1 only**, and it is a count. Every cycle-21 finding closed as dispositioned; the eleven restorations landed in the compact register the rewrite established, and the three disclosed executor choices all hold up — the nested residual pairing reads correctly against AC-DT-06 fixtures (vi) and (vii), the Accepted ordering puts the cheap-direction item ahead of the two false positives that lean on it, and both literal fixture names reproduce against AC-CO-1 and against the prior text. N1 is the one place where restoring B4's stated count of eight left the paragraph's own enumeration at nine.

## N1 — non-blocking
Claim: AC-DT-06's restored M8 clause states a fixture count of eight and then describes nine, because the absolute-path fixture cycle 21 licensed to stay sits outside the four-passing/four-failing enumeration the restored count covers.
Location: specs/directive-tooling.md:395–406 (AC-DT-06, the M8 paragraph): "For M8, eight fixtures — four passing, four failing" (:395), then four passing (:395–398), four failing (:398–402), then "A fixture named by absolute path also matches on the resolved path" (:403–404).
Evidence: **Verified by running** for the extension and for its provenance; **by reading** for the mismatch. Counting the paragraph's fixtures: passing — `<descriptor>-<timestamp>.md`, `cycle-<N>-directive.md`, `<SLUG>-directive.md`, and the relative-path-from-a-subdirectory one; failing — `<descriptor>-YYYYMMDD.md`, the no-pattern name, `docs/cycles/sub/nested-directive.md`, `docs/escaped-directive.md`; then a ninth, named by absolute path, introduced as "A fixture" and given a passing outcome. The count is the restoration cycle 21's B4 Fix prescribed ("Restore the M8 fixture set to eight"), and the ninth is the item the same Fix licensed ("The absolute-path case the rewrite adds is a fine addition and can stay"), so the executor applied the disposition as written; the mismatch is in the composite, not in the execution. It is new at this revision: at `80e5337` the paragraph named the absolute-path fixture and stated **no** count, so nothing was in tension (*observed*, by running the diff). The eight-item set reproduces against the prior text's own count — `3e064f6` states "**eight** — four passing (the three pattern fixtures and this one) and four failing" — which likewise did not include an absolute-path fixture; the absolute-path case duplicates AC-DT-19's third invocation, which is where it entered (*observed*, AC-DT-19 read at this revision: "uncommitted file, relative path from a subdirectory, absolute path, path outside the repository").
Consequence: Narrow and it lands on the Test Designer, which is the role §6 exists to feed — the same role N4's restored preamble was written for. A suite derived from AC-DT-06 builds nine M8 fixtures against a criterion that says eight, so the count cannot be used as the completeness check it is there to be: an implementer who builds eight has a defensible reading in which one of the nine was dropped, and which one is not determined by the text. Nothing executable turns on it — each of the nine is individually named with its own expected outcome, so every fixture's verdict is fixed regardless of the total — which is why this is non-blocking rather than filed beside a substantive gap.
Fix: One word, either direction. Say "nine fixtures — five passing, four failing" and move the absolute-path case into the passing enumeration beside the relative-path one; or keep "eight" and mark the ninth as outside the count, e.g. "Additionally, and duplicating AC-DT-19's absolute-path invocation, a fixture named by absolute path also matches on the resolved path." The second preserves cycle 21's reading that the eight are the set discharging M8's row and the absolute-path case is an addition.
