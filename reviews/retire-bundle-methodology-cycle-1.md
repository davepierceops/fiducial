# Review: bin/bundle-methodology (retirement, DEC-000210) — cycle 1

Verdict: ready-with-findings
Reviewed: bin/bundle-methodology, bin/tests/test_bundle.py, bin/tests/test_bundle_methodology.py @ 999675b7f8770210a70b6a0b33de9bc770d379e3 — all three absent at that ref; their content read at 0ee3882
Reviewer: Reviewer Agent (execution session)
Date: 2026-08-26
Scope: the diff 0ee3882..999675b, whole. Every changed path enumerated with `git diff --name-status`; the three removed files read at 0ee3882 (`git show`); the surviving tree grepped for `bundle-methodology`, `bundle_methodology`, and `test_bundle`; `bin/tests/run` executed; `bin/check-frontmatter --all` executed; `bin/bundle --audience chief-of-staff` executed and its emitted file set enumerated. The removal was checked against what DEC-000210 orders and against the execution directive that carries it out.
Cross-checked: decisions/log.md DEC-000210 (and its citation of the superseded DEC-000190); docs/cycles/retire-bundle-methodology-20260827T0120.md @ c695d88; bin/bundle:312 and bin/tests/test_bundle_audience.py:675-700 (AC-BA-7, the replacement coverage); OPEN-ITEMS.md; ACCEPTED-RISKS.md:12-20; specs/directive-tooling.md:2045-2055; docs/packages/bundle-methodology-spec.md; skills/review-artifact.md; roles/reviewer-agent.md.
Not inspected: the body of bin/tests/test_bundle_methodology.py — its 15 test methods were counted, not read, and no judgment is offered here on what coverage its deletion forfeits beyond DEC-000210's own ruling that the tool is retired rather than repaired; bin/bundle's own correctness, its output format, and AC-BA-1..8 other than confirming AC-BA-7's replacement assertions exist; the bundle-system PRD, which DEC-000210 names as a draft at `~/Downloads/bundle-system-prd-draft-20260825T023000.md` and which is not in this tree; the behavior of the removed tool at any ref; records under docs/history/, docs/batons/, retros/, and reviews/, which this repo does not retrofit. Two verification boundaries are narrower than the gate directive's wording: (1) `bin/tests/run` was executed at 387b718, not at 999675b — 387b718 is 999675b plus this gate's own directive file, a docs/cycles/*.md with no frontmatter, and no test in bin/tests/ reads the real repository's docs/cycles/ (every match is a fixture write into a temp repo), so the delta is inert with respect to the suite, but it was not run at 999675b itself; (2) `bin/bundle --audience chief-of-staff | wc -l` was measured by running only at 387b718 (3217) — the working tree is EXCLUSIVE, so no checkout at 0ee3882 was made, and the equality at 0ee3882 is established by construction rather than by measurement (see below), not by a second run.
Findings: 6 — 0 blocking, 4 non-blocking, 2 observations

## What was confirmed

- **Three files removed, and the removal is the ordered one.** `git diff --name-status 0ee3882 999675b` reports `D bin/bundle-methodology`, `D bin/tests/test_bundle.py`, `D bin/tests/test_bundle_methodology.py`, and one addition (RBM-5). No file is modified. Verified by running.
- **Nothing under bin/ depends on the removed files.** Grepping bin/ for `bundle-methodology`, `bundle_methodology`, and `test_bundle` returns seven hits: bin/bundle:6 and bin/tests/test_bundle_audience.py:3 name `test_bundle_audience.py` itself (a substring match, not a reference to the removed `test_bundle.py`); test_bundle_audience.py:34, :61, :124, :378 and :526 are docstring or comment prose; :525 is a test *method name* (`test_ba4_each_file_is_preceded_by_a_bundle_methodology_shaped_separator`) whose body asserts a separator shape against `bin/bundle`'s own output. A targeted grep for `import`, `from`, `run_cli`, `subprocess`, `Popen`, `check_output`, `exec`, and quoted `"bundle-methodology"` intersected with those names returns nothing. The gate directive's reading — mentions in comments only — is confirmed. Verified by running (grep); the method-name and docstring readings inferred by reading.
- **The suite is green.** `bin/tests/run` → `Ran 424 tests`, `OK (skipped=1)`, exit 0, captured to `$TMPDIR/gate-tests.txt`. failures=0. Verified by running, at 387b718 (see `Not inspected`). The count is arithmetically consistent with the change: 441 − (2 + 15) = 424, where 2 and 15 are the test methods counted in the two deleted test files at 0ee3882. See RBM-3.
- **`bin/check-frontmatter --all` exits 0**, reporting `54 file(s) matched, from 12 configured glob(s)`. Verified by running.
- **No bundle changes.** `bin/bundle --audience chief-of-staff | wc -l` is 3217. Verified by running. The equality at 0ee3882 is established by construction rather than by a second measurement: the bundle emits 32 files, none of them under bin/ (`grep -c '^bin/'` over the emitted file list is 0), and AC-BA-1 excludes docs/cycles/ from every bundle (bin/tests/test_bundle_audience.py:17, :215). All four paths the diff touches are therefore outside the bundle's input set, and the 32 bundled blobs are untouched by 0ee3882..999675b. The only header field that varies with the ref is the `Source: @ <repo HEAD>` line, one line either way. Inferred by reading, from a file set verified by running.
- **No unintended tree mutation.** `git status --porcelain` is empty at each checkpoint; no file outside reviews/ and this cycle's own directive was written.

## RBM-1 — non-blocking
Claim: OPEN-ITEMS.md carries a live open item whose entire subject is a test file this change deletes, and the change leaves it open.
Location: OPEN-ITEMS.md:1013-1023, "`bundle base` red — stale AC after the decision-log citation"
Evidence: Verified by running (grep and `sed -n '1000,1040p' OPEN-ITEMS.md`). The item names `bin/tests/test_bundle.py::test_bn10_bundle_base_yields_exactly_itself` by its full node id and asks for one of two fixes: "correct the AC, or confirm the citation-closure behavior is intended and rewrite the assertion." That file is `D` in this diff. The execution directive at docs/cycles/retire-bundle-methodology-20260827T0120.md excluded OPEN-ITEMS.md from its grep and forbade editing governed documents, so the change is compliant in leaving it — this is a gap in the cycle's closure, not a violation by the change.
Consequence: An open item stays on the board asking a reviewer to fix an assertion in a file that no longer exists, and it is not merely stale: the work it asks for is now unnecessary in a way the item cannot show. AC-BA-7 retired the positional closure mode outright, so neither of its two proposed fixes is the right answer any more. A future session reading OPEN-ITEMS.md top-to-bottom either burns a cycle rediscovering that, or — worse — reads it as evidence that a red test still exists somewhere.
Fix: A follow-up marking the item **RESOLVED** by removal under DEC-000210, naming AC-BA-7 as what superseded both proposed fixes. This is one edit to a tracker file and does not need a review cycle of its own.

## RBM-2 — non-blocking
Claim: DEC-000210 orders the retirement of `bin/bundle-methodology` "and its tests"; `bin/tests/test_bundle.py` is not one of its tests, so one of the three removals rests on the execution directive rather than on the governing decision.
Location: decisions/log.md DEC-000210 ("`bin/bundle-methodology` and its tests are retired, not repaired"); the third removal at docs/cycles/retire-bundle-methodology-20260827T0120.md:20
Evidence: Inferred by reading, from files read at 0ee3882. `bin/tests/test_bundle.py` tests `bin/bundle`, the surviving tool: its two methods invoke `bundle base` and `bundle operating-model.md`, and its module docstring states outright that it is an "AC-BN-10: `bin/bundle` — regression guard." Its removal is authorized instead by the execution directive, which grounds it in AC-BA-7, and that grounding checks out — bin/bundle:312 carries the comment "AC-BA-7: the positional path-following closure mode is removed," and bin/tests/test_bundle_audience.py:690-696 asserts the replacement behavior (`bundle ENTRY` exits non-zero, with a message pointing the caller at `--audience`). No coverage is forfeited; the two deleted tests asserted a mode that the surviving suite now asserts is rejected.
Consequence: The decision log does not record why the third file went. A reader reconstructing the branch from decisions/log.md alone finds authority for two of the three removals and none for the third, and must locate a cycle directive under docs/cycles/ to close the gap. Since decisions/log.md is the layer this repository treats as durable and docs/cycles/ is not bundled into any audience (AC-BA-1), that reconstruction path is not available to a reader working from a bundle.
Fix: Either an addendum line on DEC-000210 naming `bin/tests/test_bundle.py` and AC-BA-7 as the third removal's authority, or the same statement in the release record for this package. Neither reopens the decision.

## RBM-3 — non-blocking
Claim: The execution directive this branch lands states a post-removal test count that is arithmetically wrong — it expects 439 where 424 is correct — and states it as a stop condition's neighbor.
Location: docs/cycles/retire-bundle-methodology-20260827T0120.md, `VERIFY` line ("Expected: 439 tests, failures=0, skipped=2")
Evidence: Verified by running and by reading. Observed: `Ran 424 tests`, `OK (skipped=1)`. The directive's own `BASELINE` line expects 441 at 0ee3882. Counting `^\s+def test_` in the two deleted test files at 0ee3882 gives 2 in test_bundle.py and 15 in test_bundle_methodology.py, so 441 − 17 = 424. The 439 figure subtracts only test_bundle.py's two tests and ignores test_bundle_methodology.py's fifteen. The observed result is the correct one; the expectation is what is wrong.
Consequence: The directive is landed on this branch and merges with it, so the wrong number becomes part of the record of how this change was verified. Concretely, an execution session obeying it literally reaches a 424-vs-439 mismatch on a correct change, and the directive's posture ("stop and report, do not recover") then invites either a stop on a healthy tree or, worse, an attempt to reconcile the count by restoring something. Any later re-run against this directive hits the same trap.
Fix: Correct the `VERIFY` expectation to 439 → 424 with the 15+2 arithmetic shown, or annotate it in the release record. Note that the directive is verbatim-landed by protocol, so amending it is itself a decision about whether landed directives may be corrected in place.

## RBM-4 — non-blocking
Claim: An agreed PRD states, as an *observed* claim, an inventory of `bin/` that this change makes false.
Location: specs/directive-tooling.md:2051
Evidence: Verified by running (grep) and by reading the surrounding passage at :2045-2055. The line reads "(*observed* — `bin/` holds `aimeta`, `bundle`, `bundle-methodology`, `check-frontmatter`, `cycle-open`, `flip-agreed`, `install-hooks`, `migrate-frontmatter`, `tests`, and only `cycle-open` emits a directive skeleton)", in a document whose frontmatter is `status: agreed`. `bin/bundle-methodology` is absent at 999675b. Four other files outside bin/ also still name the tool — docs/packages/bundle-methodology-spec.md (a whole package spec, TP-2, for the now-absent tool), docs/packages/package-a-spec.md:466/:485/:487, docs/global-retro-inbox.md:227, and docs/history/inventory-20260820.md:187, which instructs a reader to "Regenerate with `bin/bundle-methodology --out ~/Downloads` before the next CoS session." The last two are dated records this repo does not retrofit; ACCEPTED-RISKS.md:16 also names the tool but as provenance ("Inherited from `bin/bundle-methodology`, which has carried the same behavior since it shipped"), which remains true as history and is not a defect. The execution directive forbade editing outside bin/, so none of this is a violation by the change.
Consequence: An agreed document carries a claim explicitly marked *observed* that no longer holds, which is precisely the drift this repository's status discipline exists to catch — the marking is what makes it worse than ordinary staleness, because a reader is told the claim was checked. Separately, docs/packages/bundle-methodology-spec.md now specs a tool with no implementation and no note that it is retired.
Fix: A follow-up package updating specs/directive-tooling.md:2051's inventory and marking docs/packages/bundle-methodology-spec.md superseded by DEC-000210. specs/directive-tooling.md is agreed, so its edit takes whatever route the metadata policy prescribes for an agreed document.

## RBM-5 — observation
Claim: The diff is not three removals and nothing else; it also adds a 37-line file.
Location: docs/cycles/retire-bundle-methodology-20260827T0120.md, added at c695d88
Evidence: Verified by running. `git diff --stat 0ee3882 999675b` reports four paths — three `D` and one `A` — for 37 insertions and 777 deletions across two commits, c695d88 (the directive landing) and 999675b (the removal). The addition is the execution directive itself, landed by that directive's own FIRST ACT protocol, and the gate directive names the same file @ c695d881 as its execution directive — so the addition is expected under the protocol even though it contradicts the gate's literal "exactly three files removed and nothing else changed."
Consequence: Nothing goes wrong at merge — landing the directive on the branch is the standing convention, and docs/cycles/ is excluded from every audience bundle. The entry exists so the gate's "nothing else changed" is not recorded as confirmed when the diff has four paths in it.
Fix: None required. Read the scope line as "exactly three files removed, plus the cycle directive the protocol lands."

## RBM-6 — observation
Claim: The suite's skipped count fell from the directive's expected 2 to an observed 1, and the change did not cause it.
Location: `$TMPDIR/gate-tests.txt` (`OK (skipped=1)`); the surviving skips at bin/tests/test_check_frontmatter.py:1440 and bin/tests/test_land.py:1967
Evidence: Verified by running and by reading. Neither deleted test file contained a skip at 0ee3882 — grepping both for `skip` returns nothing, so no skip was removed by this change. The only two skips in the tree are runtime-conditional and both live in surviving files; test_check_frontmatter.py:1440 skips on `"filesystem is case-sensitive; AC-CF-20 is not reachable here"`, which is a property of the machine, not of the ref.
Consequence: None to the change. Recorded because skipped=1 differs from the directive's stated expectation, and a reader comparing the two lines without this note could read the gap as a lost test.
Fix: None. If the count is to be used as a gate at all, it should be stated as a range or the environment-conditional skips named alongside it.

Dave should inspect: RBM-2 — whether retiring `bin/tests/test_bundle.py` falls inside DEC-000210's grant, or wants a line in the decision log; and RBM-1 — closing the now-moot OPEN-ITEMS.md entry, which is the one piece of cycle closure this change could not do under its own directive.
