# Review: process/retro.md — process-sweep-read-20260907T060000Z

Verdict: changes-required
Reviewed: process/retro.md @ fd6888ca2472e4d020f913fc4b0463e0a617efc4
Baseline: process/retro.md @ 7549f7efed0ed961536bf61620d621ebe6fbb81b
Reviewer: frontier read, fresh session, did not draft the swept text
Date: 2026-09-07
Scope: the five questions the directive names, over all 67 lines. The document cites no row, so the citations pass was run instead against the six rows under topic `retro` at the base ref — R1237, R1241, R1244, R1246, R1249, R1270 — to test whether a surviving sentence restates one instead of citing it.
Cross-checked: docs/cycles/process-sweep-20260907T040000Z.md; rules/R1408.md, the no-retrofit rule for review artifacts, as the nearest analogue to the retro sentence RE-01 names.
Not inspected: the nine untracked retro files in the clone's main working tree; they are data, not part of this delta. The sweep session's report is not committed on this branch, so the LOSS test's "listed in the sweep report's six intake candidates" limb could not be applied.
Findings: 2 — 1 blocking, 1 non-blocking
The human should inspect: RE-01. The sentence that protected retros written before this document was adopted is gone, and retros written under the old shape exist.

## Verdict (citations): ready

No bracketed id appears in the document, so no citation can fail to resolve. On the second limb of the test — a sentence restating a row instead of citing it — four of the six `retro` rows are echoed by the schema block's placeholder text: R1237 in the Evidence placeholder, R1241 in Durable insights, R1249 in Standing preferences, R1270 in Candidate methodology changes. Each echo is inside the form block, which the sweep's first criterion says to keep verbatim where the document is a form. A form's field descriptions are the form; they are not prose restating a row, and I record no finding for them. No sentence outside the block restates a row.

## Verdict (loss): changes-required

See RE-01. Everything else the diff cut is carried. The four-field frontmatter table, the six-item sections list, and the four date bullets all fold into the schema block and the one date paragraph without dropping a field, a section, or a fallback: the derivation of `date:` from the last dated artifact, the `source:` fallback, the `generated:` substitution with its statement, and the both-are-correct case are all present. The sweep moved one clause the other way — "a correction the human made inside the session belongs here" was §2 prose in the baseline and is now inside the Evidence placeholder — which is a relocation within the document, not an addition. The synthesis mechanics keep all four obligations, including computing the input set from the filename-form match rather than the directory listing, and stating the count. "What invites a retro" was an open question in the baseline and is now the document's first line as an act.

## Verdict (residue): ready

Every sentence outside the form block is an act, a condition, or a filename form. No persuasion survives: "Status of this draft", "What this document is", "A retro is an input to change, never a change itself", the frontmatter's "carries no lifecycle semantics" gloss, and "What this document does not decide" are all gone.

## Verdict (keys): ready

`[chief-of-staff, writer, copy-editor, critic]`, unchanged, with `corpus: [software, writing]` and `session: [decision]`. The document names no role in its body — its acts are attributed to "a decision session" — so no role is contradicted by an act it does not perform, and no act names a role absent from the list. The four are the decision-session roles across both corpora and each of them runs the retro this document forms.

## Verdict (loadable): ready-with-findings

See RE-02. Only one heading survives, "## The schema", and it carries its block; nothing points at a heading that is gone.

## RE-01 — blocking
Claim: The sentence exempting retros written before this document was adopted from its form was cut, and nothing in the new text replaces it.
Location: process/retro.md — absent; baseline process/retro.md @ 7549f7e, under "## Storage"
Evidence: Read against the diff. The cut sentence: "A retro or synthesis written before this document was adopted stands as it was written: it is data, not a governed document." No surviving sentence says it and the document cites no row. The corpus states the analogous rule for the other artifact class — R1408, "Never rewrite an existing review artifact to match a later version of this schema" — and process/review-artifact.md cites it at the same ref; there is no equivalent row for retros. The sweep report is not committed, so whether this is among its six intake candidates could not be checked.
Consequence: An agent asked to bring the retro corpus into shape now reads a document that states a schema and says nothing about what came before it, and rewriting a retro to match is the reasonable inference. That destroys the record the retro exists to be — the same drift R1408's Human note names as what this repo exists to prevent. The exposure is live: nine retro files sit untracked in the clone's main working tree at this session's start, written before this document.
Fix: Restore the sentence, or cite a row stating it once such a row exists — the parallel with R1408 makes it a clean intake candidate covering both artifact classes.

## RE-02 — non-blocking
Claim: The document uses `date:`, `source:`, and `generated:` in its second paragraph, twelve lines before the schema block that introduces them.
Location: process/retro.md:12-16, against the block at 31-38
Evidence: Read by inspection against the diff. The baseline opened its frontmatter section with a four-row table naming each field and its value, then discussed the dates. The sweep cut the table — the schema block carries the same content — but kept the dates paragraph in its original position, ahead of the block.
Consequence: An agent reading the document top to bottom meets three field names, and a rule for resolving disagreement between two of them, before anything says what the fields are or that there are exactly four. Recoverable by reading on, but the document no longer introduces its own vocabulary before using it.
Fix: Move the dates paragraph after the schema block, or move the schema block up.
