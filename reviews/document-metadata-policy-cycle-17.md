# Review: policies/document-metadata-policy.md — cycle 17

Verdict: ready-with-findings
Reviewed: policies/document-metadata-policy.md @ 3e0b9f48fab2af32608f4b0254604a654cedee7d
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-23
Scope: the whole file, all 296 lines, read whole against all eleven criteria of docs/global-context/review-rubric.md @ 3e0b9f4. Two passes. (a) The change since 12ecaeb, verified by running `git log --oneline 12ecaeb..HEAD -- policies/document-metadata-policy.md` (one commit, 6211312) and `git diff 12ecaeb HEAD -- policies/document-metadata-policy.md`: three globs added to the in-scope set (`docs/global-context/**`, `engagements/**`, `prose-criteria.md`), a `docs/history/**` bullet added to the out-of-scope set, `session:` added to Required fields, and `order:` added to Conditional fields. No other line changed. (b) All eleven criteria re-applied to the current text, with the widened set resolved against the repository. The file is byte-identical to its state at cf8b161.
Cross-checked: docs/global-context/core.md (preamble :10, Standing 2 :15, Standing 4 :17, Acting 13 :32, Acting 14 :33), docs/global-context/decision-layer.md, docs/global-context/review-rubric.md (:20-22, :24-25), roles/context-quality-reviewer.md (:13-18, :25-30), skills/review-artifact.md (:42-54, :63, :80-81), prose-criteria.md, `bin/aimeta/scope.py` (:18-54), `bin/aimeta/repo.py` (`role_slugs`, :265-287), `bin/bundle` (`_sort_key`, :198-202), `bin/check-frontmatter` (:1-20, `check_worktree`) — all @ 3e0b9f4; plus reviews/document-metadata-policy-cycle-13.md (D2), -cycle-14.md, -cycle-15.md, -cycle-16.md (DMP-5). The in-scope set was resolved mechanically: the twelve globs at :26-37 enumerate 51 markdown files, and `bin/check-frontmatter --all` reports "51 file(s) matched, from 12 configured glob(s)" — the same 51. Every in-scope file's `status:` and `last-reviewed:` was read and each pointer resolved to its artifact's `Verdict:` line. Baseline: `bin/check-frontmatter --all` exits 0; `bin/tests/run` exits 1 with 2 failures of 399, both the accepted AC-BN-10 pair.
Not inspected: whether the expedited and doc-only conditions are the right conditions, and whether the gate-document class is the right class — both are Dave's and were dictated; the eleven-item enumeration at :154-170 was checked for completeness against the widened scope, not for correctness. Also not inspected: `bin/` behaviour beyond reading `scope.py`, `repo.py`, `bundle` and `check-frontmatter` and running the two baseline commands — no claim is made that the hook enforces what :250-252 says it enforces, only that the in-scope set the code resolves matches the one this file states; the grandfather clause's disposition list, which `bin/aimeta/repo.py:disposition_paths` reads from a path this cycle did not resolve, and which is inert here because no in-scope document carries `status: agreed` with `last-reviewed: null`; the other 50 in-scope files, except where one was read as the other side of a cross-check row; the content of the 45 review artifacts cited by `last-reviewed` beyond each one's `Verdict:` line.
Findings: 6 — 3 non-blocking, 3 observations
Prior cycle: reviews/document-metadata-policy-cycle-16.md
Dave should inspect: DMP-6 — the two lighter paths specify their `last-reviewed` pointer to the character and the default path does not, and 45 of the 47 agreed documents in the repository now point at a sweep artifact whose verdict is `changes-required`; whether that satisfies `agreed` is unstated here and denied by roles/context-quality-reviewer.md. DMP-11 — cycle 13's D2 is still open at HEAD and was deferred to you then for the same reason it is not re-filed now.

## Closure of the cycle-16 finding

DMP-5 (the gate-document class list does not name `roles/context-quality-reviewer.md`) — **closed**, verified by reading. The path now appears at :166, beside the four peer role documents. DMP-7 below is the same shape of tail, opened by a different change.

## DMP-6 — non-blocking
Claim: The two lighter paths specify what `last-reviewed` must point at to the character; the default reviewer-gated path does not, and says nothing about the artifact's verdict or whether it must cover the document.
Location: policies/document-metadata-policy.md:84-96 (`last-reviewed:` under Required fields), against :190-202 ("The record") and :250-254.
Evidence: Verified by running. For the expedited and doc-only paths this file states the pointer form (`reviews/expedited-log.md @ <sha>`), requires that "the SHA cited in `last-reviewed` must appear in an entry in the log — same commit, same form, so a checker matches pointer to entry character-for-character or normalizes both through `git rev-parse`", and states that the log is append-only. For the default path it states only "the path to the review artifact in `reviews/` plus the reviewed commit SHA" and "`status: agreed` requires a non-null `last-reviewed`." Resolving every in-scope pointer at HEAD: of 51 files, 47 carry `status: agreed` and 4 carry `draft` (the four this cycle reviews). Of the 47, 45 cite one of `reviews/corpus-regate-cycle-1.md` through `-cycle-4.md`, each of whose `Verdict:` line reads `changes-required`; 2 cite `reviews/corpus-regate-cycle-6.md`, whose verdict reads `ready`. None cites an artifact whose verdict reads `ready-with-findings`. The four artifacts the 45 cite state a file count on their `Reviewed:` line, each followed by "(enumerated below)" — reviews/corpus-regate-cycle-3.md:4 reads "14 files @ edd8015 ... and the 51-file corpus set @ edd8015 for the two corpus-wide sweeps" — so the document is covered in the artifact's body while the verdict on the line is the sweep's, not the document's. The two `ready` pointers are the exception: reviews/corpus-regate-cycle-6.md:4 names both of its documents. Criterion 11 — an underspecified condition on the transition this file exists to govern.
Consequence: `bin/check-frontmatter --all` exits 0 over all 51 files, so the enforced condition is satisfied; the stated condition in the role document that gates these documents is not, because roles/context-quality-reviewer.md:27-28 requires "this role's verdict is `ready`" and 45 of 47 pointers resolve to `changes-required`. Read literally the repository's agreed state is illegitimate; read charitably the reader must infer that a sweep's overall verdict does not bind its members and that a per-file result is to be dug out of the sweep's body. This file is where that inference should be unnecessary, because it is the file that says what a pointer means, and it is the file a checker is written from.
Fix: Two sentences under `last-reviewed:`: state that the cited artifact must state, in its own scope, that it reviewed this document at the cited SHA; and state which verdicts satisfy `agreed`, or state explicitly that the verdict is Dave's to weigh and this field records the pointer only. The second half is a decision, not a fix.
Related: CQR-6

## DMP-7 — non-blocking
Claim: The scope widening brought `docs/global-context/**` into the enforced set and the gate-document class list was not extended to the gate documents it contains.
Location: policies/document-metadata-policy.md:154-170 (the class enumeration under the expedited path's condition 3), against :33 (`docs/global-context/**`) and docs/global-context/review-rubric.md:10-11.
Evidence: Verified by running. `git diff 12ecaeb HEAD -- policies/document-metadata-policy.md` shows `docs/global-context/**` added to the in-scope set at 6211312 with the class list untouched. Condition 3 defines the class as any document stating "a gate, a hard stop, or an enforcement rule governing how work or documents are reviewed, agreed, or released." docs/global-context/review-rubric.md:10-11 states "A file passes when it satisfies all eleven or is retired" — the pass condition of the review gate itself — and is absent from the list. skills/review-artifact.md:63 and :80-81 define the verdict vocabulary the gate turns on and state that "`ready` means ready for Dave's agreement"; it is likewise absent, while two other skills (`skills/spec-review-cycle.md`, `skills/conversation-retro.md`) are named. The list enumerates five policies, five role documents, two skills, one boundary document and `operating-model.md` — it is an enumeration of the review machinery, and no `docs/global-context/` file appears in it, because none was in scope when it was written. Criterion 11, and core.md:32 (Acting 13).
Consequence: Bounded, for the same reason DMP-5 was bounded: :154 opens "The class includes, at minimum", :170-175 states "The list is normative where it names a document, and cannot bound the class," and :152 closes condition 3 with "**When it is unclear, it is ineligible.**" The rule yields the right answer without the entries. The residue is that the list is the artifact a reader consults — it exists because the rule alone was judged insufficient — and it now omits the file that states the criteria the gate applies. A Chief of Staff proposing a ten-line expedited edit to the rubric reads a list that does not name it.
Fix: Add `docs/global-context/review-rubric.md` and `skills/review-artifact.md` to the class list. Whether `docs/global-context/core.md` and `docs/global-context/decision-layer.md` also belong is a larger question — core.md:15 states a hard stop on agreement and release — and is Dave's.
Related: DMP-5

## DMP-8 — non-blocking
Claim: `order:` is defined here as unconditionally optional and required by the review rubric under a condition, so the two files in the same bundle state different obligations for the same field.
Location: policies/document-metadata-policy.md:114-116, against docs/global-context/review-rubric.md:20-22.
Evidence: Verified by running. `order:` entered this file at 6211312 as "optional on any document"; criterion 2 of the rubric, unchanged since before the rubric's cycle 1 (`git diff 8402c23 HEAD -- docs/global-context/review-rubric.md` touches only the preamble), requires "`order:` where its position in a bundle matters." `bin/bundle:198-202` sorts unordered files after ordered ones and then by path, so no file's position is ever undefined and the rubric's condition distinguishes no state. Three in-scope files carry `order:` at HEAD (core.md `0`, decision-layer.md `1`, prose-criteria.md `11`) and 48 do not. This file carries `audience: [all-roles, human]`, so it is in every bundle; the rubric is in one.
Consequence: A reviewer can file a criterion 2 finding against any of the 48 files without `order:` and the author can defeat it by citing :114; the reverse is equally available. Neither text is wrong on its own and the reader has to infer the layering — that this file states the enforcement floor and the rubric states a review standard above it — which neither says.
Fix: One clause here: "`order:` optional on any document; the review rubric states when a document is expected to carry one." Or resolve it in the rubric, per RR-6. One of the two, not both.
Related: RR-6

## DMP-9 — observation
Claim: The in-scope set this file defines and the reviewable set the Context Quality Reviewer's role document defines diverge by exactly `vendors/**`.
Location: policies/document-metadata-policy.md:32 (`vendors/**`), against roles/context-quality-reviewer.md:15-17 ("the frontmatter in-scope set; excluding history, batons, cycles, reviews, retros, trackers, the decision log, and vendor-tooling files").
Evidence: Verified by running. Of the role document's eight exclusions, seven match no in-scope glob — `docs/history/**`, `docs/batons/`, `docs/cycles/`, `reviews/`, `retros/`, the tracker files at the repository root, and `decisions/log.md` — and subtract nothing. The eighth reaches `vendors/README.md` and `vendors/claude-code/environment-config.md`, both of which are in the 51 this file's globs enumerate.
Consequence: Recorded here, not filed here. This file's in-scope set is the enforced ground truth and `bin/aimeta/scope.py` parses it from :26-37 at runtime; the defect is in the role document's wording, and is filed there.
Fix: None in this file, unless Dave decides the other way and `vendors/**` leaves the in-scope set.
Related: CQR-5

## DMP-10 — observation
Claim: The backticked paths in the Scope section are a criterion 3 defect under the criterion's third sentence and are the file's operative content under its first two.
Location: policies/document-metadata-policy.md:26-37 and :154-170, against docs/global-context/review-rubric.md:24-25.
Evidence: Verified by reading `bin/aimeta/scope.py:29-54`, which extracts every backticked value from the bullets following "In scope (frontmatter required)" and raises `ScopeError` rather than falling back to a built-in list. The twelve globs are enforcement configuration, not references to what another document states. Two prior cycles resolved this by suppression rather than by the criterion — reviews/document-metadata-policy-cycle-16.md's Not inspected line records "the structural path references this file carries as a policy about paths, which the prior two cycles accepted as structural and which this cycle's directive excludes from report." This cycle's directive carries no such exclusion.
Consequence: None to this file, which cannot state its scope any other way. The defect is that criterion 3 has no carve-out and has now had to be switched off by directive in three consecutive cycles; that is filed against the rubric.
Fix: None here.
Related: RR-7

## DMP-11 — observation
Claim: Cycle 13's D2 — the criterion 6 finding that roughly a quarter of this file is argument rather than rule — is still open at HEAD and is not re-filed.
Location: policies/document-metadata-policy.md:12-16 (§Principle, restated normatively at :269-274 §Excluded fields), :47-48, :51-52, :64-65, and :184-185 against :250-254, where the same "enforcement checks none of this" argument is made twice.
Evidence: Verified by reading. The file is 296 lines at HEAD; reviews/document-metadata-policy-cycle-13.md:46 filed D2 as blocking with a fix naming thirteen passages and four to keep in compressed form, and its header line 12 deferred the call: "Cutting it is a judgment about whether the argument still has to travel with the rule, and that judgment is yours, not the reviewer's." Cycles 14, 15 and 16 did not carry it and the passages are unchanged. `git log -S "Git is the versioning system"` shows §Principle unchanged since the file was created at cbddf79.
Consequence: None new. Re-filing a finding a prior cycle referred to Dave, and that Dave has not ruled on, would be re-litigating rather than reviewing.
Fix: None proposed here; D2's fix stands as written.
