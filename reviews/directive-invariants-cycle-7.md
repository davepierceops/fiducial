# Review: skills/directive-invariants.md — cycle 7

Verdict: ready
Reviewed: skills/directive-invariants.md @ 6a30ff19f2394642e4f387c518262486da6f8826 (blob `80d572aad5c80eb6531e0b8ff7f5b9ae84571829`)
Reviewer: Context Quality Reviewer (execution session). This session authored no text under review, took no decision consolidated or amended here, and holds no prior position on either document. Its only writes are `docs/cycles/directive-authoring-consolidation-gate-3-20260831T194500Z.md`, this artifact, and `reviews/directive-authoring-cycle-6.md`.
Date: 2026-08-31
Scope: a confirmation gate, not a full pass, and the second one over this document. (1) Confirmation of cycle 6's F-1 against amendment d's E2, including that both shapes F-1 named — the no-refs fetch and the ref-reporting push — fall inside the exception as worded, in O-1 below. (2) The three cross-checks named on the Cross-checked line, all re-run at the reviewed blob. (3) The text the amendment introduced, read as new, with anything failing a criterion raised as a finding citing it — there is none, and O-1 records what was tested for. No new decision is taken here and nothing else in this document is re-litigated: cycle 5's O-2 (the third clause's wider bound, which the TRD's §3.2 condition 2 states wider) is unchanged and is recorded as persisting rather than re-raised, and cycle 5's O-3 and O-4 were closed at cycle 6 and are not reopened. The amendment was reconstructed by diffing this file between cycle 6's reviewed ref `efaad5f6a4db5c6005b81f2af9f1bd53a1063872` and the reviewed ref (*observed*, by running): the diff touches this file in exactly the one passage E2 names — the last sentence of the `## Stop conditions` region, replaced and the paragraph reflowed at 79 characters — and nowhere else; `git diff --stat` over the same range reports three files, this one, the other document under review, and the amendment's own directive file. Frontmatter is untouched, `status: in-review`, `last-reviewed: null`, audience unchanged. `efaad5f` is also the merge-base of the reviewed ref with `7e055d7393d96deabd5c50c6ca01e52047e306e4`, the commit carrying cycle 6's artifacts (*observed*, by running `git merge-base`), so the amendment was made against exactly the tree cycle 6 reviewed.
Cross-checked: three named cross-checks, each with its result, all *observed* by running. **(1) The disposition-prompt fence against `skills/directive-authoring.md`'s first bullet — equal, at 669 bytes each.** The first fenced block of this file's `## Working-tree disposition prompt` section (body `:85`–`:93`, fence markers at `:84` and `:94`) and the authoring skill's first bullet (`:13`–`:21`) were each flowed by the method the prior cycles used — whitespace runs collapsed with `re.sub(r"\s+", " ", s).strip()`, lines joined, the bullet's leading `- ` stripped — and compared: `fence bytes: 669`, `bullet bytes: 669`, `equal: True` from `f.encode() == b.encode()`. The expected 669 is met on both sides. Amendment d moved neither copy — E2's target is the `## Stop conditions` region and E1's is the other document's second bullet — so the byte-equality §3.2's design requires of the pair holds at the value cycle 6 established, and the fence line inventory of this file is identical to `efaad5f`'s, so no fence was added, removed or reordered. **(2) The shipped `unfenced_labelled_statements` over both documents — no hits in either**: `[]` over `skills/directive-invariants.md` and `[]` over `skills/directive-authoring.md`, the function imported from `bin/tests/test_directive.py` and called over each file's text with the shipped `DISPOSITION_LABEL` (`'WORKING-TREE DISPOSITION'`). This file's zero is the substantive one — §3.2's condition 2, the property of the one file that makes G3 hold — and it is unchanged by the amendment, which touched no line carrying the token: every occurrence was enumerated with its eligibility (*observed*, by running `eligible_lines` over the file and printing each line carrying the token). `:99` and `:103`, the two worked forms, and `:215`, the emitted literal, are all masked as fenced and therefore ineligible; `:219` is eligible but its leading content after stripping is "the label token `WORKING-TREE DISPOSITION`, ...", which does not lead with the token, so it is not a hit. Four occurrences, no hit, as cycle 6 recorded. The other document's zero is vacuous; the bare token does not occur there at all. **(3) Criterion 12 between the amended `## Stop conditions` sentence (`:154`–`:155`) and the `## Base verification` sentence (`:110`–`:114`) — they compose; no divergence to file.** The two sentences state different rules over the same event and cannot yield opposite instructions. The Stop conditions sentence is a predicate on the trigger word its own region uses three lines earlier: "On any failed command ... stop and report" (`:151`–`:152`), then "A remote operation that exits successfully is not a failed command, whatever a credential helper writes to stderr" (`:154`–`:155`) — it classifies, and its test is the operation's exit status. The Base verification sentence is evidentiary: "Judge every remote operation — fetch, push, ls-remote — by the refs it reports, not by a credential helper's noise on stderr" (`:111`–`:113`) — it selects which output carries information about where the base is, in a region whose surrounding sentences ask whether the base has moved (`:110`–`:114`). Neither sentence says exit status establishes the base; neither says refs establish failure. Their one overlapping claim — that credential-helper stderr is not evidence — they state the same way, the second more broadly ("whatever a credential helper writes") than the first ("a credential helper's noise"), which subsumes rather than contradicts. Applied together to the three shapes this session ran, they agree at every step: each fetch and the push exited 0 and so was not a failed command, and the base was then established by comparing `origin/directive-authoring-consolidation` to the reviewed ref rather than by any stderr line. The split is the one an agreed governed file already draws: `specs/bin-land.md` G4 states "Push, and take git's exit status as the push's outcome ... No behaviour keys on the content of stderr, in either direction", naming the same `fatal: failed to store: 100001` line as the case it exists to be indifferent to, and G5 puts verification on `git ls-remote` and blob-SHA comparison — exit status for the outcome, refs for the content — while `specs/bin-land-trd.md` §3.6 B2 names "the helper whose stderr output PRD G4 exists to be indifferent to". E2 moved this file's exception from a refs test onto an exit-status test and thereby onto the same axis G4 states, so the amendment narrows a criterion-12 gap rather than opening one. A scan of `skills/`, `specs/`, `policies/`, `roles/`, `docs/global-context/` and `decisions/` for "failed command", "refs it reports" and "exits successfully" returns only this file's two sentences, so no other governed file states either rule in a form that could disagree (*observed*, by running). Also cross-checked: `skills/directive-authoring.md` @ `6a30ff1`, whole; `docs/global-context/review-rubric.md`; `skills/review-artifact.md`; `roles/context-quality-reviewer.md`; `specs/bin-land.md` G4 and G5 and `specs/bin-land-trd.md` §3.6 B2, both `status: agreed`; `reviews/directive-invariants-cycle-6.md` and `reviews/directive-authoring-cycle-5.md`, both at `7e055d7393d96deabd5c50c6ca01e52047e306e4` via `git show`; the amendment record `docs/cycles/directive-authoring-consolidation-d-20260831T190000Z.md` @ `cd4135c2dd82a76688590b385d6a8f3c86a01b2c`; and, read and run, `bin/aimeta/mdmask.py` and the `unfenced_labelled_statements` check in `bin/tests/test_directive.py`.
Not inspected: the `bin/` test suite — not run by this directive, and reported as not run, so no claim is made here about whether any test passes; the check named above was imported and called over the two files, which is a claim about that function and not about the tests' status. `bin/directive` and `bin/check-directive` were not invoked and no skeleton was generated; no region was emitted and no directive was generated from this file in this session. This is a confirmation gate, so it is **not** a full twelve-criterion pass over the whole document: the per-region placeholder cross-check cycle 5 ran was not re-run, the disposition label's three parts were not re-checked against the TRD part by part, and the seventeen region bodies, the four lint sections, `## Marker syntax`, `## Preamble markers` and `## Match phrases` were re-read only as far as the three cross-checks above and the structural constraints in O-1 reach. `bin/aimeta/invariants.py` and `bin/aimeta/elements.py` were not imported or run this cycle, so no claim is made here about `parse_sections`, `Document.label_literal`, `Document.matches_label`, `Document.sole_tree_sentence` or `Document.preamble_markers`. `specs/directive-tooling-trd.md` was not re-consulted this cycle: cycle 6 closed the §3.3 and §3.4 axes and amendment d moved neither, so no claim is made here about the TRD's current text, including §3.2's condition 2, which cycle 5's O-2 left standing. Also not inspected: `docs/global-context/core.md` and `docs/global-context/decision-layer.md`; `policies/document-metadata-policy.md`; the PRD `specs/directive-tooling.md` and every acceptance-criteria document it and the TRD name; `specs/bin-land.md` and `specs/bin-land-trd.md` other than the passages named above; `decisions/log.md`; `bin/cycle-open` and `bin/check-frontmatter`'s source; the `docs/cycles/` corpus except the amendment record named above; whether any rule here is correct as engineering judgment, which this role cedes; and every governed file not named on the Cross-checked line.
Findings: none — 1 observation, and no blocking or non-blocking finding.
Prior cycle: reviews/directive-invariants-cycle-6.md

## O-1 — observation
Claim: Cycle 6's F-1 is resolved by amendment d's E2, in the form its Fix line dictated, and the landed sentence reaches both shapes F-1 named — the fetch that reports no refs and the push that reports them — because its condition is the exit status every remote operation returns; the edit introduces nothing that fails a criterion.
Location: `skills/directive-invariants.md:154-155`, against `efaad5f6a4db5c6005b81f2af9f1bd53a1063872`, and `:110-114`
Evidence: *Observed*, verified by reading the one-file diff between cycle 6's reviewed ref and the reviewed ref and by running in this session. **Cycle-6 F-1 (the exception conditioned on refs, which the no-ref fetch does not meet, leaving the Stop conditions trigger the only rule standing over a fetch that exited 0) — resolved.** E2 took the first of F-1's two offered shapes, the one the Fix named as covering both cases with one sentence, and landed its dictated wording exactly: the `## Stop conditions` region's paragraph now ends "...do not retry with different flags, and do not delete or create any ref to recover. A remote operation that exits successfully is not a failed command, whatever a credential helper writes to stderr." (`:150`–`:155`), reflowed at 79 characters. The two things E2 was required to leave alone are unchanged: the marker line `STOP CONDITIONS` (`:148`) and the `{{reviewed_ref}}` placeholder (`:150`), so the placeholder set declared for this region — `reviewed_ref` — still matches its body; and the `## Base verification` region is byte-identical to `efaad5f`'s at `:106`–`:114`. **Both shapes fall inside the exception as worded.** The condition is no longer a property the operation may decline to print but the exit status it always returns, so the refs an operation reports have stopped being load-bearing for the classification. Tested against three live shapes in this session, each with stdout and stderr captured and the status read (*observed*, by running): `git fetch origin` exited `0`, wrote exactly one stderr line, `fatal: failed to store: 100001`, and reported no refs at all — inside the exception; `git fetch origin directive-authoring-consolidation` exited `0`, wrote the same stderr line and reported `From https://github.com/davepierceops/fiducial` with ` * branch directive-authoring-consolidation -> FETCH_HEAD` — inside the exception; `git push origin directive-authoring-consolidation-gate-3` exited `0`, wrote the same stderr line and reported ` * [new branch] directive-authoring-consolidation-gate-3 -> directive-authoring-consolidation-gate-3` — inside the exception. The two shapes F-1 showed falling on opposite sides of the refs condition, seconds apart and both harmless, now fall on the same side, which is what F-1's Consequence said the region had to achieve. The wasted stop F-1 described is closed at the point it fired: an executor running the base fetch reads a line beginning `fatal:`, finds the sentence in the region that governs the trigger, and is licensed by the exit status it already has, without needing refs the command did not print. Criterion 11 is satisfied where F-1 said it was not: the condition is now a mechanical property every command reports, so the region leaves no state unresolved and no judgement to inference. The credential-helper clause is broader than the sentence it replaced, which named no stderr at all, and it is the clause that makes the rule readable without the Base verification region — the structural point E2 and its predecessor E4 were both aimed at. **Nothing new fails a criterion.** The amendment's own constraints were verified rather than assumed (*observed*, by running): the `##` heading list is identical to `efaad5f`'s, so no section is added, removed, renamed or reordered; the fenced-line inventory is identical, so no fence is added, removed or reordered; no region section's first body line changes, the amended region's being `STOP CONDITIONS` at `:148`; the `## Report format` region still carries six `- ` bullets, unchanged from cycle 6's count; frontmatter is unchanged, `status: in-review`, `last-reviewed: null`. The reflow holds the file's 79-character prose wrap: the amended paragraph's six lines measure 76, 77, 76, 75, 79 and 71 characters, and the single line in this file over 79 (`:103`, at 80, the worked sole-tree form inside a fence) is byte-identical to `efaad5f`'s and lies outside the amended passage, so the amendment introduced no over-long line. Criterion 12 was tested on the pair the sentence now sits beside and on the wider corpus, and is not failed on either: cross-check 3 above records the composition and the agreement with `specs/bin-land.md` G4 and G5.
Fix: None. The confirmation is clean and nothing on this side is left open.
