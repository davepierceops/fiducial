# Review: policies/document-metadata-policy.md — cycle 19

Verdict: changes-required
Reviewed: policies/document-metadata-policy.md @ 9160a865fc7070775fc17e9b50c55bc5610318df
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-29
Scope: policies/document-metadata-policy.md, read whole (291 lines) at 9160a865fc7070775fc17e9b50c55bc5610318df, against all twelve criteria of docs/global-context/review-rubric.md at origin/main (1bb41f7b6dbd8b0900f22fb97533e333566a9c20). Two passes. (a) The change under review, verified by running `git log -1 --format=%H -- policies/document-metadata-policy.md` (yields 9160a865, so this is the file's last touching commit) and `git show --stat 9160a865` (one file, 6 insertions, 4 deletions): the in-scope set gains `public-prose-criteria.md`, `voice.md`, `voice-template.md` and drops `prose-criteria.md`; the out-of-scope tracker bullet gains the voice inbox (`voice-inbox.md`); frontmatter flips to `status: in-review` / `last-reviewed: null`. No other line changed. (b) All twelve criteria re-applied to the whole current text, with the amended in-scope set resolved against the repository by running `bin/check-frontmatter --all` and the Package A suite. This artifact reviewed policies/document-metadata-policy.md at 9160a865 and no other document.
Cross-checked: skills/spec-review-cycle.md; roles/context-quality-reviewer.md (Scope, What it inspects, What it decides); decisions/log.md DEC-000010, DEC-000020, DEC-000030, DEC-000040, DEC-000050, DEC-000060 and DEC-000240 — the doc-only, gate-class, single-document, owner-override and prose-split rulings this policy encodes; public-prose-criteria.md, voice.md, voice-template.md (each confirmed present at the repository root and carrying YAML frontmatter with `status`, `last-reviewed` and `audience`); LEXICON.md; voice-inbox.md; the `bin/check-frontmatter --all` output captured this cycle; docs/global-context/core.md (Standing 2, Evidence 6, Evidence 9, Acting 13), docs/global-context/decision-layer.md, docs/global-context/review-rubric.md, skills/review-artifact.md; bin/aimeta/scope.py, bin/aimeta/cli.py (`in_scope_files`), bin/bundle (`GOVERNED_EXTRA_FILES`, `governed_files`), bin/tests/test_scope.py, bin/tests/test_check_frontmatter.py, bin/tests/run; docs/packages/package-a-spec.md §3.4 AC-CF-13 and §3.2 AC-SC-1/AC-SC-3; OPEN-ITEMS.md:1113, :1202, :1210-1211; reviews/expedited-log.md; reviews/document-metadata-policy-cycle-17.md, reviews/document-metadata-policy-cycle-18.md, reviews/agreeing-clusters-cycle-2.md.
Not inspected: whether the expedited and doc-only eligibility conditions are the right conditions, whether the gate-document class is the right class, and whether the three writing documents belong in the in-scope set at all — all Dave's and all dictated, which this role cedes; the bodies of public-prose-criteria.md, voice.md and voice-template.md beyond their frontmatter and, for voice-template.md, its stated exemption from review findings — those three are owed their own doc-only agreements per OPEN-ITEMS.md:1211 and are not this cycle's documents; the other 58 files the amended in-scope set matches, except where one was read as the other side of a cross-check row; the content of the review artifacts cited by `last-reviewed` across the corpus; `bin/` behaviour beyond reading bin/aimeta/scope.py, bin/aimeta/cli.py and bin/bundle and running `bin/check-frontmatter --all` and `bin/tests/run`; the grandfather clause's disposition list, inert here because no in-scope document carries `status: agreed` with `last-reviewed: null`; docs/history/, docs/batons/, docs/cycles/, retros/ and the references within them; whether the methodology is correct as a matter of engineering judgment.
Findings: 3 — 1 blocking, 2 non-blocking
Prior cycle: reviews/agreeing-clusters-cycle-2.md @ ade5dad — the artifact the last agreement of this document cited. reviews/document-metadata-policy-cycle-18.md is the last single-document cycle over this file.
Dave should inspect: DMP19-1 — three Package A tests are red on origin/main as a direct result of this amendment, and docs/packages/package-a-spec.md AC-SC-1 states that redness is the designed forcing function for an explicit update that has not been made; whether this document may be agreed while that update is outstanding is yours. DMP19-3 — whether a document the in-scope set now names may exempt part of itself from the review rubric on a decision-log ruling alone, and whether the exemption belongs in the rubric, in the reviewer's role document, or nowhere.

## DMP19-1 — blocking
Claim: Dropping `prose-criteria.md` from the in-scope set left three Package A tests asserting against this policy's own Scope section red on origin/main, and the acceptance criterion those tests implement says that redness is a demand for an explicit update this amendment did not make.
Location: policies/document-metadata-policy.md:18-33 (the in-scope bullet list), read against bin/tests/test_scope.py:82, :170 and bin/tests/test_check_frontmatter.py:625, :640; docs/packages/package-a-spec.md §3.2 AC-SC-1 and §3.4 AC-CF-13.
Evidence: Verified by running, in a worktree of origin/main @ 1bb41f7b. `bin/tests/run` exits 1 with 10 failures of 573. Seven are the recorded `cycle-mode-unavailable` landing-state failures in tests.test_directive and tests.test_directive_trd, unrelated to this change and covered by OPEN-ITEMS.md:1113. The remaining three fail on this amendment: `tests.test_scope.TestParseInScopeGlobs.test_sc1_the_derived_expectation_is_not_vacuous` — "AssertionError: 'prose-criteria.md' not found in ['policies/**', ..., 'LEXICON.md', 'public-prose-criteria.md', 'voice.md', 'voice-template.md']"; `tests.test_scope.TestMatches.test_sc3_the_governed_context_roots_are_in_scope` (path='prose-criteria.md') — "AssertionError: False is not true"; and `tests.test_check_frontmatter.TestSessionAndOrderEnforcement.test_cf13_the_governed_context_roots_are_enforced` — "AssertionError: 'invalid-audience' not found in {'missing-frontmatter', 'unmatched-glob', 'scope-summary', 'missing-session'}". Causation was verified by running, not inferred: `git archive 9160a865^ | tar -x` into a scratch tree outside this worktree, whose policy still lists `prose-criteria.md`, and in which `python3 -m unittest tests.test_scope` reports "Ran 16 tests ... OK" and the AC-CF-13 test reports "Ran 1 test ... OK". Verified by reading: docs/packages/package-a-spec.md AC-SC-1 states "The test holds the snapshot and asserts it against the real policy text, so a scope edit turns the test red and forces an explicit update," and AC-SC-3's failing assertion is titled "the global-context, engagement, and prose-criteria roots." Also verified by running: `bin/check-frontmatter --all` exits 0 and prints "NOTE in-scope: [scope-summary] 61 file(s) matched, from 14 configured glob(s)" — the enforcement path itself is green and derives the set from this file at runtime (bin/aimeta/scope.py parses the "In scope (frontmatter required)" marker), so line 51's "Enforcement (hooks) checks exactly the in-scope set" holds; the divergence is confined to the tests that assert the set. Criterion 12 — this file states the in-scope set, three governed-tooling tests state it differently, and the disagreement is live at HEAD. Criterion 12 is also the criterion Core's Acting 13 operationalizes: a changed fact changes everywhere it appears.
Consequence: The repository's own contract suite is red on main for a reason no artifact records as accepted, so the accepted-failure baseline a future cycle compares against is now three failures wider and indistinguishable from a real regression. AC-SC-1's forcing function has fired and been ignored, which trains the next scope edit to ignore it too; and AC-CF-13, whose whole purpose is to prove the in-scope set is read from this policy at runtime rather than re-implemented, no longer proves it — the assertion it makes about a third governed root is now asserting about a file that is neither in scope nor at the repository root. Agreeing this document at 9160a865 records a green gate over a red suite.
Fix: Update the three stale anchors in bin/tests to the amended set — bin/tests/test_scope.py:82 and :170 substitute `public-prose-criteria.md` (and, if the anchor is meant to hold the amendment, `voice.md` and `voice-template.md`) for `prose-criteria.md`; bin/tests/test_check_frontmatter.py:625 and :640 write the fixture at `public-prose-criteria.md`. Nothing in policies/document-metadata-policy.md changes. If instead the tests are to be left red pending a wider tooling change, that acceptance belongs in OPEN-ITEMS.md beside the `cycle-mode-unavailable` entry, named as a baseline, before this document is agreed.
Related: DMP19-2 — the same retired path left live in `bin/`, at a place the suite does not catch.

## DMP19-2 — non-blocking
Claim: `bin/bundle` still names `prose-criteria.md` as a governed file and carries a comment asserting that its extra roots are a subset of this policy's in-scope set, which this amendment made false.
Location: bin/bundle:23-29 (`GOVERNED_EXTRA_FILES` and the AC-BA-1 comment above it), read against policies/document-metadata-policy.md:18-33 and bin/bundle:85-93 (`governed_files`).
Evidence: Verified by running. `git grep -n "prose-criteria.md" -- . ':!docs/history/**'` returns 141 hits across 46 files; every hit outside `bin/` is a record rather than a live reference — reviews/, docs/cycles/, decisions/log.md and OPEN-ITEMS.md state what the split was, voice-inbox.md's three docroot-relevant hits name `public-prose-criteria.md` or describe the retirement, and policies/document-metadata-policy.md's single hit is the `public-prose-criteria.md` bullet matching as a substring. The one live-code hit outside the tests is bin/bundle:29, `GOVERNED_EXTRA_FILES = ("prose-criteria.md",)`. Verified by reading: the comment at bin/bundle:23-27 states "The metadata policy's in-scope set now names all three, so `cli.in_scope_files` already returns them and these are a subset of it in this repo," which no longer holds — the policy names neither `prose-criteria.md` nor any file at that path, and `docs/history/prose-criteria.md` is explicitly out of scope by this file's line 37. Verified by running: `python3 -m unittest tests.test_bundle_audience` reports "Ran 40 tests ... OK", because `governed_files` guards the extra file with `.is_file()` and AC-BA-1's fixture writes its own `prose-criteria.md` into a synthetic repository, so nothing fails and nothing flags it. Criterion 12.
Consequence: The bundle builder's stated governed set and this policy's in-scope set disagree, and the disagreement is invisible: the guard makes it a silent no-op here, the test makes it a silent pass, and the comment reads as current documentation of a relationship that has lapsed. A repository that restores a docroot `prose-criteria.md` for any reason would find it bundled as governed context while enforcement treats it as ungoverned — a file in the bundle with no frontmatter obligation, which is precisely the state criterion 2 and this policy's Metadata format section exist to prevent. The three files the amendment added reach bundles only through `cli.in_scope_files`, so AC-BA-1's stated intent — that a repository whose policy has not adopted the extra roots still bundles them — no longer covers the writing documents at all.
Fix: In bin/bundle, either drop `prose-criteria.md` from `GOVERNED_EXTRA_FILES` and correct the comment to name what the constant is still for, or substitute the three paths the policy now names if AC-BA-1's "bundles them anyway" intent is meant to carry over to them. That is a `bin/` change and a decision about AC-BA-1's scope, not a change to this document.
Related: DMP19-1.

## DMP19-3 — non-blocking
Claim: The amendment brings `voice-template.md` into the in-scope set, and that document exempts part of itself from being a review finding — an exemption stated in no file that governs this reviewer.
Location: policies/document-metadata-policy.md:33 (the `voice-template.md` bullet), read against voice-template.md:16-19, docs/global-context/review-rubric.md criterion 12, roles/context-quality-reviewer.md "Scope" and "What it inspects", and decisions/log.md DEC-000240.
Evidence: Verified by reading. voice-template.md:16-19 states of its Example blocks that "They are a snapshot, may lag that author's live Voice document, and are never a source for any rule; a difference between an example here and any Voice document is not a defect and is not a review finding." DEC-000240 states the same ruling and names the criterion: "the template states that examples are a snapshot, may lag the live Voice document, and are never a review finding — criterion 12 of the review rubric applies to rules, not examples." Against that: docs/global-context/review-rubric.md criterion 12 is unconditional — "Where it states a fact, a definition, or a rule another governed file also states, it states it the same way ... A disagreement is a defect, surfaced rather than resolved" — and carries no examples carve-out; roles/context-quality-reviewer.md's Scope states the in-scope set is "that set's single enumeration. Nothing is excluded from it," and its "What it inspects" is "Each document in scope, whole, against every criterion of the review rubric." Verified by running: `bin/check-frontmatter --all` exits 0 with voice-template.md in the enforced set, and no `unmatched-glob` note is emitted, so each of the 14 configured globs — including the three new ones — matched at least one file. Criterion 12, and criterion 11 in that it leaves a reviewer to infer the boundary of its own mandate.
Consequence: The next Context Quality Reviewer cycle over voice-template.md — owed, per OPEN-ITEMS.md:1211 — has two readings and no governed text to choose between them: honour an exemption its role document says does not exist, or file criterion-12 findings against the examples that DEC-000240 rules are not findings. The reviewer would be deciding the extent of its own gate, which criterion 11 makes a defect wherever wording allows it. The exemption is real and Dave's; the defect is that a document in scope is the only place it is written, so a reviewer loading Core, the rubric and its role document — the bundle it actually reads — never sees it.
Fix: State the carve-out where the reviewer reads it: one sentence in docs/global-context/review-rubric.md under criterion 12, or in roles/context-quality-reviewer.md's Scope, saying that labelled example blocks in a template document are not measured against the document they exemplify. Neither is a change to this file. Alternatively, and this is a decision rather than a fix, exclude `voice-template.md` from the in-scope set here and govern it as a tracker-like artifact — but it carries frontmatter and reaches a bundle, so exclusion would contradict this file's own Scope preamble.
Related: none.
