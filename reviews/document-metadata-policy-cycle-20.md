# Review: policies/document-metadata-policy.md — cycle 20

Verdict: ready-with-findings
Reviewed: policies/document-metadata-policy.md @ 9160a865fc7070775fc17e9b50c55bc5610318df
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-29
Scope: A confirmation pass over the cycle-19 fixes, run in a worktree of origin/main @ f74e0899d9694d43014ff0e185e6f400da5edfd3. The document is unchanged since cycle 19, verified by running `git log -1 --format=%H -- policies/document-metadata-policy.md` (yields 9160a865fc7070775fc17e9b50c55bc5610318df) and `git diff 9160a865 origin/main -- policies/document-metadata-policy.md` (empty). Criteria 1–11 test the document's own text, which has not changed by a byte since cycle 19; cycle 19's pass on them therefore stands and was not re-litigated here. Criterion 12 — the only criterion a cycle-19 fix could have moved, because every accepted fix landed in `bin/` and `OPEN-ITEMS.md` rather than in this file — was re-applied against the amended tooling and tracker, over the document's whole text (291 lines) read this cycle. The three cycle-19 findings were confirmed individually: DMP19-1 (accepted) and DMP19-2 (accepted) are fixed in their stated substance; DMP19-3 (rejected as a finding on this document) is recorded in the tracker as directed and is not re-raised. One new non-blocking finding, DMP20-1, is a residue of the DMP19-2 defect at a sibling location in the same file the fix session edited. This artifact reviewed policies/document-metadata-policy.md at 9160a865 and no other document.
Cross-checked: For DMP19-1, verified by running — `git grep -n "prose-criteria.md" -- bin/` (11 hits: four now read `public-prose-criteria.md` at the re-anchored assertions bin/tests/test_scope.py:82, :170 and bin/tests/test_check_frontmatter.py:625, :640; one is bin/bundle:29; two are recorded run logs, bin/tests/red-run-no-stubs.log:25 and bin/tests/red-run-with-stubs.log:23, which are records of a past run, not live references; four remain live and are DMP20-1's subject); `bin/tests/run` teed to a scratch file — 604 tests, 1 failure, 7 skipped, the failure being `tests.test_directive.TestCycleMode.test_ac_dt_14_bin_holds_exactly_one_directive_skeleton_generator` and the skips being six in `tests.test_directive_trd` (`TestDecisionSessionGate.test_ac_dt_16_the_superseding_decision_and_the_open_item_are_recorded`; the AC-DT-10 unknown-is-a-finding case; `TestExitStatusContract.test_q6_the_generator_uses_two_for_usage_and_three_for_preconditions` and `.test_q6_the_lint_uses_one_for_findings_and_two_for_refusals`; the OQ-Q2 and OQ-Q4 cases) plus one in `tests.test_land` (§6 FM-10, excluded by AC-LAND-T01 itself); and the three modules that carry the in-scope set run individually — `test_scope` 16 tests OK, `test_check_frontmatter` 78 tests OK, `test_bundle_audience` 40 tests OK, so no failure or skip anywhere in the suite arises from this policy's Scope section. For DMP19-2, verified by running — `bin/bundle` line 29 reads `GOVERNED_EXTRA_FILES = ("public-prose-criteria.md", "voice.md", "voice-template.md")` with no `prose-criteria.md` entry, and `bin/bundle --audience writer` exits 0 and lists 22 files including `public-prose-criteria.md` (9/22) and `voice.md` (10/22); `voice-template.md` is absent and its absence is not a finding — its audience is `[human]` by DEC-000240 and its bundle delivery is the bundle-system PRD's open question, DEC-000260. For DMP19-3, verified by running — `grep -n "DMP19-3" OPEN-ITEMS.md` returns line 1156, the review-rubric candidate item, carrying the directed sentence verbatim: "criterion 12 exemption for a template's examples (DEC-000240; reviews/document-metadata-policy-cycle-19.md DMP19-3, rejected there) — encode in the rubric or the reviewer's role, or refuse." Enforcement, verified by running: `bin/check-frontmatter --all` exits 0 and prints "NOTE in-scope: [scope-summary] 61 file(s) matched, from 14 configured glob(s)", so the set this document's Scope section states is still the set enforcement derives at runtime, and line 51 holds. Read: roles/context-quality-reviewer.md; docs/global-context/review-rubric.md; skills/review-artifact.md; reviews/document-metadata-policy-cycle-19.md, whole; docs/cycles/document-metadata-policy-cycle-19-fix-directive.md, whole, as the decision record; policies/document-metadata-policy.md, whole; bin/bundle:18-35; bin/tests/test_bundle_audience.py:1-30, :130-180, :205-220; OPEN-ITEMS.md:1089-1100 and :1156; `git log --oneline 9160a865..origin/main -- bin/ OPEN-ITEMS.md`, which shows the four fix commits b65cc7b, 70b58ba, 4cce08c and d75e1b2.
Not inspected: The substance of the three cycle-19 fixes as engineering choices — whether re-anchoring on `public-prose-criteria.md` is the right anchor, whether AC-BA-1's governed union should carry the three writing documents, and whether DMP19-3's rejection was correct — all decided in the cycle-19 fix directive and not this role's to revisit; criteria 1 through 11 against the document's text, unchanged since cycle 19 and covered by that cycle's pass, so no fresh judgment is recorded on them here; the single live suite failure `tests.test_directive.TestCycleMode.test_ac_dt_14_...` and the seven skips beyond confirming that none of them originates in test_scope, test_check_frontmatter or test_bundle_audience — they belong to the directive-tooling migration, not to this document; whether the accepted-failure baseline recorded in OPEN-ITEMS.md still describes the suite correctly after that migration; the bodies of public-prose-criteria.md, voice.md and voice-template.md; the other 58 files the in-scope set matches; `bin/` behaviour beyond reading bin/bundle and bin/tests/test_bundle_audience.py and running the suite, `bin/bundle --audience writer` and `bin/check-frontmatter --all`; docs/history/, docs/batons/, docs/cycles/, retros/; whether the methodology is correct as a matter of engineering judgment.
Findings: 1 — 1 non-blocking
Prior cycle: reviews/document-metadata-policy-cycle-19.md @ fd16aa7758407c86561318c59a713319c18c486a
Dave should inspect: The suite baseline moved under this document between cycles. Cycle 19 recorded seven `cycle-mode-unavailable` landing-state failures as the accepted red; origin/main now runs 604 tests with one failure and seven skips, the directive-tooling migration having converted most of that baseline into skips and left one live failure (`bin/cycle-open` still emits a skeleton where OPEN-ITEMS.md:1093 and TRD §3.9 step 4 say it is a forwarder). Nothing in this policy depends on it, and it is not a finding here, but whether the accepted-red baseline entry needs restating before this document is agreed is yours.

## DMP20-1 — non-blocking
Claim: `bin/tests/test_bundle_audience.py` still states AC-BA-1's governed file set as including a repo-root `prose-criteria.md`, in the suite's contract docstring and in its shared-fixture docstring, though the same file's fixture and assertion were re-anchored on the three writing documents by the cycle-19 fix.
Location: bin/tests/test_bundle_audience.py:16 (the AC-BA-1 bullet, lines 13-19) and :139 (the `BundleAudienceTestCase` fixture docstring, lines 136-144), read against policies/document-metadata-policy.md:18-33 and bin/tests/test_bundle_audience.py:167-170 and :210-216.
Evidence: Verified by running. `git grep -n "prose-criteria.md" -- bin/` returns two hits in this file that are not substrings of `public-prose-criteria.md`: line 16, "`docs/global-context/**`, `engagements/**`, and `prose-criteria.md`", closing AC-BA-1's statement of the governed set as a union; and line 139, "`engagements/`, `prose-criteria.md`), plus one file under each excluded root", describing the fixture's one-file-per-governed-root layout. `python3 -m unittest discover -s bin/tests -t bin -p "test_bundle_audience.py"` reports "Ran 40 tests ... OK", so nothing in the suite asserts against either sentence. Verified by reading: at :167-169 the fixture now writes `public-prose-criteria.md`, `voice.md` and `voice-template.md` and writes no `prose-criteria.md`, and at :210-216 the assertion is `test_ba1_writing_documents_are_governed`, asserting all three; `git log --oneline 9160a865..origin/main -- bin/` shows 4cce08c, "tests: BA-1 fixture and assertion name the writing documents (DMP19-2)", which made those two edits in this file and left both docstrings untouched. Also verified by reading: this policy's Scope section names `public-prose-criteria.md`, `voice.md` and `voice-template.md` and names no `prose-criteria.md`, and bin/bundle:29 now implements that set. Criterion 12 — this file states the governed set, a governed-tooling file states it differently, and the disagreement is live at HEAD.
Consequence: The docstring is where AC-BA-1 is stated — the suite's own preamble says the `AC-BA-` prefix is local to it because the work was dispatched from a directive rather than from the package spec, so this text, not a spec section, is the acceptance criterion a maintainer reads. It now names a governed root that neither this policy's in-scope set nor `bin/bundle` recognises, so the next edit to `GOVERNED_EXTRA_FILES` that consults the stated criterion reinstates `prose-criteria.md` and re-opens DMP19-2, which the same commit was written to close. The fixture docstring compounds it: it tells a reader the fixture holds one file per governed root and names the wrong root, so the mapping from assertion to root — the thing that makes `test_ba1_writing_documents_are_governed` legible as covering AC-BA-1's third root — reads wrong for three files. Neither sentence is asserted by any test, so the suite stays green over the divergence exactly as it did before cycle 19.
Fix: In bin/tests/test_bundle_audience.py, replace `prose-criteria.md` at :16 and :139 with the three paths the policy and `bin/bundle` now name — `public-prose-criteria.md`, `voice.md`, `voice-template.md` — so AC-BA-1 as stated matches AC-BA-1 as implemented and as enforced. That is a `bin/` change; nothing in policies/document-metadata-policy.md changes.
Related: DMP19-2 — the same retired path in the same governed set, fixed at bin/bundle and at this file's fixture and assertion, surviving in this file's prose.
