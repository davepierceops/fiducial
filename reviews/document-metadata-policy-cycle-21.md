# Review: policies/document-metadata-policy.md — cycle 21

Verdict: changes-required
Reviewed: policies/document-metadata-policy.md @ aadfe40a6a396c456b2bfb53763cf322fcc793c8
Reviewer: Context Quality Reviewer (execution session), independent — this session authored nothing under review; the Editor revision was a different session, and this session's directive author drafted that revision's directive, not the document.
Date: 2026-09-04
Scope: policies/document-metadata-policy.md, read whole (319 lines) at aadfe40a6a396c456b2bfb53763cf322fcc793c8, against all twelve criteria of docs/global-context/review-rubric.md @ fda7970ece0f0cc4d8f0fdadf2185194444f677d and LEXICON.md @ e4e62cc6375934c34e13f8ff15545f6f42185b41, from the assigned worktree at "$TMPDIR/fiducial-document-metadata-policy-cycle-21-gate", base 0da165a56a7e3990828a904b8f3f273f3dd31d41, inside the sandbox. Full-depth, not a confirmation pass: the last whole-document read was cycle 19, and cycle 20 confirmed fixes only. `Baseline` is omitted per skills/review-artifact.md — the object of review is one revision, not a range; the diff below was read to confirm the dictated items, not as the review's range. This artifact reviewed policies/document-metadata-policy.md at aadfe40a6a396c456b2bfb53763cf322fcc793c8 and no other document. The four dictated items, verified by running (a whitespace-normalizing containment check in python3 over the reviewed blob, each dictated string and each superseded string tested) and by reading the diff whole: DMP21-1 at :82-83, DMP21-2 at :279-284, DMP21-3 at :244 and :265-267, DMP21-4 at :202-206 and :272-274 — all six dictated strings present verbatim modulo the directed rewrap, and all four superseded strings gone ("`status: converging` requires no `last-reviewed`.", "A document may exclude its own revisions from this path, and the retro skill does.", "A doc-only agreement covers exactly one in-scope document", "### Eligible when all five hold"); observed. `git diff -U3 dda60a262c6eb775632ae5fefcf18fbe02d9add5..aadfe40a6a396c456b2bfb53763cf322fcc793c8 -- policies/document-metadata-policy.md` is four hunks at @@79, @@198, @@237 and @@258, which together cover the seven locations docs/cycles/document-metadata-policy-cycle-21-directive.md names (82, 201-202, 240, the insertion after 260, 265-266, 271-273, 275-279 at dda60a26) and reach nothing outside them; `git show --stat aadfe40a` is one file, 24 insertions, 15 deletions; observed. Criteria observed passing without a finding: 1 (nothing assumes the reader can open another file — the paths at :20-33, :168-187, :211-214 and :286-288 are the document's own subject or the artifact a rule mandates, and the cycle-21 addition of skills/conversation-retro.md at :204 and :274 states the excluded class inline, so no reader must open it); 2 (audience [all-roles, human], every value reserved, no `order:` needed — verified by running bin/check-frontmatter --all, exit 0); 3 (every path-shaped reference is the scope definition, the gate-document class enumeration, the log the record rule names, or the two companion-path examples — the criterion's own "a file whose subject is paths" exemption; the cycle-21 addition replaces a prose name that was false, "the retro skill", with the path plus the rule stated inline, which I judge an improvement over the text it replaces rather than a new defect); 4 (docs/global-context/core.md and docs/global-context/decision-layer.md read whole; no rule of theirs is restated here save the one DMP21-7 names); 5 (every rule is an instruction to the reading agent); 6 (the pre-existing rationale at :37-40, :48-49 and :218-220 is unchanged since cycle 19's pass; the one rationale clause the revision adds, "because this path carries a document of any size" at :282, earns its place as the reason the multi-commit allowance exists at all; the redundancy the Editor observed is DMP21-6); 7 (:9-10 states the file governs both session kinds and marks the decision-session acts **Dave's**); 8 (verified by running `grep -nw "track\|opus\|sonnet\|haiku\|prompt\|dispatch"`: no hit); 9 (the filenames it prescribes — reviews/expedited-log.md and the review artifact path — are stated conventions); 10 (verified by running bin/bundle --audience context-quality-reviewer: member 11 of 20, blob e5de98b, the reviewed revision); 11 (every act that is Dave's is marked **Dave's** at :134, :139, :196, :249 and :254, and condition 3's "When it is unclear, it is ineligible" is a bright line that carries into the doc-only path's condition 3 by its cross-reference; the one boundary no role claims is DMP21-5); 12 (cross-checked as listed below; the differences found are DMP21-5 and DMP21-7). The two checks travelling with this gate. Check 1, expedited-stretch — cleared, no finding: conditions 2 and 4 as written each foreclose the 2026-08-24 agreement independently, and condition 1 does too. Observed — specs/bin-land-trd.md is 1,856 lines at the base ref and carries `status: agreed` with `last-reviewed: reviews/expedited-log.md @ b31b75af12648585d7ac86f7c0d11ad85f883f5f`; reviews/expedited-log.md:58-60 records that agreement and states in the log's own words that the policy "frames the expedited path for small doc-only changes, not a 1,400-line TRD — Dave took it deliberately anyway"; retros/retro-20260824T160000.md evidence item 20 records the same stretch. Condition 4 (:193-195, "The document is not under `specs/`") excludes it categorically, condition 2's ten-line cap (:159-162) excludes it on size, and condition 1 (:156-158, "and so does a revision spread across two commits" / "no other tracked path") excludes it again, the log entry recording the TRD agreed "together with bin/tests/test_land.py at the same commit". The doc-only path does not readmit it: its condition 5 at :264 restates the specs/ exclusion. Check 2, enforcement admitting a well-formed `last-reviewed` pointer on a `converging` document — cleared, no sentence needed: observed at bin/aimeta/frontmatter.py @ 2e23b8445f10fb0ee680192e856af63da954ea65, validate() raises `missing-last-reviewed` only when the key is absent and the status is not `converging`, and when the key is present and non-null it checks format alone, so a well-formed pointer passes on any status. The revised sentence at :82-83 enumerates absent or `null`, and that enumeration is complete for every route this policy defines — the entry transition is frontmatter-only from `in-review` or `draft`, where `last-reviewed` is already `null` or absent (:126-131, :134-135, :139-143); a content edit while `converging` changes neither field (:135-137); and the pointer is written only at the agreement flip (:146-147). Enforcement is laxer than the policy's model, not in conflict with it, and the policy nowhere claims enforcement rejects the third value, so a sentence here would state a rule for a state the policy's own sequences cannot produce. The bin/aimeta/expedited.py path-blindness item is likewise not contradicted by the revision: the tracker's premise is one in-scope document per content commit, and :284 restates exactly that for the multi-commit case ("every content commit touches only that document"), so one SHA still selects one entry. Named verification, all run in the assigned worktree inside the sandbox: (1) bin/check-frontmatter --all, exit 0, "NOTE in-scope: [scope-summary] 62 file(s) matched, from 14 configured glob(s)" — 62 files, observed, log at "$TMPDIR/fiducial-document-metadata-policy-cycle-21-gate-frontmatter.log"; (2) `git diff --stat 0da165a56a7e3990828a904b8f3f273f3dd31d41..HEAD` — two added files, docs/cycles/document-metadata-policy-cycle-21-gate-directive.md and reviews/document-metadata-policy-cycle-21.md, and nothing else, observed; (3) this artifact's Reviewed: line names policies/document-metadata-policy.md @ aadfe40a6a396c456b2bfb53763cf322fcc793c8 character-for-character, observed; (4) `git diff --stat dda60a26..aadfe40a -- policies/document-metadata-policy.md` — one file, 24 insertions, 15 deletions, matching the Editor's report, observed.
Cross-checked: Verified by running — git fetch origin (base confirmed at 0da165a56a7e3990828a904b8f3f273f3dd31d41); git worktree list and git ls-remote origin document-metadata-policy-cycle-21-gate (no pre-existing branch or tree; one prunable entry ignored); `git cat-file -e 0da165a5:reviews/document-metadata-policy-cycle-21.md`, which fails, so the path is absent at the base ref; bin/check-frontmatter --all (exit 0, 62 files, 14 globs); bin/bundle --audience context-quality-reviewer (exit 0, 20 members, this policy 11th, docs/global-context/core.md 1st, LEXICON.md 2nd, skills/spec-review-cycle.md 20th); the diff and stat of dda60a26..aadfe40a over this file and over the whole tree; `git show --stat aadfe40a`; a hunk count of that diff (4); the python3 containment check of the six dictated and four superseded strings; `git log --oneline -5 -- policies/document-metadata-policy.md` (aadfe40, dda60a2, fef34aa, d96ef65, 4129a45) and `git show 4129a45 -- policies/document-metadata-policy.md`, the commit that introduced the converging text and set last-reviewed null; `git grep -n "^status: converging" -- '*.md'` (no hit — no document currently holds the status); `git grep -n "consistency check\|consistency sweep"` over the in-scope roots (three hits, all in this file, :255, :257, :259) and `git grep -ni "internally consistent\|internal consistency"` (roles/spec-reviewer-agent.md:18 and :52 only); `grep -n "five\|six"` over this file (:202, :244, :272 — the expedited count five, the doc-only heading six, the doc-only count six, and nothing else) and `grep -c "retro skill"` (0); `git grep -ni "all five hold\|all six hold\|five conditions\|six conditions"` over the governed roots, whose only hit outside this file is docs/rule-register/rule-register-20260825T1435.md:413, a derived artifact pinned to f9a7a5e8 by its own header and out of the in-scope set, quoting the superseded sentence as a record; `grep -nw` for the criterion-8 vocabulary; `git grep -ln "frontmatter-only"` over bin/, which locates the check at bin/flip-agreed; `git ls-tree HEAD specs/` and `wc -l` on specs/bin-land-trd.md (1,856). Verified by reading — policies/document-metadata-policy.md @ aadfe40a6a396c456b2bfb53763cf322fcc793c8, whole; docs/cycles/document-metadata-policy-cycle-21-directive.md @ 0a9a26dac7902c4ad38cd13f3ec3be6f6602e2d1, whole; reviews/document-metadata-policy-cycle-20.md @ 3aa12a53e5cd5c134b54c4f77325f306c4d12ece, whole; reviews/document-metadata-policy-cycle-19.md @ fd16aa7758407c86561318c59a713319c18c486a, whole; roles/context-quality-reviewer.md @ d202b83412d8da512b025eb7f39de4dd8a3f2e40, whole; docs/global-context/review-rubric.md @ fda7970ece0f0cc4d8f0fdadf2185194444f677d, whole; skills/review-artifact.md @ 5d593a742b6726861b7f57a6d93cc31851b2408b, whole; LEXICON.md @ e4e62cc6375934c34e13f8ff15545f6f42185b41, whole; skills/conversation-retro.md @ abd7c9cde0b71b3639edda22b8e5e2c062514cee:18-20; bin/aimeta/frontmatter.py @ 2e23b8445f10fb0ee680192e856af63da954ea65, validate() whole; retros/retro-20260824T160000.md @ 361a14d4122df05b53c7c2ebd8ff284309464d0b, evidence item 20; OPEN-ITEMS.md @ 56060d52a644c137d944e474d5a93e19e4d810ed, the cycle-12 advisory entry at :1038-1060 and the expedited.py path-blind entry at :1062-1068; and, at the base ref, docs/global-context/core.md and docs/global-context/decision-layer.md whole, operating-model.md:118-133, roles/chief-of-staff.md:28-40 and :115-125, roles/test-designer-agent.md:32-36, roles/spec-reviewer-agent.md:23-47 and :83-104, skills/spec-review-cycle.md:118-140, context-sets/spec-and-change-discipline.md:31-57 (grep hits), decisions/log.md DEC-000360 and DEC-000370, reviews/converging-model-cycle-2.md header and N-6, reviews/expedited-log.md:1-20 and :58-63, specs/bin-land-trd.md frontmatter, bin/aimeta/expedited.py:1-60, and docs/rule-register/rule-register-20260825T1435.md:1-9 and :413.
Not inspected: Whether the expedited and doc-only eligibility conditions are the right conditions, whether the gate-document class is the right class, and whether either lighter path should exist — all Dave's and all dictated, which this role cedes; the rulings behind DMP21-1 through DMP21-4, closed by the directive and not reopened, only the wording that realizes them; the other 61 files the in-scope set matches, except where one was read as the other side of a cross-check row; the content of the review artifacts `last-reviewed` cites across the corpus; bin/ behaviour beyond reading bin/aimeta/frontmatter.py validate() and bin/aimeta/expedited.py:1-60 and running bin/check-frontmatter --all and bin/bundle — bin/flip-agreed was located by grep and not read, and expedited.py's check_pointer was not read; bin/tests/run and the suite's accepted-red baseline, excluded by this cycle's directive; the in-scope file count moving from 61 at cycle 20 to 62, recorded and not investigated; bin/aimeta/expedited.py path-blindness beyond confirming the revised text does not contradict the tracker's stated premise; the grandfather clause's disposition list and whether one exists; the Editor session's own execution report, which is not in the tree (unknown) — its observation reached this session through the directive, labelled told, and was confirmed against the document; docs/history/, docs/batons/, docs/cycles/ beyond the two directives named, and retros/ beyond evidence item 20; whether the methodology is correct as a matter of engineering judgment.
Findings: 3 — 1 blocking, 2 non-blocking
Prior cycle: reviews/document-metadata-policy-cycle-20.md @ 3aa12a53e5cd5c134b54c4f77325f306c4d12ece
Dave should inspect: DMP21-5 — whether `converging` is a spec document's status, as the Lexicon and DEC-000360 state it, or reaches every in-scope document, as this policy states it; and, if the latter, who runs the exit gate for a `converging` document that is not a spec, which is a change outside this file. Separately, and recorded rather than filed: specs/bin-land-trd.md stands `agreed` on a route conditions 1, 2 and 4 now each foreclose, and this policy states no rule for an agreement landed under an earlier version of a lighter path — whether it needs one is yours.

## DMP21-5 — blocking
Claim: The document states `converging` as a status any in-scope document may hold, where the Lexicon, the operating model and the decision that created the status all state it as a spec document's status, and no governed file names who runs the exit gate for a `converging` document that is not a spec.
Location: policies/document-metadata-policy.md:70-73, :129, :134-138 and :139-145, read against LEXICON.md:40-51, operating-model.md:130-132, decisions/log.md DEC-000360, roles/test-designer-agent.md:32-36, roles/spec-reviewer-agent.md:42-47 and :86, roles/context-quality-reviewer.md whole, roles/chief-of-staff.md:33-34, skills/spec-review-cycle.md:118-135, and skills/review-artifact.md:80.
Evidence: Verified by reading. This file is document-general wherever it states the status: :71-73 "`converging` = the document's first reviewer gate has run and, on Dave's say, the document is edited freely while tests are written against it"; :134-137 "A document enters `converging` ... A content edit to a `converging` document changes neither its status nor its `last-reviewed`"; :139-145 "A revision of an `agreed` document ... may enter `converging` under the same entry rule". Against that, LEXICON.md:40 defines the term "**Converging** — of a spec document", under its "## Spec state" heading; decisions/log.md DEC-000360, the ruling that created the value, says "while converging the spec is edited freely" and "one ruling that flips the spec agreed"; operating-model.md:130-132 makes it stage 2 of the spec lifecycle and assigns stage 4's exit gate to the Spec Reviewer; roles/test-designer-agent.md:32 and roles/spec-reviewer-agent.md:44 both say "the `converging` spec". The corpus is not uniform on the other side either — roles/chief-of-staff.md:33, skills/spec-review-cycle.md:126 and :135, and skills/review-artifact.md:80 all say "document" — which is what makes this file, the single enumeration of status semantics for the whole in-scope set, the place the scope has to be settled. Verified by running: `git grep -n "^status: converging" -- '*.md'` returns no hit, so the divergence is prospective, not live; `bin/bundle --audience context-quality-reviewer` places this policy (member 11 of 20) in one bundle with LEXICON.md (2) and skills/spec-review-cycle.md (20), so a single reader meets both readings in one context; `git log --oneline -- policies/document-metadata-policy.md` and `git show 4129a45` show the text entered at 4129a45 in the converging-model cycle and set `last-reviewed: null` in the same commit, and the file has carried a null pointer since — reviews/converging-model-cycle-2.md reviewed this document at d96ef65 confirmation-scoped, and its one-model cross-check covers the status name, the entry rule, the exit rule and the three-valued build-gating rule, not the class of documents the status reaches. Criterion 12, and criterion 11 for the unowned exit gate.
Consequence: This policy is in every role's bundle. A decision session reading it may put any in-scope document into `converging` after its first gate: nothing in the text restricts it, and enforcement does not either — bin/aimeta/frontmatter.py validate() applies no per-path rule to the status. Once there, the mechanism the Revision lifecycle exists to run is off for the duration: content edits stop flipping `in-review` and stop resetting `last-reviewed` (:135-137). The exit is "the agreement flip, on Dave's ruling at the exit gate" (:137-138), but the exit gate is defined only for a spec — operating-model.md:132 assigns it to the Spec Reviewer, whose scope is documents under specs/, and roles/context-quality-reviewer.md, the role that gates every other in-scope document, does not mention `converging` or an exit gate at all, while skills/review-artifact.md:80 requires a `Baseline` line for "an exit gate over a `converging` document" without naming who writes it. A methodology document parked in `converging` therefore has an entry no governed file forbids and an exit no governed file owns. This document is itself such a candidate: it is gate-class by its own :168, it is `in-review`, and the Package A suite asserts its Scope section, so :139-145's "A revision whose tests do not change takes the ordinary route" reads as admitting the converse for it.
Fix: Surfaced, not resolved. Either scope the status in this file — a clause at :71 and at :134 restricting `converging` to documents under specs/, with :139-145 narrowing the same way, which makes this file agree with LEXICON.md:40, DEC-000360 and operating-model.md:130-132 and leaves roles/chief-of-staff.md:33, skills/spec-review-cycle.md:126 and skills/review-artifact.md:80 as the places outside this file that then need conforming; or, if the status is meant to reach every in-scope document, keep the general wording and have a governed file name the exit gate's owner for a non-spec `converging` document, which is a change to roles/context-quality-reviewer.md or the operating model and not to this file. Which of the two is Dave's; either way this document should not be agreed stating a scope the Lexicon contradicts.
Related: none.

## DMP21-6 — non-blocking
Claim: The doc-only `### Sequence` states that a content commit touches only that document twice in adjacent sentences, and the second statement is singular where the first now admits several commits.
Location: policies/document-metadata-policy.md:279-284 and :286-288.
Evidence: Verified by reading the reviewed revision and the diff dda60a26..aadfe40a. DMP21-2 ends the two-differences sentence with "and every content commit touches only that document" (:284); DMP21-3 cut the first sentence of the paragraph that followed, leaving "The content commit touches only that document — a companion tracked path (a `decisions/log.md` entry, an `OPEN-ITEMS.md` update) lands in its own commit, per the expedited path's "no other tracked path" rule." as that paragraph's whole text (:286-288). Told, and confirmed by reading: the Editor's report at pull request #316 names the adjacency. Criterion 6 — the second sentence opens by restating a rule the sentence before it states, and its own work is the companion-path rule that follows the dash. Not criterion 4, since neither Core nor the Decision Layer states this rule, and not criterion 12, since no other governed file states it differently; the Editor's report offered 4 or 12 and neither is the criterion this fails.
Consequence: A session applying the rule reads the second sentence, because that is the one carrying the "no other tracked path" cross-reference — and it says "The content commit", one commit, immediately after the sentence that made a multi-commit content revision the path's distinguishing feature. The multi-commit case this revision exists to admit is precisely the case the stale sentence gets wrong, so the reader has the count stated two ways in three lines and the wrong one is the operative one.
Fix: Cut the leading clause and the dash, leaving the paragraph as "A companion tracked path (a `decisions/log.md` entry, an `OPEN-ITEMS.md` update) lands in its own commit, per the expedited path's "no other tracked path" rule." The one-document-per-commit rule then stands once, at :284, in the number the path now requires.
Related: none.

## DMP21-7 — non-blocking
Claim: The doc-only consistency sweep is defined as extending a "within-document consistency check already required" to the document's neighbours, but the standing requirement it names is Core's changed-fact rule, which already reaches every other document.
Location: policies/document-metadata-policy.md:259-260, within the consistency-sweep definition at :257-263, read against docs/global-context/core.md Acting 13.
Evidence: Verified by reading. Core Acting 13 reads "A changed fact changes everywhere it appears. When you update a value, name, count, or reference, find every place that states the same thing — in this document and in every other." The neighbour sweep is therefore not an extension of the standing rule; it is the standing rule. Verified by running: `git grep -n "consistency check\|consistency sweep"` over policies, roles, context-sets, boundaries, skills, specs, docs/global-context, operating-model.md and LEXICON.md returns three hits, all in this file (:255, :257, :259), so no governed file states a within-document-only consistency check for this sentence to name; `git grep -ni "internally consistent\|internal consistency"` returns roles/spec-reviewer-agent.md:18 and :52 only, a Spec Reviewer duty over documents under specs/, which this path excludes by its own condition 5 at :264. Verified by running `bin/bundle --audience context-quality-reviewer`: this policy is member 11 of 20 and docs/global-context/core.md is member 1, so both sentences reach one reader in one bundle. Criterion 12, and criterion 4 in that the clause restates a rule Core states.
Consequence: This policy carries `audience: [all-roles, human]`, so every agent session that loads a role bundle reads it. The sentence tells that reader the consistency obligation it already carries stops at the document under edit and that neighbours are reached only when the doc-only path is taken. Core 13 requires the neighbour sweep on every edit, under every route; a reader who takes this file's account of the baseline under-sweeps on all the other routes, which is the failure Core 13 exists to prevent and which cycle 19's DMP19-1 and DMP19-2 and cycle 20's DMP20-1 are each an instance of. The sweep's own definition at :257-259 and its timing and attestation rules at :260-263 do not depend on the clause.
Fix: Cut the sentence "It extends the within-document consistency check already required to the document's neighbours." The definition before it is complete without it — it already names what is checked and which documents — and criterion 4 keeps Core's rule out of this file.
Related: none.
