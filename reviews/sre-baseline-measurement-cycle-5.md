# Review: engagements/sre/baseline-measurement.md — cycle 5

Verdict: ready-with-findings
Reviewed: engagements/sre/baseline-measurement.md @ b0b3540
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-23
Scope: the whole file, all eleven criteria of the review rubric @ b0b3540, judged against the current foundation. Confirmation pass within the Pass 1 re-gate confirmation cycle 4; the added question is whether instruction 2 of the part-5 directive closed SRE-BM-6 — step 6 now states the route the re-measurement takes — and what naming the destination item did to the document that defines that item.
Cross-checked: docs/global-context/core.md (Acting 12, 13; Vocabulary — decision session, execution session), docs/global-context/decision-layer.md, LEXICON.md (Evidence classes, Claim axes), operating-model.md (Change package), roles/context-quality-reviewer.md, docs/global-context/review-rubric.md, skills/review-artifact.md, engagements/sre/README.md (Artifacts, baseline-gate), engagements/sre/engagement-change-package.md, engagements/sre/speed-audit.md, engagements/sre/system-discovery.md, engagements/sre/implementer.md, engagements/sre/override-log-policy.md, engagements/cartographer.md, engagements/assistant.md, engagements/critic.md, engagements/working-with-dave.md — all @ b0b3540; plus `git diff b68fe87 821e246 -- engagements/sre/baseline-measurement.md`, docs/cycles/pass1-regate-fix-5-20260823T020500.md (instructions 1 and 2), reviews/sre-baseline-measurement-cycle-4.md and reviews/corpus-regate-cycle-4.md. Bundle membership computed mechanically over every `audience:` value in the 51-file corpus set at b0b3540.
Not inspected: whether p50/p95 is the right distribution summary as a matter of engineering judgment; the four failure modes' completeness; whether the Cartographer is the right role to re-measure, which D13 settled and this cycle does not re-open; whether the Assistant is the right runner for steps 1-5; what should happen when the re-measurement shows a regression rather than an improvement, which is engagement practice and not this role's to judge; the duplicate-rules, term-collision, audience-value, path-reference and retired-vocabulary sweeps, none of which this directive commissioned.
Findings: 1 — 1 non-blocking
Prior cycle: reviews/sre-baseline-measurement-cycle-4.md
Dave should inspect: none. SRE-BM-7 is one clause naming an item that another file defines more narrowly than the clause needs; the requirement to cite both measurements is stated either way, so the exposure is bounded.

## SRE-BM-7 — non-blocking
Claim: Step 6 directs the Implementer to cite the post-change re-measurement in "the change package's Expected-delta item," and the file that defines that item defines it as the pre-stated claim citing the baseline — so the number has no item whose definition admits it.
Location: engagements/sre/baseline-measurement.md:32-35 ("6. **Re-measure after the change** with the same stopwatch, same method. The delta claim in the change package cites both measurements (delta-verified). The Cartographer delivers the re-measurement to the Assistant; the Implementer cites it in the change package's Expected-delta item."), against engagements/sre/engagement-change-package.md:20-21 ("3. **Expected delta** — the pre-stated measurement claim, citing the baseline (or the logged override that waived it)") and :23-24 ("5. **Evidence** — what was verified, using the evidence classes precisely; what remains unverified").
Evidence: Verified by running. `git diff b68fe87 821e246 -- engagements/sre/baseline-measurement.md` appends exactly the two clauses instruction 2 dictated and changes nothing else, so SRE-BM-6's stated defect — that no document in the Cartographer's bundle said how the measurement reaches the change package — is gone: the delivery is now stated in the sentence that assigns the duty, and it terminates at the Assistant, who is in the Cartographer's bundle. What the appended text created is a destination whose own definition excludes it. A corpus-wide grep for `expected[- ]delta` over the 51-file set, excluding the directories this directive excludes, returns engagements/sre/engagement-change-package.md:20 as the only definition of the item, engagements/sre/baseline-measurement.md:29 and :35, engagements/sre/implementer.md:19, and engagements/sre/README.md:25, :32, :57, :63 — and every one of the others states the expectation as pre-stated, before implementation. This file's own step 5 does the same: "stated **before implementation**, recorded in the change package." Bundle membership computed over `audience:` at b0b3540: the `implementer` bundle (24 files) and the `assistant` bundle (26 files) each hold both this file and engagements/sre/engagement-change-package.md, so the role the clause instructs reads the instruction and the narrower definition together; the `cartographer` bundle (24 files) holds this file and not the change-package skill, which is correct — the Cartographer's duty ends at the delivery and the trailing clause is context it need not resolve. Criterion 11 ("Underspecified conditions ... are defects") and Core Acting 13 ("A changed fact changes everywhere it appears ... find every place that states the same thing — in this document and in every other — and update it too"): the reference was added and the definition it points at was not conformed.
Consequence: Bounded, because the preceding sentence — "The delta claim in the change package cites both measurements (delta-verified)" — is in the same bundle and states the requirement unconditionally, so an Implementer following it cites both numbers somewhere. What is loose is where. The Implementer assembling the package from engagements/sre/engagement-change-package.md writes item 3 to that item's definition, which admits the pre-stated claim and the baseline cite and nothing else; the post-change number then lands in item 3 against its definition, in item 5 where the evidence classes live, or in neither. Where it lands in neither, the package asserts `delta-verified` — which engagements/working-with-dave.md:28-29 defines as post-change measurement against a pre-change measurement — on the pre-change measurement alone, which is the overclaim the evidence classes exist to prevent.
Fix: Either widen the item where it is defined — engagements/sre/engagement-change-package.md:20-21 becomes "**Expected delta** — the pre-stated measurement claim, citing the baseline (or the logged override that waived it), and, after the change, the post-change measurement paired with it (delta-verified)" — or point the clause at the item that already admits it: "the Implementer cites it in the change package's Evidence item." The first keeps both halves of one delta claim in one item and is the smaller edit to this file, which then needs no edit at all.
Related: SRE-BM-5, SRE-BM-6, CN-17, BD-11
