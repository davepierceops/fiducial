# Review: engagements/sre/baseline-measurement.md — cycle 4

Verdict: ready-with-findings
Reviewed: engagements/sre/baseline-measurement.md @ b6102de
Reviewer: Context Quality Reviewer (execution session)
Date: 2026-08-23
Scope: the whole file, all eleven criteria of the review rubric @ b6102de, judged against the current foundation. Confirmation pass within the Pass 1 re-gate confirmation cycle 3; the added question is whether instruction 4 of the part-4 directive closed SRE-BM-5 under D13 — step 6 now names the Cartographer — and what naming it did to the route the measurement takes to the artifact that must cite it.
Cross-checked: docs/global-context/core.md (Vocabulary — decision session, execution session; Acting 13), docs/global-context/decision-layer.md, LEXICON.md (Evidence classes), operating-model.md (Change package), engagements/sre/README.md (Artifacts), engagements/sre/override-log-policy.md, engagements/sre/implementer.md, engagements/sre/speed-audit.md, engagements/sre/system-discovery.md, engagements/sre/engagement-change-package.md, engagements/cartographer.md, engagements/assistant.md, engagements/working-with-dave.md — all @ b6102de; plus `git diff c8e95d1 9759ed3 -- engagements/sre/baseline-measurement.md`, docs/cycles/pass1-regate-fix-4-20260823T013000.md (D13, instruction 4), and reviews/sre-baseline-measurement-cycle-3.md. Bundle membership computed mechanically over every `audience:` value in the 51-file set at b6102de.
Not inspected: whether p50/p95 is the right distribution summary as a matter of engineering judgment; the four failure modes' completeness; whether the Cartographer is the right role to re-measure, which D13 settled and this cycle does not re-open; whether the Assistant is the right runner for steps 1-5.
Findings: 1 — 1 non-blocking
Prior cycle: reviews/sre-baseline-measurement-cycle-3.md
Dave should inspect: none. SRE-BM-6 is the second half of the question D13 answered — the role is named, the route its output takes is not — and it is one sentence in one of two files.

## SRE-BM-6 — non-blocking
Claim: Step 6 now obliges the Cartographer to produce a post-change measurement that step 6 says the change package must cite, and no document in the Cartographer's bundle says how the measurement reaches that package, while the role document enumerates the Cartographer's report as two other things.
Location: engagements/sre/baseline-measurement.md:9-12 ("step 6 runs in an execution session by the Cartographer, who re-measures with the baseline's instrument; the Implementer does not measure its own change") and :32-33 ("6. **Re-measure after the change** with the same stopwatch, same method. The delta claim in the change package cites both measurements (delta-verified)."), against engagements/cartographer.md:9-10 ("its report — the question list and the rendered map — returns to the Assistant") and engagements/sre/engagement-change-package.md:4, :9 (`audience: [implementer, critic, assistant, human]`; "Produced in an execution session by the Implementer").
Evidence: Verified by running. `git diff c8e95d1 9759ed3 -- engagements/sre/baseline-measurement.md` replaces "whichever of the Cartographer or Implementer re-measures" with the D13 assignment, so SRE-BM-5's stated defect — that no document said which role — is gone; step 6's body names no runner and so needed no conforming under instruction 4's second sentence. What the assignment created is a handoff with no stated mechanism. Bundle membership computed over `audience:` at b6102de: the `cartographer` bundle resolves to 24 files and engagements/sre/engagement-change-package.md is not among them, so the Cartographer never reads the document defining the artifact its measurement must land in; engagements/sre/implementer.md is likewise absent from that bundle. engagements/sre/README.md:23-25, which the Cartographer does read, names "**change package**" as the artifact chain's terminus but its Artifacts list at :51-67 defines System Map, Measurement Baseline, Improvement Proposal, engagement working area, baseline-gate and the no-fixed-filename record, and not the change package. operating-model.md is `all-roles` and reaches the bundle, but its Change package section states the parent model's twelve items, not the engagement's lean seven. The Cartographer's own role document enumerates its report as exactly two outputs, neither of them a measurement, and engagements/sre/system-discovery.md:10 repeats that enumeration. A corpus-wide grep for `re-?measur|stopwatch|delta-verified` over the 51-file set returns this file at :10, :32, :33 and :43, engagements/sre/README.md:32, engagements/working-with-dave.md:28 and engagements/sre/speed-audit.md:29 — and none of the seven states a delivery. Criterion 1 ("Nothing in it assumes the reader can open another file" — here the file is not in the bundle at all), criterion 11, and Core Acting 13. Read-only is not the obstacle: engagements/cartographer.md:21-22 declares the role read-only throughout, and this file's step 2 makes log-derived and CI-API-derived measurement the read-only default, so the Cartographer can perform step 6 as written.
Consequence: Bounded, because the Assistant is in both bundles and is the Cartographer's stated recipient, so a route exists in practice. What is unstated is that it is the route. The concrete failure is the Implementer reaching the end of its change package with item 3's expected-delta statement from step 5 and no post-change number to pair it with — it does not produce one, D13 forbids it producing one, and nothing tells it whose to ask for or through whom. The symmetric failure is the Cartographer completing step 6 and returning a measurement inside a report its own role document says carries the question list and the rendered map, with no instruction that the measurement travels onward at all.
Fix: State the route in the sentence that assigns the duty: "step 6 runs in an execution session by the Cartographer, who re-measures with the baseline's instrument and returns the measurement to the Assistant for the change package; the Implementer does not measure its own change." Then extend engagements/cartographer.md:9-10's enumeration to name the post-change measurement as a third thing the report may carry. Adding `cartographer` to engagements/sre/engagement-change-package.md's `audience:` would also close it, and is the larger change of the two.
Related: SRE-BM-5, SA-6, CN-16, BD-11
