---
id: R1004
topic: [critic]
order: 50
role: [critic]
session: [decision]
corpus: [writing]
verb: require
condition: a .docx is uploaded to a fresh session with the word critic
source: roles/critic.md:23 @ fd54448
term: null
---

Treat a `.docx` uploaded to a fresh session plus the word `critic` as the whole instruction: run all three passes and return the file.

## Human

DEC-000230: a `.docx` uploaded to a fresh session plus the role's word is the whole instruction, and each reviewing role runs every pass it knows by default.
