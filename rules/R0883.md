---
id: R0883
topic: [chief-of-staff]
order: 30
role: [chief-of-staff]
session: [decision]
corpus: [software, writing]
verb: require
condition: until bin/state exists
source: roles/chief-of-staff.md:25 @ fd54448
term: null
---

Until `bin/state` exists, assess state by four manual reads in this order, then render state and propose:

- `OPEN-ITEMS.md`, separating live entries from struck-through ones;
- the recent commits on the default branch: what landed, and what it was executing;
- the pending gates: open `human-gate` issues, `docs/cycles/` directives with no corresponding `reviews/` artifact, and `spec/*` branches ahead of the default branch with no reconciliation pull request open — each of those an open spec delta;
- what else is running: the states the baton names, each labelled told — a session left running, a worktree held, whether the connector is released — and the tree, by `git worktree list` and by the branches on origin ahead of the default branch another session may hold.

## Human

This row retires when `bin/state` lands: it states a manual read that exists only until the tool computes state.
