---
id: R0205
topic: [core]
order: 300
role: [all]
session: [decision, execution]
corpus: [software, writing]
verb: define
condition: null
source: docs/global-context/core.md:45 @ fd54448
term: [decision layer, execution layer, shell layer]
---

Work moves through three layers: decision — chat; execution — an LLM agent session; shell — commands run in a shell. *Execute* and *execution* belong to the execution layer only, never to shell work.
