---
id: R0208
topic: [core]
order: 320
role: [all]
session: [decision, execution]
corpus: [software, writing]
verb: define
condition: null
source: docs/global-context/core.md:47 @ fd54448
term: [paste block, paste blocks, command block, command blocks, execution block, execution blocks]
---

A paste block is copied whole and pasted whole. There are two kinds. A command block is shell commands run as given — never instructions to an LLM, and never described as executing. An execution block is instructions an LLM agent session carries out — never shell commands.
