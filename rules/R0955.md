---
id: R0955
topic: [copy-editor]
order: 50
role: [copy-editor]
session: [decision]
corpus: [writing]
verb: require
condition: a .docx is uploaded to a fresh session with the word copy
source: roles/copy-editor.md:22 @ fd54448
term: null
---

Treat a `.docx` uploaded to a fresh session plus the word `copy` as the whole instruction: run every pass and return the file.

## Human

DEC-000230: a `.docx` uploaded to a fresh session plus the role's word is the whole instruction, and each reviewing role runs every pass it knows by default.
