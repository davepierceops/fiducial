---
id: R0886
topic: [chief-of-staff]
order: 60
role: [chief-of-staff]
session: [decision]
corpus: [software, writing]
verb: require
condition: null
source: roles/chief-of-staff.md:31 @ fd54448
term: null
---

When assessing state, read the pending gates: open `human-gate` issues, `docs/cycles/` directives with no corresponding `reviews/` artifact, and `spec/*` branches ahead of the default branch with no reconciliation pull request open — each of those an open spec delta.

## Human

The `status: in-review` and `status: converging` entries of this read retire with the document-lifecycle machinery (DEC-000380); an open spec branch is the delta (process/change-flow.md, "The spec lifecycle, restated").

This row retires when `bin/state` lands: it states a manual read that exists only until the tool computes state. Recorded 2026-09-06 by `docs/cycles/store-fix-2-20260906T050000Z.md` item 6.
