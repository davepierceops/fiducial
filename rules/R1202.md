---
id: R1202
topic: [command-blocks]
order: 70
role: [all]
session: [decision, execution]
corpus: [software, writing, methodology]
verb: require
condition: a block runs a sync or remote command
source: skills/command-blocks.md:38 @ fd54448
term: null
---

Name the remote and the ref in every sync or remote command a block runs, rather than leaning on branch-upstream configuration or an implied default.

## Human

A block runs in a clone whose config the author cannot see, and a `git pull` resolved through the wrong upstream is silent about it.
