---
id: R0106
topic: [coder-agent]
order: 70
role: [coder-agent, test-designer-agent]
session: [execution]
corpus: [software, writing]
verb: require
condition: a change writes an entity's primary fields
source: context-sets/spec-and-change-discipline.md:92 @ fd54448
term: null
---

A write path maintains the entity's derived fields — the values computed from primary fields rather than supplied directly — the same way every other write path does, and the package is not done until they are enumerated and each is checked.

## Human

A derived field is not "content" the way a primary field is, so the Coder and an independent Test Designer can both omit it — from the implementation and from the test plan — and share the blind spot.
