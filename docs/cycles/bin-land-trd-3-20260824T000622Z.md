# Directive: bin/land TRD — cycle 3

Route: fresh execution session. Model tier: frontier.
Base: main @ dc33f452658d015fb8e33c1325e3d05cb7a1d53d.

1. First act — write this directive file verbatim to
   docs/cycles/bin-land-trd-3-20260824T000622Z.md in the worktree stated below,
   commit it on branch bin-land-trd-3, push plain, verify by ls-remote, and
   report the SHA read back from git.

Worktree — exclusive assignment. This session works only in
$TMPDIR/fiducial-bin-land-trd-3 (fallback /tmp/claude-501/fiducial-bin-land-trd-3),
created:

    git fetch origin
    git rev-parse origin/main
    # must print dc33f452658d015fb8e33c1325e3d05cb7a1d53d — if not, STOP and surface
    git worktree add "$TMPDIR/fiducial-bin-land-trd-3" -b bin-land-trd-3 dc33f452658d015fb8e33c1325e3d05cb7a1d53d

Scratch substrates for gathering evidence are permitted outside that worktree —
a throwaway repository built by hand, as prior reviewers used. Repository work
happens only in the assigned worktree.

Sandbox constraints — read both companions, follow as told:
- docs/cycles/pass2-held-fix-20260823T180753Z.md @ b9444973de086eef7cd30e7877fb86f6a1271d8e
- docs/cycles/bin-land-flip-20260823T210300Z.md @ 8c0aeee2470297ae5f2879a72dc9be4da0ea093b
  (fetch then verify origin/main before any worktree add; never `git push -u` —
  push plain, verify by ls-remote; worktree paths under $TMPDIR)

Companions — read all before editing:
- reviews/bin-land-trd-cycle-3.md @ 04bf5724 — the findings this cycle disposes
  of. Every Fix cited below is a pointer to a field in this artifact; read the
  field, do not expect it restated here.
- docs/cycles/bin-land-trd-2-20260823T232718Z.md @ f02a1330 — the cycle-2
  decision record. All ten of its dispositions were confirmed discharged by the
  cycle-3 reviewer; do not re-litigate them.
- reviews/bin-land-trd-cycle-2.md @ 3738aa0833ff4d2640086a2950260ef32eb8983e
- specs/bin-land.md @ dc33f452 — the PRD, agreed, read-only this cycle
- specs/trd-template.md @ dc33f452 — read-only

Reviewed state (told, per the decision session's baton, corroborated by
reviews/bin-land-trd-cycle-3.md's own header): specs/bin-land-trd.md revision
landed at content commit 97888a83 and was re-gated by the Spec Reviewer at main
3412e25837a8556e0ae74d65e33037bff06184a0; the review artifact landed at
04bf5724 and merged to main at dc33f452 as PR #189. Before editing, verify that
97888a83 is in the worktree's history and that no commit since it has touched
specs/bin-land-trd.md. If either fails, STOP and surface.

2. Revise specs/bin-land-trd.md per the decisions below. Each finding decision
   is stated as intent with a pointer to the reviewer's Fix field; draft your
   own wording. Do not paste a reviewer Fix field into the document. The TRD
   proposes; it decides nothing Dave owns.

   Nothing was rejected this cycle. All four findings are accepted, and the
   absence of a rejection is a fact rather than an omission.

   D-O2 — accepted, column rule (Dave, this cycle). Stated first because the
   other three dispositions execute under it, and stated inline because this
   directive is the decision's origin. Dave's decision: §6's "Established at
   that point" column is rewritten to one rule, stated once above the table —
   each cell names the contract fields and detail keys established at that
   point, exhaustively — and all eleven rows are rewritten to that rule. Cell
   contents derive from §3.2's sequence, §3.7's step returns, and §5.2/§5.3's
   field semantics; where a cell and those sections disagree, those sections
   are normative and the cell is wrong — surface any case where the
   disagreement seems to run the other way rather than resolving it silently.
   Fix pointer: O2 Fix field.

   D-B1 — accepted, entry route. Intent: on FM-7 and FM-8 — the two failure
   paths on which a commit exists — the report's files field carries one entry
   per path in the commit, match null, class "unknown", satisfying AC-LAND-07's
   per-file requirement and AC-LAND-09's established/unestablished split. §3.7
   gains a companion sentence stating where the FM-7/FM-8 entries come from,
   since step 10 is not their source. §5.2's empty-files paragraph is
   reconciled so its [] claim stays scoped to the no-commit paths and the
   FM-7/FM-8 shape is stated beside it. The §6 rows follow from D-O2's column
   rewrite. The reviewer ruled the alternative — keeping [] and widening the
   disclaimer — the weaker route; it is not taken. Fix pointer: B1 Fix field.

   D-N1 — accepted. Intent: the qualifier "where the branch existed" is deleted
   from the FM-2, FM-3, and FM-4 cells, so prior_head reads as established
   unconditionally on every path after step 3 — the literal "created" in the
   branch-absent arm — consistent with §3.2 step 3, §3.7, §5.2, FM-9, and
   OQ-4. Discharged in practice by D-O2's column rewrite; stated as its own
   decision so the rejection-or-acceptance record is complete. No PRD edit:
   AC-LAND-09 is a floor the TRD may exceed. Fix pointer: N1 Fix field.

   D-O1 — accepted, emitted not dropped. Intent: §3.7's land.py → report.py
   paragraph gains one clause — build populates detail from the accumulated
   facts under §5.3's key table — and §5.3 gains one clause stating that a key
   present in facts but outside its stated "Emitted on" is emitted. Dave's
   selection between the two dispositions the reviewer left open: emitted,
   consistent with §5.3's stated openness ("carries no 'and no others'
   clause") and with the reviewer's own assessment that the redundant
   remote_head on FM-9 and success is harmless. Fix pointer: O1 Fix field.

3. Consistency sweep before committing, in two parts. Report what each found,
   including "nothing" if that is the result.

   3a — the §6 column seam. The rewritten column touches every failure row, and
   B1 and N1 were both defects of that column. After the rewrite, read each of
   the eleven cells against §3.2's sequence, §3.7's step returns and build
   rules, §5.2's value-domain rules, and §5.3's key table, and confirm every
   cell is exhaustive and correct under the D-O2 rule. Name any disagreement
   you find rather than resolving it silently.

   3b — the ordinary sweep. Per docs/global-context/core.md rule 13, a changed
   fact changes everywhere it appears. Sweep specs/bin-land-trd.md for any
   count, value, or cross-reference these edits have made stale — at minimum:
   §5.4's AC-LAND-T01 (whose value-domain and class assertions now meet
   files entries with match null on FM-7/FM-8); §5.2's files rules ("match is
   true, false, or null; null only with class unknown" — confirm the FM-7/FM-8
   entries satisfy it as written); §3.7's three stated exceptions to the leaf
   rule against the new files sourcing sentence; §6's FM-3-paragraph
   completeness claim; and OQ-4's argument, which N1's fix restores.

4. Frontmatter is unchanged: the document stays status draft, last-reviewed
   null. This is a revision of a draft, not a status transition. Run
   bin/check-frontmatter --all in the worktree; it must exit 0 before the TRD
   is pushed.

5. Land the revision as a second commit on bin-land-trd-3, referencing cycle 3;
   push plain; verify by ls-remote and by reading specs/bin-land-trd.md back at
   the pushed head; report the head SHA and both commit SHAs, each read back
   from git.

Do not:
- modify any file other than docs/cycles/bin-land-trd-3-20260824T000622Z.md and
  specs/bin-land-trd.md — the blast radius is those two files only. This
  explicitly excludes specs/bin-land.md and specs/trd-template.md, both agreed,
  both named above as read-only companions.
- amend AC-LAND-06, AC-LAND-07, AC-LAND-09, or any other PRD acceptance
  criterion or goal.
- retire, restate, or soften OQ-6 or §4.3 — Dave's standing decision holds it
  open.
- flip the TRD's frontmatter status or set last-reviewed.
- renumber any existing failure mode, open technical question, or §3.2 step.
- merge, open a PR, force-push, or delete any ref — merging is handled from the
  decision session.
- restate a reviewer Fix field as document text; the Fix fields are pointers to
  intent, not wording to be pasted.

STOP and surface rather than improvise: origin/main not at
dc33f452658d015fb8e33c1325e3d05cb7a1d53d at fetch; 97888a83 absent from
history, or any commit since it having touched specs/bin-land-trd.md; any
instruction unexecutable as written; concurrent tree mutation (a file this
session did not change moving, HEAD moving, an index lock).

Report: what was done, not what this file says; all SHAs read back from git;
the check-frontmatter result; both sweep results separately; each cell of the
rewritten §6 column as landed; every claim labelled observed / inferred /
told / unknown.
