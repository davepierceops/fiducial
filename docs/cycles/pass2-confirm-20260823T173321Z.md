Write this directive verbatim to docs/cycles/pass2-confirm-<timestamp>.md (timestamp ISO 8601 basic with trailing Z, matching the pass2-* siblings; generated now). Run `git checkout origin/main` then `git checkout -b pass2-confirm`. Commit the directive file, push the branch, open and squash-merge a PR via curl against the GitHub REST API using the credential-helper token held in an environment variable — never written to a file. Never invoke gh for anything, including auth checks; its errors are not evidence. Report the merged SHA. Then execute the rest on a fresh branch from origin/main, pass2-confirm-2, merged the same way; report that SHA too.

You are the Context Quality Reviewer. Read roles/context-quality-reviewer.md, docs/global-context/review-rubric.md, skills/review-artifact.md, policies/document-metadata-policy.md, docs/global-context/core.md, all whole at main, before acting.

One dictated edit precedes the review (this directive is the origin): docs/global-context/decision-layer.md rule 13, append the sentence "Every session end that hands responsibility forward emits one." — restoring an obligation a prior fix dropped; no other change. Commit it before the review begins so the review reads the restored text.

Scope: compute it, then review it.

1. Compute the set: every in-scope file (bin/check-frontmatter's 51) whose status is not agreed, plus every agreed file whose last-reviewed pointer fails the policy's rule — the cited artifact does not state, in its own scope, that it reviewed that file at the cited SHA with verdict ready or ready-with-findings. Report the computed set and its size before reviewing.
2. Review each file in the set, whole, against all twelve criteria of the rubric at main HEAD (record the SHA). Criterion 12 binds: name what each file was cross-checked against.
3. Write one review artifact per file at reviews/<stem>-cycle-<n>.md, n continuing that file's numbering, verdict first, schema exact. A clean file's artifact is the header alone.
4. Findings: blocking findings are recorded, not fixed — no file body is edited beyond the one dictated sentence. A file with blocking findings is not flipped.
5. Flips, pre-authorized by Dave in this directive: every file in the set whose verdict lands ready or ready-with-findings is flipped via bin/flip-agreed, last-reviewed citing its new artifact and the reviewed SHA. Files already agreed whose pointer failed get re-pointed the same way. Run flips as sequential standalone invocations, not a shell loop — a loop broke env python3 resolution in this sandbox once.
6. Run bin/check-frontmatter --all, exit 0 required; bin/tests/run, only the two accepted AC-BN-10 failures permitted.

Label every claim observed, inferred, told, or unknown. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface. Report: both SHAs, computed set size, verdict counts by class, files flipped, files held with blocking findings and one line each on why, check and test results, and anything needing Dave's judgment, one line each, up front.
