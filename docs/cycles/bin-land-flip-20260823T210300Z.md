Write this directive verbatim to docs/cycles/bin-land-flip-20260823T210300Z.md as your first act, after establishing a current base: run git fetch origin, confirm git rev-parse origin/main returns 4ee717d3fbe255cf5202ddb74b4727fbe0154b69 (the base this directive was authored against; if it returns something else, fetch once more, and if still different stop and surface — do not proceed on a differing base), then create a worktree via git worktree add $TMPDIR/fiducial-bin-land-flip origin/main (use /tmp/claude-501/fiducial-bin-land-flip if $TMPDIR is unset; a ../ sibling path is denied by the sandbox, executor-observed 2026-08-23) — this worktree is this directive's exclusive working-tree assignment — work only in that directory, git checkout -b bin-land-flip there, commit the directive file, push to bin-land-flip, report the SHA. Then proceed.

CONTEXT. Dave has agreed specs/bin-land.md as reviewed in cycle 7. reviews/bin-land-cycle-7.md is on main; its Reviewed line reads: Reviewed: specs/bin-land.md @ 3716ae6. The prior attempt (docs/cycles/bin-land-agree-20260823T205500Z.md @ 643c8877) completed its tracker entry but its flip step was blocked by a stale worktree base; this directive carries only the flip. Read from origin/main before acting: specs/bin-land.md (frontmatter), reviews/bin-land-cycle-7.md, policies/document-metadata-policy.md (agreement-flip and status-transition rules).

STOP CONDITION, pinned to the reviewed ref: if specs/bin-land.md at origin/main differs from the content committed at 3716ae67414b39fa1a0cf69b4719824e1740dab9, stop and surface; do not reconcile.

INSTRUCTION, one commit:
1. Agreement flip, frontmatter-only: run bin/flip-agreed for specs/bin-land.md with last-reviewed citing reviews/bin-land-cycle-7.md @ 3716ae6 — the exact form the artifact's own Reviewed line states. The commit contains nothing but the frontmatter transition to status: agreed. If bin/flip-agreed refuses or fails closed, stop and surface — do not hand-edit the frontmatter to work around it.
Push. Report the SHA with provenance labels, and report bin/flip-agreed's own output verbatim.

CONSTRAINTS, scoped to this work: touch only docs/cycles/bin-land-flip-20260823T210300Z.md and specs/bin-land.md (frontmatter only, via the tool). Do not merge — merging happens from the decision session over the connector. Do not edit LEXICON.md, OPEN-ITEMS.md, or any review artifact.

SANDBOX LORE, provenance docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650, carried as told, with three executor-observed additions from 2026-08-23: git fetch origin before any git worktree add, and confirm origin/main matches the directive's cited base — a worktree cut from a cached ref carries a stale tree silently (executor-observed: bin-land-agree branched from a stale origin/main and its flip step was unexecutable). git checkout origin/main then git checkout -b <branch> guards a stale tree — the config-unwritable rationale for that guard is wrong, executor-observed. Never invoke gh; its errors are not evidence about credentials; reason from first principles. fatal: failed to store: 100001 on stderr is keychain noise; exit status is correct; verify by ls-remote. Never git push -u: the upstream-config write fails (.git/config unwritable, executor-observed 2026-08-23) though the push itself succeeds; push plain and verify by ls-remote. Sequential standalone invocations, never a shell loop. An instruction that cannot be executed as written: stop and surface.
