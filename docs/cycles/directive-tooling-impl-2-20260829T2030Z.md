You are the Coder, an execution session on davepierceops/fiducial, clone at ~/code/fiducial. Implement bin/check-directive, the lint, with bin/aimeta/elements.py, and turn bin/tests/test_check_directive.py green. This completes TRD §3.9 migration step 2; the generator's general mode landed in package 1 and you build on it. You are a different agent from the Test Designer; you do not edit tests, the TRD, the PRD, or skills/directive-invariants.md. Where a test and the TRD disagree, stop and report.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-tooling-impl-2-20260829T2030Z.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: directive-tooling implementation package 2 (lint)", push with git push origin directive-tooling-impl-2 (no -u: the sandbox cannot write .git/config), and report the SHA.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-dt-impl-2", created by: git worktree add --no-track "$TMPDIR/fiducial-dt-impl-2" -b directive-tooling-impl-2 origin/main
Before creating it, run git worktree list; if any existing worktree holds branch directive-tooling-impl-2, or if "$TMPDIR/fiducial-dt-impl-2" already exists, stop and report. Do not touch the main tree except for the final worktree removal. Never bypass the pre-commit hook.

BASE VERIFICATION before anything else: git fetch origin (an osxkeychain "failed to store" message is noise; judge the fetch by the refs). origin/main must be at c35133a59a8f79e5802db6f740fadbe8550d664e. If it has moved, stop and report.

READ before writing, whole, from the worktree: specs/directive-tooling-trd.md (§3.1, §3.4–§3.8, §4.1, §6, §7, §8 are your contract — read all of it); specs/directive-tooling.md §4 (the judgment-only set the unchecked set must name) and §6 (AC-DT-06..13, 17, 19, and M8's three filename patterns); bin/aimeta/invariants.py, mdmask.py, directive.py and bin/directive as landed (the label, match rule, marker syntax, preamble markers and match phrases are already exposed by invariants.py — reuse them; the mask is mdmask.py — reuse it; add to those modules only what the lint needs and change no behaviour the generator relies on); bin/aimeta/cli.py and repo.py (reused unchanged); bin/tests/helpers.py (the fixture substrate and the bracketed-code convention); bin/tests/test_check_directive.py and bin/tests/test_directive_trd.py (what must go green); bin/tests/test_cross_cutting.py (AC-X-1..7, which now cover the lint once its name is in CLI_NAMES); docs/cycles/directive-tooling-impl-1-20260829T1400Z.md (package 1's directive, for the decisions it recorded).

SCOPE, one commit.
- New: bin/aimeta/elements.py — M1 through M8, one function each, over (root, relpath, text, eligible_lines, invariants), returning pass, fail with a named cause, or unknown; no exit codes, no output formatting, no element reads another's result. Match, extent, anchoring, mask and derivation per §3.6 exactly. M3 per §3.4 and §3.5: the label and match rule from the Disposition label section; extent to the first blank line; exclusive-assignment form is a git worktree add invocation plus a quoted or backticked path-shaped token; sole-tree form is the canonical sentence from the same section, literal. M2 per §3.6: cat-file -e <sha>^{commit} for resolution, then the UNPEELED object type must be a commit, then the path present in the commit's tree, then diff-tree --root --no-commit-id --name-only -r <sha> -- <path> non-empty; never git log with a pathspec; a merge commit fails citation-not-touching. Unknown is exactly the class §3.6 step 5 names — a git read that fails for a reason not attributable to the directive — and maps to element-unknown, exit 1.
- New: bin/check-directive — the house shape (shebang, argparse, sys.path insert, aimeta imports, sys.exit(cli.run(main))). One required positional path; --help exits 0; no other flag. Sequence per §3.6: resolve the path via cli.load_root and cli.relpath_of (outside the repository → path-outside-repo; absent → path-absent; both exit 2, naming no element); read once, decode UTF-8 explicitly (undecodable → exit 2); build the mask; load invariants from the methodology home at their last commit there, exactly as the generator does (absent → invariants-missing; a missing section → invariants-section-missing; uncommitted modifications → invariants-dirty, checked after the absence check, no --allow-dirty; all exit 2); decide M1–M8; report; exit.
- Report on stdout, on BOTH exit paths: one line per element M1..M8 with its result; for each failure its bracketed code from §7's table and the governed source the requirement derives from, by path — M1, M3 and M8 cite skills/directive-authoring.md, M4, M5 and M7 cite docs/global-context/core.md, M6 cites docs/global-context/decision-layer.md; then the unchecked set, naming at minimum: executability of the working-tree disposition, route, model tier, every judgment-only rule PRD §4 lists (the tests look for the words contradict, blast radius, dictated, self-contained), and mode-appropriateness of the filename. Every claim the lint makes is labelled observed or unknown and no other class; the words inferred and told do not appear in its output. Exit 0 only when all eight pass; 1 when any fails or is unknown; 2 for refused invocations (§7). Diagnostics on stderr carry the bracketed codes in the form helpers.py pins.
- The lint holds no literal for the label, the match rule, the marker syntax, the preamble markers, or any match phrase: all come from invariants.py. It writes nothing, anywhere, and runs no git subcommand beyond cat-file, diff-tree, rev-parse, status and the reads repo.py already performs.
- AC-DT-12(a) trap, stated so you do not hit it: the static scan rejects any string constant in your source that, stripped, equals one of gh, push, fetch, ls-remote, remote, clone, commit, add, stage, update-index, credential. M2's object-type step therefore cannot compare against a bare "commit" constant. Decide the type by exclusion — cat-file -t returns one of four words, so a type that is tag, blob or tree fails citation-unresolvable, and anything else that resolved is a commit — or by any other means that leaves no such constant in the file. The same scan covers elements.py and check-directive.
- bin/tests/helpers.py: add "check-directive" to CLI_NAMES and CLI_MINIMAL_ARGS with argv that clears argparse and reaches cli.load_root, so AC-X-4 gets the documented exit outside a repository. Nothing else in bin/tests/ changes.
- Do not touch bin/cycle-open, bin/tests/test_*.py, skills/, specs/, docs/global-context/, policies/, roles/.
- Commit message: "directive-tooling: bin/check-directive (migration step 2, lint)". Push with git push origin directive-tooling-impl-2.

VERIFICATION after the commit, from the worktree, output to "$TMPDIR/fiducial-dt-impl-2-run.log": bin/tests/run for the whole suite, and bin/check-frontmatter --all (must exit 0; state the count).
Expected state, and a stop if it differs: the 424 pre-existing tests green, including test_cross_cutting.py now covering both binaries; test_check_directive.py entirely green (84 tests; subTest records may exceed that count); test_directive_trd.py green except its 6 skips and test_route_and_model_are_committed_in_both_modes_and_the_author_count_stays_two, which reaches cycle mode and stays red with [cycle-mode-unavailable] until step 3; test_directive.py red on exactly the same six cases as main today — five cycle-mode refusals and test_ac_dt_14_bin_holds_exactly_one_directive_skeleton_generator. Any red outside that set is a stop: report it, do not adjust a test.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and both pushes are verified landed (git ls-remote origin directive-tooling-impl-2 shows your implementation commit SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-dt-impl-2" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref c35133a59a8f79e5802db6f740fadbe8550d664e: on any failed command, any precondition not met, any test whose expectation contradicts the TRD, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; implementation commit SHA; branch name; run-log path; pass/fail/skip counts for the whole suite and for each of test_check_directive.py, test_directive_trd.py, test_directive.py; the named list of red cases across the three files with the one-line reason each; check-frontmatter exit code and count; a numbered list of anything the TRD left you to decide, with what you chose and where in the code it lives; anything observed this directive did not anticipate; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
