# criteria rows: R1015 and R1171 stop naming a criteria document; bin/land becomes agent-facing

Provenance: issued by the fiducial decision session for the repository owner,
davepierceops; the tree it names is the one it is written to. This is the first
directive that instructs an executor to invoke bin/land; that instruction is
the human's ruling of 2026-09-16, recorded by the entry this package appends.

ROUTE AND MODEL

Route: fresh session
Model: solid general-purpose

FIRST ACT

Write this directive verbatim to docs/cycles/criteria-rows-20260917T030541Z.md, commit it alone with a
message naming the package it opens, push the branch to origin, and report the
SHA. Do this before reading anything else and before touching any other file.

DISPOSITION PROMPT

Fence note: the inner fences below are `~~~` so this directive pastes whole
inside a `~~~~` block.

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

~~~text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
~~~

Both admitted forms, worked:

~~~text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<n>", created by: git worktree add --no-track "wt/<n>" -b
<n> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
~~~

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-criteria-rows", created by: git worktree add --no-track "$TMPDIR/fiducial-criteria-rows" -b criteria-rows origin/main

Order of operations: fetch origin main, create the worktree first, then
perform FIRST ACT inside it. Holder check, before the worktree is created: run
git worktree list, and stop and report if any worktree holds branch
criteria-rows or the path already exists.

Landing, for FIRST ACT and for every later commit this session makes: run
bin/land from the worktree root — bin/land criteria-rows "<message>" <paths>
for FIRST ACT with the directive file as its one path, and bin/land
criteria-rows "<message>" with no paths for the session's work, which stages
every change in the tree. bin/land fetches, resolves the base from origin,
guards against divergence, stages, commits, pushes once, and reads the
result back; it creates criteria-rows at origin on the first landing. Its
stdout is a JSON report with every fact labelled observed or unknown, and
exit 0 means the landing was verified, not merely attempted. Paste each
report verbatim into your report. bin/land performs FIRST ACT's commit and
push; run no git commit and no git push by hand. A non-zero exit is a stop: report the JSON and the stderr
diagnostic and do nothing else to the remote. No upstream is set. The
decision session opens the pull request.

BASE VERIFICATION

Before anything else, fetch and confirm the base is at the reviewed ref
8127f1eb0778a90fca898a1dafdbaee4285a0065. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- process/decision-log.md @ 38aab413b615b65c2059356557d27bc585b0de3e — the entry form and the numbering rule
- specs/bin-land.md @ 9e61252b9ff425392a4a99a52f40456033ad924c — read §3 (user journeys J1, J2, J3) and §8 (open product question Q3) whole; J1 and J2 are the two landings this directive makes with the tool
- rules/R1013.md, rules/R1015.md, rules/R1171.md, rules/R0853.md, at the base ref — the row R1015 will cite, the two rows edited, and their sibling
- decisions/log.md, at the base ref — the last entry's number is the numbering base

TASK

Intent: two rows still name "the Criteria", a document no bundle carries since
the prose-criteria rows landed on 2026-09-09; the 2026-09-10 read
(reviews/process-criteria-refs-read-20260910T193000Z.md, F1 and F3) labels
them blocking at the next release cut. Replace each row's body with the body
dictated below, byte for byte, changing nothing in either row's frontmatter.
Then append one decision-log entry, in the form process/decision-log.md
states, numbered by its rule from the last entry on the base, dated
2026-09-16, with the dictated title, Decision line and Context line landed
verbatim as the text after the composed heading.

1. rules/R1015.md — body becomes:

~~~text
Where you find a tell that [R0853] does not name, report it and propose the line that would add it.
~~~

2. rules/R1171.md — body becomes (the Human section below the body is
   untouched):

~~~text
When the author says the work is done, emit the voice harvest — one paste block, one line per rule this session showed that the Voice document or the prose-criteria rows in this bundle do not yet state — or, where it showed none, say so in one line and emit no block.
~~~

3. Title: bin/land is agent-facing

~~~text
Decision: bin/land is agent-facing from this entry: a directive may instruct an executor to invoke it, and the executor lands the directive file and the session's work with it rather than by hand. The precondition set on 2026-08-23 — a governed usage document agreed first — is withdrawn; specs/bin-land.md's user journeys J1 and J2 are the tool's usage statement, and the directive that invokes it says what its report and exit status mean. The four untriaged finding classes against specs/bin-land-trd.md are filed on the tracker as deferred and do not hold the tool.
Context: the human's ruling of 2026-09-16 on learning the tool had been built, tested and unused since August; the sequencing decision it withdraws lives in docs/cycles/bin-land-spec-7-20260823T203500Z.md @ 8a77c2a and specs/bin-land.md §8 Q3, not in this log, so there is no entry to supersede. The first directive to invoke bin/land is the one that lands this entry.
~~~

Commits: the directive commit from FIRST ACT, then exactly one more carrying
the two row edits and the entry, message "criteria rows: R1015 cites the
tells row, R1171 names the prose-criteria rows; DEC entry makes bin/land
agent-facing". Both land with bin/land as the disposition states.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

- bin/tests/run 2>&1 | tee "$TMPDIR/criteria-rows.log" — expected OK, with the same skips the base shows.
- grep -rn -i "the Criteria" rules/ process/ README.md 2>&1 | tee -a "$TMPDIR/criteria-rows.log" — expected no output; report the count observed.
- For each of the three fenced blocks: diff the landed text against the fenced block in this directive at its committed SHA, byte for byte; report three results, expected identical.
- git diff --stat 8127f1e..HEAD 2>&1 | tee -a "$TMPDIR/criteria-rows.log" — report the file list; the intent is rules/R1015.md, rules/R1171.md, decisions/log.md and this directive, nothing else.
- Both bin/land JSON reports, verbatim, with each landing's observed remote SHA; note that bin/land was run from a worktree, and whether anything about that differed from what specs/bin-land.md describes.

STOP CONDITIONS

Pinned to the reviewed ref 8127f1eb0778a90fca898a1dafdbaee4285a0065. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

## Decisions

The three dictated texts; nothing further. The entry's number is derived
from the log on the base and reported.

## Deferred / out of scope

- The Context Quality Reviewer's intake read over this delta — the decision
  session directs it after this lands.
- The process/directive-invariants.md edit that makes bin/land the generated
  skeleton's landing step — the owed process delta.
- The one-line closure of specs/bin-land.md §8 Q3 — a spec edit, on a spec
  branch.
- The four bin-land-trd finding classes — a tracker issue.

## Execution notes

Remove the worktree after the report is composed and both landings are
verified by their reports, without force and without retry, and state the
outcome as the report's final line. The log at "$TMPDIR/criteria-rows.log" is
outside the worktree.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    criteria rows: R1015 and R1171 stop naming a criteria document; bin/land becomes agent-facing — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    Provenance — author region
    ROUTE AND MODEL — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    FIRST ACT — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    DISPOSITION PROMPT — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    COMPANIONS — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    TASK — author region
    SANDBOX — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    VERIFICATION — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    STOP CONDITIONS — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    REPORT — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    CLAIM LABELS — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2
    Decisions — author region
    Deferred / out of scope — author region
    Execution notes — author region
    SOURCE MANIFEST — process/directive-invariants.md @ 77625d370db5963b85695fba51a86faea7c1f4f2

ROUTE AND MODEL

Route: fresh session
Model: solid general-purpose
