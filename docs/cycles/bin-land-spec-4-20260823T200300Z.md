Write this directive verbatim to docs/cycles/bin-land-spec-4-20260823T200300Z.md as your first act: create a worktree via git worktree add $TMPDIR/fiducial-bin-land-spec-4 origin/main (use /tmp/claude-501/fiducial-bin-land-spec-4 if $TMPDIR is unset; a ../ sibling path is denied by the sandbox, executor-observed 2026-08-23), work only in that directory, git checkout -b bin-land-spec-4 there, commit the directive file, push to bin-land-spec-4, report the SHA. Then proceed.

CONTEXT. specs/bin-land.md is draft; cycle 3 merged to main at 7d41838866ee0c337903cfa89584721430b66b96; reviews/bin-land-cycle-3.md verdict ready-with-findings (F1 non-blocking; O1, O2, O3 observations), reviewed specs/bin-land.md @ 3db11c46f2e5ca550bb3bdcf175d98e1bf145ac1. Read from origin/main before editing: specs/bin-land.md, reviews/bin-land-cycle-3.md, reviews/bin-land-cycle-2.md, docs/global-context/core.md, LEXICON.md, and the review artifact schema (skills/review-artifact.md if present, else the schema section in skills/spec-review-cycle.md).

STOP CONDITION, pinned to the reviewed ref: if specs/bin-land.md at origin/main differs from the content committed at 3db11c46, stop and surface; do not reconcile.

INSTRUCTIONS. Three edits to specs/bin-land.md, one commit:
1. Cycle-3 F1: add the clause to AC-LAND-01b that induces the starting state G1's second arm rules on — a local tree behind the branch head as fetched — so the criterion sets up the case the sentence exists to govern. Draft per that finding's Fix field: a clause, not a new criterion.
2. Cycle-3 O2, taken by Dave's decision — this directive is the origin of the scope decision; the behaviour wording is the executor's, bounded as follows: state in G1's second arm what happens when the local tree carries commits the fetched remote head lacks (the diverged case cycle-2 F1 named). Draft the behaviour consistent with §7's asymmetric posture — refuse loudly rather than act on ambiguity; local-only commits are never silently rebased, discarded, or incorporated; the condition is named in the output. Add the matching acceptance coverage in §6 (a clause or one criterion, whichever the schema of §6 makes cleaner).
3. Cycle-3 O1: bring §1's provenance register current — it names two directive origins and now stands over four. Either name all four (cycle-1 authorship, cycle-2, cycle-3 @ 621b007e, and this directive) or restate the paragraph in a form that remains true as origins accumulate. Prefer the durable form.
Frontmatter untouched: status: draft, last-reviewed: null.

Then, as Spec Reviewer: re-gate the revised spec. Write reviews/bin-land-cycle-4.md per the review artifact schema, Reviewed naming specs/bin-land.md @ the revision commit SHA you just landed, confirming cycle-3 F1, O1, O2 addressed, plus anything new the re-check finds. Commit it separately on bin-land-spec-4. Push. Report every SHA with provenance labels.

CONSTRAINTS, scoped to this work: touch only docs/cycles/bin-land-spec-4-20260823T200300Z.md, specs/bin-land.md, reviews/bin-land-cycle-4.md. Do not merge — merging happens from the decision session over the connector. Do not edit LEXICON.md.

SANDBOX LORE, provenance docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650, carried as told, with two executor-observed additions from 2026-08-23: git checkout origin/main then git checkout -b <branch> guards a stale tree — the config-unwritable rationale for that guard is wrong, executor-observed. Never invoke gh; its errors are not evidence about credentials; reason from first principles. fatal: failed to store: 100001 on stderr is keychain noise; exit status is correct; verify by ls-remote. Never git push -u: the upstream-config write fails (.git/config unwritable, executor-observed 2026-08-23) though the push itself succeeds; push plain and verify by ls-remote. Sequential standalone invocations, never a shell loop. An instruction that cannot be executed as written: stop and surface.
