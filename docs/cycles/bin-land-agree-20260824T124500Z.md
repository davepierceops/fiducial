# Directive: bin/land — agree the spec, record the convergence

Role: Chief of Staff, acting on Dave's agreement. Route: fresh execution session.
Model tier: Sonnet 5 — mechanical work behind a tool that fails closed.
Base: main @ b31b75af12648585d7ac86f7c0d11ad85f883f5f.

Dave has agreed specs/bin-land-trd.md and bin/tests/test_land.py together. This
session records that agreement. It changes no content in either artifact.

1. First act — write this directive file verbatim to
   docs/cycles/bin-land-agree-20260824T124500Z.md in the clone stated below,
   commit it on branch bin-land-agree, push plain, verify by ls-remote, and
   report the SHA read back from git.

Clone, not worktree — the sandbox denies writes to a checkout's .git directory:

    git clone https://github.com/davepierceops/fiducial.git "$TMPDIR/fiducial-bin-land-agree"
    cd "$TMPDIR/fiducial-bin-land-agree"
    git rev-parse origin/main
    # must print b31b75af12648585d7ac86f7c0d11ad85f883f5f — if not, STOP and surface
    git checkout -b bin-land-agree b31b75af12648585d7ac86f7c0d11ad85f883f5f

Fallback if $TMPDIR is unset: /tmp/claude-501/fiducial-bin-land-agree. All
repository work happens there. Do not touch /Users/dave/code/fiducial.

Sandbox companions — read both at main b31b75af, follow as told except their
worktree language, which this directive overrides:
- docs/cycles/pass2-held-fix-20260823T180753Z.md
- docs/cycles/bin-land-flip-20260823T210300Z.md

## Why the expedited path and not a review artifact

Dave's decision, stated inline here as its origin.

specs/bin-land-trd.md went through eight reviewer-gated cycles. The last,
reviews/bin-land-trd-cycle-8.md, returned changes-required. Everything after it
was spec/test convergence rather than review: the spec and the tests were opened
together, findings flowed both ways, and the pair converged. The evidence for
this agreement is that convergence — including a mechanical check that parsed
§7's code table out of the spec, imported the test module, and diffed the two —
not a ninth reading.

The expedited path is stretched here: policies/document-metadata-policy.md
frames it for small doc-only changes, not for agreeing a 1,400-line TRD. Dave
has taken it deliberately, judging that forcing a reviewer pass to satisfy a
pointer would be ceremony over evidence that is already stronger. Record this
reasoning in the log entry itself; a later reader must find a decision, not a
shortcut.

bin/tests/test_land.py is code and carries no frontmatter, so it cannot hold a
status. The pairing lives in the log entry, which names it and the commit it was
agreed at.

## What to do

2. **Append one entry to reviews/expedited-log.md**, in the log's existing
   format — read the file and match it; do not invent a shape. The entry records:
   - the agreement date, 2026-08-24
   - specs/bin-land-trd.md at the commit it is agreed at
   - bin/tests/test_land.py at the same commit, named as agreed alongside it
   - the evidence: spec/test convergence over four rounds, the mechanical
     agreement check, and the behavioural red-gate at 440 tests / 137 failures /
     1 skip with all test_land failures behavioural
   - the reasoning above for why this path rather than a review artifact
   - the seven open technical questions, noted as open at agreement — OQ-6 among
     them — so the agreement is not read as closing them

   The append is a separate commit from the status flip. Read the entry's commit
   SHA back from git; the flip cites it.

3. **Flip the spec** with `bin/flip-agreed`. Do not hand-edit frontmatter.
   `last-reviewed` cites reviews/expedited-log.md at the SHA of the commit that
   added the entry. The tool fails closed when the cited SHA does not resolve to
   an entry in the log; if it fails, STOP and surface — do not work around it.

4. Run `bin/check-frontmatter --all`; it must exit 0.

5. Run `bin/tests/run`. Expect 440 tests, 137 failures, 1 skip, unchanged — this
   session changes no code and no assertion. A different count means something
   moved that should not have; STOP and surface.

## Do not
- modify specs/bin-land-trd.md's content. Frontmatter only, and only via
  bin/flip-agreed.
- modify bin/tests/test_land.py at all.
- implement bin/land. The Coder is the next session.
- touch the stub at bin/tests/fixtures/stub/.
- flip any other document.
- resolve, retire, or reword any open technical question.
- hand-edit frontmatter, or edit reviews/expedited-log.md other than appending
  the one entry.
- touch bin/tests/run's two known AC-BN-10 bundle failures.
- merge, open a PR, force-push, or delete any ref.

## STOP and surface rather than improvise
- origin/main not at b31b75af12648585d7ac86f7c0d11ad85f883f5f
- bin/flip-agreed failing for any reason
- check-frontmatter not exiting 0
- the suite count differing from 440/137/1
- the log's existing format not accommodating what this entry must record

## Before pushing
Push plain, verify by ls-remote, and read specs/bin-land-trd.md's frontmatter
back at the pushed head — it must read status: agreed with last-reviewed citing
the log entry's SHA. Report the head SHA and every commit SHA, each read back
from git.

## Report
What was done, not what this file says. All SHAs. The log entry as landed. The
frontmatter as read back from the pushed head. The flip-agreed output. The
check-frontmatter result and the suite counts. Any seam where you extended
beyond this directive, named as such. Every claim labelled observed / inferred /
told / unknown.
