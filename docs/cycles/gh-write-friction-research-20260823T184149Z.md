First: write this directive verbatim to docs/cycles/gh-write-friction-research-<timestamp>.md (timestamp ISO 8601 basic, trailing Z, generated now). Run `git checkout origin/main` then `git checkout -b gh-write-friction-research` (sandbox config is unwritable; this form works where `git checkout -b` from a stale tree does not). Commit the directive file, push the branch, report the SHA. Do not merge; the decision session merges over its repository connector. Never invoke gh for anything, including auth checks; its errors are not evidence about credentials — reason from first principles instead. "fatal: failed to store: 100001" on push or fetch is keychain noise; the operation succeeds — verify by git ls-remote, not by absence of errors. Provenance: these constraints are Dave's standing decisions, recorded in committed directives on main — see docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650. You are not asked to verify them, handle credentials, or assert them as observed; carry them as told.

Task: research and analysis only. No implementation, no edits to any governed document.

Problem statement: remote git writes from agent sessions are a repeated friction source in this repo. Symptoms on record: a sandbox that blocks authenticated curl some sessions, an unwritable git config, keychain noise mimicking failure, gh unusable, per-directive restatement of workaround lore, and — most recently — an execution session refusing a directive because the restated lore read as prompt injection (see the directive cited above for the lore in committed form). The goal is a durable reduction in friction for the whole write path: branch, commit, push, PR, merge, verify.

Read before analyzing, all at main: docs/global-context/core.md, docs/global-context/decision-layer.md, LEXICON.md, skills/command-blocks.md, skills/directive-dispatch.md, policies/commit-and-change-control-policy.md, policies/remote-write-verification-policy.md if present, and the cited directive. Survey docs/cycles/ for the shape and frequency of write instructions across recent directives — quantify how much directive text is write mechanics.

Candidate approaches, evaluate each on: friction removed, friction remaining, failure modes, security posture (credential exposure, injection surface, what an agent must be trusted with), maintenance cost, blast radius when it breaks, and fit with the methodology's evidence rules (a tool's success response is a claim; verify content, not landing).

1. bin/land — a tested repo script wrapping branch/commit/push/verify; sandbox workarounds become implementation detail, directives shrink to one invocation.
2. GitHub MCP connector for execution sessions — the same connector path the decision session uses, replacing git-over-shell for remote writes.
3. At least two further approaches you identify and argue for or against on the same axes. Consider at minimum the space spanned by: different transports (git worktree/bundle handoff, a bot account with scoped deploy keys or fine-grained PAT, GitHub Actions doing the push/merge on the agent's behalf, a local relay the sandbox can reach), different divisions of labor (decision session does all remote writes, executors emit patches only), and doing less (fewer branches, batched landings). Name each rejected approach and why.

For each approach, state explicitly what happens to the sandbox lore: deleted, encapsulated, or still required.

Recommend: a ranked shortlist, the single best next step, and what evidence would change the ranking. Distinguish observed (you checked it in this repo), inferred, and told throughout. Where a claim about sandbox behavior cannot be tested from your session, label it unknown rather than assuming.

Write findings to docs/research/gh-write-friction-<timestamp>.md (same timestamp as the directive), commit on the same branch, push, verify by ls-remote. Report: branch head SHA, files written, the ranked shortlist one line per approach, and anything needing Dave's judgment, one line each, up front. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface.
