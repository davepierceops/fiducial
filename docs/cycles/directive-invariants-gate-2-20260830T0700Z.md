You are the Context Quality Reviewer, an execution session on davepierceops/fiducial, clone at ~/code/fiducial. Confirmation gate, cycle 2, over skills/directive-invariants.md at its cycle-1 revision. You edit nothing but the artifact. You do not agree the document and do not flip any status.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-invariants-gate-2-20260830T0700Z.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: directive-invariants gate cycle 2", push with git push origin directive-invariants-gate-2 (no -u), and report the SHA. Never bypass the pre-commit hook.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-di-gate-2", created by: git worktree add --no-track "$TMPDIR/fiducial-di-gate-2" -b directive-invariants-gate-2 origin/directive-invariants-rev-1
Before creating it, run git fetch origin directive-invariants-rev-1, then git worktree list; if any existing worktree holds branch directive-invariants-gate-2, or if "$TMPDIR/fiducial-di-gate-2" already exists, stop and report. Entries git marks prunable are not yours; ignore them. No other session holds this branch or this worktree. Do not touch the main tree except for the final worktree removal.

BASE VERIFICATION before anything else: git fetch origin, git fetch origin directive-invariants-rev-1, git fetch origin directive-invariants-gate. Judge each fetch by the refs it reports, not by credential-helper noise on stderr. origin/directive-invariants-rev-1 must be exactly 875bfb2ab714a7011ffe45850f3709ec61ec5ca6; if it is anything else, stop and report. origin/directive-invariants-gate must contain bc2577fa70a346efd67fe7e68e9d0e8898cca0fc.

READ before writing, whole: the cycle-1 artifact via git show bc2577fa70a346efd67fe7e68e9d0e8898cca0fc:reviews/directive-invariants-cycle-1.md; the decision record via git show 2c422c67e208bf7d978b95140d05d2e7551f7e03:docs/cycles/directive-invariants-rev-1b-20260830T0615Z.md and git show bbb9d1280afe3459443b7eed69b0732026ac1997:docs/cycles/directive-invariants-rev-1c-20260830T0630Z.md; and from the worktree: roles/context-quality-reviewer.md; docs/global-context/core.md; docs/global-context/review-rubric.md; skills/review-artifact.md; skills/directive-invariants.md (the document under review, at 875bfb2); skills/directive-authoring.md; specs/directive-tooling-trd.md §3.3, §3.4, §3.5, §3.6.

TASK. Write reviews/directive-invariants-cycle-2.md in the review-artifact schema exactly. Reviewed: skills/directive-invariants.md @ 875bfb2ab714a7011ffe45850f3709ec61ec5ca6. Prior cycle: reviews/directive-invariants-cycle-1.md. State the cycle-1 baseline 7c233c1506dc6111194b5fe603f2fd2f967d4998 in the Scope line. Two parts:
1. Confirmation. For each cycle-1 entry accepted in the decision record (F-1, F-2, F-3, F-6, F-7, F-8, F-9, F-10, F-11, F-12, O-1), state in the Scope line or one observation entry whether the revision resolves it; a resolved entry needs no finding. F-4 and F-5 were rejected with recorded grounds and are not re-raised; F-3's eligibility and stripping prose is to be cross-checked against the TRD's masking rules, verified by running the shipped masker where a claim can be tested that way.
2. Full pass. Review the whole document at 875bfb2 against all twelve rubric criteria. Anything the revision introduced is a finding with the criterion cited. Known item, state it as you find it: the worked exclusive-assignment example's two path tokens.
Cross-checks named on the Cross-checked line with results: the disposition-prompt fence against skills/directive-authoring.md's first bullet, byte equality after flowing, command and result verbatim; the per-region placeholder list in the preamble against the actual {{…}} tokens in each region body; the disposition label's five parts against TRD §3.4. Not inspected is required and explicit. Verdict is ready, ready-with-findings, or changes-required — never "agreed". A clean confirmation pass is the header and nothing else.

SCOPE, one commit: reviews/directive-invariants-cycle-2.md only. Do not edit skills/, specs/, docs/global-context/, policies/, roles/, bin/, or any existing file. Commit message: "Review: skills/directive-invariants.md cycle 2 (confirmation)". Push with git push origin directive-invariants-gate-2.

VERIFICATION after the commit, from the worktree, output to "$TMPDIR/fiducial-di-gate-2-run.log": bin/check-frontmatter --all (state exit code and count). Expected state, and a stop if it differs: exit 0, 61 files / 14 globs. The bin/ test suite is not run by this directive; report it as not run.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and both pushes are verified landed (git ls-remote origin directive-invariants-gate-2 shows your commit SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-di-gate-2" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref 875bfb2ab714a7011ffe45850f3709ec61ec5ca6: on any failed command, any precondition not met, a document you were told to read that is absent, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; artifact commit SHA; branch name; run-log path; the artifact's header block verbatim; findings count by severity; per accepted cycle-1 entry, resolved or not, one line each; the three cross-check results in one line each; check-frontmatter exit code and count; anything observed this directive did not anticipate; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
