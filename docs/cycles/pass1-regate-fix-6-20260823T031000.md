# Directive — Pass 1 re-gate fixes, part 6

Date: 2026-08-23
Route: fresh
Model: cheap
Role: Implementer (execution session). Edits are dictated below.

## Working-tree rule

This session runs in a clone no other session is using. If any file this session did not change moves, or HEAD moves, or an index lock appears, stop and report; do not recover.

Starting state: main @ 483327b plus this directive's own merge. Read before editing: docs/global-context/core.md, reviews/sre-baseline-measurement-cycle-5.md, engagements/sre/engagement-change-package.md.

## Instructions

1. SRE-BM-7 — engagements/sre/baseline-measurement.md step 6, last sentence: replace "in the change package's Expected-delta item" with the name of the change-package item that holds post-change evidence, exactly as engagements/sre/engagement-change-package.md names it. Report which item you used.
2. engagements/sre/speed-audit.md step 5: after "the Cartographer re-measures against the baseline" append ", after each landing, not in batches". Restores wording part 5 dropped.
3. Run `bin/check-frontmatter --all` and `bin/tests/run`. Expected: exit 0; exactly the two pre-existing test_bn10 failures.
4. Commit on branch p1-regate-fix-6, push, open a pull request against main titled "Pass 1 re-gate: fixes, part 6" via the REST API with curl. Do not merge. No status flip. Report the SHA read back from git.

## Report shape
PR number and SHA. Files changed. Item name used at instruction 1. Instruction 3 output. Anything that could not be executed as written.
