Write this directive verbatim to docs/cycles/pass2-flips-<timestamp>.md (timestamp ISO 8601 basic, generated now). Run `git checkout origin/main` then `git checkout -b pass2-flips`. Commit the directive file, push the branch, open and squash-merge a PR via curl against the GitHub REST API using the credential-helper token held in an environment variable — never written to a file. Never invoke gh for anything, including auth checks; its errors are not evidence. Report the merged SHA. Then execute the rest on a fresh branch from origin/main, pass2-flips-2, merged the same way; report that SHA too.

Read before acting: docs/global-context/core.md; bin/flip-agreed --help and its source; policies/document-metadata-policy.md §Required fields and §Revision lifecycle; reviews/corpus-regate-cycle-1.md through -6.md, scope and verdict statements; every reviews/*-cycle-*.md artifact the re-gate produced for individual files.

Task: move every governed file the Pass 1 re-gate left clean to status: agreed, via bin/flip-agreed only — no hand-edited frontmatter.

1. Probe first. Select one file whose only covering artifact is a corpus-wide sweep artifact. Run bin/flip-agreed with last-reviewed citing that artifact and the SHA it reviewed the file at. If the tool rejects the pointer form, STOP the whole directive and report the rejection verbatim — flip nothing else.
2. If the probe lands: for each remaining governed file in the re-gate's scope, determine its pointer — the most recent review artifact whose stated scope covers that file, at the SHA that artifact states it reviewed the file at. Where two artifacts could both claim a file, or an artifact's scope statement is ambiguous about a file, do not choose: hold that file out and list it in the report.
3. Files outside the re-gate's scope (anything the cycle artifacts' scope statements never cover, including files created after 12ecaeb) are not flipped; list them.
4. Run bin/check-frontmatter --all after the flips; exit 0 required. Run bin/tests/run; only the two accepted AC-BN-10 failures permitted.

Flip only via the tool; if bin/flip-agreed fails closed on any file, hold that file out and report the failure verbatim rather than working around it. Label every claim observed, inferred, told, or unknown. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface. Report: both SHAs, count flipped, files held out with reasons, files outside scope, check-frontmatter and test results, and anything needing Dave's judgment, one line each, up front.
