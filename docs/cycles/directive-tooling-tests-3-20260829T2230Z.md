You are the Test Designer, an execution session on davepierceops/fiducial, clone at ~/code/fiducial. Make the test-side changes for TRD §3.9 migration step 3 (cycle mode in bin/directive): one fixture line per a ruling, and the parameterization of test_cycle_open.py over both binaries. You do not touch implementation code, the TRD, the PRD, or skills/directive-invariants.md.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-tooling-tests-3-20260829T2230Z.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: directive-tooling tests 3 (heading placeholder; cycle-open suite over both binaries)", push with git push origin directive-tooling-tests-3 (no -u: the sandbox cannot write .git/config), and report the SHA. Never bypass the pre-commit hook.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-dt-tests-3", created by: git worktree add --no-track "$TMPDIR/fiducial-dt-tests-3" -b directive-tooling-tests-3 origin/main
Before creating it, run git worktree list; if any existing worktree holds branch directive-tooling-tests-3, or if "$TMPDIR/fiducial-dt-tests-3" already exists, stop and report. Do not touch the main tree except for the final worktree removal.

BASE VERIFICATION before anything else: git fetch origin. origin/main must contain 24c077c525baac745cc879ecb6cd6a2864b5808d. If origin/main is beyond it, list the commits past it; proceed only if none touches bin/ or skills/directive-invariants.md, otherwise stop and report. Another workstream lands on main concurrently and does not touch those paths.

READ before writing, whole, from the worktree: bin/tests/helpers.py (directive-tooling section: invariants_sections(), the placeholder table, and every consumer of the Heading (cycle) fixture); bin/tests/test_cycle_open.py (open_cycle and every call through run_cli("cycle-open", …)); bin/tests/test_directive.py TestCycleMode; bin/tests/test_directive_trd.py; specs/directive-tooling-trd.md §3.3 (cycle-mode region table, "Row 1 has two marker forms") and §3.9 (step 3); specs/directive-tooling.md AC-CO-1 and AC-DT-15.

DECISION RECORD. Finding: TRD §3.3 has row 1 of the cycle table emitting one of two headings by selector — "# Cycle <n> Directive — <title>" for --cycle N, "# <Title> Directive" for --name SLUG — from the single section Heading (cycle), and does not say how one section body yields two forms; the fixture and the landed invariants document carry only the --cycle form. Dave's ruling, 2026-08-29: option (b) — Heading (cycle)'s first line becomes "# {{heading}}", the generator filling the whole heading from directive_identity, which §3.9 moves unchanged and which already produces both forms in bin/cycle-open. Option (a), a second heading section, was rejected. The TRD's placeholder table changes ({{cycle}} and {{title}} in this section become {{heading}}) as a rider for the TRD's next cycle; the invariants document's matching line is the Coder's, in the next directive.

SCOPE, one commit.
1. bin/tests/helpers.py: the Heading (cycle) fixture's first line becomes "# {{heading}}"; the rest of that section body is unchanged. Update the placeholder table (or whatever structure fixes which placeholders each section admits) so this section admits {{heading}}, {{date}}, {{scope_list}} and no longer {{cycle}} or {{title}}. If any other helper or test asserts on the old first line, update it to the new one and name it in the report.
2. bin/tests/test_cycle_open.py: parameterize open_cycle over the binary name so every existing test runs once through "cycle-open" and once through "directive", with the binary name visible in the failure output (subTest, or a class parameterization — your call; state which and why). AC-CO-1 through AC-CO-12 all run against both. The assertions themselves are unchanged. Expect the "directive" half red until cycle mode lands: the binary currently refuses cycle-mode invocations with [cycle-mode-unavailable]. Do not skip, mark expected-failure, or condition on the binary's behaviour — the red is the step's evidence.
3. Nothing else changes: no test in test_directive.py, test_directive_trd.py or test_check_directive.py, no implementation file, no invariants document.
Commit message: "directive-tooling: heading placeholder is {{heading}} (ruling 2026-08-29); cycle-open suite runs both binaries (§3.9 step 3)". Push with git push origin directive-tooling-tests-3.

VERIFICATION, from the worktree, output to "$TMPDIR/fiducial-dt-tests-3-run.log": bin/tests/run for the whole suite.
Expected state: test_cycle_open.py's "cycle-open" half entirely green, its "directive" half red on [cycle-mode-unavailable] and nothing else — name any "directive"-half red whose cause is not that refusal; test_directive.py red on its six known cases (five cycle-mode refusals and test_ac_dt_14_bin_holds_exactly_one_directive_skeleton_generator); test_directive_trd.py red on test_route_and_model_are_committed_in_both_modes_and_the_author_count_stays_two with 6 skips; test_check_directive.py 84/84; three pre-existing reds outside directive tooling (test_scope sc1 and sc3, test_check_frontmatter cf13) belonging to another workstream unless main has since cleared them; everything else green. If the fixture line change reddens anything in test_directive.py or test_directive_trd.py beyond the cases named, that is a finding — report it, do not adjust.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and both pushes are verified landed (git ls-remote origin directive-tooling-tests-3 shows your commit SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-dt-tests-3" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref 24c077c525baac745cc879ecb6cd6a2864b5808d: on any failed command, any precondition not met, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; commit SHA; branch name; run-log path; the diff of helpers.py in full and a summary of the test_cycle_open.py parameterization with its diff; pass/fail/skip counts for the whole suite and for test_cycle_open.py split by binary; the named red cases outside the expected set, if any, with the one-line reason each; anything observed this directive did not anticipate; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
