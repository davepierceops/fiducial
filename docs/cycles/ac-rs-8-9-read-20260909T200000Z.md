# AC-RS-8 and AC-RS-9: the read

ROUTE AND MODEL

Route: fresh
Model: frontier

FIRST ACT

Write this directive verbatim to docs/cycles/ac-rs-8-9-read-20260909T200000Z.md, commit it alone with a
message naming the package it opens, push the branch ac-rs-8-9-read to origin with a plain push
(git push origin ac-rs-8-9-read — never with -u), and report the SHA. Do this before reading anything else and before touching any other file.

Order of operations: create the worktree the disposition below names first, then
perform this first act inside it.

DISPOSITION PROMPT

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

```text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
```

Both admitted forms, worked:

```text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<n>", created by: git worktree add --no-track "wt/<n>" -b
<n> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
```

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-ac-rs-8-9-read", created by:
git worktree add --no-track "$TMPDIR/fiducial-ac-rs-8-9-read" -b ac-rs-8-9-read origin/ac-rs-8-9

Before creating it: fetch origin, list worktrees, and stop and report if any
worktree holds the branch ac-rs-8-9-read or the path already exists.

BASE VERIFICATION

Before anything else, fetch and confirm the base — the branch ac-rs-8-9, not
main — is at the reviewed ref b54b2ef, the full SHA of which you read from
`git rev-parse origin/ac-rs-8-9` and state in the report, and that origin/main
is at 152797090c3ae7242dcfd69ac49ab6d3cb9c4863. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- skills/review-artifact.md as it stands at
  docs/history/corpus-152797090c3ae7242dcfd69ac49ab6d3cb9c4863/skills/review-artifact.md
  on the base ref — the artifact schema
- specs/rule-store.md §4, AC-RS-8, AC-RS-9 and OQ-3; decisions/log.md
  DEC-000680 and DEC-000690; and the three directives
  docs/cycles/ac-rs-8-9-20260909T170000Z.md,
  docs/cycles/ac-rs-8-9-amendment-1-20260909T180000Z.md,
  docs/cycles/ac-rs-8-9-amendment-2-20260909T190000Z.md, read from the base ref
- the whole diff `git diff 152797090c3ae7242dcfd69ac49ab6d3cb9c4863..origin/ac-rs-8-9 -M100%`,
  and bin/tests/green-run-ac-rs-8-9.log

TASK

You are the Reviewer and the Skeptic/Risk, one session, two passes, each with
its own verdict. Read the delta whole — the diff, the green log, and the
executor's decisions as the three directives and the commit messages record
them. Write one review artifact in the schema at
reviews/ac-rs-8-9-read-20260909T200000Z.md, commit it, plain-push the branch.
Edit nothing else. Do not open a pull request.

Quality pass: does the branch make AC-RS-8 and AC-RS-9 true as the PRD states
them — every named file absent, no `status:` under rules/ or process/, the
corpus byte-identical under docs/history/ with the manifest matching, the
invariants document moved without a frontmatter block, `RELPATH` and R1302
repointed, `HOME_SENTINEL` at bin/bundle. Continuity: does anything the branch
did not change now point at something it moved — read bin/, process/,
README.md and the rows R1302 to R1350's bodies (not their `source:` lines,
which are history). Is the test removal sound: were the six dedicated tests
the only coverage of any surviving code path.

Skepticism pass: what would break for a user of bin/directive,
bin/check-directive, bin/bundle or bin/release on a fresh clone of this
branch that the suite does not exercise. Run each with `--help` and
bin/directive against a scratch invocation if its usage admits one; capture
to "$TMPDIR/ac-rs-8-9-read-probes.log". Name the dead code the executor
reported — cli.Context, load_context, scope.py, repo's role helpers — as a
finding with a release-impact label; it is not blocking by this package's
scope.

Every finding: location, evidence, label — defect, suggestion, or accepted
risk — and release impact. Remove the worktree at the end
(`git worktree remove "$TMPDIR/fiducial-ac-rs-8-9-read"`) and report that it
is gone.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

FENCE NOTE: the inner fence is `~~~` because this directive is itself fenced.

~~~sh
{
  bin/tests/run 2>&1 | tail -1
  grep -c '^Verdict' reviews/ac-rs-8-9-read-20260909T200000Z.md
  git diff --stat origin/ac-rs-8-9..HEAD | tail -1
  git log --oneline origin/ac-rs-8-9..HEAD | wc -l
} 2>&1 | tee "$TMPDIR/ac-rs-8-9-read-verify.log"
~~~

Expected: OK from the suite on the base; 3 verdict lines — one overall, one
per pass; a diff-stat naming the artifact and the directive only; 2 commits.

STOP CONDITIONS

Pinned to the reviewed ref b54b2ef, stated in full in the report. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    AC-RS-8 and AC-RS-9: the read — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    ROUTE AND MODEL — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    FIRST ACT — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    DISPOSITION PROMPT — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    COMPANIONS — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    TASK — author region
    SANDBOX — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    VERIFICATION — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    STOP CONDITIONS — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    REPORT — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    CLAIM LABELS — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
    SOURCE MANIFEST — process/directive-invariants.md @ 77625d3 on ac-rs-8-9
