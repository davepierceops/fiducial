You are the Coder, an execution session on davepierceops/fiducial, clone at ~/code/fiducial. Implement TRD §3.9 migration step 3: cycle mode in bin/directive, with bin/cycle-open untouched and still the executable its suite also drives. You are a different agent from the Test Designer; you do not edit tests, the TRD, or the PRD. Where a test and the TRD disagree, stop and report.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-tooling-impl-3-20260829T2300Z.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: directive-tooling implementation package 3 (cycle mode)", push with git push origin directive-tooling-impl-3 (no -u: the sandbox cannot write .git/config), and report the SHA. Never bypass the pre-commit hook.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-dt-impl-3", created by: git worktree add --no-track "$TMPDIR/fiducial-dt-impl-3" -b directive-tooling-impl-3 origin/directive-tooling-tests-3
Before creating it, run git worktree list; if any existing worktree holds branch directive-tooling-impl-3, or if "$TMPDIR/fiducial-dt-impl-3" already exists, stop and report. Do not touch the main tree except for the final worktree removal.

BASE VERIFICATION before anything else: git fetch origin. origin/directive-tooling-tests-3 must be at 7b15a7f734bbec410b620ae6e678adbc327f7df3. If it has moved, stop and report. This branch is deliberately not main: it carries the parameterized cycle-open suite and the {{heading}} fixture your work is measured against.

READ before writing, whole, from the worktree: specs/directive-tooling-trd.md (§3.3 cycle-mode table and "Row 1 has two marker forms", §3.9 whole, §4.1, §6, §7); specs/directive-tooling.md AC-CO-1..12, AC-DT-03, AC-DT-05, AC-DT-14, AC-DT-15, AC-DT-18; bin/cycle-open (every function: directive_identity, collect_documents, resolve_revisions, render_directive, bundle emission, --bundle expansion, --out containment, --allow-dirty, the not-ignored warning); bin/aimeta/directive.py, invariants.py, mdmask.py, closure.py, repo.py, cli.py; bin/directive; skills/directive-invariants.md; bin/tests/helpers.py (directive-tooling section); bin/tests/test_cycle_open.py (now runs every AC-CO test through both binaries); bin/tests/test_directive.py TestCycleMode and the cycle-mode cases in TestDispositionSlot and TestSourceManifest; bin/tests/test_directive_trd.py; docs/cycles/directive-tooling-impl-1-20260829T1400Z.md (decisions 2, 3, 5, 10 are the ones this package supersedes or completes).

DECISION RECORD. Dave's ruling (b), 2026-08-29, carried in docs/cycles/directive-tooling-tests-3-20260829T2230Z.md: the Heading (cycle) section's first line is "# {{heading}}"; the generator fills the whole heading — "Cycle <n> Directive — <title>" for --cycle N, "<Title> Directive" for --name SLUG, <Title> defaulting to the slug with hyphens read as spaces — from directive_identity, which §3.9 moves unchanged. The fixture already carries the new line; the landed invariants document does not yet.

SCOPE, two commits, in this order.

Commit 1 — skills/directive-invariants.md only: the Heading (cycle) section's first line becomes "# {{heading}}", the rest of the section unchanged; the document's placeholder inventory, if it states one, no longer lists {{cycle}} or {{title}} for that section and lists {{heading}}. Nothing else in the document changes. Commit message: "directive-tooling: Heading (cycle) carries {{heading}} (ruling 2026-08-29)". Push.

Commit 2 — cycle mode.
- Migrate the cycle-mode code into bin/aimeta/directive.py: copy directive_identity, collect_documents, resolve_revisions, the bundle emission, the --bundle closure expansion, the --out repo-relative containment (AC-CO-12), --allow-dirty and the not-ignored warning from bin/cycle-open, behaviour unchanged. Copy, do not move: bin/cycle-open keeps its own definitions and is not edited — step 4 replaces its body with a forwarder and removes the duplication. Where bin/cycle-open already delegates to an aimeta module, call that module rather than copying.
- bin/directive cycle mode: selectors --cycle N / --name SLUG (exactly one selector across the three, else exit 2 — already enforced; keep it); --title, --date (cycle-mode only; default today's local date, AC-CO-10), --out, --bundle, --allow-dirty, positional documents; --route and --model accepted in both modes; --timestamp and --write refused in cycle mode at exit 2; --descriptor's general-mode-only flags refused likewise. The destination and everything the AC-CO suite asserts stay exactly as bin/cycle-open does them.
- Emission per §3.3's cycle-mode table: sixteen regions in that order, fourteen committed, two author (WORKING-TREE DISPOSITION, whose marker line is the label from §Disposition label as in general mode; and Execution notes, whose marker line is the heading carried by §Execution notes' first body line). Row 1 is one region and one manifest entry whichever selector produced it, its marker token the text after the # run on the emitted first line. Rows 6 and 7 carry §Decisions and §Deferred verbatim, so AC-CO-3's strings now come from the document, not from your source (AC-DT-02 scans your source against them). {{heading}}, {{date}}, {{scope_list}}, {{route}}, {{model}} substituted; {{reviewed_ref}} and {{companion_list}} as package 1 emits them. Route and model are emitted, never checked (Q5 ruling (c)). The FM-G7 self-check runs in cycle mode as in general mode, over eligible lines. The manifest's sixteen entries follow the same forms as general mode's.
- Remove the [cycle-mode-unavailable] refusal and its comment.
- Do not touch bin/cycle-open, bin/tests/, specs/, docs/global-context/, policies/, roles/, or any skills/ file other than commit 1's.
- Commit message: "directive-tooling: bin/directive cycle mode (migration step 3)". Push with git push origin directive-tooling-impl-3.

VERIFICATION after commit 2, from the worktree, output to "$TMPDIR/fiducial-dt-impl-3-run.log": bin/tests/run for the whole suite, and bin/check-frontmatter --all (state exit code and count).
Expected state, and a stop if it differs: test_cycle_open.py 62/62 green — both halves; test_directive.py red on exactly one case, test_ac_dt_14_bin_holds_exactly_one_directive_skeleton_generator (step 4); test_directive_trd.py green except its 6 skips; test_check_directive.py 84/84; three pre-existing reds outside directive tooling (test_scope sc1 and sc3, test_check_frontmatter cf13) belonging to another workstream unless main has since cleared them; everything else green. Any red outside that set is a stop: report it, do not adjust a test.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and all pushes are verified landed (git ls-remote origin directive-tooling-impl-3 shows your commit-2 SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-dt-impl-3" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref 7b15a7f734bbec410b620ae6e678adbc327f7df3: on any failed command, any precondition not met, any test whose expectation contradicts the TRD, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; commit-1 SHA; commit-2 SHA; branch name; run-log path; pass/fail/skip counts for the whole suite and for each of test_cycle_open.py (split by binary), test_directive.py, test_directive_trd.py, test_check_directive.py; the named red cases with the one-line reason each; check-frontmatter exit code and count; a numbered list of anything the TRD left you to decide, with what you chose and where in the code it lives; the list of functions copied from bin/cycle-open, for step 4's deletion; anything observed this directive did not anticipate; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
