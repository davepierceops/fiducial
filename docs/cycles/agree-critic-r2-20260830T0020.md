You are an execution session on davepierceops/fiducial, clone at ~/code/fiducial. One task: doc-only agreement of roles/critic.md at its new content commit — one log entry, then one frontmatter-only flip — as two commits on a continuation of the triage branch.

SYNC FIRST: from the main tree, git fetch origin (an osxkeychain "failed to store" message is noise; judge the fetch by the refs). Confirm origin/agree-criteria-r2 exists and its tip is d560689fe69ad190bb340682e5a2c10a4238e009; if not, stop and report.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-agree-critic-2", created after the fetch above by: git worktree add --no-track "$TMPDIR/fiducial-agree-critic-2" -b agree-critic-r2 origin/agree-criteria-r2
Before creating it, run git worktree list; if any existing worktree holds a branch named agree-critic-r2, stop and report. Do not touch the main tree's checkout. All pushes go to origin agree-critic-r2.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/agree-critic-r2-20260830T0020.md in the worktree, commit it alone with message "Directive: doc-only agreement of roles/critic.md (revision 2)", push, and report the SHA.

PRECONDITION: roles/critic.md reads status: in-review and last-reviewed: null, and git log -1 --format=%H -- roles/critic.md yields d560689fe69ad190bb340682e5a2c10a4238e009; if not, stop and report.

EDIT 1 — reviews/expedited-log.md. Append the following line verbatim as the last entry of the file, after the last existing entry (the public-prose-criteria.md revision 2 entry), changing nothing else:

- 2026-08-29 — roles/critic.md @ d560689fe69ad190bb340682e5a2c10a4238e009 — AI-smell pass names the Criteria's structure defects without a count, following the Criteria's third cut; agreed via doc-only cycle (DEC-000230)

Commit reviews/expedited-log.md alone with message "expedited-log: roles/critic.md revision 2 agreed via doc-only cycle". Push.

EDIT 2 — the flip. Run bin/flip-agreed once, as a single standalone invocation (never inside a loop), for roles/critic.md with the review pointer exactly:
reviews/expedited-log.md @ d560689fe69ad190bb340682e5a2c10a4238e009
The tool commits on its own. Verify before pushing that the flip commit touches only roles/critic.md and only its frontmatter (git show --stat and git show; the diff is status: in-review → status: agreed and last-reviewed: null → last-reviewed: reviews/expedited-log.md @ d560689fe69ad190bb340682e5a2c10a4238e009, nothing else). If the tool fails, or the diff is anything else, stop and report; do not edit frontmatter by hand and do not retry with different flags. Push.

Run bin/check-frontmatter --all from the worktree (must exit 0). Then confirm no in-scope file on this branch is in-review: git grep -l "^status: in-review" -- '*.md' should return nothing; report any hit. Do not open a pull request. Never merge anything.

CLEANUP — after the report is composed and all pushes are verified landed: from the main tree, run git worktree remove "$TMPDIR/fiducial-agree-critic-2" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS: on any failed command, any precondition not met, or any tree mutation you did not intend, including your own — stop and report; do not rebase, do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; log-entry commit SHA; flip commit SHA and its stat; check-frontmatter exit code; in-review grep result; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
