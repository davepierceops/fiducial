You are a reviewer execution session on davepierceops/fiducial, clone at ~/code/fiducial. Read-only pass over retros/retro-triage-board.md — the 2026-08-05 retrospective board whose fifteen action items AI-1 through AI-15 have no recorded dispositions — producing one disposition per item against the repository as it stands today. Your only write is one new file in retros/. You do not edit the board, any governed document, OPEN-ITEMS.md, or anything else.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/board-pass-20260831T213000Z.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: 08-05 board disposition pass", push with git push origin board-pass-20260831 (no -u), and report the SHA. Never bypass the pre-commit hook.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-board-pass", created by: git worktree add --no-track "$TMPDIR/fiducial-board-pass" -b board-pass-20260831 6f9cc79634d6fadf1fbd64180d3658a68fa98f8f
Before creating it, run git fetch origin, then git worktree list; if any worktree holds branch board-pass-20260831, if a branch of that name already exists locally or on origin, or if "$TMPDIR/fiducial-board-pass" already exists, stop and report. Entries git marks prunable are not yours; ignore them. Do not touch the main tree except for the final worktree removal.

BASE VERIFICATION before anything else: git fetch origin. Judge every remote operation by the refs it reports and its exit status, not by credential-helper noise on stderr; an operation that exits 0 and reports no refs is not a failure. The reviewed ref 6f9cc79634d6fadf1fbd64180d3658a68fa98f8f must equal origin/main or be an ancestor of it. In the worktree, retros/retro-triage-board.md must exist and contain items labelled AI-1 through AI-15; if not, stop and report.

READ before writing, whole: retros/retro-triage-board.md; OPEN-ITEMS.md, in particular the sections "Retrospective session 2026-08-31 — follow-ups" and "Topic walk 2026-08-31 — rulings"; retros/retro-synthesis-20260831T163000.md, in particular its 08-05-board disposition table if one exists; decisions/log.md entries DEC-000280 through DEC-000300. Where an item's disposition turns on whether something exists in the tree, read the tree — grep, ls, git log — rather than inferring from the trackers.

TASK. Write retros/board-dispositions-20260831T213000Z.md: a header naming the board, this directive, and the reviewed ref; then one entry per item AI-1 through AI-15, each with three fields — Disposition: landed | superseded | still-open | refused, with the evidence (path, entry, or DEC id, verified in the tree at the reviewed ref); Evidence class: observed (you read it in the tree) or inferred (you concluded it from trackers); and one line of Notes where the mapping is not obvious. Three dispositions are pre-ruled by the decision session and are verified rather than re-decided: AI-1 disposes into the Topic walk ruling 10 (the SLO criterion's remove-and-relocate); AI-8 is resolved by ruling 13 (the lore home is the tooling-facts artifact); AI-15 landed via T17 (the same-turn encoding line and the retro skill's standing-preferences prompt). If your reading of the tree contradicts a pre-ruled disposition, record the contradiction in Notes and keep the pre-ruled disposition — the decision session triages it. Close the file with one line stating whether the board can now be called synthesized: it can if every item carries a disposition.

SCOPE, one commit after the directive-file commit: retros/board-dispositions-20260831T213000Z.md only. Commit message: "retros: 08-05 board dispositions (AI-1..AI-15)". Push with git push origin board-pass-20260831.

VERIFICATION after the commit, from the worktree, output to "$TMPDIR/fiducial-board-pass-run.log": grep -c "^Disposition:" retros/board-dispositions-20260831T213000Z.md (expected 15); bin/check-frontmatter --all (state exit code and count). Expected state, and a stop if it differs: 15 dispositions; check-frontmatter exit 0, 62 files / 14 globs. The bin/ test suite is not run by this directive; report it as not run.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and both pushes are verified landed (git ls-remote origin board-pass-20260831 shows your content commit SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-board-pass" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref 6f9cc79634d6fadf1fbd64180d3658a68fa98f8f. Cannot execute as written: stop and report. Concurrent tree mutation: stop and report. On any failed command, any precondition not met, a document you were told to read that is absent, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT

- the directive-file commit SHA
- the content commit SHA, and the branch it is on
- the run-log path
- the fifteen dispositions, one line each
- the closing synthesized-or-not line, verbatim
- check-frontmatter exit code and count, with the tree it was observed in; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status, as the final line

Label every claim observed, inferred, told, or unknown.
