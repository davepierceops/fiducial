# Directive: bin/land TRD — cycle 2

Route: fresh execution session. Model tier: frontier.
Base: main @ 6ac15118ae2c80531ab1c476595c419d6263141a.

1. First act — write this directive file verbatim to
   docs/cycles/bin-land-trd-2-20260823T232718Z.md in the worktree stated below,
   commit it on branch bin-land-trd-2, push, and report the SHA read back from
   git.

Worktree — exclusive assignment. This session works only in
$TMPDIR/fiducial-bin-land-trd-2 (fallback /tmp/claude-501/fiducial-bin-land-trd-2),
created:

    git fetch origin
    git rev-parse origin/main
    # must print 6ac15118ae2c80531ab1c476595c419d6263141a — if not, STOP and surface
    git worktree add "$TMPDIR/fiducial-bin-land-trd-2" -b bin-land-trd-2 6ac15118ae2c80531ab1c476595c419d6263141a

Scratch substrates for gathering evidence are permitted outside that worktree —
a throwaway repository built by hand, as both prior reviewers used. Repository
work happens only in the assigned worktree.

Sandbox constraints — read both companions, follow as told:
- docs/cycles/pass2-held-fix-20260823T180753Z.md @ b9444973de086eef7cd30e7877fb86f6a1271d8e
- docs/cycles/bin-land-flip-20260823T210300Z.md @ 8c0aeee2470297ae5f2879a72dc9be4da0ea093b
  (fetch then verify origin/main before any worktree add; never `git push -u` —
  push plain, verify by ls-remote; worktree paths under $TMPDIR)

Companions — read all before editing:
- reviews/bin-land-trd-cycle-2.md @ 3738aa0833ff4d2640086a2950260ef32eb8983e
  — the findings this cycle disposes of. Every Fix cited below is a pointer to
  a field in this artifact; read the field, do not expect it restated here.
- docs/cycles/bin-land-trd-1-20260823T221914Z.md @ 3de6098be15d67b2eb48eb46759b1f01ca1ba837
  — the cycle-1 decision record. Its twelve dispositions were all confirmed
  discharged by the cycle-2 reviewer; do not re-litigate them.
- reviews/bin-land-trd-cycle-1.md @ 90c31bcc7179c88715d19dbefe4c9404f1dd064c
- specs/bin-land.md @ 3a8e55e350cad0e4532ff0e2ea03bd9299572114 — the PRD, agreed,
  read-only this cycle
- specs/trd-template.md @ ba513f824993ab5cd1d0e9e7ce8134a93bb491c5 — read-only
- reviews/bin-land-cycle-5.md @ ee0445ee591a63cb6c3acc12ec3a36c6f03c723e — O1
- reviews/bin-land-cycle-4.md @ d857d83e93a9b3ce8d0df0c922708002f968b760 — O1, O2, O3

Reviewed state. specs/bin-land-trd.md landed at content commit
03c521912fd8ffe1cc3758c06d6f463d3da4d43c and was re-gated by the Spec Reviewer
at main be52de1661f80dc1923390f76b808056df638be6; the review artifact is
reviews/bin-land-trd-cycle-2.md, landed at 3738aa08 and merged to main at
6ac15118 as PR #187. Before editing, verify that 03c52191 is in the worktree's
history and that no commit since it has touched specs/bin-land-trd.md. If either
fails, STOP and surface.

2. Revise specs/bin-land-trd.md per the decisions below. Each is stated as
   intent with a pointer to the reviewer's Fix field; draft your own wording.
   Do not paste a reviewer Fix field into the document. The TRD proposes; it
   decides nothing Dave owns.

   Nothing was rejected this cycle. All six findings are accepted, and the
   absence of a rejection is a fact rather than an omission.

   D-B1 — accepted. Intent: §3.7's single leaf rule stops contradicting §5.2
   and §6. `files` is exempt from the leaf rule — `build` emits it as a list
   whose unknown-ness is carried per entry or by the absence of entries, per
   §5.2, and no union type enters the parse contract. And §3.7 states where
   `verification` gets its value: computed inside `build` from §5.2's
   biconditional over what `facts` holds, rather than carried in `facts` by any
   step — `"complete"` where the head was confirmed and every comparison
   matched, `"incomplete"` where a step past the commit established it was not,
   `null` with `class: "unknown"` where nothing was established. §5.2 and §6 are
   already correct and do not change. Fix pointer: B1 Fix field.

   D-N1 — accepted, buy the criterion. Intent: §5.4 gains one entry,
   **AC-LAND-T03**, on the precedent AC-LAND-T01a set at cycle 1. Given: the
   named branch exists locally and carries a commit the base does not, local
   HEAD on another branch and at or behind the base. Requires: the invocation
   stops before staging in FM-3's shape; `detail.branch_head` carries
   `<branch>`'s prior tip; that tip is still reachable from `<branch>`
   afterwards. It is a TRD-stage criterion under §5.4's existing declaration,
   not an amendment to PRD §6. Do not amend AC-LAND-01c — that is a content
   edit to an agreed spec and is out of scope this cycle. Fix pointer: N1 Fix
   field.

   D-N2 — accepted, row route (Dave, this cycle). Intent: §6 gains a row for
   the state where step 3's read succeeds and returns no line for `<branch>`
   nor for `main`, so the branch-absent arm has no base. Detected by an empty
   step-3 result; `detail.stage` is `resolve`; nothing established but
   `branch`. The mode is genuinely distinct from a read that failed and is not
   folded into FM-1.

   Number it **FM-11 and append it**. Do not renumber any existing failure
   mode: FM-3, FM-7, and FM-10 are cited by number across
   reviews/bin-land-trd-cycle-1.md, reviews/bin-land-trd-cycle-2.md, and
   docs/cycles/bin-land-trd-1-20260823T221914Z.md, and renumbering would strand
   every citation. The resulting out-of-order table is accepted deliberately.

   Two cross-references travel with it and are part of this decision, not of
   the sweep: §5.3's `resolve` token is scoped to "FM-1's class, where the
   remote read is what failed" and must widen to carry FM-11; and §7's
   exit-code table maps code 3 to "FM-1 through FM-6", which must reach FM-11 —
   it is a precondition failure before any write. Fix pointer: N2 Fix field,
   first route.

   D-N3 — accepted. Intent: §3.7's enumeration of what each step returns names
   `branch_head` alongside `local_head` for step 5, so `Report.build` has a
   stated source for the field §5.3 requires on the second check's refusal.
   Fix pointer: N3 Fix field.

   D-O1 — accepted. Intent: §5.3's `detail` key table gains a `remote_head`
   row — emitted on FM-8, meaning the head `ls-remote` returned for `<branch>`
   after the push — so §6's FM-8 row stops naming a key the table does not
   define. Fix pointer: O1 Fix field.

   D-O2 — accepted. Intent: §6's FM-10 row is repointed to §3.2 step 11's
   second bullet, where this cycle's FM-10 material now lives. Its OQ-4
   citation is correct and stays. Fix pointer: O2 Fix field.

   D-OQ6 — OQ-6 stays open, unchanged (Dave, this cycle). Do not retire it, do
   not restate it, and do not soften §4.3. The document is correct as it
   stands: under git transport AC-LAND-06's comparison adds nothing to the
   `ls-remote` check, and §4.3 says so. Dave has weighed both alternatives —
   closing OQ-6 as an accepted risk, or opening a PRD cycle to amend
   AC-LAND-06 — and taken neither this cycle. No edit to specs/bin-land.md.
   The question is his and stays open on the record.

   D-OQ2 — accepted, restate (Dave, this cycle). Intent: §9's OQ-2 entry is
   brought into agreement with §3.4, which already states that the guard makes
   refusal mechanical on both routes. What OQ-2 asks is now only whether the
   PRD should *say* so, not what the tool would do. Keep the entry and keep its
   pointer to reviews/bin-land-cycle-5.md O1 — do not retire it; the spine
   carries no other pointer to that finding. Both of its named resolvers remain
   PRD content edits and remain out of scope.

   D-OQ5 — accepted, close and report (Dave, this cycle). Intent: OQ-5 is
   closed rather than carried. The decision is that the tool is permitted to
   move HEAD onto `<branch>`, because refusing would rule out J1 in any session
   whose worktree was not created on the target branch — which is the ordinary
   opening state, the first invocation being the one that creates the branch.
   With that, §3.3's "HEAD moves" bullet records a settled decision rather than
   an open question.

   The reporting half is the other side of it: `detail` gains a `prior_branch`
   key — the branch HEAD was on before step 6, emitted where that was not
   `<branch>` — so a mutation of the session's working tree appears in the
   report rather than only in this document. Add the row to §5.3's key table,
   and name step 6 as its source in §3.7's enumeration of what each step
   returns, on the same footing D-N3 puts `branch_head` on.

   Retire OQ-5's identifier rather than reusing it, on the precedent §9 already
   sets for OQ-8 and PRD §8 sets for its own Q1–Q4, and update §9's stated
   count of open questions with it.

   D-TMPL — accepted, one line (Dave, this cycle). This is the reviewer's Dave
   list item 5, and it is cycle 1's D-O1 revisited — not a cycle-2 finding.
   Intent: §7's "Flag mechanism: none" paragraph gains one line recording that
   declining to name a flag backend is a **departure** from specs/trd-template.md
   §7's requirement, accepted by Dave, citing
   docs/cycles/bin-land-trd-1-20260823T221914Z.md @ 3de6098b as where that
   acceptance is recorded. The reasoning §7 already carries is correct and
   stays; what the line adds is that the departure was deliberate rather than
   overlooked. Do not edit specs/trd-template.md — an N/A escape there is
   outside this blast radius and takes its own cycle.

3. Consistency sweep before committing, in two parts. Report what each found,
   including "nothing" if that is the result.

   **3a — the §3.7 seam, swept against the sections it must agree with.** B1
   and N3 are both §3.7 contradicting another section, and both arrived in the
   same cycle that wrote §3.7 to make that class of defect structurally hard to
   reintroduce. This cycle's edits go into the same seam. Before committing,
   read §3.7 line by line against §5.2's rules, §5.3's two tables, and §6's
   failure rows, and confirm that every field §5.2 and §5.3 require has a
   stated source in §3.7's step enumeration, and that every rule §3.7 states
   produces the value §5.2 and §6 say that field carries. Name any disagreement
   you find rather than resolving it silently.

   **3b — the ordinary sweep.** Per docs/global-context/core.md rule 13, a
   changed fact changes everywhere it appears. Sweep specs/bin-land-trd.md for
   any count, value, or cross-reference these edits have made stale — at
   minimum: §9's open-question count and every mention of OQ-5 after its
   retirement; §7's exit-code table and §8's claims against the added FM-11;
   §5.4's AC-LAND-T01 enumeration, which reaches "each failure mode in §6
   except FM-10" and so now reaches FM-11; §6's own completeness claim in the
   FM-3 paragraph; and §5.3's two tables against the three keys added this
   cycle.

4. Frontmatter is unchanged: the document stays `status: draft`,
   `last-reviewed: null`. This is a revision of a draft, not a status
   transition. Run `bin/check-frontmatter --all` in the worktree; it must exit 0
   before the TRD is pushed.

5. Land the revision as a second commit on bin-land-trd-2, referencing cycle 2;
   push plain; verify by ls-remote and by reading specs/bin-land-trd.md back at
   the pushed head; report the head SHA and both commit SHAs, each read back
   from git.

Do not:
- modify any file other than docs/cycles/bin-land-trd-2-20260823T232718Z.md and
  specs/bin-land-trd.md — the blast radius is those two files only. This
  explicitly excludes specs/bin-land.md and specs/trd-template.md, both agreed,
  both named above as read-only companions.
- amend AC-LAND-06, AC-LAND-01c, or any other PRD acceptance criterion or goal.
- flip the TRD's frontmatter status or set last-reviewed.
- renumber any existing failure mode, open technical question, or §3.2 step.
- merge, open a PR, force-push, or delete any ref — merging is handled from the
  decision session.
- restate a reviewer Fix field as document text; the Fix fields are pointers to
  intent, not wording to be pasted.

STOP and surface rather than improvise: origin/main not at
6ac15118ae2c80531ab1c476595c419d6263141a at fetch; 03c52191 absent from history,
or any commit since it having touched specs/bin-land-trd.md; any instruction
unexecutable as written; concurrent tree mutation (a file this session did not
change moving, HEAD moving, an index lock).

Report: what was done, not what this file says; all SHAs read back from git; the
check-frontmatter result; both sweep results separately; the §9 open-question
list as it stands after OQ-5's retirement, with its count; every claim labelled
observed / inferred / told / unknown.
