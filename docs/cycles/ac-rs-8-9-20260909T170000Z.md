# AC-RS-8 and AC-RS-9: retire the lifecycle tooling, move the corpus to history

ROUTE AND MODEL

Route: fresh
Model: solid general-purpose

FIRST ACT

Write this directive verbatim to docs/cycles/ac-rs-8-9-20260909T170000Z.md, commit it alone with a
message naming the package it opens, push the branch ac-rs-8-9 to origin with a plain push
(git push origin ac-rs-8-9 — never with -u), and report the SHA. Do this before reading anything else and before touching any other file.

Order of operations: create the worktree the disposition below names first, then
perform this first act inside it, so the branch the push creates is the branch
the worktree holds.

DISPOSITION PROMPT

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

```text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
```

Both admitted forms, worked:

```text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<n>", created by: git worktree add --no-track "wt/<n>" -b
<n> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
```

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-ac-rs-8-9", created by:
git worktree add --no-track "$TMPDIR/fiducial-ac-rs-8-9" -b ac-rs-8-9 origin/main

Before creating it: fetch origin, list worktrees, and stop and report if any
worktree holds the branch ac-rs-8-9 or the path already exists.

BASE VERIFICATION

Before anything else, fetch and confirm the base is at the reviewed ref
152797090c3ae7242dcfd69ac49ab6d3cb9c4863. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- specs/rule-store.md — §4 non-goals, G11, AC-RS-8, AC-RS-9, OQ-3 — and
  decisions/log.md entries DEC-000680 and DEC-000690, read from the base ref
  152797090c3ae7242dcfd69ac49ab6d3cb9c4863
- bin/aimeta/invariants.py, bin/aimeta/scope.py, bin/install-hooks,
  bin/rulestore/store.py, bin/tests/run, rules/R1302.md, rules/R1304.md and
  policies/document-metadata-policy.md, read from the base ref
  152797090c3ae7242dcfd69ac49ab6d3cb9c4863

TASK

You are the Coder. Land AC-RS-8 and AC-RS-9 of the agreed PRD. Five commits
after the directive's, in this order, each pushed with a plain push to origin
ac-rs-8-9, the suite green at every commit. Every commit uses `--no-verify`:
the hook this package retires may be installed in this clone. Do not open a
pull request. Do not edit anything under docs/history/, docs/cycles/,
reviews/, retros/ or decisions/ except as an item below states.

1. **Compute the corpus before deleting the tool that defines it.** In the
   worktree at the base ref, list every path `git ls-files` returns that
   `aimeta.scope.matches` accepts against the globs
   `aimeta.scope.load_globs` parses from the policy at the base ref. Partition
   it by the `audience` value in each file's frontmatter: `[human]` and not.
   Write the three lists — in scope, human, agent-facing — with their counts
   to "$TMPDIR/ac-rs-8-9-corpus.txt". Read the counts against PRD §5's
   baseline (62, 8, 54 at 4a118f5): a difference is expected, since the tree
   has moved; report it as observed against told. Then remove
   `skills/directive-invariants.md` from the agent-facing list. That list,
   as it stands, is the corpus this package moves. No commit for this item.

2. **AC-RS-8.** `git rm` bin/check-frontmatter, bin/flip-agreed,
   bin/install-hooks, bin/migrate-frontmatter, reviews/expedited-log.md, and
   bin/tests/test_check_frontmatter.py, test_flip_agreed.py,
   test_install_hooks.py, test_migrate_frontmatter.py,
   test_expedited_log.py. For every module under bin/aimeta/, grep the tree
   for what still imports it; `git rm` a module and its test only when
   nothing kept imports it. bin/cycle-open, bin/land and their tests stay
   whatever they import; report, for each, what it imports and whether the
   suite still exercises it. Edit bin/tests/run if it enumerates the
   removed tests. Commit: `retire: AC-RS-8 — the lifecycle tooling and its
   tests`.

3. **Move the invariants document.** `git mv skills/directive-invariants.md
   process/directive-invariants.md`. Remove its frontmatter block whole —
   the `---` fences and every line between; the body starts at
   `# Directive Invariants`. Change `RELPATH` in bin/aimeta/invariants.py
   to the new path and repoint every other occurrence of the old path in
   bin/ and bin/tests/ — fixtures included — that the grep finds. Commit:
   `invariants: move to process/directive-invariants.md (DEC-000680)`.

4. **Re-key the rows.** In rules/R1302.md, replace the one occurrence of
   `skills/directive-invariants.md` in the body with
   `process/directive-invariants.md`; change no other line, the `source:`
   line included. Read rules/R1304.md: if its body names the old path,
   make the same edit; if it does not, leave it untouched and report so.
   Commit: `rules: R1302 names process/directive-invariants.md (DEC-000680)`.

5. **AC-RS-9.** `git mv` every file in the corpus list from item 1 to
   `docs/history/corpus-152797090c3ae7242dcfd69ac49ab6d3cb9c4863/`,
   preserving each file's relative path beneath it. Write
   `docs/history/corpus-152797090c3ae7242dcfd69ac49ab6d3cb9c4863-manifest.txt`
   beside it: one line per moved file, original path and blob SHA from
   `git ls-files -s` at the base ref. Remove any directory the moves leave
   empty. Commit: `history: AC-RS-9 — the agent-facing corpus at 1527970`.

6. **Sweep.** Grep the whole tree, excluding docs/history/, docs/cycles/,
   reviews/, retros/ and decisions/, for `check-frontmatter`,
   `flip-agreed`, `install-hooks`, `migrate-frontmatter`,
   `skills/directive-invariants.md`, and each directory the corpus vacated
   (`roles/`, `context-sets/`, `boundaries/`, `skills/` and the rest as item 1
   found them). Fix a reference in bin/, process/ or README.md; report every
   other one. Then the green log at
   bin/tests/green-run-ac-rs-8-9.log. Commit: `sweep: references to the
   retired tooling and the moved corpus`.

Remove the worktree at the end
(`git worktree remove "$TMPDIR/fiducial-ac-rs-8-9"`) and report that it is
gone.

Report also carries: the corpus counts from item 1 and the file
"$TMPDIR/ac-rs-8-9-corpus.txt"'s path; the aimeta modules removed and the
ones kept, each with its importer; the diff-stat per commit; and the suite's
summary line at each commit.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

From the worktree, after the last commit and before removing the worktree:

FENCE NOTE: the inner fence is `~~~` because this directive is itself fenced.

~~~sh
{
  bin/tests/run 2>&1 | tail -1
  ls bin/check-frontmatter bin/flip-agreed bin/install-hooks bin/migrate-frontmatter reviews/expedited-log.md 2>&1 | grep -c 'No such file'
  test -f process/directive-invariants.md && echo invariants-present
  test -f skills/directive-invariants.md && echo old-invariants-present
  head -1 process/directive-invariants.md
  grep -c 'status:' process/directive-invariants.md
  grep -n 'RELPATH = ' bin/aimeta/invariants.py
  grep -c 'process/directive-invariants.md' rules/R1302.md
  ls docs/history/ | grep -c 'corpus-152797090c3ae7242dcfd69ac49ab6d3cb9c4863'
  find docs/history/corpus-152797090c3ae7242dcfd69ac49ab6d3cb9c4863 -name '*.md' | wc -l
  wc -l < docs/history/corpus-152797090c3ae7242dcfd69ac49ab6d3cb9c4863-manifest.txt
  git diff --stat origin/main..HEAD -M100% | tail -1
  git diff --numstat origin/main..HEAD -M100% -- 'docs/history/corpus-*/' | awk '$1 != 0 || $2 != 0' | wc -l
  grep -rl 'status:' rules/ process/ | wc -l
  bin/bundle --keys > "$TMPDIR/ac-rs-8-9-keys.log" 2>&1; echo "keys exit $?"
  git log --oneline origin/main..HEAD | wc -l
} 2>&1 | tee "$TMPDIR/ac-rs-8-9-verify.log"
~~~

Expected: OK from the suite; 5 missing; invariants-present and no
old-invariants-present line; the head line is `# Directive Invariants`; 0
status lines in it; RELPATH naming the new path; 1 in R1302; 2 entries in
docs/history/ matching — the directory and the manifest; a find count equal
to the manifest's line count and to item 1's agent-facing count less one; a
diff-stat whose files are bin/, bin/tests/, rules/R1302.md, the moved files,
the manifest, the directive and at most README.md; 0 renamed corpus files
with content changes; 0 files under rules/ or process/ carrying `status:`;
keys exit 0; 6 commits.

STOP CONDITIONS

Pinned to the reviewed ref 152797090c3ae7242dcfd69ac49ab6d3cb9c4863. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    AC-RS-8 and AC-RS-9: retire the lifecycle tooling, move the corpus to history — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    ROUTE AND MODEL — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    FIRST ACT — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    DISPOSITION PROMPT — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    COMPANIONS — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    TASK — author region
    SANDBOX — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    VERIFICATION — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    STOP CONDITIONS — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    REPORT — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    CLAIM LABELS — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
    SOURCE MANIFEST — skills/directive-invariants.md @ abd4293a4c7ed40e59ad63d53a7afa7df5c1e71a
