# Directive — Pass 1 re-gate fixes, part 2: tooling and residuals

Date: 2026-08-22
Route: fresh
Model: solid general-purpose
Role: Implementer (execution session).

## Working-tree rule

This session runs in a clone no other session is using. If any file this session did not change moves, or HEAD moves, or an index lock appears, stop and report; do not recover.

Starting state: branch p1-regate-fix @ 9f95328, checked out from origin. All work lands on that branch; PR #130 stays open and is not merged. Read in full before editing: docs/global-context/core.md, policies/document-metadata-policy.md (on this branch), bin/aimeta/repo.py, bin/aimeta/scope.py, bin/tests/test_scope.py, bin/tests/test_check_frontmatter.py.

## Context

Part 1 (PR #130) widened the audience-value rule in the metadata policy to include role documents under engagements/ and removed the root README.md in-scope glob. The tooling that enforces the policy was not updated; bin/check-frontmatter --all and three tests now fail. This directive conforms the code to the policy as it now reads, and closes two prose residuals.

## Instructions

### Code — test-first
1. Run bin/tests/run. Record the failing set: expected test_bn10_bundle_base_yields_exactly_itself, test_bn10_transitive_body_references_are_followed_in_this_repo (pre-existing, leave them), test_cf19_all_reports_how_many_files_matched, test_sc1_the_derived_expectation_is_not_vacuous, test_sc3_non_directory_globs_are_fnmatch_over_the_whole_path.
2. Write one new test before changing any code: a role document at engagements/<name>.md or engagements/sre/<name>.md yields its basename slug as a valid audience value, and a non-role file under engagements/ (engagements/working-with-dave.md, engagements/sre/README.md) does not. Determine "role document" by its first heading beginning "# Role:"; if bin/aimeta/repo.py already has a different discriminator for roles/, use that one consistently and say which. Run; confirm it fails.
3. Change bin/aimeta/repo.py:role_slugs to include role documents under engagements/** by that rule. Run; confirm the new test passes and bin/check-frontmatter --all exits 0.
4. test_sc1, test_sc3, test_cf19: replace the README.md glob fixture with another glob from the policy's current in-scope list that satisfies each test's purpose (non-vacuity anchor, fnmatch-over-whole-path fixture, expected-match count). Do not weaken an assertion; if a test's purpose cannot be served by any current glob, stop and report.
5. Run bin/tests/run. Expected: exactly the two pre-existing test_bn10 failures remain.

### Prose residuals
6. engagements/sre/README.md, Ceremony section (~line 47-48): delete the sentence restating "every ceremonial element is trivially overridable by Dave, the override is logged". The rule lives in engagements/sre/override-log-policy.md only. If the section's remaining sentence no longer stands, reduce the section to "Overrides are governed by the override log policy in this bundle."
7. skills/evidence-review.md step 6: restore the rule in full — "A recommendation is a next step, never a ship call; the ship call is the Release Manager's; signal no-ship by marking the gap `blocking`." Remove the "per the Skeptic/Risk Agent role" pointer. (Reverses part 1's DR-19 consolidation: the role document is not in this skill's bundles.)

### Checks
8. bin/check-frontmatter --all: report output; expected exit 0.
9. Commit on p1-regate-fix without --no-verify. Push. Do not open a new PR; PR #130 carries it. Do not merge. Report the SHA read back from git.

## Report shape
Failing set at step 1. Discriminator used at step 2 and why. Per-test fixture substitution at step 4. Step 5 and step 8 output. SHA. Anything that could not be executed as written.
