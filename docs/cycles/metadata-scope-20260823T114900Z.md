Write this directive verbatim to docs/cycles/metadata-scope-<timestamp>.md (timestamp ISO 8601 basic, generated now). Run `git checkout origin/main` then `git checkout -b metadata-scope`. Commit the directive file, push the branch, open and squash-merge a PR via curl against the GitHub REST API using the credential-helper token held in an environment variable — never written to a file. Report the merged SHA. Then execute the rest on a fresh branch from origin/main, metadata-scope-2, merged the same way; report that SHA too.

Read before editing: docs/global-context/core.md; policies/document-metadata-policy.md whole; docs/global-context/decision-layer.md; roles/context-quality-reviewer.md §Scope; bin/check-frontmatter and its glob configuration and tests; every file under roles/ and engagements/ whose first heading is # Role:.

Edits, each an instruction, individually refusable:

1. policies/document-metadata-policy.md §Scope, in-scope list: add docs/global-context/**, engagements/**, prose-criteria.md. Out-of-scope list: add docs/history/** — history, explicitly excluded.
2. Same file, Required fields: add `session:` — required on role documents (first heading # Role:), one of decision | execution, stating the session kind the role runs as; not permitted on non-role documents. Add `order:` as an optional field — an integer fixing the document's position within a bundle, lower first; absent means after all ordered files.
3. docs/global-context/decision-layer.md, §State and record: add one rule — "A baton is one paste block handed in chat: nothing committed, no file, no execution session. Every session end that hands responsibility forward emits one." Number it per the file's scheme.
4. Every role document under roles/ and engagements/: add `session:` to frontmatter. decision: chief-of-staff, assistant. execution: every other role document, including context-quality-reviewer, cartographer, critic, implementer, and all *-agent roles. If any role document's own text states a session kind contradicting this list, stop and surface before writing that file.
5. bin/check-frontmatter glob configuration: add the three new in-scope globs; enforce session: presence and value on role documents and its absence elsewhere; enforce order: as integer where present. Extend its tests in the existing layout: new tests confirmed failing before the enforcement change, then green. Report red and green counts.

Every content edit to these draft files stays a draft edit; flip no status. A changed fact changes everywhere: after edit 1, grep the corpus for statements of the in-scope set and update any that enumerate it. Label every claim observed, inferred, told, or unknown. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface. Report: both SHAs, per-instruction one-line outcome, red/green counts, files touched by the consistency grep, and anything needing Dave's judgment, one line each, up front.
