Write this directive verbatim to docs/cycles/pass2-holdout-fix-2-<timestamp>.md (timestamp ISO 8601 basic, generated now). Run `git checkout origin/main` then `git checkout -b pass2-holdout-fix-b`. Commit the directive file, push the branch, open and squash-merge a PR via curl against the GitHub REST API using the credential-helper token held in an environment variable — never written to a file. Never invoke gh for anything, including auth checks; its errors are not evidence. Report the merged SHA. Then execute the rest on a fresh branch from origin/main, pass2-holdout-fix-b-2, merged the same way; report that SHA too.

Read before editing: docs/global-context/core.md, docs/global-context/decision-layer.md, docs/global-context/review-rubric.md, policies/document-metadata-policy.md, each whole at main; reviews/document-metadata-policy-cycle-17.md DMP-6 and DMP-7; reviews/review-rubric-cycle-2.md RR-6.

Decisions, dictated here (this directive is the origin), each an instruction, individually refusable:

1. docs/global-context/decision-layer.md rule 13, replace the rule's text with: "A baton is one paste block handed in chat: nothing committed, no file, no execution session. It carries pointers and state; every decision it carries already lives in a committed artifact." Keep the rule's bold lead-in style.
2. docs/global-context/core.md, Vocabulary, Baton entry: repair the doubled wording — the entry currently opens "the package a decision session hands its successor decision session: the package of unfinished responsibility". Recast so "package" appears once: "what a decision session hands its successor decision session: the package of unfinished responsibility — state, open questions, decisions in flight — that lets the receiver continue without the conversation that produced it." Rest of the entry unchanged.
3. docs/global-context/review-rubric.md criterion 2: replace "and `order:` where its position in a bundle matters" with "and `order:` if it must load before another file in the same bundle".
4. policies/document-metadata-policy.md, `last-reviewed:` under Required fields, add: the cited artifact must state, in its own scope, that it reviewed this document at the cited SHA; and `status: agreed` requires that artifact's verdict to be `ready` or `ready-with-findings` — non-blocking findings do not bar agreement; Dave weighs them when he agrees; `changes-required` never satisfies. Match the section's register; state the rule, no argument.
5. Same file, gate-document class list (expedited condition 3): add docs/global-context/core.md, docs/global-context/decision-layer.md, docs/global-context/review-rubric.md, skills/review-artifact.md.

These files are draft or in-review; flip no status. The pre-commit hook flipping an agreed file to in-review is enforcement's act, not a flip, and is expected. A changed fact changes everywhere: grep live governing text for the replaced rule-13 wording and the replaced criterion-2 clause; records stay untouched. Run bin/check-frontmatter --all, exit 0 required; bin/tests/run, only the two accepted AC-BN-10 failures permitted. Label every claim observed, inferred, told, or unknown. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface. Report: both SHAs, per-instruction outcome one line each, grep results, check and test results, and anything needing Dave's judgment, one line each, up front.
