# Store fix pass 4: dissolutions, the intake checklist, and the cross-topic sweep

ROUTE AND MODEL

Route: fresh
Model: frontier

FIRST ACT

Create the worktree named in the disposition below first, then write. Write
this directive verbatim to docs/cycles/store-fix-4-20260906T070000Z.md in
that worktree, commit it alone with a message naming the package it opens
(`store: fix pass 4 — directive`), push the branch store-fix-4 to origin with
a plain `git push origin store-fix-4`, never with `-u`, and report the SHA.
Do this before reading anything else and before touching any other file.
Base verification below runs before this act.

FENCE NOTE: the author-written regions (TASK, VERIFICATION) fence with tildes
so they survive transport inside a paste block; the generator's regions keep
their backtick fences and the paste block carrying this directive is opened
with four backticks for that reason. Treat a tilde fence exactly as a backtick
fence.

DISPOSITION PROMPT

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

```text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
```

Both admitted forms, worked:

```text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<name>", created by: git worktree add --no-track "wt/<name>" -b
<name> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
```

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-store-fix-4", created by:
git worktree add --no-track "$TMPDIR/fiducial-store-fix-4" -b store-fix-4 origin/store-fix-3

BASE VERIFICATION

Before anything else, fetch origin/store-fix-3 and origin/main and confirm
the base — the branch store-fix-3, not main — is at the reviewed ref
fd0461bce15f79811e7f0199cc0d0e6befe99f54, and that origin/main is at
a184967413681950a2e8ffa2ab3e16be552f7158, the branch's merge base. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- the four prior store directives under docs/cycles/ (store-all-topics,
  store-fix-1, store-fix-2, store-fix-3), read from the base ref fd0461bce15f79811e7f0199cc0d0e6befe99f54
  — the rulebook and its amendments, in force except where this directive
  says otherwise
- docs/rule-register/store-summary-20260906T060000Z.md @ fd0461bce15f79811e7f0199cc0d0e6befe99f54
  and the three fix logs beside it
- every file under rules/ and under process/, read from the base ref
  fd0461bce15f79811e7f0199cc0d0e6befe99f54 — 613 rows, 9 process files
- docs/rule-register/shortlist.py @ 80a241c94d9ca712dc7abce76a5d8f1408f9e088
  — the near-duplicate shortlist from Pass 3b; read its usage from its
  header; item 9 runs it over the store's bodies
- sources read from the ref fd54448 where an item rewrites from them:
  policies/project-setup-requirements.md @ 5f1f5829f68c39f3804ffbc87086093430da9202,
  docs/global-context/review-rubric.md @ fda7970ece0f0cc4d8f0fdadf2185194444f677d
- process/change-flow.md, read from the base ref fd0461bce15f79811e7f0199cc0d0e6befe99f54
- decisions/log.md @ d81c41a6ab60288764d8a3898cd66b354b3c664b

TASK

Apply Dave's rulings from his fourth read of the store, on a branch cut from
store-fix-3. Ids never change; every change goes in the fix log
docs/rule-register/store-fix-4-20260906T070000Z.md (id, topic before, topic
after or `retired`, keys changed, item), committed with the last item. One
commit per numbered item, pushed after each; per-topic maps updated in the
same commit; summary regenerated last.

Standing note on fix pass 3's report: your reading of the checklist rule's
counter-example (the copy editor's correction list and the twelve criteria
left unmerged) was right for the copy editor and is overridden for the
criteria by item 6 below. R0585's `Supersedes:` is governed vocabulary and
stays.

1. REMOTE WRITE VERIFICATION to three rows, role chief-of-staff (the only
   role that makes connector writes) plus every execution role for row 1:
   (1) "The repository's log is the record of what landed. After any remote
   write, read it back from the fetched remote before reporting — that it
   landed, and that what landed is what you sent (size, commit stats). State
   only SHAs you read from the log." (2) "After a write that timed out or
   came back unconfirmable, read state before anything else: landed —
   continue from it; not landed — re-create it once; the read itself fails —
   state is unknown, stop." (3) "A second consecutive failure of that kind
   is a signal, not a retry: stop and establish state. Auth errors,
   not-found, and your own malformed calls don't count." Every other row in
   the topic retires, disposition `ruled`; the definitions it carried
   (qualifying failure, connector write) retire with it — row 3 states the
   exclusions inline.

2. OUTLINE to zero rows: process/outline.md is the skill; every row in
   topic outline retires `process`. General rule, applied to any remaining
   skill-derived topic: a skill that is a sequence of steps is a process
   document; only a standing obligation the steps do not carry stays a row.

3. SPEC-AND-CHANGE-DISCIPLINE dissolves: red-gate rows (R0083–R0087) retire
   where test-designer states them (R1150, R1152, R1158) and move there
   where it does not; open-delta and concurrency rows (R0090–R0099) retire
   where change-flow or process/decomposition.md states them and move to
   change-flow where not; R0100–R0105 become ONE checklist row in
   decision-layer, keyed to the four decision roles: update the loose-end
   tracker at its checkpoints — flush at session end; account for every
   item before a release gate; land unresolved scan findings; produce on
   demand; surface an item when it becomes relevant; R0106+R0107 become one
   row in coder-agent, also keyed test-designer-agent: a write path
   maintains the entity's derived fields as every other write path does,
   and the package is not done until they are enumerated and checked.
   R0081, R0082, R0088, R0089 retire (describes). The topic ceases.

4. SPEC-GATING dissolves: R0604 (a document's version is the SHA of the last
   commit touching it) and R0611 (agreed means Dave agreed it; the only
   word for it) move to lexicon as define rows with `term: [version]` and
   `term: [agreed]`; R0692, R0695, R0697 retire as duplicates of
   change-flow/test-designer; every other row retires `machinery` (the
   audience, role-document, order, null, superseded/deprecated, and
   excluded-metadata rows are the row model's keys and the tool's now).
   The topic ceases.

5. COMMIT-CONTROL to two rows plus a process document: "Push plainly; never
   force-push." (R0564+R0565, every role that pushes) and R0570 (routine
   change: open the PR and merge it; the gate is elsewhere). R0574–R0576
   retire as change-flow duplicates. R0566, R0568, R0703–R0709 become
   process/project-setup.md — the adoption checklist a human runs once per
   repo: branch protection in the forge (no force-push, no deletion, PR
   only, no admin bypass); the runner's force-push deny in every permission
   mode; the frontmatter-era items dropped; keyed role chief-of-staff,
   session decision, corpus software, opening with the draft line. R0533
   retires (describes).

6. CONTEXT-QUALITY-CRITERIA becomes the intake checklist. One row, keyed
   role context-quality-reviewer, session execution, trigger "At intake,
   test the proposed row against every criterion:", listing — from the
   surviving criteria: readable inside a bundle with no other file open; no
   path references; carries at least one selecting key, values from the
   keys in use; names its session kind; tiers not model names; contributes
   something no rule already in its bundles does; consistent with every
   rule in force, disagreements raised as defects and left unresolved; and
   from Dave's rulings of 2026-09-05: states an obligation or defines a
   term an agent needs, never describes; one trigger per row, stated in the
   body; a negation adds nothing a positive rule already covers; a ban
   names an incident, not a fear; a document's form is a process document,
   not rows; a merged row states the rule at its shortest, not the sum of
   its sources; states what its terms mean nowhere — the Lexicon does; a
   rule a tool enforces is the tool's; rows fired together as one act are
   one checklist row, rows fired independently are separate. Plus two rows
   binding the report: cite the criterion each finding fails (R0266); name
   what you cross-checked (R0284). Everything else in the topic retires
   (`describes` or `checklist`). Rename the topic `intake`.

7. SOURCE-OF-TRUTH dissolves: R0757–R0761 move to lexicon as define rows
   (`term: [PRD]`, `[TRD]`, `[acceptance criteria]`, `[architecture
   summary]`, `[tracker issue]`); R0770, R0775, R0776 retire as duplicates
   of core's R0183 and the Spec Reviewer's scan rows; R0763–R0769 become
   one row in core, keyed chief-of-staff and coder-agent: "A durable rule
   lives in the store, never only in vendor tooling. An adapter is derived
   from it: update the store first, keep the adapter short, note every
   deliberate deviation." R0762 retires (describes). The topic ceases.

8. TEST-PLAN-REVIEW's row (R1496, the merged checklist keyed reviewer-agent)
   moves to topic reviewer, so the test-designer topic carries only the
   Test Designer's rows.

9. CROSS-TOPIC SWEEP. Run docs/rule-register/shortlist.py over the store's
   row bodies (adapt its input from the register's table to rules/*.md; do
   not commit the adaptation unless it is a clean script under
   docs/rule-register/, named store-near-20260906T070000Z.py). For every
   near-pair across two topics, apply the intake criterion "contributes
   something no rule already in its bundles does": if the two rows state one
   obligation and are loaded together by any role's query, keep the row in
   the topic loaded more broadly (core over decision-layer over a role
   topic), retire the other with disposition `duplicate` naming the
   survivor; if they differ in obligation, keep both and list the pair.
   Known pairs to start from: R0787→R0456; R0808→the skeptic's stance row;
   R0574–R0576→change-flow; the operating-model rows merged into core
   against their decision-layer twins. Report the pair count, the retired
   count, and the kept-both list.

10. REGENERATE docs/rule-register/store-summary-20260906T070000Z.md,
    listing every process/ file with its keys and every topic with its row
    count. Commit with the fix log.

Commit messages: `store: fix 4.<n> — <one clause>`. Push after every commit.
Do not open a pull request. Remove the worktree at the end
(`git worktree remove "$TMPDIR/fiducial-store-fix-4"`) and report that it is
gone.

rules/, process/, docs/rule-register/ are outside the frontmatter hook's
in-scope set; the hook should not fire. If it does, stop and report; do not
bypass it.

The report also carries: "Decisions taken", by item; the sweep's pair
count, retired count, and kept-both list; and "Rows worth reading first",
five.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

From the worktree, after the last commit and before removing the worktree:

~~~sh
{
  ls rules/*.md | wc -l
  for t in outline spec-and-change-discipline spec-gating source-of-truth-policy context-quality-criteria; do grep -l "^topic: \[$t\]" rules/*.md | wc -l; done
  grep -l '^topic: \[remote-write-verification-policy\]' rules/*.md | wc -l
  grep -l '^topic: \[commit-control\]' rules/*.md | wc -l
  grep -l '^topic: \[intake\]' rules/*.md | wc -l
  ls process/*.md | wc -l
  grep -h '^topic: ' rules/*.md | sort | uniq -c | sort -rn
  grep -c '^| R' docs/rule-register/store-fix-4-20260906T070000Z.md
  git -C "$TMPDIR/fiducial-store-fix-4" log --oneline origin/store-fix-3..HEAD | wc -l
  git -C "$TMPDIR/fiducial-store-fix-4" diff --stat origin/store-fix-3..HEAD | tail -1
} 2>&1 | tee "$TMPDIR/store-fix-4-verify.log"
~~~

Expected: a row count below 613; five zeros for the dissolved topics; 3
remote-write rows; 2 commit-control rows; 3 intake rows; 10 process files;
the topic table; the fix log's line count; 11 commits; the diff-stat.

STOP CONDITIONS

Pinned to the reviewed ref fd0461bce15f79811e7f0199cc0d0e6befe99f54. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    Store fix pass 4: dissolutions, the intake checklist, and the cross-topic sweep — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    ROUTE AND MODEL — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    FIRST ACT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    DISPOSITION PROMPT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    COMPANIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    TASK — author region
    SANDBOX — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    STOP CONDITIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    REPORT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    CLAIM LABELS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    SOURCE MANIFEST — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
