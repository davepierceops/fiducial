Write this directive verbatim to docs/cycles/bin-land-spec-7-20260823T203500Z.md as your first act: create a worktree via git worktree add $TMPDIR/fiducial-bin-land-spec-7 origin/main (use /tmp/claude-501/fiducial-bin-land-spec-7 if $TMPDIR is unset; a ../ sibling path is denied by the sandbox, executor-observed 2026-08-23) — this worktree is this directive's exclusive working-tree assignment — work only in that directory, git checkout -b bin-land-spec-7 there, commit the directive file, push to bin-land-spec-7, report the SHA. Then proceed.

CONTEXT. specs/bin-land.md is draft; cycle 6 merged to main at 176b84221f379f754ba803a6c129275ff4b61eb9; reviews/bin-land-cycle-6.md verdict ready (0 blocking, 0 non-blocking, 2 observations), reviewed specs/bin-land.md @ 582bf53272847454fd81e1205c1fefb8f4deb2ab. Read from origin/main before editing: specs/bin-land.md, reviews/bin-land-cycle-6.md, docs/global-context/core.md, LEXICON.md, and the review artifact schema (skills/review-artifact.md if present, else the schema section in skills/spec-review-cycle.md).

STOP CONDITION, pinned to the reviewed ref: if specs/bin-land.md at origin/main differs from the content committed at 582bf532, stop and surface; do not reconcile.

INSTRUCTIONS. One edit to specs/bin-land.md, one commit:
1. Q3 closure — this directive is the origin of this wording; downstream artifacts point here: in §8, record Q3 as closed by Dave's sequencing decision: a governed standing write-path document — stating when an agent invokes bin/land and what its output means — must be agreed before the tool becomes agent-facing, where agent-facing means the first directive instructing an executor to invoke it. Implementation (tests confirmed red, then code to green) may proceed under the metadata policy's build-gating rule without waiting on that document. Retire Q3's identifier as Q1, Q2, and Q4 were retired. Record the section as carrying no open questions, in whatever form §8's closing paragraph already uses for retired identifiers.
Cycle-6's O1 and O2, and all standing observations from prior cycles, are out of scope by Dave's decision; do not take them.
Frontmatter untouched: status: draft, last-reviewed: null.

Then, as Spec Reviewer: re-gate the revised spec. Write reviews/bin-land-cycle-7.md per the review artifact schema, Reviewed naming specs/bin-land.md @ the revision commit SHA you just landed, confirming Q3 closed, plus anything new the re-check finds. Commit it separately on bin-land-spec-7. Push. Report every SHA with provenance labels.

CONSTRAINTS, scoped to this work: touch only docs/cycles/bin-land-spec-7-20260823T203500Z.md, specs/bin-land.md, reviews/bin-land-cycle-7.md. Do not merge — merging happens from the decision session over the connector. Do not edit LEXICON.md.

SANDBOX LORE, provenance docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650, carried as told, with two executor-observed additions from 2026-08-23: git checkout origin/main then git checkout -b <branch> guards a stale tree — the config-unwritable rationale for that guard is wrong, executor-observed. Never invoke gh; its errors are not evidence about credentials; reason from first principles. fatal: failed to store: 100001 on stderr is keychain noise; exit status is correct; verify by ls-remote. Never git push -u: the upstream-config write fails (.git/config unwritable, executor-observed 2026-08-23) though the push itself succeeds; push plain and verify by ls-remote. Sequential standalone invocations, never a shell loop. An instruction that cannot be executed as written: stop and surface.
