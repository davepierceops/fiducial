You are the Coder, an execution session on davepierceops/fiducial, clone at ~/code/fiducial. Implement TRD §3.9 migration steps 1 and 2 for the directive tooling: land skills/directive-invariants.md, then bin/directive in general mode with the modules it needs, and turn the generator's tests green. You are a different agent from the Test Designer who wrote the suite; you do not edit tests, the TRD, or the PRD. Where a test and the TRD disagree, stop and report.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-tooling-impl-1-20260829T1400Z.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: directive-tooling implementation package 1", push the branch to origin, and report the SHA.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-dt-impl", created by: git worktree add --no-track "$TMPDIR/fiducial-dt-impl" -b directive-tooling-impl-1 origin/main
Before creating it, run git worktree list; if any existing worktree holds branch directive-tooling-impl-1, or if "$TMPDIR/fiducial-dt-impl" already exists, stop and report. Do not touch the main tree except for the final worktree removal.

BASE VERIFICATION before anything else: git fetch origin (an osxkeychain "failed to store" message is noise; judge the fetch by the refs). origin/main must be at ac48748c159af008e79d6e0ac0892c469ff213e9. If it has moved, stop and report.

READ before writing, whole, from the worktree: specs/directive-tooling-trd.md (agreed; §3.1–§3.3, §3.9, §4.1, §6, §7 are your contract — read all of it); specs/directive-tooling.md (agreed; §6 AC-DT-01..19); bin/tests/helpers.py (its directive-tooling section, especially invariants_sections(), is the schema the tests assert against and the model for the real document's section set); bin/tests/test_directive.py and bin/tests/test_directive_trd.py (what must go green); bin/tests/stubs/README.md; bin/cycle-open and bin/aimeta/cli.py, repo.py, closure.py (the house shape and the modules you reuse unchanged); skills/directive-authoring.md (its first bullet under "Writing the directive file" is text you carry verbatim); docs/cycles/directive-tooling-tests-20260828T1700.md (the register a directive's invariant regions are written in); policies/document-metadata-policy.md (frontmatter the new skill must carry).

SCOPE, in two commits, in this order.

Commit 1 — migration step 1, no code. Land skills/directive-invariants.md.
- Frontmatter: status draft, audience [chief-of-staff, human], and whatever else the metadata policy requires of a draft; bin/check-frontmatter --all must exit 0 with it present. Your invariants parser must skip this leading frontmatter block; the test fixture document carries none.
- Schema exactly as TRD §3.3 "The invariants document's own format": one # title; twenty-one ## sections at column 0, no third level; the section names are the twenty-one helpers.py invariants_sections() lists, spelled identically; a section's body runs from its heading to the next ## heading, with the rule that the first non-blank line of a body is always body — Decisions, Deferred and Execution notes carry a ## marker line as their first body line and are still one section each.
- A committed region's marker line is the first line of its body (§3.3). Placeholders are exactly the ones §3.3's general-mode table fixes per region, {{name}} syntax, and no others; the cycle-mode sections (Heading (cycle), Decisions, Deferred, Execution notes) carry the AC-CO-3 strings bin/cycle-open's render_directive holds today, moved verbatim, plus the {{cycle}}, {{title}}, {{date}}, {{scope_list}} placeholders the fixture uses.
- Working-tree disposition prompt: carries skills/directive-authoring.md's first bullet under "Writing the directive file" verbatim, flowed, inside its fence, as §3.3 states; states that a disposition is required, names both admitted forms ("exclusive assignment", "sole-tree"), and states the "labelled statement" requirement; shows both forms worked inside a fence. The label literal WORKING-TREE DISPOSITION appears in this document only inside fenced blocks (§3.2 condition 2) — the committed test reads the real file and asserts it.
- Disposition label, Marker syntax, Preamble markers, Match phrases: the content §3.3 and §3.4 fix, strings fenced, in the shape the fixture's sections show. The canonical sole-tree sentence is the helpers.py SOLE_TREE_SENTENCE literal.
- Stop conditions must contain both phrases "cannot execute as written" and "concurrent tree mutation"; First act must contain a form of write, commit, push, and "report the SHA"; Report format must list at least two fields; Claim labels must name observed, inferred, told, unknown.
- Register: every region an executor will read is written as directive prose, in the voice of the Test Designer directive named above — not as a rule restated from Core. Sandbox constraints states what this corpus has learned and no more: commands run inside the sandbox; gh cannot reach the GitHub API, so a directive that wants a pull request gets a pushed branch and a report line saying so, and the decision session opens it; no credential ever enters a file or stdout. Base verification and Stop conditions are pinned to {{reviewed_ref}}. Report format names the directive-file commit SHA as its first field and the worktree-removal status as its last.
- Commit message: "directive-tooling: skills/directive-invariants.md (migration step 1)". Push.

Commit 2 — migration step 2, general mode only.
- New: bin/aimeta/invariants.py (the only reader of the invariants document: sections by name, substitution pass, the label literal, the match rule, marker syntax, preamble markers, match phrases; reads committed content from the methodology home via repo.methodology_home, repo.last_commit_sha, repo.file_at_rev — never the working tree; FM-G1/G2/G3/G4 per §6), bin/aimeta/mdmask.py (§3.5's pure eligible-lines-and-leading-content function, importing nothing of this repository), bin/aimeta/directive.py (general-mode region set and emission order per §3.3, substitution, manifest, the FM-G7 self-check using mdmask), bin/directive (the house shape: shebang, argparse, sys.path insert, aimeta imports, sys.exit(cli.run(main))).
- Flags: --descriptor SLUG (general selector), --title, --timestamp YYYYMMDDThhmmss (UTC, general-mode only), --route, --model, --write (a switch, no value; destination is docs/cycles/<descriptor>-<YYYYMMDDThhmmss>Z.md computed by the tool; existing destination refuses, FM-G5), --allow-dirty. --cycle N, --name SLUG, --date, --out and positional documents are accepted by argparse so the selector rule and the flag-collision rule return exit 2 exactly as §3.9 states; a well-formed cycle-mode invocation refuses with a bracketed diagnostic code and exit 2 in this package, with a comment naming step 3 as where it is replaced. Exit statuses per §7: 0 skeleton produced; 2 usage; 3 precondition (FM-G1..G5, G7). Diagnostic codes per §7's table, in the bracketed form helpers.py pins; stderr claims are labelled observed or unknown only (AC-DT-11).
- Manifest: the final region, its preamble read from the Source manifest section; one entry per region in emission order, exactly the forms "<marker> — <path> @ <full sha>" and "<marker> — author region"; the SHA is the last commit touching the invariants document in the methodology home. Fourteen entries, two author (WORKING-TREE DISPOSITION and TASK), twelve committed. The author region for the disposition is the label from the Disposition label section's fence, emitted unfenced at column 0 followed by a colon and a blank line — the only unfenced label the skeleton carries.
- AC-DT-02 is a static scan you must satisfy by construction: no string constant and no source line in bin/directive, directive.py or invariants.py may equal, after whitespace normalization, any non-separator line of any committed section's body — that includes every marker token (ROUTE AND MODEL, TASK, ...) and every placeholder line. Address sections by their heading names, take every marker from the section body's first line, and hold no region text of your own.
- bin/tests/helpers.py: fold make_home_repo into make_home so make_home is a git repository with the invariants document committed (keep make_home_repo as a thin alias so existing imports stand); add "directive" to CLI_NAMES and CLI_MINIMAL_ARGS with argv that clears argparse. Do NOT add "check-directive" — its binary does not exist and adding the name reddens AC-X-1..7. Nothing else in bin/tests/ changes.
- Do not touch bin/cycle-open, bin/tests/test_*.py, specs/, docs/global-context/, policies/, roles/, or any skills/ file other than the new one.
- Commit message: "directive-tooling: bin/directive general mode (migration step 2)". Push.

VERIFICATION after commit 2, from the worktree, output to "$TMPDIR/fiducial-dt-impl-run.log": bin/tests/run for the whole suite, and bin/check-frontmatter --all (must exit 0; state the count).
Expected state, and a stop if it differs: every pre-existing test green (424 before this work); every test_directive.py case green except those that drive cycle mode (they stay red until step 3 — name each red case and confirm its failure is a cycle-mode refusal, nothing else); test_check_directive.py entirely red on the absent binary (expected); test_directive_trd.py: the two TestInvariantsDocumentIsGovernedText cases and the generator-side TestSingleSourceLabel cases green, the lint-side and CLI_NAMES cases red (expected). Any red not attributable to cycle mode or the absent lint is a stop: report it, do not adjust a test.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and both pushes are verified landed (git ls-remote origin directive-tooling-impl-1 shows your commit-2 SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-dt-impl" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref ac48748c159af008e79d6e0ac0892c469ff213e9: on any failed command, any precondition not met, any test whose expectation contradicts the TRD, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; commit-1 SHA; commit-2 SHA; branch name; run-log path; pass/fail/skip counts for the whole suite and for each of test_directive.py, test_directive_trd.py, test_check_directive.py; the named list of red test_directive.py cases with the one-line reason each; check-frontmatter exit code and count; a numbered list of anything the TRD left you to decide, with what you chose and where in the code it lives; anything observed this directive did not anticipate, including the byte-equality test §3.3 names, which does not exist in the suite; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
