# Store fix pass 5: the fifth read — change-flow, chief-of-staff, command-blocks, decision-layer, intake

ROUTE AND MODEL

Route: fresh
Model: frontier

FIRST ACT

Create the worktree named in the disposition below first, then write. Write
this directive verbatim to docs/cycles/store-fix-5-20260906T090000Z.md in
that worktree, commit it alone with a message naming the package it opens
(`store: fix pass 5 — directive`), push the branch store-fix-5 to origin with
a plain `git push origin store-fix-5`, never with `-u`, and report the SHA.
Do this before reading anything else and before touching any other file.
Base verification below runs before this act.

FENCE NOTE: the author-written regions (TASK, VERIFICATION) fence with tildes
so they survive transport inside a paste block; the generator's regions keep
their backtick fences and the paste block carrying this directive is opened
with four backticks for that reason. Treat a tilde fence exactly as a backtick
fence.

DISPOSITION PROMPT

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

```text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
```

Both admitted forms, worked:

```text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<name>", created by: git worktree add --no-track "wt/<name>" -b
<name> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
```

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-store-fix-5", created by:
git worktree add --no-track "$TMPDIR/fiducial-store-fix-5" -b store-fix-5 origin/store-fix-4

BASE VERIFICATION

Before anything else, fetch origin/store-fix-4 and origin/main and confirm
the base — the branch store-fix-4, not main — is at the reviewed ref
2b5d8dc8ef339bb6c9dc9effffa58fb0ff51043f, and that origin/main is at
a184967413681950a2e8ffa2ab3e16be552f7158, the branch's merge base. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- the five prior store directives under docs/cycles/ (store-all-topics,
  store-fix-1 through store-fix-4), read from the base ref
  2b5d8dc8ef339bb6c9dc9effffa58fb0ff51043f — the rulebook and its amendments,
  in force except where this directive says otherwise
- docs/rule-register/store-summary-20260906T070000Z.md and the four fix logs
  beside it, read from the base ref 2b5d8dc8ef339bb6c9dc9effffa58fb0ff51043f
- every file under rules/ and under process/, read from the base ref
  2b5d8dc8ef339bb6c9dc9effffa58fb0ff51043f — 497 rows, 10 process files
- docs/rule-register/store-near-20260906T070000Z.py, read from the base ref
  2b5d8dc8ef339bb6c9dc9effffa58fb0ff51043f — item 12 runs it
- process/change-flow.md and process/decomposition.md, read from the base ref
  2b5d8dc8ef339bb6c9dc9effffa58fb0ff51043f
- policies/decision-log-policy.md @ 8a6efb0c49255a28b42f7ed2ba7c0cbe83cec2cb, read from the ref fd54448 — item 8 rewrites its entry form
  and id arithmetic from it
- decisions/log.md @ d81c41a6ab60288764d8a3898cd66b354b3c664b

TASK

Apply Dave's rulings from his fifth read of the store, on a branch cut from
store-fix-4. Ids never change; every change goes in the fix log
docs/rule-register/store-fix-5-20260906T090000Z.md (id, topic before, topic
after or `retired`, keys changed, item), committed with the last item. One
commit per numbered item, pushed after each; per-topic maps updated in the
same commit; summary regenerated last. A merged row states the rule at its
shortest, not the sum of its sources; where this directive gives a body in
quotation marks, that body is used verbatim; where it gives a body as a
description, write it to the intake checklist (R0264). Every merge keeps the
lowest id of its inputs and the union of their role lists unless the item
says otherwise.

1. LEXICON, definitions by term. (a) New row: `term: [known gap, known
   gaps]`, body "A known gap is a place where the evidence for a change
   stops short of what the release claims: an unverified behavior, an
   unresolved finding, a boundary not yet crossed." (b) R0083 (red-gate)
   moves from test-designer to lexicon; a define carries only `term`, so
   its role, session, and corpus keys go to null. (c) R1512 retires into
   R0757: one PRD definition; R0757's term list becomes the union of both.
   (d) R0493 stays as the `mechanical check` define with body "Mechanical
   checks are lint, type, and static-analysis checks; they pass as part of
   green." (e) R0580 and R0594 move from decision-log-policy to lexicon as
   defines, `term: [DEC ID, decision ID]` and `term: [live decision]`,
   bodies at their shortest.

2. CHANGE-FLOW to 18 rows. R0467 and R0471 retire into the R0470 define,
   body "The per-change stages — acceptance criteria through the release
   gate — run once per meaningful change, in order, each complete before the
   next, and only once the spec is closed and its test suite accepted as
   red-gate evidence." R0477, R0479, and R0094 retire into R0474, one
   chief-of-staff row: while a spec branch is open, edits land on it ungated
   and are gated together at its close; findings between spec and tests
   route through the decision session; the spec's test suite is directed
   under a convergence directive derived from the open spec, not a change
   package; a directive issued mid-delta cites the branch and pins its SHA
   as instructions inside its execution block. R0484 retires into R0478,
   one test-designer-agent row: write tests from the open spec's testable
   claims while the branch is open; select each unit's tests from that
   suite; run them and confirm they fail on bad logic, not merely on an
   absent import, before any implementation. R0482 retires into R0472,
   keyed chief-of-staff and spec-reviewer-agent, body "Before a unit's work
   starts, its acceptance criteria are written, derived from the PRD/TRD
   the Spec Reviewer has gated; that gate reaches specs and nothing else."
   R0473 retires (`ruled`: the container gate; intake replaced it under
   DEC-000380). R0466 retires (`ruled`: names no shape; R0465 and R0470
   carry it). R0492 loses "and never merge them into one pass". Every
   other change-flow row stays as it is.

3. CHIEF-OF-STAFF to 8 rows. R0883 retires into R0880, body "On invocation,
   assess state — the tracker, the recent commits on the default branch,
   the pending gates, and what else is running: sessions, worktrees, the
   connector, branches on origin ahead of the default branch — render it,
   and propose the next step in the first response." R0888 and R0890 retire
   into R0889, body "One decision session holds the connector at a time.
   Before a connector write, re-read what else is running; if a second
   decision session is active, write nothing from either until Dave settles
   which holds it." R0892 retires (`ruled`: contradicts CS7-1, which R0720
   and R0725 carry). R0894 retires into the R0893 define, body "Pre-staging
   is drafting, not landing: it agrees no document, closes no delta,
   releases nothing, and makes no call that is Dave's." R0897, R0898, and
   R0899 retire into R0896, body "Read a pasted execution report yourself.
   Capture the directive's path and landing SHA first, asking for either
   that is missing; then work its items one at a time, answering every
   question each raises before moving on." R0932 retires into R0931, body
   "Before the next major work item, propose handoff to a successor
   decision session in one line; a wave-off drops it for that item." R0933
   retires into decision-layer's R0252, whose body gains "— naming, each
   labelled told, any execution session left running, any worktree held,
   and whether the connector is released." R0935 and R0936 retire
   (`duplicate`: core's decision-session definition; core's R0458). R0934's
   body becomes "Change governed text only through intake or an open delta;
   never edit it directly." R0937 and R1138 stay.

4. COMMAND-BLOCKS to 13 rows, each merge keeping the general rule and
   folding the instance or the how-to into it: R1198 into R1196; R1201
   into R1199; R1204 into R1202 (its body gains "`origin` counts: it is a
   remote name"); R1210 into R1208; R1216 into R1215; R1219 into R1218;
   R1222 into R1220; R1226 into R1228. R1224 retires (`ruled`: the
   container talking). R1200, R1205, R1206, R1212, R1213 stay.

5. DECISION-LAYER. R0226 and R0227 retire into R0225, body "Ask one question
   at a time, the one that matters most; name the item before the choice
   and shape it yes/no where it can be; after handing over a report or a
   block, restate the question still open." R0248b retires into R0248a,
   body "Keep one document under review at a time; 'ship' or 'done'
   advances it exactly one step, never to the end." R0100 keys to
   chief-of-staff only, body "Keep the loose-end tracker current: flush open
   items before the session ends, surface an item when it becomes relevant,
   and account for every item before a release gate or a delta's close."

6. CONTEXT-QUALITY-REVIEWER dissolves into intake. R0947 and R0948 retire
   (`duplicate`: R0264 is the rubric; R0266 cites the criterion). R0946
   retires (`ruled`: no documents to gate). R0950 re-keys to topic intake
   unchanged. R0944 re-keys to topic intake, body "Record each intake as a
   review artifact: what landed, or what was refused and against which
   row." R0266's body becomes "Cite the intake criterion it fails in every
   finding you raise." The topic ceases.

7. CONVERGENCE absorbs reconciliation: R1489, R1490, R1491 move to topic
   convergence, bodies unchanged; the reconciliation topic ceases. Add one
   row to convergence, keyed chief-of-staff, session decision: "A revision
   of an agreed spec opens a new spec branch and goes through the entry read
   and the close the same way." Then dedupe the close checks against the
   Spec Reviewer's R1126 by the sweep's criterion (one obligation, loaded
   together): retire the narrower duplicate, list any pair kept.

8. DECISION-LOG-POLICY to three rows plus a process document. The rows:
   R0583 (append only, never edit — body at its shortest), a merged row
   "One decision per entry." (new; R0585 and R0588 retire into it only
   where they state form — see next), and R0592 (reverse by a superseding
   entry). R0585, R0586, R0589, R0591 retire into process/decision-log.md,
   which carries the entry form, the id arithmetic, the collision rule, and
   the no-author rule, keyed role chief-of-staff, session decision, corpus
   software, opening with the draft line and citing the three rows by id.
   R0581 and R0588 retire (`process`: the document states them). R0580 and
   R0594 went to lexicon in item 1.

9. EXECUTOR ROWS held as undecidable under never-observed — R0013, R0144,
   R0159, R0182, R0820, R1380, R1389 — stay exactly as they are; Dave has
   not ruled. List them in the report with each one's topic and body.

10. VERIFICATION-BOUNDARIES: confirm from the fix-4 log that R0787→R0456 and
    R0808→R1058 landed; if either survives, retire it now (`duplicate`).

11. CROSS-TOPIC SWEEP, second run: docs/rule-register/store-near-20260906T070000Z.py
    over the store after item 10, same criterion as fix 4 item 9, same
    report shape. Shortlist and reasoning to
    store-near-candidates-20260906T090000Z.md and
    store-near-sweep-20260906T090000Z.md.

12. REGENERATE docs/rule-register/store-summary-20260906T090000Z.md,
    listing every process/ file with its keys and every topic with its row
    count. Commit with the fix log.

Commit messages: `store: fix 5.<n> — <one clause>`. Push after every commit.
Do not open a pull request. Remove the worktree at the end
(`git worktree remove "$TMPDIR/fiducial-store-fix-5"`) and report that it is
gone.

rules/, process/, docs/rule-register/ are outside the frontmatter hook's
in-scope set; the hook should not fire. If it does, stop and report; do not
bypass it.

The report also carries: "Decisions taken", by item; item 7's kept-pair list;
item 9's seven rows; the sweep's pair count, retired count, and kept-both
list; and "Rows worth reading first", five.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

From the worktree, after the last commit and before removing the worktree:

~~~sh
{
  ls rules/*.md | wc -l
  for t in change-flow chief-of-staff command-blocks decision-layer intake convergence decision-log-policy; do printf '%s ' "$t"; grep -l "^topic: \[$t\]" rules/*.md | wc -l; done
  for t in context-quality-reviewer reconciliation; do grep -l "^topic: \[$t\]" rules/*.md | wc -l; done
  grep -l 'known gap' rules/*.md | wc -l
  ls process/*.md | wc -l
  grep -h '^topic: ' rules/*.md | sort | uniq -c | sort -rn
  grep -c '^| R' docs/rule-register/store-fix-5-20260906T090000Z.md
  git -C "$TMPDIR/fiducial-store-fix-5" log --oneline origin/store-fix-4..HEAD | wc -l
  git -C "$TMPDIR/fiducial-store-fix-5" diff --stat origin/store-fix-4..HEAD | tail -1
} 2>&1 | tee "$TMPDIR/store-fix-5-verify.log"
~~~

Expected: a row count near 440; change-flow 18, chief-of-staff 8,
command-blocks 13, decision-layer 17, intake 5, convergence 13 or fewer,
decision-log-policy 3; two zeros for the dissolved topics; 1 row naming the
known-gap term; 11 process files; the topic table; the fix log's line count;
13 commits; the diff-stat.

STOP CONDITIONS

Pinned to the reviewed ref 2b5d8dc8ef339bb6c9dc9effffa58fb0ff51043f. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    Store fix pass 5: the fifth read — change-flow, chief-of-staff, command-blocks, decision-layer, intake — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    ROUTE AND MODEL — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    FIRST ACT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    DISPOSITION PROMPT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    COMPANIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    TASK — author region
    SANDBOX — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    STOP CONDITIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    REPORT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    CLAIM LABELS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    SOURCE MANIFEST — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
