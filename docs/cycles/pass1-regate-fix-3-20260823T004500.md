# Directive — Pass 1 re-gate fixes, part 3

Date: 2026-08-23
Route: fresh
Model: frontier
Role: Implementer (execution session). Not a review cycle; edits are dictated below.

## Working-tree rule

This session runs in a clone no other session is using. If any file this session did not change moves, or HEAD moves, or an index lock appears, stop and report; do not recover.

Starting state: origin/main after PR #133's merge plus this directive's own merge. Read in full before editing: docs/global-context/core.md, docs/global-context/decision-layer.md, LEXICON.md, reviews/corpus-regate-cycle-2.md and its twelve companion artifacts. Artifacts give locations; this directive gives dispositions and governs where they differ.

## Decisions

- D9. The Context Quality Reviewer's `ready` verdict precedes `agreed` for every governed context document, except on the two paths the document metadata policy names — expedited and doc-only — which reach `agreed` with no reviewer. The exceptions are stated in the role, by the policy's own names for them.
- D10. D3 stands: the Cartographer is an execution session. Its report has one recipient, the Assistant.
- D11. Review-artifact stems: every file under engagements/sre/ (and any future subdirectory of engagements/ or roles/) uses `<parent-dir>-<basename>`, unconditionally. The non-unique-basename rule is subsumed.
- D12. The no-release-gate rule lives once, in engagements/sre/README.md; engagements/critic.md cites it by title.

## Instructions

### Blocking
1. OM-1 — operating-model.md line ~126: delete "The same gate covers any canonical document, methodology documents included." Replace with: "Methodology and other governed context documents are gated by the Context Quality Reviewer." Sweep operating-model.md for any other clause extending the Spec Reviewer to non-spec documents; conform.
2. CQR-4 — roles/context-quality-reviewer.md: the `agreed` clause becomes "A governed context document reaches `agreed` only after this role's verdict is `ready`, except on the expedited path and the doc-only cycle the document metadata policy defines, which reach `agreed` without a reviewer." D9.
3. CG-1 — engagements/cartographer.md: rewrite the body to execution register. "Dave asks; you dig; you answer" → "Dave directs the discovery; you run it and return the report." Every "hand that to Dave", "you give him the questions" → the report goes to the Assistant, who triages and carries questions to Dave. One recipient throughout. Keep the procedure content. D10.
4. RA-4 — skills/review-artifact.md Filenames: replace the non-unique-basename sentence with "A file under a subdirectory of engagements/ or roles/ has stem `<parent-dir>-<basename>`; all others use `<basename>`." D11.
5. SRE-BM-4 — engagements/sre/baseline-measurement.md line 9-10: "Steps 1-5 run in a decision session by the Assistant; step 6 runs in an execution session by whichever of the Cartographer or Implementer re-measures. All three read the whole file." Per D4, no audience change.
6. SA-4 and SA-1 — engagements/sre/speed-audit.md: line 9-10 reader clause names all three execution roles: "The Cartographer, Implementer, and Critic read it for the steps assigned to them; they do not run the play." Lines 18-21: replace "by the system-discovery procedure" and "by the baseline-measurement procedure" with what each step produces — "the Cartographer maps the pipeline, producing the System Map" and "capture per-stage distributions, producing the Measurement Baseline" — both terms as the README's Artifacts list defines them.

### Non-blocking
7. CQR-3 — roles/context-quality-reviewer.md Scope: replace the three path-shaped references with their titles: "the frontmatter in-scope set, the global-context documents, the engagement documents, and the Public Prose Criteria".
8. CR-1 / DR-23 — engagements/critic.md line 12-13: replace the duplicated sentence with "An engagement has no release gate, per the engagement README." D12.
9. LX-2 / DR-24 — LEXICON.md Provenance class entry: name the four classes and point at Core by title; delete the restated rule sentence.
10. DMP-4, WR-5, WD-2, PC-3 — apply the artifact's Fix as written for each (reviews/document-metadata-policy-cycle-15.md, writer-cycle-2.md, working-with-dave-cycle-4.md, prose-criteria-cycle-2.md). For PC-3: prose-criteria.md second line after the heading — "Read by the Writer role, a decision session."
11. TC-5 — engagements/sre/implementer.md (two uses), engagement-change-package.md, speed-audit.md: "agreed" of an Improvement Proposal or its ACs → "given Dave's go" or the nearest natural form. Leave any use of `agreed` that refers to a governed document's status.

### Checks
12. Sweep the 51-file corpus set for: "methodology documents included"; "agreed" outside the metadata-policy status sense in engagements/**; the no-release-gate sentence appearing in more than one file; any path-shaped reference this directive introduced. Report each remaining occurrence.
13. Run `bin/check-frontmatter --all` and `bin/tests/run`. Expected: exit 0 and exactly the two pre-existing test_bn10 failures.
14. Commit on branch p1-regate-fix-3, push, open a pull request against main titled "Pass 1 re-gate: fixes, part 3" via the REST API with curl. Do not merge. No status flip. Report the SHA read back from git.

## Report shape
PR number and SHA. Files changed, one line each with instruction numbers. Instruction 12 and 13 output. Anything that could not be executed as written; anywhere an artifact's Fix and this directive conflicted and how it was resolved.
