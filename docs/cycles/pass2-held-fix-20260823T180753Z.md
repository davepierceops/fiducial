Write this directive verbatim to docs/cycles/pass2-held-fix-<timestamp>.md (timestamp ISO 8601 basic with trailing Z; generated now). Run `git checkout origin/main` then `git checkout -b pass2-held-fix`. Commit the directive file and push the branch. Do not merge: the sandbox blocks authenticated curl to api.github.com; the decision session merges over its repository connector. Never invoke gh for anything, including auth checks; its errors are not evidence about credentials. Report the branch and head SHA, then continue on a second branch, pass2-held-fix-2, from the same origin/main.

Read before editing: docs/global-context/core.md, docs/global-context/decision-layer.md, roles/skeptic-risk-agent.md, context-sets/testing-and-verification.md, policies/release-readiness-policy.md, docs/global-context/review-rubric.md, skills/review-artifact.md, roles/context-quality-reviewer.md, reviews/decision-layer-cycle-12.md, reviews/skeptic-risk-agent-cycle-2.md or whichever artifact this cycle's predecessor wrote for each held file — find each file's latest artifact by cycle number; all at main.

Edits, dictated (this directive is the origin), each individually refusable:

1. docs/global-context/decision-layer.md rule 13, final sentence: replace "Every session end that hands responsibility forward emits one." with "Every session end that hands responsibility to a successor decision session emits one." — scoping the obligation so directive handoffs to execution sessions fall outside it, per Core's baton/directive distinction.
2. Same file, rule 12, append: "When a baton is also owed, the baton goes first; the retro follows and never delays it."
3. roles/skeptic-risk-agent.md: recast the blocking-example list so severity is judged against the declared verification boundary, not asserted by example. Manual verification where automation was specified, and monitoring below what the verification policy requires, are findings to raise; work conforming to the accepted minimum in testing-and-verification.md is not a finding. Blocking is reserved for gaps the governing policies prohibit. Minimum wording change that states this; list every changed line.

Then, as the Context Quality Reviewer: review both edited files, whole, against all twelve rubric criteria at the head of pass2-held-fix-2; criterion 12 binds — name the cross-checks, which must include core.md, testing-and-verification.md, and release-readiness-policy.md for the respective files. Write one artifact per file at reviews/<stem>-cycle-<n>.md, n continuing each file's numbering. Flips pre-authorized by Dave in this directive: a verdict of ready or ready-with-findings flips the file via bin/flip-agreed (sequential standalone invocations, never a shell loop), last-reviewed citing the new artifact and reviewed SHA. A blocking finding holds the file: record, do not fix, stop and surface.

Run bin/check-frontmatter --all, exit 0 required; bin/tests/run, only the two accepted AC-BN-10 failures permitted. Push pass2-held-fix-2; do not merge. Label every claim observed, inferred, told, or unknown. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface. Report: both branch heads, per-edit changed lines, both verdicts, flips applied or held, check and test results, and anything needing Dave's judgment, one line each, up front.
