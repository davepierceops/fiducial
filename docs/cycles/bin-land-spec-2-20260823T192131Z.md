First: write this directive verbatim to docs/cycles/bin-land-spec-2-<timestamp>.md (timestamp ISO 8601 basic, trailing Z, generated now). Run `git checkout origin/main` then `git checkout -b bin-land-spec-2` (guards a stale tree). Commit the directive file, push, verify by git ls-remote, report the SHA. Do not merge; the decision session merges over its repository connector. Never invoke gh; its errors are not evidence about credentials. "fatal: failed to store: 100001" on stderr during fetch/push/ls-remote is keychain noise; git's exit status is already correct — trust the exit status. These constraints are Dave's standing decisions, recorded on main — docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650. Carry them as told.

Read before editing, all at main: specs/bin-land.md, reviews/bin-land-cycle-1.md, docs/global-context/core.md, policies/document-metadata-policy.md.

Task: revise specs/bin-land.md per Dave's dispositions below (this directive is the origin), then re-review as the Spec Reviewer, cycle 2.

Dispositions, each individually refusable:

1. B1 — accept, resolution dictated: G1 becomes conditional. bin/land <branch> <message> [files...] means "put this commit on that branch": branch absent on origin → create from origin/main HEAD after fetch; branch present → land on top of its current head, and print that prior head SHA in the output before landing so the report carries what was extended. No flags, no modes. The invocation names the branch every time; intent is fully stated by the invocation. AC-LAND-01 rewritten to cover both arms. Staleness is detected by evidence trail — the printed prior head in the executor's report — not prevented by the tool.

2. N2 — accept, resolution dictated: delete the requirement that the tool know or match the literal keychain string. The tool relies on git exit status alone. A note may record that stderr noise exists and is ignored by design; no behavior keys on it.

3. N1 — accept: define the output fields in the spec (branch, head SHA, prior head SHA or "created", per-file blob match result, verification outcome), leaving serialization format to the TRD. Rewrite AC-LAND-07 to test field presence, not format.

4. Q4 — resolve: dissolved by disposition 1. Directive-file-first landing is invocation one; the work is invocation two; same form. Remove the open question.

5. Q2 (naming) and Q3 (governed usage doc before agent-facing) — remain open, restated as the spec's only open questions. Do not resolve either.

Frontmatter: the spec is draft; content edits keep it draft.
