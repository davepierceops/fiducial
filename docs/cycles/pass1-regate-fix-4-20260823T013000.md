# Directive — Pass 1 re-gate fixes, part 4

Date: 2026-08-23
Route: fresh
Model: solid general-purpose
Role: Implementer (execution session). Not a review cycle; edits are dictated below.

## Working-tree rule

This session runs in a clone no other session is using. If any file this session did not change moves, or HEAD moves, or an index lock appears, stop and report; do not recover.

Starting state: main @ 3f449f6 plus this directive's own merge. Read in full before editing: docs/global-context/core.md, reviews/corpus-regate-cycle-3.md and its six companion artifacts. Artifacts give locations; this directive gives dispositions and governs where they differ.

## Decisions

- D13. The Cartographer re-measures after a change (baseline-measurement step 6). The Implementer does not measure its own change.
- D10 restated. The Cartographer's report, including unknowns, has one recipient: the Assistant, who triages and carries questions to Dave.

## Instructions

1. SA-5 — engagements/sre/speed-audit.md step 1 (~line 18-19): "unknowns go to Dave; Dave decides what to ask the client" → "unknowns go to the Assistant in the Cartographer's report; the Assistant carries to Dave what needs his decision, and Dave decides what to ask the client." engagements/sre/system-discovery.md lines ~24-25: conform to the same routing.
2. OM-2 — operating-model.md change-flow step 1: role attribution becomes "(PM/EM/Owner + Architect + Spec Reviewer for specs; Context Quality Reviewer for governed context documents)".
3. DMP-5 — policies/document-metadata-policy.md gate-document class list: add roles/context-quality-reviewer.md alongside the four peer role documents.
4. SRE-BM-5 — engagements/sre/baseline-measurement.md line 9-10: step 6 "runs in an execution session by the Cartographer, who re-measures with the baseline's instrument; the Implementer does not measure its own change." D13. Conform step 6's body if it names a different runner.
5. SRE-ECP-4 — engagements/sre/engagement-change-package.md reader clause: "The Assistant and the Critic read it; neither produces one."
6. CR-2 — engagements/critic.md line 12: "per the engagement README" → "per the Engagement Pack".
7. Sweep engagements/** and operating-model.md for any remaining "to Dave" routing of a Cartographer output, and for "Spec Reviewer" attached to non-spec documents. Report each occurrence.
8. Run `bin/check-frontmatter --all` and `bin/tests/run`. Expected: exit 0; exactly the two pre-existing test_bn10 failures.
9. Commit on branch p1-regate-fix-4, push, open a pull request against main titled "Pass 1 re-gate: fixes, part 4" via the REST API with curl. Do not merge. No status flip. Report the SHA read back from git.

## Report shape
PR number and SHA. Files changed, one line each with instruction numbers. Instruction 7 and 8 output. Anything that could not be executed as written.
