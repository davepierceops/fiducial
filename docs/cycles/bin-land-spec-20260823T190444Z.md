First: write this directive verbatim to docs/cycles/bin-land-spec-<timestamp>.md (timestamp ISO 8601 basic, trailing Z, generated now). Run `git checkout origin/main` then `git checkout -b bin-land-spec` (guards a stale tree). Commit the directive file, push, verify by git ls-remote, report the SHA. Do not merge; the decision session merges over its repository connector. Never invoke gh; its errors are not evidence about credentials — reason from first principles. "fatal: failed to store: 100001" on push/fetch is keychain noise; the operation succeeds — verify by ls-remote, not absence of errors. These constraints are Dave's standing decisions, recorded on main — docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650. Carry them as told.

Read before writing, all at main: docs/global-context/core.md, policies/document-metadata-policy.md, policies/remote-write-verification-policy.md if present, skills/directive-authoring.md, docs/research/gh-write-friction-20260823T184149Z.md, LEXICON.md.

Task: author the spec for bin/land at specs/bin-land.md, status: draft, then review it as the Spec Reviewer and write the review artifact. Two acts, same session; the artifact records both.

Spec content, dictated by Dave in the decision session (this directive is the origin):

Purpose: one invocation replaces the hand-run remote-write sequence in execution sessions; all sandbox workarounds become tested implementation detail, never agent-facing instruction.

Behavior, bin/land <branch> <message> [files...]:
1. Fetch origin, branch from origin/main HEAD (stale-tree guard).
2. Stage named files; all changes if none named.
3. Commit with the given message.
4. Push; treat "fatal: failed to store: 100001" as non-fatal noise.
5. Verify by ls-remote AND per-file blob-SHA comparison against FETCH_HEAD — content verification, not landing confirmation. The research findings document demonstrates the technique; cite it.
6. Output: branch, head SHA, per-file blob match, machine-readable, each claim labeled observed or unknown.
7. Exit 0 only on full verification; on any failure print what was established and what was not. Never retry a write; never delete or force-push anything.

Out of scope, stated in the spec: merging (decision session only, over the repository connector), invoking gh (never), credential extraction or display (the script never touches a token beyond what git does internally), retries.

Non-goal: controlling sandbox variance; the script survives it.

Open question the spec must state and leave open for the TRD/AC stage: whether directive-file-first landing becomes a mode or stays two invocations.

Naming: the spec notes the binary name is provisional pending Dave's LEXICON check; do not add land to LEXICON.md or edit any governed document other than creating this spec.

Then, as the Spec Reviewer: review the spec whole against the review rubric where applicable and the Spec Reviewer role's own gate criteria; verdict-first artifact at reviews/bin-land-cycle-1.md per the review artifact schema. Blocking findings: record, do not fix, stop and surface. Ready or ready-with-findings: report; the agreement flip is Dave's and does not happen this session.

Run bin/check-frontmatter --all, exit 0 required. Push the branch, verify by ls-remote. Report: branch head SHA, spec path, verdict, findings one line each, anything needing Dave's judgment up front, every claim labeled observed / inferred / told / unknown. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface.
