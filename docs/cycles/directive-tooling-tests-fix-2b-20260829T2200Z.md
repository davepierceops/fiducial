You are the Test Designer, an execution session on davepierceops/fiducial, clone at ~/code/fiducial, continuing directive-tooling test fix 2. The previous run stopped on a finding; a ruling has been made and this directive carries it. The worktree and the uncommitted two-test edit from that run are your starting state.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-tooling-tests-fix-2b-20260829T2200Z.md in the worktree named below, commit it ALONE — do not include the pending test edit in this commit; stage only the directive file — with message "Directive: directive-tooling test fix 2b (slot matched by exact form)", push with git push origin directive-tooling-tests-fix-2 (no -u), and report the SHA. Never bypass the pre-commit hook.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in the existing worktree at "$TMPDIR/fiducial-dt-tests-fix-2", on branch directive-tooling-tests-fix-2, which the previous run created and left in place. Do not create a worktree. If git worktree list does not show that path on that branch, stop and report.

BASE VERIFICATION before anything else: git fetch origin. The worktree's HEAD must equal origin/directive-tooling-tests-fix-2, whose tip commit is the fix-2 directive-file commit ("Directive: directive-tooling test fix 2 …") with parent e7996fe70a5ea57404c7958e36a9063abdacbfa5. git status must show exactly one modified file, bin/tests/test_check_directive.py, and git diff must touch only test_ac_dt_06_iv_a_generated_skeleton_with_the_slot_filled_passes and test_ac_dt_10_a_failed_git_read_is_reported_unknown_and_exits_non_zero. If any of that differs, stop and report.

DECISION RECORD. Finding from the fix-2 stop report: test_ac_dt_06_iv fills the disposition slot with a plain first-occurrence string replace of the label-and-colon; the first occurrence in a generated skeleton is the sole-tree worked example inside the prompt region's fence, so the fenced example is rewritten and the real slot stays blank, and M3 fails [disposition-form-absent]. The defect predates both fixes (verified by the previous run against the pristine test). Dave's ruling, 2026-08-29: option (a) — the test locates the slot by its exact form, the line consisting of the label and a colon and nothing else, which in a generated skeleton is only the author slot; the fenced examples carry text after the colon. Option (b), fence-aware matching via the mask, was rejected as the heavier tool.

SCOPE — one test, then one commit carrying both tests' edits.
test_ac_dt_06_iv only: replace the first-occurrence string replace with a line-wise fill — split the skeleton into lines, find the line equal to the label followed by a colon (strip trailing whitespace before comparing; assert exactly one such line exists, with a message naming the count), replace that one line with the filled disposition text (the same two-line exclusive-assignment text the test already uses, split into its two lines), rejoin. The reviewed-ref fill from the previous run stays as written. The docstring gains one sentence citing this ruling. test_ac_dt_10 stays exactly as the previous run left it. Nothing else changes.
Commit message: "directive-tooling: AC-DT-06(iv) fills both author slots, slot matched by exact form; AC-DT-10 defeats the cited object only (rulings 2026-08-29)". Push with git push origin directive-tooling-tests-fix-2.

VERIFICATION, from the worktree, output to "$TMPDIR/fiducial-dt-tests-fix-2-run.log": bin/tests/run for the whole suite.
Expected state, and a stop if it differs: test_check_directive.py 84/84 green; test_directive.py red on exactly six cases (five [cycle-mode-unavailable] refusals and test_ac_dt_14_bin_holds_exactly_one_directive_skeleton_generator); test_directive_trd.py red on exactly test_route_and_model_are_committed_in_both_modes_and_the_author_count_stays_two, with 6 skips; three pre-existing reds outside directive tooling — test_scope sc1 and sc3, test_check_frontmatter cf13 — which belong to another workstream; everything else green. Any red outside that set is a stop: report it, do not widen the edit.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and the push is verified landed (git ls-remote origin directive-tooling-tests-fix-2 shows your fix commit SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-dt-tests-fix-2" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref e7996fe70a5ea57404c7958e36a9063abdacbfa5: on any failed command, any precondition not met, the test not green after the ruled edit alone, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; fix commit SHA; branch name; run-log path; the diff of the two tests against origin/directive-tooling-impl-2, in full; pass/fail/skip counts for the whole suite and for test_check_directive.py; the named red cases across the suite with the one-line reason each; anything observed this directive did not anticipate; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
