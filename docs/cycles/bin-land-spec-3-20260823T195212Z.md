Write this directive verbatim to docs/cycles/bin-land-spec-3-20260823T195212Z.md as your first act: create worktree via git worktree add ../fiducial-bin-land-spec-3 origin/main, work only in that directory, git checkout -b bin-land-spec-3 there, commit the directive file, push to bin-land-spec-3, report the SHA. Then proceed.

CONTEXT. specs/bin-land.md is draft; cycle 2 merged to main at 442b1177930b0d0776260d38fa5e2c3e2a3dc356; reviews/bin-land-cycle-2.md verdict ready-with-findings (F1, F2 non-blocking; O1, O2 observations), reviewed specs/bin-land.md @ 7801f363814493421dd5c9efae020dfc1f780073. Read from origin/main before editing: specs/bin-land.md, reviews/bin-land-cycle-2.md, docs/global-context/core.md, LEXICON.md, skills/review-artifact.md if present else the review artifact schema in skills/spec-review-cycle.md.

STOP CONDITION, pinned to the reviewed ref: if specs/bin-land.md at origin/main differs from the content committed at 7801f363, stop and surface; do not reconcile.

INSTRUCTIONS. Three edits to specs/bin-land.md, one commit:
1. F1 (reviews/bin-land-cycle-2.md): in §4 G1's second arm, state the missing behaviour — the landing base is the branch head as fetched from origin during this invocation; the local tree's prior state never supplies the base; a local tree behind the fetched head does not alter the outcome. Draft the sentence per F1's Fix field; keep it to the one statement the finding says is missing.
2. F2 (reviews/bin-land-cycle-2.md): minimal edit to AC-LAND-04 so it forbids deriving the success determination from stderr rather than forbidding reading stderr; passthrough into the report remains permitted, aligning the criterion with G4.
3. Q2 closure — this directive is the origin of this wording; downstream artifacts point here: in §8, record Q2 as closed by decision: the binary keeps the name land; no LEXICON.md entry is added; binaries are not methodology vocabulary, per the precedent of bundle, flip-agreed, and cycle-open. Retire Q2's identifier as Q1 and Q4 were retired. Q3 remains the section's only open question.
Frontmatter untouched: status: draft, last-reviewed: null.

Then, as Spec Reviewer: re-gate the revised spec. Write reviews/bin-land-cycle-3.md per the review artifact schema, Reviewed naming specs/bin-land.md @ the revision commit SHA you just landed, confirming F1 and F2 discharged and Q2 closed, plus anything new the re-check finds. Commit it separately on bin-land-spec-3. Push. Report every SHA with provenance labels.

CONSTRAINTS, scoped to this work: touch only docs/cycles/bin-land-spec-3-20260823T195212Z.md, specs/bin-land.md, reviews/bin-land-cycle-3.md. Do not merge — merging happens from the decision session over the connector. Do not edit LEXICON.md.

SANDBOX LORE, provenance docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650, carried as told: git checkout origin/main then git checkout -b <branch> guards a stale tree — the config-unwritable rationale is wrong, executor-observed. Never invoke gh; its errors are not evidence about credentials; reason from first principles. fatal: failed to store: 100001 on stderr is keychain noise; exit status is correct; verify by ls-remote. Sequential standalone invocations, never a shell loop. An instruction that cannot be executed as written: stop and surface.
