# Store fix pass 2: keys, definitions, templates, and the chief of staff

ROUTE AND MODEL

Route: fresh
Model: frontier

FIRST ACT

Create the worktree named in the disposition below first, then write. Write
this directive verbatim to docs/cycles/store-fix-2-20260906T050000Z.md in
that worktree, commit it alone with a message naming the package it opens
(`store: fix pass 2 — directive`), push the branch store-fix-2 to origin with
a plain `git push origin store-fix-2`, never with `-u`, and report the SHA.
Do this before reading anything else and before touching any other file.
Base verification below runs before this act.

FENCE NOTE: the author-written regions (TASK, VERIFICATION) fence with tildes
so they survive transport inside a paste block; the generator's regions keep
their backtick fences and the paste block carrying this directive is opened
with four backticks for that reason. Treat a tilde fence exactly as a backtick
fence.

DISPOSITION PROMPT

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

```text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
```

Both admitted forms, worked:

```text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<name>", created by: git worktree add --no-track "wt/<name>" -b
<name> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
```

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-store-fix-2", created by:
git worktree add --no-track "$TMPDIR/fiducial-store-fix-2" -b store-fix-2 origin/store-fix-1

BASE VERIFICATION

Before anything else, fetch origin/store-fix-1 and origin/main and confirm
the base — the branch store-fix-1, not main — is at the reviewed ref
531cb596faa3869dc8625fa5fca9ae4b1386e22c, and that origin/main is at
a184967413681950a2e8ffa2ab3e16be552f7158, the branch's merge base. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- docs/cycles/store-all-topics-20260906T020000Z.md @ a964324 and
  docs/cycles/store-fix-1-20260906T040000Z.md @ bf5c1bc7ce217595a3f7e876384d37724682e3e8,
  both read from the base ref 531cb596faa3869dc8625fa5fca9ae4b1386e22c — the
  rulebook and its first amendment; in force except where this directive
  says otherwise
- docs/rule-register/store-summary-20260906T040000Z.md @ 531cb596faa3869dc8625fa5fca9ae4b1386e22c
  and docs/rule-register/store-fix-1-20260906T040000Z.md @ 531cb596faa3869dc8625fa5fca9ae4b1386e22c
  — the store's record after fix pass 1
- every file under rules/, read from the base ref 531cb596faa3869dc8625fa5fca9ae4b1386e22c
  — 883 files
- the source of each template this pass turns into a process document, read
  from the ref fd54448: specs/trd-template.md @ d3431749d44ca67ea3fc50ac90398cedb7b0f98e,
  specs/prd-template.md @ 0f9581de94640edae16567e522327a00e061f155,
  voice.md @ 7c5c86b37f821dfef4b37548353862f61ee7f94e,
  skills/outline.md @ 5d593a742b6726861b7f57a6d93cc31851b2408b,
  skills/review-artifact.md @ 5d593a742b6726861b7f57a6d93cc31851b2408b,
  roles/chief-of-staff.md @ 0154e2ab4a6db29fc84da47100b62ceca5c85a57
- process/change-flow.md @ 81310de105b0f0b4133117fa4c209baea9d84b09 — the
  form and gate every new process document follows
- decisions/log.md @ d81c41a6ab60288764d8a3898cd66b354b3c664b — DEC-000380,
  DEC-000400

TASK

Apply Dave's rulings from his second read of the store, on a branch cut from
store-fix-1. Ids never change; every change is recorded in a fix log,
docs/rule-register/store-fix-2-20260906T050000Z.md, one line per row
touched (id, topic before, topic after or `retired`, keys changed, item
number), committed with the last item. One commit per numbered item, pushed
after each. Every per-topic store-map whose rows change is updated in the
same commit; the summary is regenerated last. Where a ruling states a test,
apply it and record every row it touches; where it quotes, apply it exactly.

1. RESTORE, as define rows carrying `term` and nothing else (see item 7 for
   the define-row shape): R0465 `term: [meaningful change]`, R0531
   `term: [production-grade]` — surviving rows use both terms. Restore voice
   R1577 (the readership and current goal) merged with R1578 into one
   `require` row: "Write for <the readership R1577 names>, toward <the goal
   it names>; when the goal shifts toward driving adoption, put the register
   question to Dave" — the two placeholders filled from the fd54448 text. Fix the one-cell defect in
   store-summary-20260906T040000Z.md ("21 topics" → "22 topics") in the
   regenerated summary of item 10, not by amending history.

2. CORPUS: the value `methodology` is dropped until a row needs it. Every
   `corpus: [software, writing, methodology]` becomes `[software, writing]`.
   No row is keyed `[methodology]` alone; if one exists, name it and leave
   it for a ruling.

3. VERIFICATION re-keyed: topic verification's rows carry
   `role: [test-designer, coder, reviewer, skeptic, release-manager]`,
   using the store's actual role slugs (`-agent` where the topic map kept
   it); execution-session rows to the first four, decision-session rows
   (R0145, R0147, R0074, and any other that binds the reader of evidence)
   also to release-manager and chief-of-staff.

4. `all` IS NOT A VALUE. Every row keyed `role: [all]` gets an explicit role
   list, decided per row by asking which roles act on this rule — not by
   expanding the wildcard. Core will mostly come out as the full list; that
   is fine when decided. Record in the fix log, for every row, the list
   chosen; and in the report, the list of every row where you kept every
   role, so those can be spot-checked. The set of roles in use is computed
   from the role documents' slugs at fd54448 and from every `role` value
   already in the store; state it in the report.

5. TEMPLATES ARE PROCESS DOCUMENTS. A row that states what a document
   contains — its sections, fields, footer, form — rather than what an agent
   does is the template's, and the template is a `process/` document
   selected by key. For each of: specs/trd-template.md, specs/prd-template.md,
   voice.md, skills/outline.md, skills/review-artifact.md — write
   process/<basename>.md from the fd54448 text, with frontmatter carrying
   `order`, `role` (the roles that use the form: trd → architect-agent and
   spec-reviewer-agent; prd → chief-of-staff and spec-reviewer-agent; voice
   → writer, copy-editor, critic; outline → writer; review-artifact → every
   role that writes a review artifact), `session`, `corpus`, and no status
   field. The document's prose is the template as it stood, conformed to
   the store's vocabulary (open/closed, no `converging`, no `cycle`; the
   review-artifact schema carries the amended verdict rule and drops its
   `Prior cycle` field). Then in the matching topics (trd, prd-template,
   voice, outline, review-artifact-schema) retire every row whose content is
   now the template's, disposition `template` in the fix log, and keep only
   rows stating an obligation an agent performs. Expect the five topics to
   fall from about 150 rows to about 40; report the actual numbers.
   Duplicate pairs R0498/R1560 and R0500/R1561 in trd: keep the fuller,
   retire the other.

6. CHIEF OF STAFF. Topic chief-of-staff keeps its standing rules only —
   on invocation assess state and propose; read pasted output yourself;
   capture the directive path and landing SHA first; pre-staging drafts and
   never decides; escalate ambiguity; propose the handoff before the next
   major item and drop it on a wave-off; do no execution work; recommend
   rotation when the expensive context is spent; the baton rows that add to
   decision-layer's; the connector-contention rows. Expect about twelve.
   Two procedures become process documents, written from the fd54448 role
   text in the change-flow document's form (principle, then the sequence,
   then what it does not decide), keyed `role: [chief-of-staff]`,
   `session: [decision]`, `corpus: [software]`: process/decomposition.md
   (R0901–R0916, R0925–R0930 — tranches, the decomposition doc, package
   directives, staleness, concurrency and claiming) and
   process/spec-test-suite.md (R0917–R0923 — directing the open spec's test
   suite after the entry read). Their rows retire with disposition `process`.
   The state-assessment rows R0883–R0888 get `## Human` noting they retire
   when bin/state lands, and stay. Both process documents are drafts for
   Dave's read; say so in their first line as change-flow's revision 1 did.

7. DEFINITIONS carry `term` only. Every row with `verb: define` and a
   non-null `term` has its `role`, `session`, and `corpus` keys removed —
   the key lines deleted, not set to null; the bundle tool pulls these rows
   by scanning selected bodies for their terms. Every `verb: define` row
   with `term: null` is not a definition: re-verb it to what it is (require,
   forbid, stop) with the body restated as an instruction, or retire it
   under the describes test; record each.

8. COPY EDITOR: retire R0951 and R0952 (description); fold R0986 into
   R0971; re-key R0999 to `role: [chief-of-staff]` (it binds whoever
   summons the session). Apply the same three checks to every other role
   topic: a "short form" or "runs as a session" define retires; a
   "run at tier X" row is re-keyed to chief-of-staff.

9. C020 stays three rows (Dave has not ruled it back to two); the lexicon
   topic name stays lowercase; both are noted, not changed.

10. REGENERATE docs/rule-register/store-summary-20260906T050000Z.md with a
    new column for process documents written, and a section listing every
    process/ file on the branch with its keys. Commit with the fix log.

Commit messages: `store: fix 2.<n> — <one clause>`. Push after every
commit. Do not open a pull request. Remove the worktree at the end
(`git worktree remove "$TMPDIR/fiducial-store-fix-2"`) and report that it is
gone.

rules/, process/, and docs/rule-register/ are outside the frontmatter hook's
in-scope set; the hook should not fire. If it does, stop and report; do not
bypass it. Note specs/ and voice.md are in scope: you write process/ copies
and do not touch the originals.

The report also carries: "Decisions taken", grouped by item; the rows where
every role was kept (item 4); the per-topic before/after counts for item 5;
and "Rows worth reading first", five.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

From the worktree, after the last commit and before removing the worktree:

~~~sh
{
  ls rules/*.md | wc -l
  grep -l '^role: \[all\]' rules/*.md | wc -l
  grep -l 'methodology' rules/*.md | wc -l
  grep -l '^verb: define' rules/*.md | xargs grep -l '^role:' | wc -l
  grep -l '^verb: define' rules/*.md | xargs grep -l '^term: null' | wc -l
  ls process/*.md
  grep -h '^topic: ' rules/*.md | sort | uniq -c | sort -rn | head -40
  grep -c '^| R' docs/rule-register/store-fix-2-20260906T050000Z.md
  git -C "$TMPDIR/fiducial-store-fix-2" log --oneline origin/store-fix-1..HEAD | wc -l
  git -C "$TMPDIR/fiducial-store-fix-2" diff --stat origin/store-fix-1..HEAD | tail -1
} 2>&1 | tee "$TMPDIR/store-fix-2-verify.log"
~~~

Expected: a row count below 883 by the number retired; 0 rows keyed all; 0
rows mentioning methodology; 0 define rows carrying a role key; 0 define rows
with a null term; eight process files (change-flow plus the seven written
here); the topic table; the fix log's line count; 11 commits; the diff-stat.

STOP CONDITIONS

Pinned to the reviewed ref 531cb596faa3869dc8625fa5fca9ae4b1386e22c. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    Store fix pass 2: keys, definitions, templates, and the chief of staff — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    ROUTE AND MODEL — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    FIRST ACT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    DISPOSITION PROMPT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    COMPANIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    TASK — author region
    SANDBOX — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    STOP CONDITIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    REPORT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    CLAIM LABELS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    SOURCE MANIFEST — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
