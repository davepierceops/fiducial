Write this directive verbatim to docs/cycles/bin-land-spec-5-20260823T201300Z.md as your first act: create a worktree via git worktree add $TMPDIR/fiducial-bin-land-spec-5 origin/main (use /tmp/claude-501/fiducial-bin-land-spec-5 if $TMPDIR is unset; a ../ sibling path is denied by the sandbox, executor-observed 2026-08-23), work only in that directory, git checkout -b bin-land-spec-5 there, commit the directive file, push to bin-land-spec-5, report the SHA. Then proceed.

CONTEXT. specs/bin-land.md is draft; cycle 4 merged to main at ad0af0eef700bd5816c749b5eadbd40fb39cc3d6; reviews/bin-land-cycle-4.md verdict ready-with-findings (F1, F2 non-blocking; 4 observations), reviewed specs/bin-land.md @ d13691371e2285d41d9bd141958a61df55476c09. Read from origin/main before editing: specs/bin-land.md, reviews/bin-land-cycle-4.md, docs/global-context/core.md, LEXICON.md, and the review artifact schema (skills/review-artifact.md if present, else the schema section in skills/spec-review-cycle.md).

STOP CONDITION, pinned to the reviewed ref: if specs/bin-land.md at origin/main differs from the content committed at d1369137, stop and surface; do not reconcile.

INSTRUCTIONS. Two edits to specs/bin-land.md, one commit:
1. Cycle-4 F1: apply the Fix stated in reviews/bin-land-cycle-4.md F1 — widen AC-LAND-01b's given so it covers the ordinary tree-at-head case alongside the behind case, per that Fix field's own wording. The Fix field is the source; do not invent an alternative.
2. Cycle-4 F2: revise the §1 sentence that describes the tool as landing what the executor already committed, so §1 no longer describes a tree G1's second arm refuses. State what the tool does in terms consistent with G1 as now written; the wording is yours. Do not weaken G1 to rescue the sentence.
The four cycle-4 observations are out of scope by Dave's decision; do not take them.
Frontmatter untouched: status: draft, last-reviewed: null.

Then, as Spec Reviewer: re-gate the revised spec. Write reviews/bin-land-cycle-5.md per the review artifact schema, Reviewed naming specs/bin-land.md @ the revision commit SHA you just landed, confirming cycle-4 F1 and F2 discharged, plus anything new the re-check finds. Commit it separately on bin-land-spec-5. Push. Report every SHA with provenance labels.

CONSTRAINTS, scoped to this work: touch only docs/cycles/bin-land-spec-5-20260823T201300Z.md, specs/bin-land.md, reviews/bin-land-cycle-5.md. Do not merge — merging happens from the decision session over the connector. Do not edit LEXICON.md.

SANDBOX LORE, provenance docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650, carried as told, with two executor-observed additions from 2026-08-23: git checkout origin/main then git checkout -b <branch> guards a stale tree — the config-unwritable rationale for that guard is wrong, executor-observed. Never invoke gh; its errors are not evidence about credentials; reason from first principles. fatal: failed to store: 100001 on stderr is keychain noise; exit status is correct; verify by ls-remote. Never git push -u: the upstream-config write fails (.git/config unwritable, executor-observed 2026-08-23) though the push itself succeeds; push plain and verify by ls-remote. Sequential standalone invocations, never a shell loop. An instruction that cannot be executed as written: stop and surface.
