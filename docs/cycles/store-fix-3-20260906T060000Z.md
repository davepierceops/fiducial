# Store fix pass 3: the spec reviewer, checklists, the skeptic, the retro, and commentary

ROUTE AND MODEL

Route: fresh
Model: frontier

FIRST ACT

Create the worktree named in the disposition below first, then write. Write
this directive verbatim to docs/cycles/store-fix-3-20260906T060000Z.md in
that worktree, commit it alone with a message naming the package it opens
(`store: fix pass 3 — directive`), push the branch store-fix-3 to origin with
a plain `git push origin store-fix-3`, never with `-u`, and report the SHA.
Do this before reading anything else and before touching any other file.
Base verification below runs before this act.

FENCE NOTE: the author-written regions (TASK, VERIFICATION) fence with tildes
so they survive transport inside a paste block; the generator's regions keep
their backtick fences and the paste block carrying this directive is opened
with four backticks for that reason. Treat a tilde fence exactly as a backtick
fence.

DISPOSITION PROMPT

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

```text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
```

Both admitted forms, worked:

```text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<name>", created by: git worktree add --no-track "wt/<name>" -b
<name> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
```

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-store-fix-3", created by:
git worktree add --no-track "$TMPDIR/fiducial-store-fix-3" -b store-fix-3 origin/store-fix-2

BASE VERIFICATION

Before anything else, fetch origin/store-fix-2 and origin/main and confirm
the base — the branch store-fix-2, not main — is at the reviewed ref
4655e83cd9f8f18e4dca5f87a5ffa8f18b4e70ca, and that origin/main is at
a184967413681950a2e8ffa2ab3e16be552f7158, the branch's merge base. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- the three prior store directives, read from the base ref 4655e83cd9f8f18e4dca5f87a5ffa8f18b4e70ca:
  docs/cycles/store-all-topics-20260906T020000Z.md @ a964324,
  docs/cycles/store-fix-1-20260906T040000Z.md @ bf5c1bc7ce217595a3f7e876384d37724682e3e8,
  docs/cycles/store-fix-2-20260906T050000Z.md @ b39414b88e4fcc946ed2f1f1f9a3ab8a3251cf10
  — the rulebook and its amendments, in force except where this directive
  says otherwise
- docs/rule-register/store-summary-20260906T050000Z.md @ 4655e83cd9f8f18e4dca5f87a5ffa8f18b4e70ca
  and the two fix logs beside it — the store's record after fix pass 2
- every file under rules/ and under process/, read from the base ref
  4655e83cd9f8f18e4dca5f87a5ffa8f18b4e70ca — 765 rows, 8 process files
- sources this pass rewrites from, read from the ref fd54448:
  roles/spec-reviewer-agent.md @ e4110f0cc3e47a245a51289b9aa00639ccf05fdb,
  roles/skeptic-risk-agent.md @ 0f9ce8a9aff973c3d3c6a686f371cad1cfb456b1,
  skills/evidence-review.md @ 12557a85f46192283244ea06ad00f390ff925a67,
  skills/conversation-retro.md @ abd7c9cde0b71b3639edda22b8e5e2c062514cee,
  public-prose-criteria.md @ 06b6923a94bb21611b7dc7dfa5a608a69a23302d
- process/change-flow.md @ 81310de105b0f0b4133117fa4c209baea9d84b09 — the
  entry read and the close, which item 1 conforms the Spec Reviewer to
- decisions/log.md @ d81c41a6ab60288764d8a3898cd66b354b3c664b

TASK

Apply Dave's rulings from his third read of the store, on a branch cut from
store-fix-2. Ids never change; every change goes in the fix log
docs/rule-register/store-fix-3-20260906T060000Z.md (id, topic before, topic
after or `retired`, keys changed, item), committed with the last item. One
commit per numbered item, pushed after each; per-topic maps updated in the
same commit; summary regenerated last. Tests are applied across the scope
they name and every row touched is logged; quoted text is applied exactly.

1. SPEC REVIEWER conforms to process/change-flow.md. The Spec Reviewer reads
   a spec at exactly two points: the entry read, when the branch opens and
   before the Test Designer starts; and the close, when spec and tests
   cohere. R1101 and R1103 become one row saying so, and that Dave agrees at
   the close on that read. The nineteen "confirm…" rows R1107–R1125 are the
   entry read's checklist, in three rows (item 2's shape): the general
   checklist (R1107–R1115), the PRD checklist (R1116–R1121), the TRD
   checklist (R1122–R1125), each opening "In the entry read, confirm:".
   The close is one checklist row from R1126–R1132: read the branch's whole
   diff and the tests with the red-gate result; check exactly coherence and
   that red was behavioral; file a contract-without-claim as a spec finding
   and a claim-without-test as a test gap, both for triage; leave adequacy
   to the Reviewer; record branch point, reviewed SHA, and the tests'
   acceptance. R1133/R1134 → one row on how a scan reports. R1137–R1139 →
   one row on what a Depth 3 sweep is (contradictions across everything;
   governed text is the Context Quality Reviewer's); the *recommending* of
   one (R1138) moves to the chief-of-staff topic, merged into its deep-read
   row or, if none exists, written as one: "Propose a deep read at
   milestone moments — a major version cut, a significant structural
   change — without being asked." R1098–R1100 retire (describes).

2. CHECKLIST RULE, every topic: rows sharing one trigger that are always
   performed together as one act are one row with a list body, the trigger
   stated once. Rows sharing a trigger but fired independently (a defect
   list a copy editor applies as each defect appears) stay separate. Apply
   across the store; log each merge with the trigger it shares.

3. COMMENTARY PURGE, every row: a body that explains why it changed — cites
   a DEC, cites process/change-flow.md, says "restated", "retires",
   "supersedes", "DEC-000380 retires…" — is cut back to the instruction. The
   explanation goes to `## Human` only where it is the rule's reason, not
   its history; otherwise it is dropped (the fix logs are the history).

4. NEVER-OBSERVED TEST, every `forbid` row: a ban on a behaviour that no
   session has ever exhibited — the corpus was written from fears as well as
   failures — retires, disposition `never-observed`. Judge from the rule's
   own `## Human` and source context: a ban that names an incident stays; a
   ban with no incident behind it and a positive rule already covering the
   ground goes. R0851 retires under this test. Log every retirement with a
   one-clause reason; where you cannot tell, keep the row and list it.

5. PUBLIC PROSE CRITERIA: R0813, R0815, R0816 retire (describes). R0821–
   R0827 → one row: "Signal each claim's tier in the sentence: relayed,
   demonstrated, grounded, or opinion." — the tier definitions are the
   Lexicon's, pulled by term; R0826 folds into the Lexicon's `claim
   strength` define. R0835–R0840 → one row: "Say each point once; cut
   restatements. Length and structure are the author's call per piece." The
   general rule: a merged row that restates what its terms mean is
   restating the Lexicon; state the rule and let `term` bring the meanings.
   Apply across the store.

6. FIRST SECTIONS: re-read the opening section of every source file at
   fd54448 against the describes test, specifically — the framing lives
   there. Retire what it catches, logged `describes`.

7. RETRO: topic conversation-retro renames to retro. Its form — frontmatter
   fields, section names and contents, filename pattern, the date rules,
   synthesis mechanics (`covers:`, input-set computation, cross-project
   read) — becomes process/retro.md, keyed to the four decision roles,
   session decision, corpus [software, writing], opening with the draft
   line. Rows remaining, six: ground every retro in evidence from the
   conversation (R1237); durable and new, not restatements (R1241+R1242);
   read and write nothing remote while producing it (R1244); hand it over as
   one Markdown file in the chat (R1246); name a repeated preference as a
   candidate standing rule and stop there (R1249–R1252, one row); a
   proposed methodology change is a finding for a delta, not a decision
   (R1270). R1240 retires (never-observed: no trigger invites a retro once
   the old end-of-session rule is gone). Everything else retires `template`.

8. SKEPTIC to eight rows: the pass as one checklist (R1073, R1357, R1358,
   R1359, R1360, R1074 — inputs; list the claims; match each to its
   evidence; report the unsupported and the overbroad); calibration
   (R1068–R1072 + R1089, one positive row: a risk is a finding, not a
   blocker; `blocking` only where policy forbids shipping with it; R1092/
   R1093 at most as its `## Human`); stance (R1058+R1061); the tells
   (R1077, unchanged); severity and scope (R1088, R1090, R1091 — three
   rows, three triggers); output (R1094–R1096 + R1363, R1364, one row:
   findings with labels and a recommended next step, no ship call, blocking
   is how you say don't ship); R1352/R1353 fold into the checklist's
   trigger. R1057, R1075, R1076 retire.

9. PROCESS FILES uniform: process/change-flow.md's frontmatter becomes
   `corpus: [software]` and gains `role:` listing the roles the change flow
   binds (every role); all eight process files then carry order, role,
   session, corpus. R1414 — the amended verdict rule (one overall verdict,
   most severe of the passes) — is restored as a row in
   review-artifact-schema: a rule with teeth stays a row even when the form
   also states it.

10. REGENERATE docs/rule-register/store-summary-20260906T060000Z.md; commit
    with the fix log.

Commit messages: `store: fix 3.<n> — <one clause>`. Push after every commit.
Do not open a pull request. Remove the worktree at the end
(`git worktree remove "$TMPDIR/fiducial-store-fix-3"`) and report that it is
gone.

rules/, process/, docs/rule-register/ are outside the frontmatter hook's
in-scope set; the hook should not fire. If it does, stop and report; do not
bypass it.

The report also carries: "Decisions taken", by item; the never-observed
retirements with reasons, and the rows kept as undecidable; the checklist
merges with their triggers; and "Rows worth reading first", five.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

From the worktree, after the last commit and before removing the worktree:

~~~sh
{
  ls rules/*.md | wc -l
  grep -l '^topic: \[conversation-retro\]' rules/*.md | wc -l
  grep -l '^topic: \[retro\]' rules/*.md | wc -l
  grep -l '^topic: \[skeptic\]' rules/*.md | wc -l
  grep -l '^topic: \[spec-reviewer-agent\]' rules/*.md | wc -l
  grep -l 'DEC-000380 retires\|process/change-flow.md restates\|supersedes' rules/*.md | wc -l
  ls process/*.md | wc -l
  grep -L '^role:' process/*.md | wc -l
  grep -c '^| R' docs/rule-register/store-fix-3-20260906T060000Z.md
  git -C "$TMPDIR/fiducial-store-fix-3" log --oneline origin/store-fix-2..HEAD | wc -l
  git -C "$TMPDIR/fiducial-store-fix-3" diff --stat origin/store-fix-2..HEAD | tail -1
} 2>&1 | tee "$TMPDIR/store-fix-3-verify.log"
~~~

Expected: a row count below 765; 0 conversation-retro rows; 6 retro rows; 8
skeptic rows; spec-reviewer-agent at the count item 1 produces (about 10);
0 bodies carrying the commentary phrases; 9 process files; 0 process files
without a role key; the fix log's line count; 11 commits; the diff-stat.

STOP CONDITIONS

Pinned to the reviewed ref 4655e83cd9f8f18e4dca5f87a5ffa8f18b4e70ca. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    Store fix pass 3: the spec reviewer, checklists, the skeptic, the retro, and commentary — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    ROUTE AND MODEL — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    FIRST ACT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    DISPOSITION PROMPT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    COMPANIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    TASK — author region
    SANDBOX — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    STOP CONDITIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    REPORT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    CLAIM LABELS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    SOURCE MANIFEST — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
