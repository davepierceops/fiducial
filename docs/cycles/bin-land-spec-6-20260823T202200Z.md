Write this directive verbatim to docs/cycles/bin-land-spec-6-20260823T202200Z.md as your first act: create a worktree via git worktree add $TMPDIR/fiducial-bin-land-spec-6 origin/main (use /tmp/claude-501/fiducial-bin-land-spec-6 if $TMPDIR is unset; a ../ sibling path is denied by the sandbox, executor-observed 2026-08-23), work only in that directory, git checkout -b bin-land-spec-6 there, commit the directive file, push to bin-land-spec-6, report the SHA. Then proceed.

CONTEXT. specs/bin-land.md is draft; cycle 5 merged to main at 904cec2ee3d17dbd65da175250968afd7fdc6cf9; reviews/bin-land-cycle-5.md verdict ready-with-findings (F1 non-blocking; O1, O2 observations), reviewed specs/bin-land.md @ ba9c0dfbcc707206a42f7ebe7f4ac316286fa63a. Read from origin/main before editing: specs/bin-land.md, reviews/bin-land-cycle-5.md, docs/global-context/core.md, LEXICON.md, and the review artifact schema (skills/review-artifact.md if present, else the schema section in skills/spec-review-cycle.md).

STOP CONDITION, pinned to the reviewed ref: if specs/bin-land.md at origin/main differs from the content committed at ba9c0dfb, stop and surface; do not reconcile.

INSTRUCTIONS. One edit to specs/bin-land.md, one commit:
1. Cycle-5 F1: apply the Fix stated in reviews/bin-land-cycle-5.md F1 — widen AC-LAND-01a's given the same way cycle 5 widened 01b's, per that Fix field's own wording. The Fix field is the source; do not invent an alternative. Nothing else changes.
Cycle-5's O1 and O2, and cycle-4's standing observations, are out of scope by Dave's decision; do not take them.
Frontmatter untouched: status: draft, last-reviewed: null.

Then, as Spec Reviewer: re-gate the revised spec. Write reviews/bin-land-cycle-6.md per the review artifact schema, Reviewed naming specs/bin-land.md @ the revision commit SHA you just landed, confirming cycle-5 F1 discharged, plus anything new the re-check finds. Commit it separately on bin-land-spec-6. Push. Report every SHA with provenance labels.

CONSTRAINTS, scoped to this work: touch only docs/cycles/bin-land-spec-6-20260823T202200Z.md, specs/bin-land.md, reviews/bin-land-cycle-6.md. Do not merge — merging happens from the decision session over the connector. Do not edit LEXICON.md.

SANDBOX LORE, provenance docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650, carried as told, with two executor-observed additions from 2026-08-23: git checkout origin/main then git checkout -b <branch> guards a stale tree — the config-unwritable rationale for that guard is wrong, executor-observed. Never invoke gh; its errors are not evidence about credentials; reason from first principles. fatal: failed to store: 100001 on stderr is keychain noise; exit status is correct; verify by ls-remote. Never git push -u: the upstream-config write fails (.git/config unwritable, executor-observed 2026-08-23) though the push itself succeeds; push plain and verify by ls-remote. Sequential standalone invocations, never a shell loop. An instruction that cannot be executed as written: stop and surface.
