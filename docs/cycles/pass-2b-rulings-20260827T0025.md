Pass 2b divergence rulings. Read this whole block before acting.

FIRST ACT — land this directive.
Working-tree disposition: EXCLUSIVE. Create the tree with `git worktree add "$TMPDIR/pass-2b-rulings" -b pass-2b-rulings origin/main` from the main clone and work only there.
Write this entire block, verbatim, to docs/cycles/pass-2b-rulings-20260827T0025.md. Commit with message "docs/cycles: land pass-2b-rulings directive". Push with `git push origin pass-2b-rulings`. Verify with `git ls-remote origin pass-2b-rulings`. Report "landed <path> as <sha>" using the SHA read back from git.

REVIEWED REF: a3acb75ef47d4eaa8295bfe2524a967742df8f85 (main). Input: docs/rule-register/rule-clusters-20260825T1600.md, clusters C014, C015, C018, C019, C020, C023, C024, C033, C040, C042, C055 — filed as agreeing in Pass 2, reported divergent-on-reading by the agreeing-clusters collapse executor. Dave ruled on each in a decision session 2026-08-26; this block is the record. Every ruling below is *told*. Read each cited file at the reviewed ref before editing.

STOP CONDITIONS — stop and report, do not recover:
- `git rev-parse origin/main` is not a3acb75 before your first content commit.
- Any tree mutation you did not intend, including your own: a file this session did not change moves, HEAD moves, or an index lock appears.
- Any push fails.

RULINGS — EDIT (three):

C014 — skills/spec-review-cycle.md, Procedure step 10. Delete the final sentence: "The SHA it cites must resolve to a recorded agreement entry." Reason: it extends the expedited-log rule to full-cycle flips, which cite a review artifact, not a log entry; the metadata policy states the check and is the home. Nothing else in step 10 changes.

C015 — roles/chief-of-staff.md, "Open spec deltas", second bullet. Replace "per the Spec and Change Discipline context set" so the bullet reads: "Check the claim before proposing a second concurrent tranche — the Lexicon defines a claimed document; the Spec and Change Discipline context set caps concurrency at two tranches on disjoint territory — and where the territory overlaps, propose serial execution or a different project." Reason: the claim rule lives in the Lexicon and the cap in the context set; the old pointer named only the file that lacks the claim rule.

C018 — operating-model.md, Agents / Must. Replace the bullet "distinguish mocked, contract, live, browser, and production verification" with: "label every verification claim with its evidence class, per the Lexicon". Reason: the old bullet hand-listed five of the eight classes and said distinguish rather than label; an agent could satisfy it without ever writing a class.

RULINGS — NO EDIT (eight), accepted as consistent, recorded so the decision is not lost:
C019 specifications canonical — definition, summary, and thesis line at three depths; no contradiction.
C020 reconciliation gates once — Lexicon definition, operating-model step-1 clause, commit-policy mechanics; numbers agree.
C023 escalate ambiguity — chief-of-staff adds a timing (resolve before packaging), not a restatement.
C024 live tests not in every unit run — policy's "normally" is followed by its exceptions; skeptic line is posture.
C033 force-push denied — commit policy states two layers and points at setup for the server-side one.
C040 "prompt" retired — core's line is scoped to artifacts; Lexicon carries the map and the approval-prompt carve-out.
C042 verification output shape — T&V already names itself an expansion of the standard shape.
C055 first response is the artifact — decision-layer governs a request; chief-of-staff governs invocation.

STATUS: the pre-commit hook flips each edited agreed document to in-review with last-reviewed null. Expected; do not flip any status by hand and do not run bin/flip-agreed.

CONSISTENCY: after each edit, sweep the edited file for any other sentence that restates the removed or replaced text and report it; do not edit beyond the ruling without reporting first.

VERIFY: `bin/check-frontmatter --all` green apart from the standing `WARN README.md: [unmatched-glob]`. Record `bin/bundle --audience chief-of-staff | wc -l` before and after.

COMMIT AND PUSH: one commit per edited file, message "pass-2b: <file> — C0nn". Push with `git push origin pass-2b-rulings`. Verify with `git ls-remote origin pass-2b-rulings`.

DO NOT: edit any file other than the three named plus this directive; touch the register or clusters files; open a pull request; merge; delete branches or worktrees; flip status by hand.

REPORT:
1. Directive path and landed SHA.
2. Commit SHAs in order with `git diff --stat`.
3. Per-cluster line for C014, C015, C018: edited, with the exact new text.
4. Any restatement found by the consistency sweep, with location.
5. `bin/check-frontmatter --all` result; documents now in-review, listed.
6. CoS bundle line count before and after.
Label every claim observed, inferred, told, or unknown.
