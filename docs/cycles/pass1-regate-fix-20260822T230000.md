# Directive — Pass 1 reconciliation re-gate: fixes

Date: 2026-08-22
Route: fresh
Model: frontier
Role: Implementer (execution session). Not a review cycle; edits are dictated below.

## Working-tree rule

This session runs in a clone no other session is using. If any file this session did not change moves, or HEAD moves, or an index lock appears, stop and report; do not recover.

Starting state: main @ 8cdc0b9 plus this directive's own merge. Read in full before editing: docs/global-context/core.md, docs/global-context/decision-layer.md, LEXICON.md, reviews/corpus-regate-cycle-1.md. Per-file artifacts under reviews/ give line locations; consult them only for locations. This directive states every disposition; where an artifact's Fix differs, this directive governs.

## Decisions

- D1. The client-write-access guardrail is overridable, by Dave only, logged. The override log is the record that write access was granted.
- D2. The engagement Skeptic is renamed Critic (file, slug, heading, prose). The Skeptic/Risk Agent keeps its name.
- D3. The Cartographer runs as an execution session; its question list and rendered map are its execution report.
- D4. Audiences are never narrowed to resolve a session-kind mismatch; the file states who runs it and who reads it.
- D5. Spec Reviewer gates specs only (PRD, TRD, derived ACs). Context Quality Reviewer reviews every governed context document; a `ready` verdict precedes Dave's agreement. No cross-review.
- D6. prose-criteria.md keeps its claims ladder as a claim-strength axis, distinct from Core's provenance ladder; its `inferred` tier becomes `grounded`. Reverses the 2026-08-20 disposition.
- D7. docs/global-context/inventory.md is history.
- D8. An engagement has no release gate; agents do not inquire into or reason about the client's release timing.

## Instructions

### Moves
1. `git mv docs/global-context/inventory.md docs/history/inventory-20260820.md`. No edits.
2. `git mv engagements/skeptic.md engagements/critic.md`. Heading "Role: Critic". Replace audience value `skeptic` with `critic` repository-wide. Replace prose references to the engagement Skeptic (never the Skeptic/Risk Agent) with "Critic" across engagements/**, roles/writer.md, prose-criteria.md. Report files touched.

### Frontmatter
3. engagements/working-with-dave.md: add `implementer` to audience.
4. skills/review-artifact.md: add `critic` to audience.
5. roles/writer.md: add `order: 10`. prose-criteria.md: add `order: 11`.
6. policies/document-metadata-policy.md: audience-value rule becomes — values are the basename slug of any role document under roles/ or engagements/ (including engagements/sre/), plus reserved `all-roles`, `all-decision-roles`, `human`; `all-decision-roles` selects every role that runs as a decision session. Remove the `README.md` gate-class entry and the `README.md` in-scope glob. Replace the "Orchestrators may select context" line with "Bundle membership is declared by the document's audience value; no reader selects it." Add `retros/` to the Out-of-scope list.

### Session kind and boundaries
7. engagements/cartographer.md: "runs as an execution session; its report — the question list and the rendered map — returns to the Assistant." engagements/sre/system-discovery.md: conform.
8. engagements/sre/engagement-change-package.md line 9: "Produced in an execution session by the Implementer. The Assistant reads it; it does not produce one." engagements/sre/speed-audit.md line 9: "Runs in a decision session. The Implementer and Critic read it for their step-5 assignments; they do not run the play."
9. roles/spec-reviewer-agent.md: scope is the PRD, TRD, and their ACs; delete every clause extending the gate to canonical or methodology documents. skills/spec-review-cycle.md: conform. roles/context-quality-reviewer.md: Scope becomes one rule — "Every governed non-code file: the frontmatter in-scope set plus docs/global-context/*.md, engagements/**/*.md, prose-criteria.md; excluding history, batons, cycles, reviews, retros, trackers, the decision log, and vendor-tooling files." What it cedes: "The PRD, TRD, and their acceptance criteria are the Spec Reviewer's. Nothing else is." Add: "A document reaches `agreed` only after this role's verdict is `ready`; agreement is Dave's." docs/global-context/review-rubric.md scope sentence: cite the Context Quality Reviewer role's rule instead of "every non-code file in this repository". Add under the rubric heading: "A criterion is a test for a stated rule and may name it; criterion 4 does not apply to this file."
10. engagements/sre/implementer.md, Constraints: replace the "does not review its own diff" clause with "The diff is reviewed by the client's pull-request gate. A Critic read happens only when Dave requests one; do not request it and do not review your own diff."

### Override policy
11. engagements/working-with-dave.md: delete "not negotiable, not overridable"; replace with "Overridable by Dave only, logged; the log entry is the record that write access was granted." engagements/sre/override-log-policy.md, Exclusions: add "Only Dave overrides. The client's humans do not; the agent does not." Line 28-30: "engagement retro" → "engagement review (not a conversation retro)".

### engagements/sre/README.md
12. Replace "The client's humans hold the release gate on their own systems" with "An engagement has no release gate. Release is the client's concern, not this role's; do not inquire into or reason about the client's release timing." engagements/critic.md line 12 takes the identical sentence.
13. Add section "Artifacts": one line each, shape and location, no procedure — System Map; Measurement Baseline (per-stage p50/p95, date range, run count, known confounds); Improvement Proposal; engagement working area (client-hosted repository designated at kickoff); baseline-gate; and the convention "a record with no fixed filename, created on first use, kept in the engagement working area" (for the override log and the Assistant's notes). Lift wording from system-discovery, baseline-measurement, speed-audit, assistant, override-log-policy; those files keep their procedure and cite "the Artifacts list" by name, not by path. Replace "the system-discovery skill is the procedure" and "the baseline-measurement skill is the procedure" with what each produces.
14. Delete the Key principles list. Keep "changes land as pull requests through the client's own gates" here once; cut it from implementer.md and speed-audit.md.
15. At the first mention of the Critic: "A Critic read is advisory; its verdict is input to Dave and gates nothing."

### Other sre files
16. baseline-measurement.md: cut the opening restatement (file opens at "The baseline-gate procedure"); Override section becomes "An override of this gate names the measurement debt in the change package."
17. speed-audit.md: "Agreed means Dave says yes" → "Dave's go: the proposal records it and the change package cites it."

### Writer and prose criteria
18. roles/writer.md: first line after heading "The Writer runs as a decision session." Delete "Nothing publishes on agent judgment…". Final bullet → "When Dave says the piece is done, state that a Critic read (advisory, reads the piece against the Public Prose Criteria) and a Context Quality Reviewer pass (checks it as LLM context, if it will be bundled) are available, and stop. Do not start either."
19. prose-criteria.md: rename `inferred` → `grounded`; add "This ladder is claim strength — how firmly a sentence may assert — not provenance; provenance is stated per Core." Delete the Trust-model publication restatement, keeping only a clause Core does not carry if one exists. Rewrite the four author-addressed lines (register revisit, length revisit, venue, open tell list) as agent instructions or cut them.
20. LEXICON.md: add **Claim strength** (prose ladder, four tiers named) vs **Provenance class** (Core's four); add **Tier** — model tier vs claim tier.

### Review artifact convention
21. skills/review-artifact.md, Filenames: add "Where the basename is not unique in the corpus, the stem is `<parent-dir>-<basename>`" and "On rename or split the cycle number restarts at 1; `Prior cycle` names the predecessor stem." Cut the Gap-labels paragraph restating LEXICON.

### Duplicate consolidation
22. For each of DR-1..DR-14, DR-19, DR-20 in reviews/corpus-regate-cycle-1.md sweep (a): keep the rule at the proposed home; delete every other restatement. Where remaining context needs a pointer, write "per <document title>", never a path. DR-10: project-setup-requirements.md requirement 2 becomes "the in-scope set the document metadata policy defines." DR-15..18, DR-21, DR-22 are covered above or exempt.

### Checks
23. Sweep the repository (excluding docs/history/, docs/batons/, docs/cycles/, reviews/, retros/) for: `skeptic` as audience value or engagement-role reference; "engagement retro"; path-shaped references this directive introduced; `inferred` in prose-criteria.md. Report each remaining occurrence with path and line.
24. Run `bin/check-frontmatter --all` and `bin/tests/run`. Report output; two pre-existing test failures are known.
25. Commit on branch p1-regate-fix, push, open a pull request against main titled "Pass 1 re-gate: fixes" via the REST API with curl. Do not merge. No status flip. Report the SHA read back from git.

## Report shape

PR number and SHA. Files changed, one line each with instruction numbers applied. Instruction 23 and 24 output. Anything that could not be executed as written, and every place an artifact's Fix conflicted with this directive and how it was resolved.
