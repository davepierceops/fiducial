# Directive: bin/land TRD — cycle 1

Route: fresh execution session. Model tier: frontier.
Base: main @ e02da93af0c7ba127ef99fab373b08beb42f84f0.

1. First act — write this directive file verbatim to
   docs/cycles/bin-land-trd-1-20260823T221914Z.md in the worktree stated below,
   commit it on branch bin-land-trd-1, push, and report the SHA read back from
   git.

Worktree — exclusive assignment. This session works only in
$TMPDIR/fiducial-bin-land-trd-1 (fallback /tmp/claude-501/fiducial-bin-land-trd-1),
created:

    git fetch origin
    git rev-parse origin/main
    # must print e02da93af0c7ba127ef99fab373b08beb42f84f0 — if not, STOP and surface
    git worktree add "$TMPDIR/fiducial-bin-land-trd-1" -b bin-land-trd-1 e02da93af0c7ba127ef99fab373b08beb42f84f0

Scratch substrates for gathering evidence are permitted outside that worktree —
a throwaway repository built by hand, as the cycle-1 reviewer used. Repository
work happens only in the assigned worktree.

Sandbox constraints — read both companions, follow as told:
- docs/cycles/pass2-held-fix-20260823T180753Z.md @ b9444973de086eef7cd30e7877fb86f6a1271d8e
- docs/cycles/bin-land-flip-20260823T210300Z.md @ 8c0aeee2470297ae5f2879a72dc9be4da0ea093b
  (fetch then verify origin/main before any worktree add; never `git push -u` —
  push plain, verify by ls-remote; worktree paths under $TMPDIR)

Companions — read all before editing:
- reviews/bin-land-trd-cycle-1.md @ 90c31bcc7179c88715d19dbefe4c9404f1dd064c
  — the findings this cycle disposes of. Every Fix cited below is a pointer to
  a field in this artifact; read the field, do not expect it restated here.
- specs/bin-land.md @ 3a8e55e350cad0e4532ff0e2ea03bd9299572114 — the PRD, agreed
- specs/trd-template.md @ ba513f824993ab5cd1d0e9e7ce8134a93bb491c5
- reviews/bin-land-cycle-4.md @ d857d83e93a9b3ce8d0df0c922708002f968b760 — O1, O2, O3
- reviews/bin-land-cycle-5.md @ ee0445ee591a63cb6c3acc12ec3a36c6f03c723e — O1
- docs/cycles/bin-land-trd-20260823T220000Z.md @ cca53d23cee323697ec45655f87431a873fc6a38
  — the TRD's authorship directive and provenance origin

Reviewed state. specs/bin-land-trd.md landed at content commit
47758d14f499ddd04fe5b705b93f680d6d76580e and was reviewed by the Spec Reviewer
at main b82dc36407d703df50347fa8ad7f8ef0f80453c1; the review artifact is
reviews/bin-land-trd-cycle-1.md, landed at 90c31bcc. Before editing, verify that
47758d14 is in the worktree's history and that no commit since it has touched
specs/bin-land-trd.md. If either fails, STOP and surface.

2. Revise specs/bin-land-trd.md per the decisions below. Each is stated as
   intent with a pointer to the reviewer's Fix field; draft your own wording.
   Do not paste a reviewer Fix field into the document. The TRD proposes; it
   decides nothing Dave owns.

   D-B1 — accepted. The base-establishment mechanism stands: §3.3's
   `git checkout -B` is kept (Dave, this cycle; the alternative of reopening
   reviews/bin-land-cycle-4.md O1 was declined). Intent: the ancestry guard must
   cover the ref step 6 moves, not local HEAD alone, and must refuse in the
   same shape as FM-3 when it fails; and §3.3's "HEAD moves" bullet must stop
   stating its safety claim as established. Fix pointer: B1 Fix field.

   D-B2 — accepted. Mechanism unchanged. Intent: staging is authoritative over
   the index, so content staged before the invocation cannot enter the commit
   and AC-LAND-02 holds; §5.1's index row and §3.3's "what the working tree
   holds afterwards" paragraph are corrected to what git actually does. Fix
   pointer: B2 Fix field.

   D-B3 — accepted, TRD-side only. Intent: §4.3 states plainly that under git
   transport the per-file comparison adds nothing to the `ls-remote` check, and
   names which check would catch the class
   policies/remote-write-verification-policy.md records; OQ-6 is re-ranked to
   the question the goal depends on — whether PRD G5's stated purpose is
   achieved by AC-LAND-06 as written. Fix pointer: B3 Fix field.
   No edit to specs/bin-land.md in this cycle. Amending AC-LAND-06 is a content
   edit to an agreed spec: it flips that document to in-review and takes its own
   full reviewer cycle. The decision stays open at OQ-6 and is Dave's.

   D-B4 — accepted. Intent: §5.2's value-domain rules for `prior_head` and
   `verification` take the `files` rule's shape, so the rule list stops
   contradicting itself and §6's failure rows on every failure path. §6 is
   already correct and does not change. Fix pointer: B4 Fix field.

   D-B5 — accepted. Intent: this repository's pre-commit hook is declared as a
   fifth boundary in §3.6 and §4.2, carrying its production surface, its
   representation, an evidence class of assumed, and a deferred-verification
   path. Declaring it is the point; leaving it assumed is a permitted outcome
   of the declaration. Fix pointer: B5 Fix field.

   D-B6 — accepted. Intent: §3 gains an interfaces subsection naming, for each
   new module, what it exposes and what it returns — enough that land.py and
   report.py, written by separate agents from this text, would meet. This is
   the one gate item the document does not answer at all. Fix pointer: B6 Fix
   field.

   D-N1 — accepted, collapse route (Dave, this cycle). Intent: §3.2 step 3
   becomes a single remote read answering both branch presence and the
   branch-absent base, so §8's "one `ls-remote` before the write" is true as
   written. Fix pointer: N1 Fix field, second route.

   D-N2 — accepted, narrow route (Dave, this cycle). Intent: AC-LAND-T01's own
   text exempts FM-10, which emits nothing; §3.2 step 11's rule is narrowed to
   the terminal paths the sequence reaches; and the usage-error path is stated
   explicitly — argparse's diagnostic on stderr, exit 2, nothing on stdout.
   §3.1's parsing responsibility is unchanged and no FM-0 is added. Fix
   pointer: N2 Fix field, second route.

   D-N3 — accepted. Intent: §5.3 enumerates the permitted `detail.stage`
   values, one per step of §3.2 the sequence can stop at, including the steps
   that currently have no token; the enumeration is added to AC-LAND-T01 so the
   set is tested rather than documented. Fix pointer: N3 Fix field.

   D-N4 — accepted, lighter route (Dave, this cycle). Intent: §5.2 states that
   an empty `files` list where no commit was made is not a claim about the
   commit's contents, and that `detail.stage` is what distinguishes it from
   FM-5. No union type is introduced. D-N3 is what makes that recovery path
   firm. Fix pointer: N4 Fix field, second option.

   D-O1 — accepted as a departure. No document change. Dave accepts §7's
   declining to name a flag backend where specs/trd-template.md §7 requires one
   for a subject with no runtime surface, on the reasoning §7 already carries.
   Recorded here because a decision recorded nowhere is a decision lost. Do not
   edit specs/bin-land-trd.md for this finding, and do not edit the template:
   an N/A escape in specs/trd-template.md is outside this blast radius and
   takes its own cycle.

   D-O2 — accepted. Intent: the three unlabelled assertions about git's own
   behaviour — §3.3's final paragraph, §5.1's index row, and §3.2 step 4's
   premise — carry a provenance class, on the model of §4.1's `file://` bullet.
   Where the honest label is *inferred*, that is the signal to run it. The
   §5.1 claim is the one D-B2 corrects. Fix pointer: O2 Fix field.

   D-O3 — accepted, retire (Dave, this cycle). Intent: OQ-8 is retired rather
   than answered. The TRD decides that no minimum `git` version is asserted and
   puts the reason on the record; the identifier is retired rather than reused,
   on the precedent PRD §8 sets for its own Q1–Q4, so a reader of the cycle-1
   review artifact can still find what OQ-8 referred to. §9's stated count and
   every cross-reference to OQ-8 are updated with it. Fix pointer: O3 Fix
   field.

3. Consistency sweep before committing. Per docs/global-context/core.md rule
   13, a changed fact changes everywhere it appears: sweep specs/bin-land-trd.md
   for any count, value, or cross-reference these edits have made stale — §9's
   open-question count and any reference to OQ-8, the network-operation counts
   in §8 against the revised §3.2, §5.1's state table against the revised
   staging behaviour, and §6's failure rows against the revised §5.2 rules.
   Report what the sweep found, including "nothing" if that is the result.

4. Frontmatter is unchanged: the document stays `status: draft`,
   `last-reviewed: null`. This is a revision of a draft, not a status
   transition. Run `bin/check-frontmatter --all` in the worktree; it must exit 0
   before the TRD is pushed.

5. Land the revision as a second commit on bin-land-trd-1, referencing cycle 1;
   push plain; verify by ls-remote and by reading specs/bin-land-trd.md back at
   the pushed head; report the head SHA and both commit SHAs, each read back
   from git.

Do not:
- modify any file other than docs/cycles/bin-land-trd-1-20260823T221914Z.md and
  specs/bin-land-trd.md — the blast radius is those two files only. This
  explicitly excludes specs/bin-land.md and specs/trd-template.md, both agreed,
  both named above as read-only companions.
- flip the TRD's frontmatter status or set last-reviewed.
- merge, open a PR, force-push, or delete any ref — merging is handled from the
  decision session.
- restate a reviewer Fix field as document text; the Fix fields are pointers to
  intent, not wording to be pasted.

STOP and surface rather than improvise: origin/main not at
e02da93af0c7ba127ef99fab373b08beb42f84f0 at fetch; 47758d14 absent from history,
or any commit since it having touched specs/bin-land-trd.md; any instruction
unexecutable as written; concurrent tree mutation (a file this session did not
change moving, HEAD moving, an index lock).

Report: what was done, not what this file says; all SHAs read back from git; the
check-frontmatter result; the consistency-sweep result; the §9 open-question
list as it stands after D-O3; every claim labelled observed / inferred / told /
unknown.
