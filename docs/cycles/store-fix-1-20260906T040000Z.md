# Store fix pass 1: rulings from the first read of the full store

ROUTE AND MODEL

Route: fresh
Model: frontier

FIRST ACT

Create the worktree named in the disposition below first, then write. Write
this directive verbatim to docs/cycles/store-fix-1-20260906T040000Z.md in
that worktree, commit it alone with a message naming the package it opens
(`store: fix pass 1 — directive`), push the branch store-fix-1 to origin with
a plain `git push origin store-fix-1`, never with `-u`, and report the SHA.
Do this before reading anything else and before touching any other file.
Base verification below runs before this act.

FENCE NOTE: the author-written regions (TASK, VERIFICATION) fence with tildes
so they survive transport inside a paste block; the generator's regions keep
their backtick fences and the paste block carrying this directive is opened
with four backticks for that reason. Treat a tilde fence exactly as a backtick
fence.

DISPOSITION PROMPT

A working-tree disposition is required, and it is stated below as its own
labelled statement. The governed rule it answers to:

```text
**Every directive states its working-tree disposition** — either an exclusive
assignment (a named directory plus the command creating it) or an explicit
sole-tree declaration. A prohibition is not a disposition. The disposition is
stated as its own labelled statement, exactly one per directive, mechanically
distinguishable from incidental mention of trees or commands elsewhere in the
file; the label's fixed form, the canonical sole-tree sentence, and a worked
example of each form are stated in the Directive Invariants document, which is
their one definition. Two sessions sharing a tree mutate each other's
preconditions; prefer not splitting work across trees.
```

Both admitted forms, worked:

```text
WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "wt/<name>", created by: git worktree add --no-track "wt/<name>" -b
<name> origin/main

WORKING-TREE DISPOSITION: This session works in the sole tree at the clone root.
```

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a
worktree at "$TMPDIR/fiducial-store-fix-1", created by:
git worktree add --no-track "$TMPDIR/fiducial-store-fix-1" -b store-fix-1 origin/store-all

BASE VERIFICATION

Before anything else, fetch origin/store-all and origin/main and confirm the
base — the branch store-all, not main — is at the reviewed ref
23c8196fc6f1097eec6ad945b853b105b1f7d01d, and that origin/main is at
a184967413681950a2e8ffa2ab3e16be552f7158, the branch's merge base. Judge every remote operation — fetch, push, ls-remote — by
the refs it reports, not by a credential helper's noise on stderr. If the base
has moved, stop and report; do not rebase, and do not proceed against a
different base.

COMPANIONS

Read these whole, from the working tree, at the revision each names, before
writing anything:

- docs/cycles/store-all-topics-20260906T020000Z.md @ a964324, read from the
  base ref 23c8196fc6f1097eec6ad945b853b105b1f7d01d — the rulebook
  this pass amends; its GENERAL RULINGS, DIVERGENT CLUSTERS, TOPIC MAP and
  RULINGS BY ROW stay in force except where this directive says otherwise
- docs/rule-register/store-summary-20260906T020000Z.md @ 23c8196fc6f1097eec6ad945b853b105b1f7d01d
  and the 37 per-topic store maps beside it (each named
  store-map-<topic>-20260906T020000Z.md, read from the base ref) — the
  store's own record of what was written from what
- every file under rules/, read from the base ref
  23c8196fc6f1097eec6ad945b853b105b1f7d01d — 973 files
- docs/rule-register/rule-register-20260904T210000Z.md @ 45b02c7b88a0e08f4872713aa39f840a59117423
  — for the rows this directive names by id that are not in rules/ (the
  ones being restored)
- process/change-flow.md @ 81310de105b0f0b4133117fa4c209baea9d84b09
- bin/directive and bin/check-directive at main
  (a184967413681950a2e8ffa2ab3e16be552f7158) — the tools whose output and
  checks the directive-invariants test reads; run `bin/directive --help` and
  read check-directive's M1–M8 from the source
- decisions/log.md @ d81c41a6ab60288764d8a3898cd66b354b3c664b — DEC-000380,
  DEC-000400

TASK

Apply Dave's rulings from his first read of the full store, on a branch cut
from store-all. Rows are edited, moved, or removed in place; ids never
change; every change is recorded. One commit per numbered item below, in
order; push after each. The rulings are exact where they quote; where they
state a test, apply it across every topic named, and record each row it
touches in the fix log.

THE FIX LOG. docs/rule-register/store-fix-1-20260906T040000Z.md — one line
per row touched: id, topic before, topic after (or `retired`), keys changed,
the item number here that caused it. Committed with the last item. Every
per-topic store-map whose rows change gets its lines updated in the same
commit as the change, and the store-summary is regenerated in the last
commit.

1. RESTORE R0256 and R0257 to topic decision-layer, role the four decision
   roles, session decision: "A directive is self-contained: the executor
   needs it and the repository, and nothing from the conversation that
   produced it." / "Write a directive so the report it returns is triageable
   by the next decision session on its own." The store-all directive retired
   them on a claim that directive-invariants stated them; it does not. Order
   them where decision-layer's directive rules sat.

2. RESTORE R1448–R1451 into topic review-artifact-schema, in open/closed
   vocabulary: a named defect class takes one ruling and its instances
   dispose under it; a finding below the reviewed document's stage routes to
   the loose-end tracker rather than blocking the close. Session decision;
   role: every role that reads review artifacts (the four decision roles).

3. RETIRE R0440 (describes the team; the obligation is core's R0166) and
   R0442 (the double-negative form of R0482).

4. DESCRIBES-NOT-OBLIGES TEST, every topic: a row that describes how the
   project runs, states a thesis, or characterizes a role rather than
   stating an obligation an agent can act on or a term a bundle needs
   defined, is retired, with `describes` as its disposition in the fix log
   so the operating-model process document can be written from that list
   later. A define row whose term a surviving row uses stays.

5. NEGATION TEST, widened: a forbid whose only content is the negation of a
   require anywhere in the same topic (not only adjacent) is retired.

6. DISSOLVE topic operating-model, per Dave:
   a. To topic core, role all, both sessions: R0455 (state assumptions),
      R0456 (label every verification claim with its evidence class), R0457
      (update the docs when behavior changes), R0462 (never approve or gate
      what you produced), R0460 (never weaken verification to satisfy an
      implementation), R0461 (never claim live behavior from mocked
      evidence), and one escalation row merging R0458 and R0530: escalate
      when requirements are ambiguous, risk tolerance is unclear, evidence
      is insufficient, a product tradeoff exists, security, privacy, or
      operational concern arises, reviewers disagree materially, or the
      session's role is unspecified; when in doubt escalate; never escalate
      a routine implementation decision existing guidance resolves.
      Continue core's order numbering after its last row.
   b. To topic change-flow, corpus software, each row keyed to the role that
      performs the stage: R0470–R0492 and R0466, R0467, R0493 — the Test
      Designer confirms red (R0478, R0484), the Coder turns green (R0485,
      R0486 keyed coder and test-designer), the Reviewer runs the quality
      pass (R0487), the Skeptic the skeptic pass (R0488), the Release Manager
      the package (R0489), the Spec Reviewer the spec reads (R0472, R0480),
      the Chief of Staff the flow's ordering rows (R0466, R0467, R0470,
      R0471, R0474, R0477, R0479, R0481, R0482, R0483, R0490, R0492, R0493)
      and R0473 (Context Quality Reviewer gates governed text). R0464 (the
      seven primary controls) stays a define row in change-flow, role all
      decision roles. Where a row plainly binds a role not named here, key
      it to that role and say so.
   c. To topic trd, corpus software, role architect: R0497–R0501b (deploy vs
      release, flags).
   d. To topic release-manager, corpus software, role release-manager: R0502–
      R0506 (the change package and the response shape; R0506 also keyed to
      reviewer and skeptic), and R0509–R0520 merged into ONE row, the
      definition of done as a list: implemented; mechanical checks pass;
      relevant verification run; evidence summarized; boundaries documented;
      known gaps explicit; quality and skeptic passes done; release
      readiness clear; the release gate cleared; Dave has enough to assess
      risk. R0527 (escalate for human code inspection) to release-manager,
      role release-manager and chief-of-staff.
   e. Retired as description under item 4: R0444, R0445, R0446, R0448,
      R0450, R0451, R0452, R0453, R0459, R0465, R0531, and any other
      operating-model row not named in a–d, each with `describes` in the
      log. R0443 retires as a restatement of R0478. R0498 stays (trd). R0459's
      "never treat passing tests as shippability" survives inside the
      definition-of-done row.
   f. The operating-model store-map is rewritten to show the new home of
      every id; the topic ceases to exist.

7. TOOL-OWNED-RULE TEST, topics directive-invariants and command-blocks: a
   row whose obligation bin/directive emits in its template regions or
   bin/check-directive enforces (M1–M8) is retired, disposition `tool-owned`
   in the fix log, naming the region or check. Read the tool's actual output
   (run `bin/directive` in general mode against a scratch descriptor,
   `--write` to the worktree, then delete the file before committing) and
   the linter's source; do not judge from memory. What survives is the
   authoring craft the tools cannot do: TASK content, companion citation
   form, verification design, fence rules, report design.

8. REGENERATE docs/rule-register/store-summary-20260906T040000Z.md (new
   timestamp; the old one stays as history), one line per topic with the
   same columns, totals, and a line stating rows retired by this pass by
   disposition: describes, negation, tool-owned, ruled.

Commit messages: `store: fix 1.<n> — <one clause>`. Push after every
commit. Do not open a pull request. Remove the worktree at the end
(`git worktree remove "$TMPDIR/fiducial-store-fix-1"`) and report that it is
gone.

rules/ and docs/rule-register/ are outside the frontmatter hook's in-scope
set; the hook should not fire. If it does, stop and report; do not bypass it.

The report also carries: "Decisions taken", grouped by item; the count of
rows each test retired, by topic; and "Rows worth reading first", five.

SANDBOX

Commands run inside the sandbox. `gh` cannot reach the GitHub API from here,
so a directive that wants a pull request gets a pushed branch and a report line
saying so, and the decision session opens it. No credential ever enters a file
or stdout.

VERIFICATION

Run the verification this directive names, from the working tree it assigns
you, with the output captured to a file. State each result and the log's path.
A step you did not run is reported as not run, never as passed.

From the worktree, after the last commit and before removing the worktree:

~~~sh
{
  ls rules/*.md | wc -l
  grep -l '^topic: \[operating-model\]' rules/*.md | wc -l
  ls rules/R0256.md rules/R0257.md rules/R1448.md rules/R0440.md rules/R0442.md 2>&1 | grep -c 'No such'
  grep -h '^topic: ' rules/*.md | sort | uniq -c | sort -rn | head -40
  grep -c '^| R' docs/rule-register/store-fix-1-20260906T040000Z.md
  git -C "$TMPDIR/fiducial-store-fix-1" log --oneline origin/store-all..HEAD | wc -l
  git -C "$TMPDIR/fiducial-store-fix-1" diff --stat origin/store-all..HEAD | tail -1
} 2>&1 | tee "$TMPDIR/store-fix-1-verify.log"
~~~

Expected: a row count below 973 by the number retired; 0 operating-model
rows; exactly 2 "No such" (R0440 and R0442 gone, the three restored present);
the topic table with change-flow present and operating-model absent; the fix
log's line count equal to rows touched; 9 commits (directive plus items
1–8); the diff-stat showing the row files changed, added, and deleted.

STOP CONDITIONS

Pinned to the reviewed ref 23c8196fc6f1097eec6ad945b853b105b1f7d01d. Cannot execute as written: stop
and report. Concurrent tree mutation: stop and report. On any failed command,
any precondition not met, or any tree mutation you did not intend, including
your own — stop and report; do not retry with different flags, and do not
delete or create any ref to recover. A remote operation that exits successfully
is not a failed command, whatever a credential helper writes to stderr.

REPORT

- the directive file's commit SHA
- every commit SHA this session landed, in order, and the branch they are on
- what was verified, how, and where the run log is
- every count reported, with the tree it was observed in — the clone's main
  tree, or the worktree the directive assigns; a sandboxed run says so
- anything observed this directive did not anticipate
- the worktree-removal status — or, under the sole-tree form, that no worktree
  existed

CLAIM LABELS

Label every claim observed, inferred, told, or unknown.

SOURCE MANIFEST

One entry per emitted region, in emission order: the marker that begins the
region, and either the committed path it was read from at the revision named
or an author-region marking.

    Store fix pass 1: rulings from the first read of the full store — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    ROUTE AND MODEL — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    FIRST ACT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    DISPOSITION PROMPT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    WORKING-TREE DISPOSITION — author region
    BASE VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    COMPANIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    TASK — author region
    SANDBOX — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    VERIFICATION — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    STOP CONDITIONS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    REPORT — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    CLAIM LABELS — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
    SOURCE MANIFEST — skills/directive-invariants.md @ 4c9cd22b01d3387bbc4d62e20a7d26bc5e0ab035
