You are the Test Designer, an execution session on davepierceops/fiducial, clone at ~/code/fiducial. Apply two ruled fixes to bin/tests/test_check_directive.py. You do not touch implementation code, the TRD, the PRD, or any other test.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-tooling-tests-fix-2-20260829T2130Z.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: directive-tooling test fix 2 (roundtrip slots; narrow git-read defeat)", push with git push origin directive-tooling-tests-fix-2 (no -u: the sandbox cannot write .git/config), and report the SHA. Never bypass the pre-commit hook.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-dt-tests-fix-2", created by: git worktree add --no-track "$TMPDIR/fiducial-dt-tests-fix-2" -b directive-tooling-tests-fix-2 origin/directive-tooling-impl-2
Before creating it, run git worktree list; if any existing worktree holds branch directive-tooling-tests-fix-2, or if "$TMPDIR/fiducial-dt-tests-fix-2" already exists, stop and report. Do not touch the main tree except for the final worktree removal.

BASE VERIFICATION before anything else: git fetch origin (an osxkeychain "failed to store" message is noise; judge the fetch by the refs). origin/directive-tooling-impl-2 must be at e7996fe70a5ea57404c7958e36a9063abdacbfa5. If it has moved, stop and report. This branch is deliberately not main: it carries the lint the fixed tests run against.

READ before writing, whole, from the worktree: bin/tests/test_check_directive.py; bin/tests/helpers.py (directive-tooling section: citation_fixtures, directive_fixture, the placeholder strings the generator emits); bin/aimeta/elements.py (M2's resolution steps and its unknown-versus-fail classification — read, do not edit); specs/directive-tooling-trd.md §3.4 (fixtures iv and v), §3.6 (M2 and step 5's definition of unknown), §7.

DECISION RECORD. Two findings from the implementation package 2 report (docs/cycles/directive-tooling-impl-2-20260829T2030Z.md @ 3209f18acc146be278210c32f6ac2182af5573c9, PR #234). Dave's rulings, 2026-08-29:
1. test_ac_dt_06_iv_a_generated_skeleton_with_the_slot_filled_passes asserts whole-lint exit 0 on a generated skeleton with only the disposition slot filled; general mode has no reviewed-ref flag, so M1 fails on the author placeholder. Ruling (a): the test also fills the reviewed-ref slot and keeps asserting whole-lint exit 0. Option (b), asserting M3 only, was rejected: this test is the suite's only end-to-end meeting of generator output and lint reading, and it already caught one defect this package.
2. test_ac_dt_10_a_failed_git_read_is_reported_unknown_and_exits_non_zero makes .git/objects unreadable; on git 2.55 that defeats repository discovery itself, so the lint refuses at exit 2 before any element runs. Ruling (a): the fixture defeats the read narrowly — the cited object only — so discovery succeeds and the failure lands inside M2 as unknown. Option (b), accepting either outcome, was rejected.

SCOPE — two tests, one commit.
1. test_ac_dt_06_iv: after filling the disposition slot, also replace the reviewed-ref placeholder the generator emits (read its exact text from bin/aimeta/directive.py; the skeleton carries it in BASE VERIFICATION and STOP CONDITIONS — replace every occurrence) with self.reviewed_ref. Keep assert_pass with elements=("M3",) or widen it to the full ELEMENTS — your call; state which and why in the docstring. Leave test_ac_dt_06_v untouched.
2. test_ac_dt_10: replace the chmod on .git/objects with one that makes only the cited companion commit's object unreadable — the loose object file .git/objects/<first two hex>/<remaining hex> for self.companion_sha (the test repository is fresh, so the object is loose; assert the file exists before chmod, and restore its mode in a cleanup so the temp tree can be removed). The assertions stay: no traceback, non-zero exit, "unknown" in the output, the element named. If the lint instead classifies this defeat as a fail — [citation-unresolvable] rather than element-unknown — that is an implementation finding about M2's unknown classification, not a test problem: stop, report the exact stderr git produced and the code the lint emitted, and do not widen the fixture to make it pass.
Each test's docstring gains one sentence citing this ruling. No other test, helper, fixture, or comment changes. Commit message: "directive-tooling: AC-DT-06(iv) fills both author slots; AC-DT-10 defeats the cited object only (ruling 2026-08-29)". Push with git push origin directive-tooling-tests-fix-2.

VERIFICATION, from the worktree, output to "$TMPDIR/fiducial-dt-tests-fix-2-run.log": bin/tests/run for the whole suite.
Expected state, and a stop if it differs: test_check_directive.py 84/84 green; test_directive.py red on exactly six cases (five [cycle-mode-unavailable] refusals and test_ac_dt_14_bin_holds_exactly_one_directive_skeleton_generator); test_directive_trd.py red on exactly test_route_and_model_are_committed_in_both_modes_and_the_author_count_stays_two, with 6 skips; three pre-existing reds outside directive tooling — test_scope sc1 and sc3, test_check_frontmatter cf13, all on a prose-criteria.md glob — which belong to another workstream and are not yours; everything else green. Any red outside that set is a stop: report it, do not widen the edit.

GH: never invoke gh. Push the branch; the decision session opens the pull request.

CLEANUP — after the report is composed and both pushes are verified landed (git ls-remote origin directive-tooling-tests-fix-2 shows your fix commit SHA): from the main tree, run git worktree remove "$TMPDIR/fiducial-dt-tests-fix-2" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref e7996fe70a5ea57404c7958e36a9063abdacbfa5: on any failed command, any precondition not met, either test that cannot be made green by the ruled edit alone, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; fix commit SHA; branch name; run-log path; the diff of the two tests, in full; pass/fail/skip counts for the whole suite and for test_check_directive.py; the named red cases across the suite with the one-line reason each; anything observed this directive did not anticipate; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
