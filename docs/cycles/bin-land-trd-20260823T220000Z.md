# Directive: bin/land TRD authorship

Role: Architect Agent. Base: main @ 930c49580f24fbd725f5440c7315c27a7d91063f.

1. First act — write this directive file verbatim to
   docs/cycles/bin-land-trd-20260823T220000Z.md in the worktree stated below,
   commit it on branch bin-land-trd, push, and report the SHA read back from git.

Worktree — exclusive assignment. This session works only in
$TMPDIR/fiducial-bin-land-trd (fallback /tmp/claude-501/fiducial-bin-land-trd),
created:

    git fetch origin
    git rev-parse origin/main
    # must print 930c49580f24fbd725f5440c7315c27a7d91063f — if not, STOP and surface
    git worktree add "$TMPDIR/fiducial-bin-land-trd" -b bin-land-trd 930c49580f24fbd725f5440c7315c27a7d91063f

Sandbox constraints — read both companions, follow as told:
- docs/cycles/pass2-held-fix-20260823T180753Z.md @ 9f5f4c9d8ce06d1c5489bf3b5a3248b5386fe650
- docs/cycles/bin-land-flip-20260823T210300Z.md @ 8c0aeee2470297ae5f2879a72dc9be4da0ea093b
  (fetch then verify origin/main before any worktree add; never `git push -u` —
  push plain, verify by ls-remote; worktree paths under $TMPDIR)

Companions — read all before drafting, each at main 930c4958 unless a SHA is given:
- specs/bin-land.md — the PRD this TRD sits beneath
- specs/trd-template.md — required sections and skeleton
- reviews/bin-land-cycle-4.md, reviews/bin-land-cycle-5.md, reviews/bin-land-cycle-7.md
  — the ridden observations enumerating TRD-stage material
- docs/research/gh-write-friction-20260823T184149Z.md @ 49bd6ff4 — substrate and
  verification technique
- docs/global-context/core.md — claim labelling

2. Author specs/bin-land-trd.md — new file, frontmatter per the template
   skeleton (status: draft, last-reviewed: null, audience: [all-roles, human]) —
   covering every template section. The TRD proposes; it decides nothing Dave
   owns. Anything that is product judgment goes to §9 as an open technical
   question naming what would resolve it.

3. Address these; each is stated as intent — draft your own wording:
   a. Name the report serialization format: it carries G6's five fields with
      *observed*/*unknown* labels and supports a mechanical-parse test, which
      the TRD names (AC-LAND-07's deferral).
   b. Reconcile failure-path output with the five-field contract — G1's refusal
      names both heads and the local head is not among the five
      (reviews/bin-land-cycle-4.md O3).
   c. State the base-establishment mechanism for the at-or-behind case — which
      posture, and what the working tree holds afterwards
      (reviews/bin-land-cycle-4.md O1 names the candidates).
   d. The branch-absent diverged case: state the tool's behaviour, or carry it
      in §9 (reviews/bin-land-cycle-5.md O1).
   e. State the test substrate as a decision — bare repository over file://, or
      a stated alternative (the PRD §6 preamble carries it as *inferred*).
   f. Template §6 failure modes cover the pre-staging refusal path, which the
      PRD J3 trigger does not enumerate (reviews/bin-land-cycle-4.md O2).
   g. Template §2: SLOs for J1–J3; where a journey has no SLO, say so
      explicitly and record it in §9.

4. Run bin/check-frontmatter --all in the worktree; it must exit 0 before the
   TRD is pushed.

5. Land the TRD as a second commit on bin-land-trd; push plain; verify by
   ls-remote and by reading the file back at the pushed head; report the head
   SHA and both commit SHAs.

Do not:
- modify specs/bin-land.md, LEXICON.md, or any other existing file — the blast
  radius is this directive file and specs/bin-land-trd.md only
- merge, open a PR, force-push, or delete any ref — merging is handled from the
  decision session

STOP and surface rather than improvise: origin/main not at
930c49580f24fbd725f5440c7315c27a7d91063f at fetch; any instruction unexecutable
as written; concurrent tree mutation (a file this session did not change
moving, HEAD moving, an index lock).

Report: what was done, not what this file says; all SHAs read back from git;
the check-frontmatter result; the §9 open-question list; every claim labelled
observed / inferred / told / unknown.
