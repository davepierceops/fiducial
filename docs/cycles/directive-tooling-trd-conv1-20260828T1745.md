You are an execution session on davepierceops/fiducial, clone at ~/code/fiducial. Carry out convergence round 1: apply the Test Designer's triaged findings to specs/directive-tooling-trd.md on branch directive-tooling-trd. The findings live in the Test Designer's report, carried here as dispositions; the tests themselves are already on the branch and are not yours to edit except where a disposition below says a test moves.

FIRST ACT — directive file. Write this entire directive verbatim to docs/cycles/directive-tooling-trd-conv1-20260828T1745.md in the worktree named below (create the worktree first, then write), commit it alone with message "Directive: convergence round 1 for directive-tooling TRD", push, and report the SHA.

WORKING-TREE DISPOSITION (exclusive assignment): this session works only in a worktree at "$TMPDIR/fiducial-dt-conv1", created by: git worktree add "$TMPDIR/fiducial-dt-conv1" directive-tooling-trd
Before creating it, run git worktree list; if any existing worktree holds branch directive-tooling-trd, stop and report. Do not touch the main tree.

BASE VERIFICATION before any edit: git fetch origin (an osxkeychain "failed to store" message is noise; judge the fetch by the refs). Both origin/directive-tooling-trd and local directive-tooling-trd must be at fb8e53674781d76bde6ee34f8711a978391506e8. If either has moved, stop and report.

READ before editing, whole, from the worktree: specs/directive-tooling-trd.md; bin/tests/test_directive.py, test_check_directive.py, test_directive_trd.py, and helpers.py (the tests are the counterparty — your edits must make the TRD say what the coherent tests already assert, except where a disposition says otherwise); specs/directive-tooling.md where a disposition cites it.

DISPOSITIONS — one entry per finding. "The tests' reading" means the reading the Test Designer's report states the tests were written to; make the TRD state it in its own words.
- F-A10 + Q5, RULED by Dave 2026-08-28, option (c): the generator emits route and model into a committed region; the lint does not check them (they stay in AC-DT-08's unchecked set). §3.3's cycle-mode table gains a ROUTE AND MODEL region sourced like general mode's; M5's preamble-marker list gains the corresponding entry; §9 OQ-Q5 closes — remove it from the open list, per the no-closed-questions rule the document carries. The general-mode region already exists; state that both modes now carry it and AC-DT-14's route-and-model clause is satisfied by the cycle emission.
- F-A1 accept, TRD half only: M2 gains an object-type step — cat-file -t must return exactly commit — before the touch test; state that a lightweight tag is thereby indistinguishable from its commit and only an annotated tag is rejectable. Do NOT edit the PRD; AC-DT-09's "annotated" wording is a recorded rider on the PRD's next opening cycle.
- F-A2 accept: state that the marker beginning the statement's own region does not count as preceding it for M5's ordering rule (the tests' reading). Do not add FIRST ACT to the preamble list — the general statement covers every mode's region layout where the list would need maintaining.
- F-A3 accept: add the §3.3 subsection fixing the invariants document's own format — heading level, section-name form, the sections §3.4's three parts occupy, the canonical sole-tree sentence's home, and the match-phrases section §3.6's compiled phrases read from. Write it to match bin/tests/helpers.py's invariants_sections() as it stands (21 sections, ## headings, the Match phrases section), so the fixture becomes an instance of the spec rather than the spec — verify the correspondence by reading the helper before writing the subsection, and report any point where you judged the helper wrong rather than the TRD; do not silently canonize a helper defect.
- F-A4 accept: state that a committed region's marker line is the first line of its committed section body, and an author region's marker is a generator literal AC-DT-02 does not reach, having no committed content (the tests' reading).
- F-A5 accept: the lint's invariants read is scoped to committed content, with a lint-side FM-G3 analogue (a new FM-L code refusing an uncommitted edit to the invariants document). State it in §3.6 step 4 and the §7 table. The Q9 single-source argument is the reason; carry it in one sentence. Note in the report: TestInvariantsDocumentDependency.test_the_lints_read_is_not_scoped_to_committed_content was written to the old text and inverts — rewrite that one test to assert the committed-content behavior, rename it accordingly, and say so.
- F-A6 accept: §4.1 states that M2's and M8's failing fixtures substitute (a non-touching SHA; a non-matching name) rather than subtract, and why omit= cannot build them.
- F-A7 accept, TRD-side resolution: name one condition that returns unknown rather than a coded finding — the mode-000 object-store read the tests use is an environment fault, so name the class "a git read that fails for a reason the lint cannot attribute to the directive" and map it to element-unknown, exit 1. AC-DT-10 stays as agreed; do not edit the PRD.
- F-A8 accept, TRD-side: state that AC-DT-04's author-text clause is satisfied by construction — the generator's inputs (§5) include no directive text, so no author text can reach an exit path — and that the clause's enforceable content lives in the lint's G4 posture. Do not edit the PRD; a clarifying restatement is a recorded rider on the PRD's next opening cycle.
- F-A9 accept: §4.1 states that the CLI_NAMES and make_home changes land with the implementation, not with the tests, and that make_home_repo is the additive test-side form.
- F-A11 accept: the cycle-mode table row 1 names both marker forms (--cycle N's "Cycle <n> Directive — <title>" and --name SLUG's "# <Title> Directive") and states how AC-DT-05's partition and AC-DT-18's counts read under each.

The four removal categories still bind: no provenance tags, no changelog prose, no closed questions (OQ-Q5 is removed, not marked closed), no per-citation SHAs. Frontmatter stays exactly: status: draft, last-reviewed: null, audience: [human].

SCOPE: edit specs/directive-tooling-trd.md, and within bin/tests/ only the single F-A5 test named above. No other file; no ref creation or deletion, no merge, no PR.

AFTER EDITS: run bin/tests/run — the 424 pre-existing tests must stay green, and state the new modules' counts (reds are expected; what must not appear is an error in the pre-existing set or a collection failure). Run bin/check-frontmatter --all — exit 0. Commit the TRD edits and the one test edit together with message "directive-tooling TRD: convergence round 1 — Q5 ruled (c), F-A1..A11 dispositions". Push.

CLEANUP — after the report is composed and all pushes are verified landed: from the main tree, run git worktree remove "$TMPDIR/fiducial-dt-conv1" (no --force). If it fails, report the failure; do not retry. Your report's final line states whether the worktree was removed.

STOP CONDITIONS, pinned to reviewed ref fb8e53674781d76bde6ee34f8711a978391506e8: on any failed command, any precondition not met, or any tree mutation you did not intend, including your own — stop and report; do not retry with different flags, do not delete or create any ref to recover.

REPORT: directive-file commit SHA; amendment commit SHA; per-finding list of edits with section locations; the F-A3 helper-vs-TRD judgment calls if any; the F-A5 test rewrite confirmed; test-suite state (pre-existing count green, new-module counts); check-frontmatter exit code; anything observed that this directive did not anticipate; worktree-removal status as the final line. Label every claim observed, inferred, told, or unknown.
