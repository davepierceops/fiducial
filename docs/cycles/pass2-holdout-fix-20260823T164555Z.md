Write this directive verbatim to docs/cycles/pass2-holdout-fix-<timestamp>.md (timestamp ISO 8601 basic, generated now). Run `git checkout origin/main` then `git checkout -b pass2-holdout-fix`. Commit the directive file, push the branch, open and squash-merge a PR via curl against the GitHub REST API using the credential-helper token held in an environment variable — never written to a file. Never invoke gh for anything, including auth checks; its errors are not evidence. Report the merged SHA. Then execute the rest on a fresh branch from origin/main, pass2-holdout-fix-2, merged the same way; report that SHA too.

Read before editing: docs/global-context/core.md; reviews/review-rubric-cycle-2.md, reviews/decision-layer-cycle-11.md, reviews/document-metadata-policy-cycle-17.md, reviews/context-quality-reviewer-cycle-4.md, each whole; the four files they review, each whole, at main.

Decisions, dictated here (this directive is the origin), each an instruction, individually refusable:

1. Baton (DL-1): docs/global-context/decision-layer.md rule 13 stands unchanged. docs/global-context/core.md's Vocabulary entry for Baton: replace "the artifact a decision session hands its successor decision session" with "the package a decision session hands its successor decision session"; no other change to the entry. Delete docs/batons/ and every file in it.
2. Scope (CQR-5/DMP-9): roles/context-quality-reviewer.md §Scope: replace the enumerated in-scope list with a description deferring to the policy's enforced set — every file frontmatter enforcement reaches — retaining only exclusions that subtract files from that set; vendors/** is in scope; drop exclusions that subtract nothing. The policy's §Scope is the single enumeration.
3. Rubric criterion 5: delete the clause routing findings to "the instruction-writing criteria"; the criterion's test stands.
4. Rubric criterion 3: append one sentence — a file whose subject is paths (a scope definition, a glob configuration) states them without defect.
5. Rubric: add criterion 12 — the file contradicts no other governed file; the reviewer names what was cross-checked. Match the rubric's register.
6. policies/document-metadata-policy.md: cut body text that argues for rules rather than stating them (cycle 13 D2, confirmed at cycle 17). Delete, do not relocate. Rules, scope entries, field definitions, and sequences stay byte-identical; only rationale prose goes. List every deleted passage in the report by section and first words.
7. Execute every finding marked blocking in the four review artifacts, and every non-blocking finding whose fix the artifact states unambiguously. Where a fix would contradict any decision above, the decision wins — stop and surface rather than reconcile silently. List any non-blocking finding left unexecuted, with why.

These files are draft or in-review; flip no status. A changed fact changes everywhere: after edits, grep the corpus for references to docs/batons/, the seven dropped exclusions, and the deleted rubric clause; update live governing text only — records (cycles, reviews, retros, history) stay untouched. Run bin/check-frontmatter --all, exit 0 required; bin/tests/run, only the two accepted AC-BN-10 failures permitted. Label every claim observed, inferred, told, or unknown. Cannot execute as written → stop and surface. Concurrent tree mutation → stop and surface. Report: both SHAs, per-instruction outcome one line each, deleted-passage list, findings executed and skipped, grep-touched files, check and test results, and anything needing Dave's judgment, one line each, up front.
