# Directive: bin/land TRD — cycle 9 revision

Role: Architect Agent. Route: fresh execution session. Model tier: frontier.
Base: main @ 5efc28a253e8546378ac0f698f7f0c33b823b26f.

1. First act — write this directive file verbatim to
   docs/cycles/bin-land-trd-9-20260824T023000Z.md in the worktree stated below,
   commit it on branch bin-land-trd-9, push plain, verify by ls-remote, and
   report the SHA read back from git.

Worktree — exclusive assignment for repository work. This session's repository
work happens only in $TMPDIR/fiducial-bin-land-trd-9 (fallback
/tmp/claude-501/fiducial-bin-land-trd-9); throwaway scratch repositories outside
it are permitted for evidence-gathering probes. Created:

    git fetch origin
    git rev-parse origin/main
    # must print 5efc28a253e8546378ac0f698f7f0c33b823b26f — if not, STOP and surface
    git worktree add "$TMPDIR/fiducial-bin-land-trd-9" -b bin-land-trd-9 5efc28a253e8546378ac0f698f7f0c33b823b26f

Sandbox constraints — read both companions at main 5efc28a2, follow as told:
- docs/cycles/pass2-held-fix-20260823T180753Z.md
- docs/cycles/bin-land-flip-20260823T210300Z.md
  (fetch then verify origin/main before any worktree add; never `git push -u`;
  push plain, verify by ls-remote; worktree paths under $TMPDIR)

Companions — read all before editing, at main 5efc28a2:
- reviews/bin-land-trd-cycle-8.md — the findings this cycle disposes of. Each
  Fix cited below is a pointer to a field in this artifact; read the field, do
  not expect it restated here. It carries an N2 and two observations this
  directive does not name — read and dispose of all three; report what you did
  with each.
- docs/cycles/bin-land-trd-7-20260824T015000Z.md @ a6c0f323 — the cycle-7
  decision record.
- specs/bin-land.md — the PRD, agreed, read-only this cycle
- specs/trd-template.md — read-only

## Decisions

Stated inline where this directive is the origin of the decision.

### D9-B1 — accept, in two parts. Dave chose the structural half over the reviewer's recommended tie-break.

**Part one, the false sentence.** §5.3's gloss paragraph says detail.prior_branch
appears on a landing that succeeded. The table row establishes it on the success
path only where step 6 found HEAD on a branch other than <branch>. Under the
ceiling the gloss is false, and false on the J2 success path. Correct it.

**Part two, the structural fix.** §5.3's table is the sole normative voice within
§5.3. State that the prose surrounding the table explains it and asserts nothing
on its own — no prose passage in §5.3 states or implies which paths carry which
key. This is the same collapse cycle 5 performed across sections, applied within
the one home that survived it.

The reviewer recommended instead extending §5.3's tie-break clause to reach its
own prose. Dave rejected that route: a tie-break says which voice wins a
disagreement, where making the prose non-normative means the disagreement cannot
arise. Recorded so the rejection is auditable — the reviewer's route is not
taken. Fix pointer: B1 Fix field.

**Do not silently relocate a requirement.** Audit every prose passage in §5.3
before declaring it non-normative. Where a passage carries a requirement the
table does not, that is a gap in the table, and closing it is a design decision
— STOP and surface rather than moving the requirement into the table yourself.

### D9-N1 — accept
Finding: §3.7's build rule keys emission on facts membership — the floor's
discriminant — and has no case for what the ceiling contemplates.
Resolution: rewrite the rule so emission is keyed on §5.3's table, not on
whether a fact was accumulated. Accumulation and emission are now separate
questions, and the table answers the second. Fix pointer: N1 Fix field.

### D9-N3 — accept, state the correspondence
Finding: §3.2 step 5 promises the report says which of the two checks refused,
and that promise is met only by a derivation the reader must perform unaided.
The cycle-8 reviewer verified the biconditional by probe on every FM-3 path and
ruled it sound but unstated.
Resolution: state the correspondence in §3.2 step 5 — detail.branch_head present
means the second check refused, absent means the first did. State it as the
document's own claim, not as a derivation walked through. Fix pointer: N3 Fix
field.

## Consistency sweep before committing — report each part, including "nothing"

9a — the non-normative seam. Read every prose passage in §5.3 against the table
under the new rule. Report, per passage, whether it is explanatory as written,
was corrected, or carries a requirement the table lacks. The last case is a STOP,
not an edit.

9b — the same defect one level out. §5.3 stated its normative content twice, and
that is what produced B1. Check whether any other section does the same: a table
or list carrying normative content with prose beside it that restates or glosses
it. At minimum §5.2, §5.4 and §6. Report what you find; do not fix a second
instance without surfacing it first, since the remedy there may not be the one
chosen here.

9c — the ordinary sweep. Per docs/global-context/core.md rule 13, a changed fact
changes everywhere it appears. Sweep for any count, value, or cross-reference
these edits have made stale — at minimum §5.3's tie-break clause enumeration
(§3.2, §3.7, §5.2, §6), which D9-B1 leaves as it stands and which should be
re-read for whether the non-normative rule makes any part of it redundant.

## Do not
- modify any file other than docs/cycles/bin-land-trd-9-20260824T023000Z.md and
  specs/bin-land-trd.md — the blast radius is those two files only. This
  explicitly excludes specs/bin-land.md and specs/trd-template.md, both agreed,
  both named above as read-only companions.
- amend AC-LAND-06, AC-LAND-07, AC-LAND-09, or any other PRD acceptance
  criterion or goal.
- reintroduce a second home for emission scope, or a second voice within §5.3.
- retire, restate, or soften OQ-6 or §4.3 — Dave's standing decision holds it
  open.
- flip the TRD's frontmatter status or set last-reviewed; it stays status draft,
  last-reviewed null.
- renumber any existing failure mode, open technical question, or §3.2 step.
- add a new failure mode or acceptance criterion without surfacing it first.
- merge, open a PR, force-push, or delete any ref — merging is handled from the
  decision session.
- restate a reviewer Fix field as document text; the Fix fields are pointers to
  intent, not wording to be pasted. Draft your own wording throughout.
- treat bin/tests/run's two pre-existing AC-BN-10 bundle failures as in scope.
- resolve a genuine design question by choosing; STOP and surface it.

## STOP and surface rather than improvise
- origin/main not at 5efc28a253e8546378ac0f698f7f0c33b823b26f at fetch
- any instruction unexecutable as written
- concurrent tree mutation in the assigned worktree (a file this session did not
  change moving, HEAD moving, an index lock)
- a §5.3 prose passage carrying a requirement the table does not — a table gap,
  and which way to close it is Dave's
- the non-normative rule making any §5.3 paragraph vacuous rather than
  explanatory, such that deleting it would be the honest edit

## Before pushing
Run bin/check-frontmatter --all in the worktree; it must exit 0.

Land the revision as a second commit on bin-land-trd-9, referencing cycle 9;
push plain; verify by ls-remote and by reading specs/bin-land-trd.md back at the
pushed head; report the head SHA and both commit SHAs, each read back from git.

## Report
What was done, not what this file says. All SHAs read back from git. The
check-frontmatter result. The diff stat. All three sweep results separately,
with 9a reported per passage. What you did with the cycle-8 artifact's N2 and
its two observations. Any seam where you extended beyond a decision's stated
scope, named as such. Every claim labelled observed / inferred / told / unknown.
