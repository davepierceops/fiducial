# Directive — Pass 1 re-gate fixes, part 5

Date: 2026-08-23
Route: fresh
Model: cheap
Role: Implementer (execution session). Edits are dictated below.

## Working-tree rule

This session runs in a clone no other session is using. If any file this session did not change moves, or HEAD moves, or an index lock appears, stop and report; do not recover. If `git checkout -b` fails to move HEAD, stop and report.

Starting state: main @ c116eb1 plus this directive's own merge. Read before editing: docs/global-context/core.md, reviews/sre-speed-audit-cycle-4.md, reviews/sre-baseline-measurement-cycle-4.md.

## Instructions

1. SA-6 — engagements/sre/speed-audit.md step 5: the re-measure clause gets its subject: "the Cartographer re-measures against the baseline." The Implementer and Critic clauses stand.
2. SRE-BM-6 — engagements/sre/baseline-measurement.md step 6: append "The Cartographer delivers the re-measurement to the Assistant; the Implementer cites it in the change package's Expected-delta item." The Cartographer does not read the change-package skill; no audience change.
3. Run `bin/check-frontmatter --all` and `bin/tests/run`. Expected: exit 0; exactly the two pre-existing test_bn10 failures.
4. Commit on branch p1-regate-fix-5, push, open a pull request against main titled "Pass 1 re-gate: fixes, part 5" via the REST API with curl. Do not merge. No status flip. Report the SHA read back from git.

## Report shape
PR number and SHA. Files changed. Instruction 3 output. Anything that could not be executed as written.
