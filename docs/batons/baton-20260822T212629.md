# Baton — fiducial — 2026-08-22T21:26Z

Predecessor: docs/batons/baton-20260823T003000.md (superseded by this file)
Written by: decision session, cycle 25
Main at hand-off: 0e077539ef0f7c57a631920e5c9630d2c50b1d8a

## Kickoff line for the next session

    fiducial, Pass 1 cycle 26: writing/. Baton at docs/batons/baton-20260822T212629.md.

Upload with it: the regenerated methodology bundle (`bin/bundle-methodology --out ~/Downloads`), `docs/global-context/core.md`, `docs/global-context/decision-layer.md`, `docs/global-context/review-rubric.md`, `LEXICON.md`.

## What landed this session

- Cycle 25 (PR #123, squash → 0e07753): `engagements/comfy/` moved unedited to `docs/history/engagements/comfy/` (twelve files). Seven generalized files at `engagements/sre/`: README, override-log-policy, implementer, baseline-measurement, engagement-change-package, speed-audit, system-discovery. Five originals archived with no copy — cartographer, skeptic-engagement, chief-of-staff-engagement (successors: `engagements/cartographer.md`, `skeptic.md`, `assistant.md`), client-credentials-policy (guardrail in `working-with-dave.md`), override-log.md (engagement data).
- Directive: `docs/cycles/comfy-archive-and-generalize-20260822T195900.md` @ 306a2f5 (pre-squash). Execution report is not committed; its undictated judgments are listed below.
- Slug decision: `engagements/sre/`, not `brownfield-sre`. Greenfield is a later problem.
- Session-kind conflict resolved by fiat: `engagement-change-package.md` runs in an execution session (matches implementer). Dave: "pick one, it'll probably be wrong, we'll fix it on the next engagement."

## Judgments in engagements/sre/ the directive did not dictate (executor's, accepted)

- README "parent model in one breath" and "what survives unchanged" removed whole.
- Override-log-policy: credentials exclusion dropped (restates working-with-dave.md); Skeptic verdict removed from the overridable set (summoned, not a gate).
- system-map.md prescribed filename dropped; "client-hosted repository designated at kickoff" retained as engagement-shape.
- system-discovery step 3 keeps "tag provenance" without enumerating tags.
- "Terraform or equivalent" retained as engagement-shape in implementer and system-discovery.
- `all-roles` expanded to the five engagement slugs; `client` dropped from every audience.
- Carve-out procedure (named/scoped/justified/granted/expiring/registered) from client-credentials-policy exists nowhere after archive. Treated as Comfy-specific. Revisit on the next engagement.

## Pass 1 remaining

1. `writing/` — seven governed files. Rubric review cycle, Context Quality Reviewer, all eleven criteria. Cycle 26.
2. Reconciliation re-gate of the whole corpus. Add to the known items: `engagements/sre/` has never been rubric-reviewed (produced by directive, not by a review cycle).

## Pass 2 additions from this session

- Metadata policy scope glob: `engagements/**` is out of scope — `bin/check-frontmatter --all` reaches neither `engagements/sre/` nor `docs/history/`. Fold into the existing `docs/global-context/**` scope fix. Decide whether `docs/history/**` is explicitly excluded.
- Baton delivery: canonical text defines what a baton is (Core, LEXICON) and says nothing about delivery. Decided this session: a baton is delivered like a directive — committed to `docs/batons/`, one paste block, reports SHA — with one difference: the writer is the decision session itself, via repository tooling, because no execution is involved. Each baton names its predecessor as superseded. Needs to land in Core or the Decision Layer.
- `docs/batons/` accumulates superseded files; convention (keep all vs. keep latest) undecided.
- The Comfy override-log and carve-out register are engagement data now in `docs/history/`; on the next engagement, decide where live engagement data resides (working repo, per system-discovery).

## Pass 2 carried forward (from predecessor, unchanged)

See the predecessor baton's Pass 2 list; nothing there was resolved this session. The `bundle-methodology` stamp still reads `davepierceops/ai`.
