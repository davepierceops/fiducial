# Cross-topic sweep — store fix pass 4 item 9

Derived artifact. Directive: `docs/cycles/store-fix-4-20260906T070000Z.md`, branch `store-fix-4`. The mechanical
input is `docs/rule-register/store-near-candidates-20260906T070000Z.md`, generated by
`docs/rule-register/store-near-20260906T070000Z.py` over the 513 rows the store held after item 8.

The test applied to each near-pair is the intake criterion R0264 carries: *contributes something no rule already
in its bundles does*. A pair retires one row only where both hold — the two rows state one obligation, **and**
some role's query loads both. Where the two rows sit in different bundles, no bundle sees the repetition and
both stay; where they state different obligations, both stay.

**Pairs emitted: 55. Rows retired: 16. Pairs touching a retirement: 22. Pairs kept both: 33.**

## Retired, with the survivor

| retired | survivor | why |
|---|---|---|
| R0787 (verification-boundaries) | R0456 (core) | one obligation — label every verification claim with its evidence class; core is loaded by every role, the boundaries topic by eight |
| R0788 (verification-boundaries) | R0030 (lexicon) | one obligation — label every unresolved boundary gap; R0030 names the four labels and is loaded by every role |
| R0480 (change-flow) | R1481 (convergence) | one obligation — the close's read over the branch's whole diff with the tests; R1481 also names the role that gated the spec |
| R0018a (lexicon) | R1481 (convergence) | same obligation, stated a third time in the lexicon |
| R0018b (lexicon) | R0481 (change-flow) | one obligation — one ruling by Dave over the reviewed diff, recorded naming the SHA |
| R1486a (convergence) | R0481 (change-flow) | same obligation; R0481 takes its "nothing else closes it" clause |
| R1483a (convergence) | R1126 (spec-reviewer-agent) | one obligation — the close's exactly-two checks; R1126 is the fuller close checklist and carries the same role |
| R1152 (test-designer) | R1468 (convergence) | one obligation — the Test Designer starts after the entry read and Dave's go, against the spec as it stands |
| R0015 (lexicon) | R1468 (convergence) | same obligation; R1468 takes its "whatever its verdict" clause |
| R0016 (lexicon) | R1468 (convergence) | same obligation, stated a third time in the lexicon |
| R1154 (test-designer) | R1473 (convergence) | one obligation — spec and tests cohere claim by claim |
| R1150 (test-designer) | R0484 (change-flow) | one obligation — confirm the tests fail on bad logic, not an absent import, before implementation |
| R0010 (lexicon) | R1491 (reconciliation) | one obligation — the delta lands through one pull request carrying the whole accumulated diff |
| R1556 (trd) | R0799 (verification-boundaries) | one obligation — a durable boundary is declared in the TRD's standing section; R0799 is loaded by eight roles, R1556 by one |
| R1157 (test-designer) | R1479 (convergence) | one obligation — verify each disposition against the counterparty's artifact and disclose deviation |
| R0808 (verification-boundaries) | R1058 (skeptic) | one obligation — challenge the confidence the evidence does not support; named by the directive |

## Kept both

The 33 pairs where neither row retires, by why they stay.

**No role's query loads both (21 pairs).** The Copy Editor, the Critic and the Writer carry parallel rows with
near-identical text, and each role loads only its own: R0954/R1002, R0954/R1162, R1002/R1162, R0957/R1006,
R0955/R1004, R0991/R1023, R0975/R1019, R1001/R1161, R0953/R1001, R0953/R1161, R0997/R1027, R0997/R1174,
R1027/R1174, R0998/R1028, R0956/R1005, R0990/R1021, R0988/R1022. Also R0505/R0555, R0505/R0551, R0551/R1039
— release-manager-agent against change-control, disjoint role lists — and R0091/R1156, the C097 mirror pair:
the spec-editing session and the Test Designer, deliberately two rows.

**Different obligations (12 pairs).** R1029/R1176 — each names its own role's tier. R0110/R0456 — the boundary
of a claim is not the class of a claim. R0819/R0985, R0818/R0971, R0846/R0977, R0849/R0970 — the Criteria state
what the prose must be; the Copy Editor's rows state the pass that finds where it is not. R0181/R0872 — a false
positive of the normalizer. R0482/R1533 — when acceptance criteria are written against where they are derived
from. R0499/R0573 — flags and canaries against where the go/no-go attaches. R0017/R0471 and R0017/R1153 —
implementing against an open spec is narrower than running no per-change stage against one. R1101/R1468 — the
Spec Reviewer's two reads against the Test Designer's scope.

The remaining 22 of the 55 emitted pairs each have one row in the retired column above; they are not listed
again here.
