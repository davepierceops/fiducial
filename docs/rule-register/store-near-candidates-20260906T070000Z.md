# Store near-pairs — store fix pass 4 item 9

Derived artifact. Input: every row under `rules/` on branch `store-fix-4`, read before item 9 applied its retirements. Generated by `docs/rule-register/store-near-20260906T070000Z.py`; Python 3 standard library only, no network, deterministic. Analysis only.

Thresholds: token-set Jaccard >= 0.35 OR difflib SequenceMatcher ratio >= 0.55, over the normalized text (body + condition, lowercased, punctuation stripped, whitespace collapsed, stopwords removed, `## Human` excluded). Stopword list length: 132. Pairs are same-verb and cross-topic only. Rows read: 513. Pairs emitted: 55.

`roles meet` is whether the two rows' `role` lists intersect — whether any role's query loads both. A `define` row carries no `role` key: it is pulled by term, so a define pair always meets.

| pair | id A | id B | topic A | topic B | verb | jaccard | ratio | roles meet | rule A | rule B |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | R0954 | R1002 | copy-editor | critic | require | 1.000 | 1.000 | no | When a document this role names is absent from your context, ask the author for it before acting on anything it governs, and act on it as read, never as remembered. | When a document this role names is absent from your context, ask the author for it before acting on anything it governs, and act on it as read, never as remembered. |
| 2 | R0954 | R1162 | copy-editor | writer | require | 1.000 | 1.000 | no | When a document this role names is absent from your context, ask the author for it before acting on anything it governs, and act on it as read, never as remembered. | When a document this role names is absent from your context, ask the author for it before acting on anything it governs, and act on it as read, never as remembered. |
| 3 | R0957 | R1006 | copy-editor | critic | require | 1.000 | 1.000 | no | When the instruction names a subset of passes, run that subset and no other. | Where the instruction names a subset of passes, run that subset and no other. |
| 4 | R1002 | R1162 | critic | writer | require | 1.000 | 1.000 | no | When a document this role names is absent from your context, ask the author for it before acting on anything it governs, and act on it as read, never as remembered. | When a document this role names is absent from your context, ask the author for it before acting on anything it governs, and act on it as read, never as remembered. |
| 5 | R0955 | R1004 | copy-editor | critic | require | 0.706 | 0.908 | no | Treat a `.docx` uploaded to a fresh session plus the word `copy` as the whole instruction: run every pass and return the file. | Treat a `.docx` uploaded to a fresh session plus the word `critic` as the whole instruction: run all three passes and return the file. |
| 6 | R0991 | R1023 | copy-editor | critic | require | 0.889 | 0.905 | no | Preserve the uploaded file's formatting, headings, links, and layout. | Preserve the uploaded file's formatting, headings, links, and layout in the file you return. |
| 7 | R0456 | R0787 | core | verification-boundaries | require | 0.400 | 0.886 | yes | Label every verification claim with its evidence class. | Label every verification claim with one of the lexicon's evidence classes. |
| 8 | R1029 | R1176 | critic | writer | require | 0.600 | 0.875 | yes | Run the Critic at the frontier tier. | Run the Writer at the frontier tier. |
| 9 | R0975 | R1019 | copy-editor | critic | require | 0.667 | 0.830 | no | Run all four report passes, and anchor each finding as a comment on the passage it is about, prefixed with the pass name, stating what is wrong and what would resolve it. | Anchor every finding as a comment on the passage it is about, prefixed with the pass name, stating what is wrong, what it rests on, and what would resolve it. |
| 10 | R0030 | R0788 | lexicon | verification-boundaries | require | 0.667 | 0.787 | yes | Mark every known, unresolved boundary gap with one of the four release-impact labels — `blocking`, `deferred`, `accepted-risk`, `not-material`. `not-material` is a label, not an exemption from labelling. | Mark every known, unresolved boundary gap with a release-impact label; `not-material` is one of the labels, not an exemption from labelling. |
| 11 | R1001 | R1161 | critic | writer | require | 0.636 | 0.745 | no | Read the Public Prose Criteria and the Voice document before you read the author's document, every time. | Read the Public Prose Criteria and the Voice document before you write anything, every time. |
| 12 | R0953 | R1001 | copy-editor | critic | require | 0.636 | 0.741 | no | Read the Public Prose Criteria and the Voice document before you read the document you are editing, every time. | Read the Public Prose Criteria and the Voice document before you read the author's document, every time. |
| 13 | R0018b | R0481 | lexicon | change-flow | require | 0.739 | 0.595 | yes | A spec is agreed by one ruling from Dave over that diff, recorded in the decision log naming the reviewed SHA and accepting the tests as the spec's red-gate evidence. | At a spec branch's close, put the reviewed diff to Dave: one ruling by Dave agrees the spec and accepts the tests as its red-gate evidence, recorded in the decision log naming the reviewed SHA. |
| 14 | R1154 | R1473 | test-designer | convergence | require | 0.478 | 0.734 | yes | Work against the spec and its acceptance criteria as they stand while its branch is open: every testable claim the spec makes gets a test asserting it, and every test asserts something the spec states. | Treat neither the spec nor its tests as final until they cohere: every testable claim the spec makes has a test asserting it, and every test asserts something the spec states. |
| 15 | R0110 | R0456 | verification | core | require | 0.333 | 0.724 | yes | State the boundary of every verification claim you make — what the evidence behind it covers, and where that coverage stops. | Label every verification claim with its evidence class. |
| 16 | R0480 | R1481 | change-flow | convergence | require | 0.647 | 0.714 | yes | At a spec branch's close, read the branch's whole diff — from the branch point, not from any status transition — together with the tests. | Close an open spec branch with one read by the role that gated the spec, over the branch's whole diff from the branch point together with the tests. |
| 17 | R0953 | R1161 | copy-editor | writer | require | 0.636 | 0.710 | no | Read the Public Prose Criteria and the Voice document before you read the document you are editing, every time. | Read the Public Prose Criteria and the Voice document before you write anything, every time. |
| 18 | R0018a | R0480 | lexicon | change-flow | require | 0.500 | 0.704 | yes | When a spec branch closes, the Spec Reviewer reads the branch's whole diff, from the branch point, together with the tests written against the spec. | At a spec branch's close, read the branch's whole diff — from the branch point, not from any status transition — together with the tests. |
| 19 | R0819 | R0985 | public-prose-criteria | copy-editor | require | 0.500 | 0.678 | yes | Make every line carrying irony, wordplay, or idiom read correctly for a reader who takes it literally. | In the many-languages read, read every line carrying irony, wordplay, or idiom as a reader who takes it literally would, and flag each line that reads wrongly taken literally. |
| 20 | R0110 | R0787 | verification | verification-boundaries | require | 0.200 | 0.678 | yes | State the boundary of every verification claim you make — what the evidence behind it covers, and where that coverage stops. | Label every verification claim with one of the lexicon's evidence classes. |
| 21 | R0016 | R1152 | lexicon | test-designer | require | 0.444 | 0.670 | yes | While a spec branch is open, the Test Designer writes the spec's test suite against the spec as it stands. | Begin once the entry read has run and Dave has said to proceed, then write the spec's test suite against the spec as it stands while its branch is open. |
| 22 | R0018a | R1481 | lexicon | convergence | require | 0.421 | 0.642 | yes | When a spec branch closes, the Spec Reviewer reads the branch's whole diff, from the branch point, together with the tests written against the spec. | Close an open spec branch with one read by the role that gated the spec, over the branch's whole diff from the branch point together with the tests. |
| 23 | R0484 | R1150 | change-flow | test-designer | require | 0.364 | 0.630 | yes | Select the unit's tests from the spec's suite, run them, and confirm them failing on bad logic — not merely on an absent import — before any implementation. | Run the red-gate before handing the tests to the Coder: confirm they fail on bad logic, not merely on an absent import, before anything is implemented. |
| 24 | R0997 | R1027 | copy-editor | critic | forbid | 0.250 | 0.625 | no | Never share a session with the Critic or the Writer. | Never run in a session shared with the Copy Editor or the Writer. |
| 25 | R0998 | R1028 | copy-editor | critic | require | 0.462 | 0.625 | no | Treat every tracked change and every comment as a proposal: the author accepts or rejects each, and nothing you emit changes the text until the author does. | Treat every comment you leave as a proposal the author accepts or rejects. |
| 26 | R0956 | R1005 | copy-editor | critic | forbid | 0.308 | 0.613 | no | Begin the work as soon as the instruction arrives — no greeting, no question about what to do, no offer to begin. | Answer a critic instruction with the work itself: do not greet, do not ask what to do, and do not offer to begin. |
| 27 | R0018a | R1485 | lexicon | convergence | require | 0.350 | 0.611 | yes | When a spec branch closes, the Spec Reviewer reads the branch's whole diff, from the branch point, together with the tests written against the spec. | Give Dave the close's diff — the branch's whole diff together with the tests — to read before he rules on it. |
| 28 | R0091 | R1156 | change-flow | test-designer | require | 0.500 | 0.603 | no | Under a directive to edit specs, edit specs and nothing else: a test change the spec needs is a finding handed to the decision session. | A Test Designer edits tests and nothing else; a spec change it needs is a finding to the decision session. |
| 29 | R0818 | R0971 | public-prose-criteria | copy-editor | require | 0.600 | 0.429 | yes | Quote speech rather than introducing it with a bare colon. | Quote speech introduced by a bare colon, as a correction rather than a finding. |
| 30 | R0997 | R1174 | copy-editor | writer | forbid | 0.300 | 0.600 | no | Never share a session with the Critic or the Writer. | Never share a session with the Copy Editor or the Critic, and never run their passes on your own draft. |
| 31 | R0181 | R0872 | core | trd | require | 0.000 | 0.588 | yes | Read the repository before asserting its state. | Take responsibility for every section of the TRD you draft. |
| 32 | R0478 | R1150 | change-flow | test-designer | require | 0.250 | 0.586 | yes | Translate the open spec's testable claims into test code, run it, and confirm it fails on bad logic — not merely on an absent import — while the branch is open, so the result is the close's evidence. | Run the red-gate before handing the tests to the Coder: confirm they fail on bad logic, not merely on an absent import, before anything is implemented. |
| 33 | R0018b | R1486a | lexicon | convergence | require | 0.480 | 0.581 | yes | A spec is agreed by one ruling from Dave over that diff, recorded in the decision log naming the reviewed SHA and accepting the tests as the spec's red-gate evidence. | Close an open spec branch on one ruling by Dave, recorded in decisions/log.md naming the reviewed SHA; nothing else closes it. |
| 34 | R0846 | R0977 | public-prose-criteria | copy-editor | require | 0.300 | 0.580 | yes | Prefer descriptive, searchable titles over clever ones, place key terms early, and keep the structure skimmable. | In the discoverability and cold-reader pass, check that the title is searchable, that key terms appear early, and that the structure is skimmable. |
| 35 | R0988 | R1022 | copy-editor | critic | require | 0.333 | 0.580 | no | Return the author's own `.docx`, with every correction in it as a tracked change attributed to `Copy Editor`. | Return the author's own `.docx` with your findings as comments and nothing else changed. |
| 36 | R0849 | R0970 | public-prose-criteria | copy-editor | require | 0.333 | 0.579 | yes | Write venue-independent prose, with no platform-specific formatting dependencies. | Correct platform-specific formatting. |
| 37 | R1027 | R1174 | critic | writer | forbid | 0.364 | 0.578 | no | Never run in a session shared with the Copy Editor or the Writer. | Never share a session with the Copy Editor or the Critic, and never run their passes on your own draft. |
| 38 | R1157 | R1479 | test-designer | convergence | require | 0.412 | 0.574 | yes | Verify each disposition you execute against the spec as it stands, and disclose any deviation from the disposition in your report. | Verify each disposition's edit against the counterparty's artifact — the tests when revising the spec, the spec when revising the tests — and disclose any deviation from the disposition in your report. |
| 39 | R0016 | R1154 | lexicon | test-designer | require | 0.227 | 0.563 | yes | While a spec branch is open, the Test Designer writes the spec's test suite against the spec as it stands. | Work against the spec and its acceptance criteria as they stand while its branch is open: every testable claim the spec makes gets a test asserting it, and every test asserts something the spec states. |
| 40 | R0017 | R0471 | lexicon | change-flow | forbid | 0.333 | 0.562 | yes | Implement nothing against an open spec; implementation waits on the spec closing agreed. | Run no per-change stage against an open spec and implement nothing against one: implementation begins only once the spec is closed and its test suite accepted as red-gate evidence. |
| 41 | R0482 | R1533 | change-flow | prd-template | require | 0.200 | 0.559 | yes | Write explicit acceptance criteria for the unit of work before the work starts. | Derive per-unit acceptance criteria from the PRD, whether they live in the PRD itself or in the unit's own artifact. |
| 42 | R0499 | R0573 | trd | change-control | require | 0.222 | 0.559 | yes | Where deploy and release are separate events, separate them with feature flags or canaries. | Where deploy and release are separate events, treat a merged pull request as not yet a released change and attach the explicit go/no-go at the release decision. |
| 43 | R0799 | R1556 | verification-boundaries | trd | require | 0.300 | 0.551 | yes | Declare durable or repeated boundaries in the project TRD's standing verification boundary section or verification ledger; drift from it is a continuity finding. | When a change makes a boundary movement permanent, reflect it in the TRD's standing verification boundaries. |
| 44 | R0481 | R1486a | change-flow | convergence | require | 0.500 | 0.542 | yes | At a spec branch's close, put the reviewed diff to Dave: one ruling by Dave agrees the spec and accepts the tests as its red-gate evidence, recorded in the decision log naming the reviewed SHA. | Close an open spec branch on one ruling by Dave, recorded in decisions/log.md naming the reviewed SHA; nothing else closes it. |
| 45 | R0990 | R1021 | copy-editor | critic | require | 0.455 | 0.533 | no | Put one comment at the top of the returned document giving the count of changes and the count of findings by pass; state a zero rather than omitting it. | Place one comment at the top of the document: the count of findings by pass, and for the Skeptic pass the claims with no support first, then the claims with flawed support, then the claims-list mismatches. State a count of zero rather than omitting it. |
| 46 | R0017 | R1153 | lexicon | test-designer | forbid | 0.417 | 0.531 | no | Implement nothing against an open spec; implementation waits on the spec closing agreed. | Implement nothing against an open spec; while its branch is open it takes tests and nothing else. |
| 47 | R1126 | R1483a | spec-reviewer-agent | convergence | require | 0.435 | 0.531 | yes | At the close: - read the spec branch's whole diff, from the branch point to the reviewed SHA, and the tests written against the spec together with the Test Designer's red-gate result; - make exactly two checks and no others: coherence — every testable claim the spec makes has a test asserting it, and every test asserts something the spec states — and that the red-gate result is present and behavioral, the tests having failed on bad logic rather than on an absent import; - file a test asserting a contract the spec does not state as a spec finding, and a testable claim with no test as a test-side gap, leaving both for triage; - leave test adequacy — whether the suite tests enough, at the right levels, with the right boundaries mocked — to the Reviewer's quality review; - record in the close's review artifact the branch point, the reviewed SHA, and the tests' acceptance on Dave's ruling that agrees the spec. | At a spec branch's close, make exactly two checks over the tests and no others: - that the spec and the tests cohere: every testable claim the spec makes has a test asserting it, and every test asserts something the spec states; - that the Test Designer's red-gate result is present and behavioral — the tests failing on bad logic, not on an absent import. Test adequacy is not this read's question and stays with quality review. |
| 48 | R0010 | R1491 | lexicon | reconciliation | require | 0.476 | 0.506 | yes | Close a delta by putting its whole accumulated diff through one read — once per delta, never once per edit — landing it on the default branch as a pull request. | Land a spec branch on the default branch through one pull request carrying the branch's whole accumulated diff. |
| 49 | R1152 | R1468 | test-designer | convergence | require | 0.483 | 0.407 | yes | Begin once the entry read has run and Dave has said to proceed, then write the spec's test suite against the spec as it stands while its branch is open. | Write tests against an open spec branch and nothing else: the Test Designer starts once the entry read over the spec as it stands has run and Dave has said to proceed, and runs the red gate while the branch is open so its result is the close's evidence. Implement nothing against an open spec, and run no per-change stage against one. |
| 50 | R0505 | R0555 | release-manager-agent | change-control | require | 0.357 | 0.476 | no | Where the change is consequential, reference its `human-gate` tracker issue in the change package. | Take the `human-gate` issue as canonical for the existence and state of a pending gate, and the change package as canonical for its evidence. |
| 51 | R0480 | R1485 | change-flow | convergence | require | 0.474 | 0.283 | no | At a spec branch's close, read the branch's whole diff — from the branch point, not from any status transition — together with the tests. | Give Dave the close's diff — the branch's whole diff together with the tests — to read before he rules on it. |
| 52 | R0505 | R0551 | release-manager-agent | change-control | require | 0.385 | 0.469 | no | Where the change is consequential, reference its `human-gate` tracker issue in the change package. | Open one `human-gate` tracker issue for a change when it is ready for the gate. The issue is the canonical record that the gate is pending, one issue per pending change. |
| 53 | R0015 | R1152 | lexicon | test-designer | require | 0.364 | 0.457 | yes | Tests are written against an open spec only after a reviewer gate has run over the spec as it stands — whatever its verdict — and Dave has said to proceed. | Begin once the entry read has run and Dave has said to proceed, then write the spec's test suite against the spec as it stands while its branch is open. |
| 54 | R0551 | R1039 | change-control | release-manager-agent | require | 0.400 | 0.446 | no | Open one `human-gate` tracker issue for a change when it is ready for the gate. The issue is the canonical record that the gate is pending, one issue per pending change. | Before presenting a consequential change to Dave, confirm its `human-gate` tracker issue is open and linked. |
| 55 | R1101 | R1468 | spec-reviewer-agent | convergence | require | 0.364 | 0.440 | yes | Read a spec at exactly two points: the entry read, when the branch opens and before the Test Designer starts, and the close, when spec and tests cohere. Dave agrees the spec at the close, on that read. | Write tests against an open spec branch and nothing else: the Test Designer starts once the entry read over the spec as it stands has run and Dave has said to proceed, and runs the red gate while the branch is open so its result is the close's evidence. Implement nothing against an open spec, and run no per-change stage against one. |
