# Topic digest — one card per source file

Derived artifact. Analysis only; no governed file, the register, or the clusters artifact was edited.

Inputs, at the revisions the directive names:

- `docs/rule-register/rule-register-20260904T210000Z.md` @ 45b02c7b88a0e08f4872713aa39f840a59117423 — 1600 rows, the Pass 3 extraction
- `docs/rule-register/rule-clusters-20260904T223000Z.md` @ acdfef73fc04bed73d6f854b3f66fe8df5411519 — 206 clusters over 675 rows, 925 singletons
- `decisions/log.md` @ d81c41a6ab60288764d8a3898cd66b354b3c664b — DEC-000380, the retirement ruling

## Totals

| | count |
|---|---|
| Rows (observed) | 1600 |
| Distinct rules, corpus-wide (observed) | 1131 |
| Rows retiring under DEC-000380 (inferred) | 124 |
| Rows surviving (inferred) | 1476 |
| Distinct rules surviving, corpus-wide (inferred) | 1043 |
| Definitions among the surviving rows (observed) | 312 |

**Reconciliation.** 124 retiring rows plus 1476 surviving rows is 1600, the register's row count. 88 distinct rules retire — 12 clusters every row of which retires, plus 76 retiring singletons — and 1131 minus 88 is 1043, the surviving distinct count above. The per-card rows sum to 1600.

**How each count is reproduced.** *Rows*: lines in the register beginning `| R`, grouped by its `file` column; the per-file counts match the clusters artifact's per-file view exactly, and both sum to 1600. *Distinct, corpus-wide*: 206 clusters plus 925 singletons, as the clusters artifact states. *Distinct, per topic*: each cluster the topic has at least one row in, counted once, plus that topic's singletons — so a rule stated in five files is counted in all five, and the per-topic distinct counts sum to 1351 rather than to 1131. *Retiring rows*: the criterion below, applied row by row. *Distinct rules retiring*: a cluster retires only if every row in it retires (12 clusters do); a singleton retires if its row does (76 do). *Surviving* for a topic is its distinct count minus the retiring clusters it sits in and its retiring singletons; those per-topic figures sum to 1257. *Definitions*: surviving rows whose register `verb` is `define`.

**How the retirement criterion was applied.** DEC-000380 retires a named list: the `status` and `last-reviewed` frontmatter fields, the agreement flip, per-document review cycles and their artifacts, the expedited and doc-only paths, the frontmatter in-scope set, the pre-commit hook, and the tools `check-frontmatter`, `flip-agreed`, `install-hooks`, `migrate-frontmatter`, and `reviews/expedited-log.md`. A row was retired only where its obligation is one of those named things and nothing else. Two consequences follow from reading the list strictly, and both were resolved in favour of survival, as the directive requires. First, frontmatter fields the ruling does not name — `audience`, `session`, `order`, `superseded-by`, and the requirement that the block exist at all — are not on the list, so their rows survive even though they sit in a header block the rebuild is unlikely to keep. Second, a rule whose substance outlives the machinery survives even where its sentence is written in machinery terms: that nothing is implemented against a spec Dave has not agreed, that a spec is gated before he agrees it, that a decision is recorded in the log, that a review states what it did not inspect. 72 rows are borderline in that second sense — they survive here, and their wording will need rewriting rather than deleting. They are: R0017, R0018, R0020, R0092, R0269, R0469, R0471, R0472, R0480, R0481, R0503, R0510, R0571, R0577, R0595, R0602, R0607, R0611, R0625, R0626, R0627, R0628, R0629, R0630, R0631, R0632, R0633, R0687, R0688, R0689, R0690, R0691, R0692, R0693, R0694, R0695, R0696, R0697, R0700, R0701, R0702, R0886, R0893, R0903, R0934, R0949, R1101, R1102, R1126, R1132, R1152, R1256, R1270, R1275, R1406, R1409, R1410, R1411, R1412, R1418, R1420, R1436, R1437, R1440, R1447, R1451, R1457, R1482, R1485, R1487, R1488, R1493.

Every count below is **observed** — computed from the two companions. Every reading of what a topic obliges, every divergence summary, and every proposal is **inferred**.

## Cards

54 cards, one per source file the register read, ordered by rows extracted, largest first.

### 1. policies/document-metadata-policy.md — 108 rows → 86 distinct → 34 after retirement

**What it obliges.** This topic is almost entirely the document-lifecycle machinery DEC-000380 retired. What it obliged an agent to do was operate a header block on every governed file: put a `status` on it, keep a `last-reviewed` pointer to the artifact that last reviewed it and the SHA reviewed, flip those two fields in a commit that touches nothing else when Dave agrees the document, and take one of three routes to that flip — the full review cycle, the expedited path for a ten-line fix, or the doc-only path for something co-authored with Dave in the artifact pane. A long middle section fences those two short routes off from anything that states a gate, anything under `specs/`, and anything Dave found a single finding in. What survives is a smaller set: that the version of a document is the SHA of the last commit that touched it; that `agreed` is the repo's verb and `approved` is not; the `audience`, `session` and `order` fields, which the ruling did not name; and the build-gating rules — you may cite a draft spec but not implement or test against one, you write tests against a converging spec and implement against an agreed one, and you say in your report what state the spec is in and do no more than that state admits.

**Definitions.** 12: this file's session-kind scope, project PRD/TRD adoption, a document's version, `agreed`, a role document, `all-decision-roles`, `order`, null-equals-absent, the build-gating scope and "build against", citing versus building against a draft, methodology documents under context loading, and bundle membership by `audience`.

**Shares rules with.** LEXICON.md (C002, C006, C017, C023, C024); context-sets/spec-and-change-discipline.md (C017); docs/global-context/core.md (C030); engagements/sre/override-log-policy.md (C036); operating-model.md (C002, C006, C017, C023, C024); policies/decision-log-policy.md (C036, C134); roles/chief-of-staff.md (C006); roles/spec-reviewer-agent.md (C002); roles/test-designer-agent.md (C017, C024); skills/spec-review-cycle.md (C002, C006, C023, C024, C038, C140).

**Divergent.**

- **C002** — The flip's `last-reviewed` cites different things on different routes — the expedited log with the step-1 content SHA, or the exit gate's artifact and reviewed SHA. This topic sits on both sides of the split: R0616 asks only that the field be non-null, R0660 asks that the artifact it names exist, and R0667 pins it to the log.
- **C017** — `converging` means different spans in different files. This topic takes the narrowest position: only a document under `specs/` may hold the status at all (R0609, R0610), which forbids what LEXICON permits.
- **C024** — What is barred before agreement differs. This topic is the strictest — R0694 forbids implementing *and testing* against a draft or in-review spec, where LEXICON and the operating model forbid only implementing, and the Test Designer is elsewhere told to write tests during convergence.

**Retiring.** 77 rows: the `status` and `last-reviewed` fields and every transition between their values, the agreement flip commit, the expedited and doc-only paths with their five and six conditions and their gate-document exclusion list, `reviews/expedited-log.md` and its append-only rule, and the definition of which files the header applies to.

**Proposal.** merge into `spec-gating`. Reason: strip the retiring 77 and what is left is not a metadata policy — it is the build-gating rule plus a handful of vocabulary and bundle facts, which belong with the spec rules they govern rather than in a policy named for a header block that no longer exists.

### 2. operating-model.md — 93 rows → 71 distinct → 70 after retirement

**What it obliges.** This is the spine: it says who decides what, and in what order work moves. Dave owns product direction, prioritization, risk tolerance, release decisions and the judgment of whether evidence is sufficient; agents design, specify, implement, test, review and document, and escalate rather than decide whenever requirements are ambiguous, risk tolerance is unclear, evidence is thin, reviewers disagree, or the session's own role was never stated. The change flow is twelve numbered stages, each completed before the next: spec, gate, converge, agree, then per change — acceptance criteria, architecture summary, red-gate, implement to green, quality review, skeptic/risk review, release package, release gate. Two rules carry most of the weight: nothing is built that has no written acceptance criteria, and tests are written and confirmed failing on bad logic — not on a missing import — before any implementation, at stage 3 and again at stage 7. You may not approve your own artifact, equate green tests with shippability, claim live behavior from mocked evidence, or call a change done because code was written. A meaningful change ships a change package of twelve named parts, and a consequential one also references its human-gate tracker issue.

**Definitions.** 18: the file's session-kind scope, Dave's and the agents' roles, the primary control, spec-first, trust from evidence, specifications as canonical, Dave's ownership and his primary question, the primary controls list, a meaningful change, the spec lifecycle and per-change stage groupings, mechanical checks, the consequential class by reference, deploy versus release, the standard response shape, and production-grade software.

**Shares rules with.** LEXICON.md (C002, C006, C017, C023, C024, C048); context-sets/spec-and-change-discipline.md (C004, C012, C014, C017); context-sets/testing-and-verification.md (C001, C027, C099); docs/global-context/core.md (C001, C018, C034, C059); docs/global-context/review-rubric.md (C018, C034); engagements/sre-critic.md (C001); engagements/sre/README.md (C004); engagements/sre/implementer.md (C065, C121); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C001, C004); policies/commit-and-change-control-policy.md (C004, C037, C128); policies/decision-log-policy.md (C034); policies/document-metadata-policy.md (C002, C006, C017, C023, C024); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004, C014, C018); policies/verification-boundary-policy.md (C001, C004, C027, C048); roles/architect-agent.md (C129); roles/chief-of-staff.md (C005, C006, C009); roles/release-manager-agent.md (C027, C037, C059, C068); roles/reviewer-agent.md (C126); roles/skeptic-risk-agent.md (C001, C037); roles/spec-reviewer-agent.md (C002, C067); roles/test-designer-agent.md (C012, C017, C024); skills/command-blocks.md (C004); skills/spec-review-cycle.md (C002, C006, C023, C024); skills/test-plan-review.md (C012); specs/prd-template.md (C014); specs/trd-template.md (C014, C130, C131).

**Divergent.**

- **C002** — See the metadata card. This topic's R0480 and R0481 state the exit gate as a review of the diff from the transition commit plus a ruling by Dave; they are on the exit-gate side of the split, not the expedited-log side.
- **C004** — Most files put both session kinds in scope; this one does too (R0439). The divergence is elsewhere in the cluster — one policy narrows a section to decision sessions, the engagement files restrict scope to an engagement, and the remote-write policy widens to any tool-mediated remote mutation.
- **C017** — See the metadata card. This topic (R0469) ties converging to the spec lifecycle stages and to revisions that re-enter it, which is wider than `specs/`-only and narrower than LEXICON's.
- **C024** — This topic (R0471) forbids running the per-change stages against a converging spec — it bars implementation but not testing, where the metadata policy bars both.
- **C121** — Both this topic and the engagement Implementer require acceptance criteria before work starts; R0482 requires them explicit and written, the Implementer accepts them brief. Work that clears the engagement bar can fail this one.
- **C125** — The file names its own primary control twice and the two lists differ — R0441 has evidence, specifications, tests, reviews, boundaries, operational signals and known gaps; R0464 has specification, test plan, implementation evidence, independent review, boundaries, operational readiness and release judgment. An agent asked what the controls are gets two answers from one document.

**Retiring.** 1 row: R0476, the frontmatter-only status transition that enters a spec into converging.

**Proposal.** keep as topic `operating-model`. Reason: 70 of its 71 distinct rules survive, and it is the only file that states the stage order and the escalation triggers; nothing else can absorb it.

### 3. skills/spec-review-cycle.md — 66 rows → 56 distinct → 33 after retirement

**What it obliges.** Most of this topic is the retired cycle: how to open a fresh conversation per cycle, carry only findings and prior directives into it, triage each finding with Dave, write a cycle directive recording one decision per finding, hand the revised documents back for a re-gate at full depth or confirmation scope, and land the agreement flip on Dave's go. What survives is the convergence protocol and its exit gate, which is real work and not bookkeeping. While a spec is converging, one execution session edits the spec and another writes tests against it; neither may touch the other's artifact, and every finding crosses between them only through the decision session's triage. The pair is not done until they cohere: every testable claim in the spec has a test asserting it, and every test asserts something the spec states — a test that had to invent a contract is a finding against the spec, a spec claim no test can be derived from is a finding against the tests. The exit gate then makes exactly two checks and no others: that coherence holds, and that the red-gate result is present and behavioral. Test adequacy is explicitly not judged there; it stays with quality review. Reconciliation is separate: a spec branch reaches the default branch only through one pull request that carries the whole accumulated diff, and a spec that still describes something the tranche did not build is not reconciled.

**Definitions.** 3: this skill's scope over the reviewer-gated cycle only, reconciliation as agreement with what was built rather than with intentions, and the finding-direction rule for an invented contract or an underivable claim.

**Shares rules with.** LEXICON.md (C002, C006, C023, C024, C087); engagements/sre-critic.md (C114); operating-model.md (C002, C006, C023, C024); policies/document-metadata-policy.md (C002, C006, C023, C024, C038, C140); roles/chief-of-staff.md (C006); roles/context-quality-reviewer.md (C011); roles/copy-editor.md (C011); roles/critic.md (C011); roles/reviewer-agent.md (C011); roles/skeptic-risk-agent.md (C011); roles/spec-reviewer-agent.md (C002, C011, C083, C084); roles/test-designer-agent.md (C024, C044, C084, C183); skills/directive-authoring.md (C198); skills/evidence-review.md (C011).

**Divergent.**

- **C002** — See the metadata card. This topic states the exit-gate form of the flip (R1486) — `last-reviewed` naming the exit gate's artifact and the reviewed SHA — which is the side the expedited path contradicts.
- **C024** — This topic (R1471) forbids both running a change package against a converging spec and implementing against it, and separately requires tests be written against it — so it agrees with the operating model and contradicts the metadata policy's bar on testing.

**Retiring.** 27 rows: the cycle's step sequence and its fresh-conversation rule, the loop with its agreement bar and gate cadence, the cycle directive, the full-depth and confirmation-scoped re-gate, and the agreement-flip commits at the exit gate and at reconciliation.

**Proposal.** split into `convergence` and `reconciliation`. Reason: the surviving half is two distinct procedures that share nothing but the retired cycle that used to wrap them — converging a spec against its tests, and closing a spec branch through one pull request.

### 4. LEXICON.md — 64 rows → 52 distinct → 49 after retirement

**What it obliges.** This topic is the corpus's dictionary, and its obligations are mostly obligations of naming. It fixes the six evidence classes an agent must attach to every verification claim — mock-, contract-, live-, browser- and production-verified, and unverified — and the four release-impact labels every material boundary gap must carry: blocking, deferred, accepted-risk, not-material. A blocking gap must be resolved before release, and a gap is not blocking merely because it is waiting on Dave. It fixes the branch vocabulary — tranche, spec branch, open spec delta, claimed, reconciliation — and the rule that a delta never spans two tranches and a claimed document is never claimed by a second delta. It separates two things the word "tier" was doing: a model tier is frontier, solid general-purpose or cheap, a claim tier is one of the four claim-strength tiers, and any use of the word must say which. It retires a list of words outright: `prompt`, `dispatch`, `sync block`, and `track` as the name of a directive's parts. And it carries the touch rule: any file you edit for another reason is conformed to this lexicon as part of that edit.

**Definitions.** 36: the branch and delta vocabulary, the six evidence classes, the four release-impact labels plus not-material, claim strength and provenance class as independent axes, the model-tier and claim-tier split, Top K, the directive vocabulary (directive, directive file, execution block, instruction, baton, companion document, command block, context set), the approval-prompt carve-out, and the ordinary sense of "track".

**Shares rules with.** context-sets/spec-and-change-discipline.md (C017); context-sets/testing-and-verification.md (C049, C050, C051, C052, C090); docs/global-context/core.md (C054, C055, C056, C094, C095); operating-model.md (C002, C006, C017, C023, C024, C048); policies/commit-and-change-control-policy.md (C088, C089); policies/document-metadata-policy.md (C002, C006, C017, C023, C024); policies/release-readiness-policy.md (C053); policies/verification-boundary-policy.md (C025, C048); public-prose-criteria.md (C033); roles/chief-of-staff.md (C006); roles/skeptic-risk-agent.md (C025, C053); roles/spec-reviewer-agent.md (C002); roles/test-designer-agent.md (C017, C024, C025); skills/boundary-audit.md (C025); skills/command-blocks.md (C055); skills/spec-review-cycle.md (C002, C006, C023, C024, C087).

**Divergent.**

- **C002** — Its R0018 states the exit from converging as one exit gate plus one ruling by Dave; the divergence over what `last-reviewed` then cites is settled in other files, not here.
- **C017** — This topic takes the widest position: converging is the status a spec holds whenever it is not agreed (R0014, R0020), which permits what the metadata policy's `specs/`-only rule forbids.
- **C024** — This topic (R0017) forbids implementing during converging and says nothing about testing — narrower than the metadata policy, and consistent with the operating model.
- **C025** — The trigger differs across the cluster: this topic (R0030) labels every *material* gap, where the boundary audit labels every *unresolved* gap and the Test Designer labels every gap the test plan leaves open. A resolved-but-material gap is labelled here and not there.

**Retiring.** 4 rows: that lexicon changes enter through a review cycle, and the three rows defining `converging` as a status value and its entry and re-entry transitions.

**Proposal.** keep as topic `lexicon`. Reason: it is the only place the evidence classes, the release-impact labels and the tier split are defined, and 36 of its 49 surviving rules are definitions — which makes it the test case for whether definitions become rows at all.

### 5. docs/global-context/core.md — 61 rows → 57 distinct → 57 after retirement

**What it obliges.** This is the layer every session loads first, and it says a later layer may add rules but never waives one of these. Its obligations are about honesty and scope. Every assertion about state, results, verification or completeness is a claim, and every claim is labelled observed, inferred, told or unknown — an unlabelled one is treated as unknown. A passing check proves the check, not the claim; a tool's success response is itself a claim, so you confirm the content landed before reporting it, read current state before retrying a write that appeared to fail, and report only what the operator reported if you cannot read the write back. You read governed text before emitting anything it governs and read the repository before asserting its state. You do what was asked and say so and stop if the work needs more. Two sources disagreeing is surfaced, never resolved by taking the newer one, and a canonical document conflicting with something derived from it is a hard stop on that item. It also fixes the three layers — decision, execution, shell — and the vocabulary for what passes between them: directive, directive file, execution block, command block, companion document, baton. The executor's first act is always to write the directive to a file, commit, push and report the SHA.

**Definitions.** 20: the layering rule, what counts as a claim and the unknown default, what a passing check proves, a tool response as a claim, the decision and execution session definitions, Core's non-authorization over production, the session boundary as role not capability, the three layers, and the directive vocabulary — paste block, command block, execution block, directive, directive file, instruction, companion document, handoff, baton.

**Shares rules with.** LEXICON.md (C054, C055, C056, C094, C095); context-sets/testing-and-verification.md (C001); docs/global-context/review-rubric.md (C018, C019, C034, C061); engagements/cartographer.md (C028); engagements/sre-critic.md (C001, C058); engagements/sre/system-discovery.md (C028); engagements/working-with-dave.md (C001); operating-model.md (C001, C018, C034, C059); policies/decision-log-policy.md (C034); policies/document-metadata-policy.md (C030); policies/remote-write-verification-policy.md (C029, C106, C107); policies/source-of-truth-policy.md (C018, C060, C104); policies/verification-boundary-policy.md (C001); roles/chief-of-staff.md (C031); roles/context-quality-reviewer.md (C035); roles/critic.md (C035); roles/release-manager-agent.md (C059); roles/skeptic-risk-agent.md (C001); skills/command-blocks.md (C055); skills/conversation-retro.md (C019); skills/directive-authoring.md (C019); skills/directive-invariants.md (C028, C029, C031, C061, C102, C105, C108); skills/evidence-review.md (C058).

**Divergent.**

- **C019** — Generated filenames are `descriptor-timestamp` in ISO 8601 basic format everywhere, but the required precision differs. This topic (R0196) requires only the basic format; directive authoring adds that date and time must both be present; the retro skill adds UTC and the `Z`. A filename that satisfies this topic fails the retro skill's rule.

**Retiring.** none.

**Proposal.** keep as topic `core`. Reason: every rule here survives, it is loaded first by every session, and it is the only statement of the claim-labelling and stop-and-report rules the rest of the corpus assumes.

### 6. roles/chief-of-staff.md — 60 rows → 47 distinct → 47 after retirement

**What it obliges.** This is the decision-session role that opens a working session and keeps the queue. Its first act on invocation is to assess state and propose a next step — not to greet, ask what to work on, or ask permission to look — and it does that by a fixed read sequence: open items, recent commits on the default branch, pending gates, and what else is running, including worktrees and branches another session may hold. It holds the GitHub connector alone; if a second decision session is live, neither writes until Dave settles which holds it, and a connector timeout is treated as contention before it is treated as a fault. Its main product is decomposition: read the agreed PRD and TRD whole, propose tranches for Dave to approve, then break an approved tranche into the smallest independently executable packages in dependency order, writing one decomposition doc per tranche that records the spec SHAs it derived from. It may pre-stage — draft, not land — but may not decide a consequential call under cover of pre-staging, and it may not execute packages, review or test implementation, assess risk, or make architecture decisions. Ambiguity that would force an agent to decide goes to Dave first.

**Definitions.** 8: the role's session kind and short form, pre-staging as drafting, the decomposition doc as the durable artifact, the deliberately unsettled strictness of the staleness re-check, acceptance criteria as a separate execution-time input, the Test Designer's convergence work belonging to the convergence directive, and a converging spec not being an open delta.

**Shares rules with.** LEXICON.md (C006); docs/global-context/core.md (C031); docs/global-context/decision-layer.md (C109); operating-model.md (C005, C006, C009); policies/document-metadata-policy.md (C006); roles/copy-editor.md (C078); roles/critic.md (C078); skills/directive-invariants.md (C031); skills/spec-review-cycle.md (C006).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `chief-of-staff`. Reason: nothing retires, and the connector-contention and decomposition rules exist nowhere else in the corpus.

### 7. context-sets/testing-and-verification.md — 54 rows → 40 distinct → 40 after retirement

**What it obliges.** This topic separates two things the corpus keeps insisting are not the same: a test executes a controlled scenario, and verification is the confidence claim you draw from that evidence. Every verification claim states its boundary — what is verified, what is not, where the missing side is verified, the release-impact label if it is not, and who owns the follow-up — and no test is allowed to imply more than it proves. A test that passes before implementation is a broken test. Each test level is tied to the claim it can support: unit for deterministic logic, contract for external assumptions, live smoke for real services, browser for browser-only behavior, production synthetics for ongoing health including the TRD's SLO targets per Top K journey. The bulk of the file is the four "useful for / does not prove" tables, which is where an agent goes to find out that a mock does not prove third-party auth or deployment config, and a headless DOM is not browser rendering. Production-verified claims additionally state which SLO was checked, the error budget remaining, and whether budget state fed the release decision. A named list of seventeen areas — external APIs, auth, environment variables, service workers, payments, quotas, time, and the rest — is boundary-sensitive by default, which means do not overclaim, not must be overtested.

**Definitions.** 15: testing versus verification, a test versus a confidence claim, a pre-implementation pass as a broken test, the five evidence classes' proves/does-not-prove pairs, error-budget exhaustion as release-relevant, and boundary-sensitive as a no-overclaim rule.

**Shares rules with.** LEXICON.md (C049, C050, C051, C052, C090); docs/global-context/core.md (C001); engagements/sre-critic.md (C001); engagements/working-with-dave.md (C001); operating-model.md (C001, C027, C099); policies/verification-boundary-policy.md (C001, C027); roles/release-manager-agent.md (C027); roles/skeptic-risk-agent.md (C001, C101); skills/boundary-audit.md (C008).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `verification`. Reason: nothing retires, and the proves/does-not-prove tables are the corpus's only concrete guidance on what a given test level licenses you to say.

### 8. public-prose-criteria.md — 50 rows → 34 distinct → 34 after retirement

**What it obliges.** This governs any public piece Dave publishes, and it is read before any work on a piece, alongside the Voice document, which wins where the two speak to the same thing. Every claim sits in one of four strength tiers — relayed, demonstrated, grounded, opinion — and the tier must be legible in the sentence itself, carried by verb and frame, never by narrated self-correction. Dressing an opinion as grounded is a defect. Comprehension may never depend on native-English-only cues: speech is quoted rather than introduced with a bare colon, and any line carrying irony, wordplay or idiom must still read correctly for a reader who takes it literally — without simplifying the register to get there. Naming is anonymized by default in both directions: criticize practices, not companies or people, be equally sparing in praise, and credit the known author of any borrowed idea. Length comes from the author, not from a standing shape, and after a wordy draft the target is ten percent under budget. LLM use is disclosed once at site level, never per piece. The file closes with seven named prose tells — load-bearing, em-dash cascades, "it's not X, it's Y", triadic rhythm, hedge-then-assert, thesis restatement, empty summary sentences — each a defect wherever it appears, with new tells proposed to the author rather than added.

**Definitions.** 7: the criteria's reach over any author's public prose, review passes as advisory, the ladder as claim strength not provenance, wordy as duplicated ideas, the author's word target versus the writer's cuts, the declared-series exception, and the Voice document as the source of venue.

**Shares rules with.** LEXICON.md (C033); engagements/working-with-dave.md (C066); roles/copy-editor.md (C007, C074, C148, C149, C152, C153, C154); roles/critic.md (C007, C066, C147, C157); roles/writer.md (C007); skills/outline.md (C007); voice.md (C007, C074, C151, C155, C156).

**Divergent.**

- **C153** — When discoverability and readability conflict, this topic (R0847) rules that readability wins, which licenses changing the text. The Copy Editor's rule (R0978) requires reporting the conflict and leaving the text alone. The two order opposite actions on the same passage; this topic is the permissive side.

**Retiring.** none.

**Proposal.** keep as topic `prose-criteria`. Reason: nothing retires, and it is the parent the Writer, Copy Editor, Critic and Outline topics all read before acting.

### 9. roles/copy-editor.md — 49 rows → 42 distinct → 42 after retirement

**What it obliges.** The Copy Editor runs as a chat with the author's document uploaded — no working tree, no directive — and reads the Criteria and the Voice document before reading the document, every time. Its work splits cleanly in two. Corrections are made as tracked changes: spelling, punctuation, agreement, broken links, one form for a term spelled several ways, wrong words, sentences one small change would disambiguate, facts stated two ways in two sections, platform-specific formatting, and speech introduced by a bare colon. The test before making any of them is whether two copy editors working from the same authority would make the same change — if they could reasonably differ, it is not a correction. Improving rather than correcting is forbidden. Findings are made as anchored comments across four report passes: claims and their tiers, discoverability, methodology-and-voice mechanics, and literal readability. Delivery is exacting: the author's own `.docx` back, one correction per tracked change, formatting preserved, one top comment giving the counts by pass with zero stated rather than omitted, and a pre-delivery check that no text outside a tracked change differs from the uploaded file — re-run until it passes. Everything it emits is a proposal the author accepts or rejects.

**Definitions.** 3: the role's short form, its session kind, and speech introduced by a bare colon being a correction rather than a finding.

**Shares rules with.** docs/global-context/decision-layer.md (C110); public-prose-criteria.md (C007, C074, C148, C149, C152, C153, C154); roles/chief-of-staff.md (C078); roles/context-quality-reviewer.md (C011); roles/critic.md (C007, C011, C042, C078, C079, C081, C082, C163, C164, C168, C169, C170, C171, C172); roles/reviewer-agent.md (C011); roles/skeptic-risk-agent.md (C011); roles/spec-reviewer-agent.md (C011); roles/writer.md (C007, C042, C079); skills/evidence-review.md (C011); skills/outline.md (C007, C082); skills/spec-review-cycle.md (C011); voice.md (C007, C074, C166, C167).

**Divergent.**

- **C153** — See the prose-criteria card. This topic is the restrictive side: R0978 requires reporting a readability-versus-discoverability conflict and leaving the text alone, where the Criteria license changing it.

**Retiring.** none.

**Proposal.** keep as topic `copy-editor`. Reason: nothing retires, and the correction/finding split and the delivery check are stated nowhere else.

### 10. skills/directive-invariants.md — 49 rows → 46 distinct → 46 after retirement

**What it obliges.** This is the one definition of what a directive must contain, read by the generator that writes directives and by the lint that checks them. Its execution-side rules are what an execution session is actually held to: write the directive verbatim to its named path, commit it alone, push, and report the SHA before reading anything else or touching any other file; fetch and confirm the base is at the reviewed ref first, and judge every remote operation by the refs it reports rather than by whatever a credential helper writes to stderr; read the companions whole, from the working tree, at the revision each names, before writing anything; run the named verification from the assigned tree with output captured to a file, and report a step you did not run as not run, never as passed. Four stop conditions end the session rather than improvising around it: cannot execute as written, concurrent tree mutation, any failed command or unmet precondition, and any unintended tree mutation — with no retry under different flags and no ref created or deleted to recover. The report has a fixed set of lines, every claim is labelled, and a source manifest names where each emitted region came from. The rest is generator and lint mechanics: the disposition label's exact literal, what makes a line eligible, how stripping and masking work, and the canonical sole-tree sentence reproduced word for word.

**Definitions.** 21: the file's own role as the single definition, section and region structure, placeholder syntax, the angle-bracket slot carve-out, the two worked disposition forms, the sandbox and `gh` limitation, a successful remote operation not being a failure, the disposition label literal, the match and eligibility rules, stripping, the statement extent, the exclusive-assignment form, a wrong-form disposition as a lint miss, the canonical sole-tree sentence, the marker definition, the admitted pre-first-act markers, and the lint's phrase blocks.

**Shares rules with.** docs/global-context/core.md (C028, C029, C031, C061, C102, C105, C108); docs/global-context/review-rubric.md (C061); engagements/cartographer.md (C028); engagements/sre/system-discovery.md (C028); policies/remote-write-verification-policy.md (C029); roles/chief-of-staff.md (C031).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `directive-invariants`. Reason: nothing retires; it is the only definition of the disposition label and the stop conditions, and the lint depends on its text being exact.

### 11. policies/commit-and-change-control-policy.md — 47 rows → 31 distinct → 30 after retirement

**What it obliges.** Two mechanisms with one name. Commit control is how a change reaches the default branch: plain `git push` is allowed and needs no approval, force-push is denied both client-side by a runner deny rule that holds in every permission mode and server-side by branch protection, and routine changes go in by pull request with no gate firing at the merge. Change control is when a change reaching *users* needs an explicit human go. The consequential class is an exhaustive list of thirteen kinds — auth, schema and data migration, security and privacy controls, irreversible operations, first exposure of a surface, breaking public interfaces, pricing and entitlements, user data visibility, a verification-boundary move, core architecture, any code path for a Top K journey whose error budget is at or below twenty percent, and a boundary-moving architecture summary — and anything touching none of it is routine. If you are unsure, treat it as consequential and ask. Each pending gate gets one tracker issue, derived from the change package and never written fresh, with one chat line naming the change, pointing at the issue and asking for the go — not restating the evidence. Absence of a response is not a go. For a consequential change it is the exposure that is gated, not the merge: adding a dark flag is routine, flipping it is the release.

**Definitions.** 26: commit versus change control, the exhaustive consequential class and its thirteen members, the pending-gate section's session scope, the issue as canonical for the gate's existence and the change package as canonical for its evidence, the human-gate label, plain push, what counts as a deny, the two gates, exposure-not-merge, merged-is-not-released, and the reconciliation charge.

**Shares rules with.** LEXICON.md (C088, C089); context-sets/spec-and-change-discipline.md (C004); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C004); operating-model.md (C004, C037, C128); policies/project-setup-requirements.md (C069); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004); policies/verification-boundary-policy.md (C004); roles/release-manager-agent.md (C037); roles/skeptic-risk-agent.md (C037); skills/command-blocks.md (C004).

**Divergent.**

- **C004** — This topic is the source of the divergence in the session-kind cluster: R0532 puts both kinds in scope for the policy, but R0549 narrows the pending-gate-visibility section to decision sessions only. An agent reading the file's header gets a different answer than one reading that section.

**Retiring.** 1 row: R0578, which explains that a document reads `agreed` on the default branch because the transition setting it is a frontmatter-only commit.

**Proposal.** split into `commit-control` and `change-control`. Reason: the two halves share a filename and nothing else — one governs how code reaches a branch, the other when a human must say go — and the consequential class is cited from four other topics that have no interest in force-push rules.

### 12. skills/command-blocks.md — 43 rows → 36 distinct → 36 after retirement

**What it obliges.** This governs any fenced block of shell commands the corpus hands to a human or an agent to run as given. A block must run verbatim as pasted, with no manual step inside the fence — manual steps go in prose outside it. Any command producing evidence captures its output to a named path, where evidence means output cited later or leaving the session. Sync and remote commands name the remote and the ref explicitly, never leaning on upstream configuration, and nothing downstream may act on the result until the exit status has been checked. No block pushes the default branch; it pushes a branch and the decision session merges by pull request. A block may not contain a construct that can end the shell it is pasted into, so preconditions are guarded by branching that prints and falls through rather than exiting. One purpose per block, no placeholders — an unknown value is asked as a question above the block. Every block states its expected output in one line below it, and its blast radius above it where destructive. Where a human relays output between blocks, one block goes per turn. The file closes with eleven conformance criteria that restate the rules as a checklist, and an untested block is still a block and still non-conformant.

**Definitions.** 8: what a command block is and that blocks appear outside directives entirely, what counts as evidence, `origin` as a remote name, the one-block-per-turn rule's reach, the shell-ending constructs, and an untested block still being non-conformant.

**Shares rules with.** LEXICON.md (C055); context-sets/spec-and-change-discipline.md (C004); docs/global-context/core.md (C055); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C004); operating-model.md (C004); policies/commit-and-change-control-policy.md (C004); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004); policies/verification-boundary-policy.md (C004); skills/directive-authoring.md (C085).

**Divergent.**

- **C004** — This topic (R1193) puts both session kinds in scope, which is the majority position in the cluster; the divergence is with the files that narrow or widen it.
- **C046** — The file states the expected-output rule three times with different requirements — R1220 asks only for the line, R1235 adds the blast radius where destructive, R1222 additionally requires the expected output be something observed in the environment the block will run in. A block satisfying R1220 alone ships a destructive command with no blast radius stated.

**Retiring.** none.

**Proposal.** keep as topic `command-blocks`. Reason: nothing retires, and the shell-safety and expected-output rules are the corpus's only protection against a pasted block killing the session it runs in.

### 13. roles/spec-reviewer-agent.md — 42 rows → 34 distinct → 34 after retirement

**What it obliges.** The Spec Reviewer is the hard gate on spec quality. It runs as an execution session and returns a review artifact; triage of what it finds happens elsewhere, in a decision session. It gates a PRD or TRD when first authored and again on every revision, and Dave may not agree a spec or a revision without its sign-off. Its checklist is fixed: all required sections present and substantively answered, no section contradicting another, every claim traceable to a parent artifact or marked an open question, the TRD's NFRs instantiating the PRD's dimensions, the TRD's journeys tracing to the PRD's Top K, SLOs defined per journey or explicitly marked unverified, no section overstating confidence, and open questions naming what would resolve them. It adds document-specific checks before agreement — for a PRD, concrete functional outcomes rather than feature lists, explicit risk tolerance, criteria concrete enough to derive tests from; for a TRD, named component responsibilities, every external dependency as a boundary carrying an evidence class, and explicit technical non-goals. It also owns continuity scanning at three depths, from the PRD/TRD/criteria spine up to the whole methodology, flagging inconsistencies for Dave rather than resolving them. At the exit gate it makes two checks and no others, and it does not judge test adequacy or review governed instruction documents.

**Definitions.** 3: the role's session kind and output, what it owns and gates, and its distinction from the Reviewer and Skeptic/Risk agents.

**Shares rules with.** LEXICON.md (C002); operating-model.md (C002, C067); policies/document-metadata-policy.md (C002); policies/source-of-truth-policy.md (C020, C073); roles/architect-agent.md (C076, C077); roles/context-quality-reviewer.md (C011); roles/copy-editor.md (C011); roles/critic.md (C011); roles/reviewer-agent.md (C011); roles/skeptic-risk-agent.md (C011); roles/test-designer-agent.md (C084); skills/evidence-review.md (C011); skills/spec-review-cycle.md (C002, C011, C083, C084); specs/prd-template.md (C177); specs/trd-template.md (C076, C077).

**Divergent.**

- **C002** — See the metadata card. This topic's R1126 and R1132 anchor the exit gate to the transition commit and require Baseline and Reviewed lines in the artifact — the exit-gate side of the split.
- **C020** — The Source of Truth policy (R0776) makes a continuity scan mandatory on every spec revision. This topic offers R1105 — a scan at any depth, on demand — beside R1104's automatic Depth 1, so an agent reading only R1105 would treat every scan as optional. This topic is the permissive side.

**Retiring.** none.

**Proposal.** keep as topic `spec-review`. Reason: nothing retires; the gate on a spec survives DEC-000380 as a frontier read, and the depth-1/2/3 scan definitions have no other home.

### 14. roles/skeptic-risk-agent.md — 41 rows → 25 distinct → 25 after retirement

**What it obliges.** This role's whole job is the gap between confidence and evidence. It reviews the evidence chain rather than the code — the spec, acceptance criteria, test plan, implementation summary, results, mocks and fixtures, boundary declarations, live and browser evidence, release claims and operational notes — and asks what confidence is being inferred that the evidence does not support. It works from seven standing assumptions: an implementation can be plausible but wrong, tests can be green while proving less than claimed, mocks can hide production failures, fixtures can encode stale assumptions, agent summaries can omit uncertainty, deployment config can differ from local, and external services can fail in ways the tests do not cover. It flags eight named false-confidence claims wherever they appear — coverage proves correctness, the fixture worked before, the agent says so, no test failed, not tested means not risky, local dev proves production config, type checks prove runtime, unit tests prove background-worker behavior. It marks every material gap with a release-impact label, judged against the change's declared boundary rather than against the examples, and reserves blocking for gaps the policies actually prohibit. It is explicitly not allowed to assume every risk is a blocker, that every gap needs automation, or that theoretical concerns should stop progress — and it does not make the ship call.

**Definitions.** 5: the role's session kind and gate force versus the advisory Critic, the evidence chain as the primary object of review, blocking as reserved for policy-prohibited gaps, and the commonly-blocking and commonly-deferred lists.

**Shares rules with.** LEXICON.md (C025, C053); context-sets/testing-and-verification.md (C001, C101); docs/global-context/core.md (C001); engagements/sre-critic.md (C001, C116, C117); engagements/working-with-dave.md (C001); operating-model.md (C001, C037); policies/commit-and-change-control-policy.md (C037); policies/release-readiness-policy.md (C053); policies/verification-boundary-policy.md (C001, C021, C025); roles/context-quality-reviewer.md (C011); roles/copy-editor.md (C011); roles/critic.md (C011); roles/release-manager-agent.md (C037); roles/reviewer-agent.md (C011); roles/spec-reviewer-agent.md (C011); roles/test-designer-agent.md (C025); skills/boundary-audit.md (C025); skills/evidence-review.md (C011, C021, C176); skills/spec-review-cycle.md (C011).

**Divergent.**

- **C025** — See the LEXICON card. This topic (R1087) labels every *material* gap, which is the LEXICON and boundary-policy position; the boundary audit and the Test Designer trigger on different sets.

**Retiring.** none.

**Proposal.** keep as topic `skeptic`. Reason: nothing retires, and the seven assumptions and eight false-confidence claims are the corpus's only adversarial checklist.

### 15. skills/conversation-retro.md — 41 rows → 35 distinct → 33 after retirement

**What it obliges.** A retro is produced only when Dave asks for one, in a decision session, entirely from the conversation and the local tree — no remote is read or written while producing one. It captures what transfers: techniques, workflows, belief changes, engineering practices, with implementation details that do not transfer left out, emphasizing what is new or materially changed in the session. The schema is fixed: Context in one paragraph; Evidence as numbered concrete observations with quotes of pivotal moments and no interpretation; Interpretation with each item referencing the evidence numbers it rests on; Standing preferences as candidate rules each naming where the preference was stated before; Durable insights; Candidate methodology changes phrased as inputs, never decisions. A preference Dave states that he has stated before becomes a candidate standing rule in its own section; a one-off correction stays in Evidence and is never merged with them. The retro is handed to Dave in the chat that produced it, and that is where the procedure ends — placing it in the repository is a separate later step by a separate session. Retros live in the project repo under `retros/` and never travel to the methodology repo. A synthesis reads a project's retros and carries a `covers:` list naming every one it read, so the next synthesis computes its input set by comparing those lists rather than by listing the directory.

**Definitions.** 3: retros as an input to change rather than a change mechanism, cross-project synthesis as a read operation with no shared corpus repo, and the grandfathering of retros predating the skill.

**Shares rules with.** docs/global-context/core.md (C019); docs/global-context/decision-layer.md (C063); docs/global-context/review-rubric.md (C019); engagements/sre-critic.md (C063); skills/directive-authoring.md (C019).

**Divergent.**

- **C019** — See the core card. This topic (R1257) is the strictest in the cluster: it requires UTC, the `Z` designator, and both date and time components, so a filename that satisfies Core's rule fails this one.

**Retiring.** 2 rows: that a methodology revision surfaced by a retro takes the full review cycle whatever lighter path it would otherwise qualify for, and that the retro header carries no lifecycle semantics.

**Proposal.** keep as topic `retro`. Reason: 33 of 35 distinct rules survive, and the schema plus the no-remote rule are stated nowhere else; only the two rows that fenced retros off from the expedited path go.

### 16. docs/global-context/decision-layer.md — 40 rows → 38 distinct → 38 after retirement

**What it obliges.** This is loaded after Core by decision sessions only, and it is explicitly not given to an execution session. It is about how to talk to Dave and what to hand him. Ask one question at a time, the one that matters most, naming the item before the choice and using y/n where possible. Lead with the point — terse, bullets over paragraphs, no preamble. Triage pasted output to one line per item needing his judgment, up front, and hold or discard the rest. Say a landmine once, in one line, while handing over the work — never repeat it, gate on it, or reopen his decision. When work has been ruled, emit it in the same response rather than offering it; the first response to a request is the artifact — a block, a draft, a path — not a plan for it. When the decision is his, give options, tradeoffs, and ask. Log a decision in the session that makes it, check the decision log before governing what it already governs, and enter a spoken standing rule into governed text the same turn. Batons are one paste block of decisions, open questions and pointers, with no computed state in them and one labelled exception for a state the successor would not know to check. Model choice is by workload stated as a tier, never by name.

**Definitions.** 4: what a landmine is, that cannot-execute is Core's stop rather than the landmine rule, a loose-end tracker as a record rather than derived state, and the document under review as an artifact separate from its discussion.

**Shares rules with.** engagements/sre-critic.md (C063); roles/chief-of-staff.md (C109); roles/copy-editor.md (C110); skills/conversation-retro.md (C063).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `decision-layer`. Reason: nothing retires, and it is the only file stating how a decision session talks to Dave — which is orthogonal to every other topic.

### 17. specs/trd-template.md — 39 rows → 37 distinct → 36 after retirement

**What it obliges.** The TRD is the standing technical specification — canonical for how the system is built, sitting under the PRD, drafted by the Architect and not in force until Dave agrees it. It is deliberately slow-moving: it changes when the architecture changes, not once per feature, and per-feature design belongs in the per-change architecture summary instead. The template names the sections and what each must actually answer: system shape in a few sentences; an SLO, a measurement mechanism and an alerting threshold for each of the PRD's Top K journeys, with any journey lacking one named unverified and recorded as an open question; components and responsibilities, interfaces, external dependencies; the standing verification boundary section, where each durable boundary names its production surface, how it is currently represented, and what verifying it would take; data shapes and who is authoritative for state; how the system fails, how failure is detected, what the user sees, and how it recovers; operational concerns; the project's release model and where the release decision sits relative to commit and deploy; a concrete technical target for every NFR dimension the PRD defines, or an explicit N/A; explicit technical non-goals; and standing unknowns each naming what would resolve it. It also asks for boring, understandable designs.

**Definitions.** 5: the TRD's place in the spine, its slow-moving nature, Dave's agreement as what puts it in force, SLOs as the technical instantiation of the PRD's user outcomes, and the TRD as the anchor tracker issues derive from.

**Shares rules with.** context-sets/production-grade-software.md (C013); context-sets/spec-and-change-discipline.md (C014); operating-model.md (C014, C130, C131); policies/source-of-truth-policy.md (C014); roles/architect-agent.md (C075, C076, C077, C159); roles/spec-reviewer-agent.md (C076, C077); specs/prd-template.md (C014, C202, C203, C204, C205).

**Divergent.**

- **C206** — The file states the durable-boundary declaration twice with different required fields — R1553 asks for the production surface, its representation, and what verifying it would take; R1554 asks for the production surface, its representation, its evidence class, and the deferred-verification path. A TRD satisfying one leaves a field the other requires empty.

**Retiring.** 1 row: R1575, opening a new project TRD at status draft with `last-reviewed` null.

**Proposal.** keep as topic `trd`. Reason: 36 of 37 distinct rules survive; only the frontmatter opening line goes, and the section-by-section requirements are what an Architect and a Spec Reviewer both work from.

### 18. policies/remote-write-verification-policy.md — 36 rows → 34 distinct → 31 after retirement

**What it obliges.** This governs any tool-mediated remote mutation, and its premise is that a tool's response is not evidence. The repository's own log, read back against the fetched remote, is the authority on what landed; you state SHAs read from that log and never invent one. Before reporting any tool-mediated write you verify that what landed is what was intended — comparing the response's size field against the content sent, and reading the landed commit's files-changed, insertions and deletions against the expected blast radius — and a mismatch on either is a failed write whatever the response said. It carries a two-failure detector: a single transport failure is noise, two in a row is a signal that the tooling is degraded or two sessions are contending for the same transport, at which point you stop and establish state rather than absorbing the second as another retry. It says precisely which failures qualify — timeouts, writes returning success but unconfirmable on read-back, transport errors — and which do not: auth errors, not-found, and your own malformed call. A read-back that itself times out fires immediately. Connector writes are limited to a file creation or a small verified diff; regenerating a governed document whole over the connector is forbidden, and anything larger goes to an execution session with a working tree. A reported tool failure is a claim, classified before any remedy is chosen.

**Definitions.** 9: the policy's reach over both session kinds and any tool-mediated remote mutation, one failure as noise and two as signal, the qualifying and non-qualifying failure lists, the timed-out-but-landed case, why the detector is kept, what a connector write is, what rule 2 counts, and a tool defect as an environment fact needing no second instance.

**Shares rules with.** context-sets/spec-and-change-discipline.md (C004); docs/global-context/core.md (C029, C106, C107); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C004); operating-model.md (C004); policies/commit-and-change-control-policy.md (C004); policies/project-setup-requirements.md (C144); policies/release-readiness-policy.md (C004); policies/source-of-truth-policy.md (C004); policies/verification-boundary-policy.md (C004); skills/command-blocks.md (C004); skills/directive-invariants.md (C029).

**Divergent.**

- **C004** — This topic is the widening side of the session-kind cluster: R0719 puts in scope not only both session kinds but any tool-mediated remote mutation, which is broader than every other member's scope.

**Retiring.** 3 rows: that the pre-commit hook does not run on a connector write, that a connector write must set every frontmatter field the metadata policy requires, and that an execution session runs `bin/check-frontmatter --all` before the pull request merges.

**Proposal.** keep as topic `remote-writes`. Reason: 31 of 34 distinct rules survive, and the two-failure detector and the size/stats read-back are the corpus's only defence against a lying tool response.

### 19. policies/verification-boundary-policy.md — 34 rows → 22 distinct → 22 after retirement

**What it obliges.** A verification boundary is the point where evidence stops — the line between what has been checked and what is merely assumed, mocked, deferred, or accepted as risk — and this policy's whole obligation is to make those lines visible and intentional rather than implicit. Every meaningful mock, stub, fake, fixture, simulated service, generated response, assumed external behavior, browser substitute or deployment assumption gets a declared boundary, and a declaration names eleven things: boundary name, production surface, representation mechanism, verification class, verified claims, unverified claims, deferred verification path, release impact, cadence, failure response, and the owner or trigger for follow-up. Nine named events require creating or updating a declaration — adding a mock, changing a fixture for external data, adding or changing an integration, adding browser or service-worker behavior, changing auth, changing production config assumptions, changing monitoring, discovering a test that passed despite unverified production behavior, and shipping with a known unverified dependency. Durable boundaries are declared in the TRD's standing section or a project ledger. Fast unit tests stay fast and deterministic with live and browser checks kept out of the default suite, and verification cadence is chosen by the risk of the boundary rather than by a fixed schedule. Before release, every material boundary is verified, deferred with a named path, accepted by Dave, or marked not material. Implicit unknowns are forbidden.

**Definitions.** 6: what a verification boundary is, a mock as a claim about our side of the contract, the boundary-type list's relation to the boundary-sensitive list, the common boundary types, and where boundary information may live.

**Shares rules with.** LEXICON.md (C025, C048); context-sets/spec-and-change-discipline.md (C004, C057); context-sets/testing-and-verification.md (C001, C027); docs/global-context/core.md (C001); engagements/sre-critic.md (C001); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C001, C004); operating-model.md (C001, C004, C027, C048); policies/commit-and-change-control-policy.md (C004); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004); roles/release-manager-agent.md (C027); roles/skeptic-risk-agent.md (C001, C021, C025); roles/test-designer-agent.md (C025); skills/boundary-audit.md (C025); skills/command-blocks.md (C004); skills/evidence-review.md (C021).

**Divergent.**

- **C004** — This topic (R0778) puts both session kinds in scope — the majority position; the divergence lies with the members that narrow or widen.
- **C025** — See the LEXICON card. This topic (R0788) labels every *material* gap, agreeing with LEXICON and disagreeing with the boundary audit's unresolved-gap trigger.

**Retiring.** none.

**Proposal.** keep as topic `verification-boundaries`. Reason: nothing retires, and the eleven declaration fields and nine trigger events are cited by the Skeptic, the Release Manager, the Spec Reviewer and the TRD template alike.

### 20. skills/review-artifact.md — 34 rows → 34 distinct → 34 after retirement

**What it obliges.** This fixes the shape of the one artifact every review procedure emits — exactly one per cycle, with the role document saying what to inspect and this skill saying where the answer goes. Each thing has one home and no other: the sign-off or ship call in Verdict; required changes in entries marked blocking; advisory items in entries marked non-blocking; the required follow-up in the per-finding Fix, which is named Fix and not Recommendation; risks and gaps in Consequence and Not inspected; evidence and scope in Scope and Cross-checked; and what Dave should look at himself in Dave should inspect. Verdict takes one of three values — ready, ready-with-findings, changes-required — and never the word agreed, since ready means ready for Dave's agreement. Reviewed is stated as `path @ sha`. Not inspected is always stated explicitly, because "nothing" is a claim and not a default. A clean pass is the header block and nothing else, with no prose. Each finding entry carries an id, severity, Claim, Location, Evidence, Consequence, Fix and Related, and Evidence distinguishes what was verified by running from what was inferred by reading. A finding whose evidence line cannot be filled in is an observation, not a finding; so is an entry whose Consequence cannot say concretely what goes wrong.

**Definitions.** 5: the scope as one document, one cycle, one verdict; a per-entry agreement log not being an artifact in this sense; and the observation-versus-finding rules for an unfillable Evidence or Consequence line.

**Shares rules with.** engagements/sre-critic.md (C115).

**Divergent.** none.

**Retiring.** none.

**Proposal.** rename to `review-artifact-schema`. Reason: nothing retires, but the file's own naming rules are tied to `reviews/<stem>-cycle-<n>.md`, and once per-document cycles are gone the topic is a schema for review output generally, not a cycle artifact.

### 21. roles/critic.md — 30 rows → 29 distinct → 29 after retirement

**What it obliges.** The Critic is advisory — nothing it emits gates a piece, and the author reads every word and decides. It runs as a chat with the document uploaded, reads the Criteria and the Voice document first every time, and runs three passes. The Skeptic pass comes first, before it looks at any claims list: enumerate every claim in the piece yourself, locate its support, and test whether that support carries the claim, reporting only flaws you can demonstrate — a stated support that does not carry its claim, a claim with no support, a tier the support does not earn. "Support not evaluable from the piece" is a valid verdict. Only afterwards does it compare its own enumeration against the author's claims list and report every claim on one and not the other. The second pass reads adversarially for generated-prose tells, checking the piece against every tell the Criteria name and proposing the line that would add any new one it finds rather than adding it. The third reads the piece as the Voice document describes its author, flagging breaks in either direction and citing the Voice line each finding rests on. Every finding is anchored to a passage; a finding it cannot anchor is not reported. It returns the author's own file with comments and nothing else changed, and it does not write replacement prose.

**Definitions.** 3: the role's session kind, its advisory standing, and support-not-evaluable as a valid verdict.

**Shares rules with.** docs/global-context/core.md (C035); engagements/working-with-dave.md (C066); public-prose-criteria.md (C007, C066, C147, C157); roles/chief-of-staff.md (C078); roles/context-quality-reviewer.md (C011, C035); roles/copy-editor.md (C007, C011, C042, C078, C079, C081, C082, C163, C164, C168, C169, C170, C171, C172); roles/reviewer-agent.md (C011); roles/skeptic-risk-agent.md (C011); roles/spec-reviewer-agent.md (C011); roles/writer.md (C007, C042, C079, C174); skills/evidence-review.md (C011); skills/outline.md (C007, C082); skills/spec-review-cycle.md (C011); voice.md (C007, C173).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `critic`. Reason: nothing retires, and the enumerate-then-compare ordering of the Skeptic pass is a real constraint that no other topic states.

### 22. skills/outline.md — 29 rows → 28 distinct → 28 after retirement

**What it obliges.** An outline is the piece's contract with itself: what it claims, how firmly, and where. It runs inside a Writer session, after reading the Criteria and the Voice document, and produces one document in the document pane in a fixed order. First, in chat before the document exists, the Writer inventories every topic in the source material as a flat numbered list in the order it appeared, leaving nothing out and ranking nothing — importance is the author's call, the inventory is the Writer's — then asks which few are the top-level topics and proposes where the rest sit beneath them. Second, the thesis in one sentence at the tier the piece will earn; the Writer may propose one but may not proceed to claims on a thesis the author has not accepted. Third, every claim the piece will make, numbered, one line each, with its tier on the line — the tier the author's evidence earns, not the tier the argument wants, and marked unknown with a question where the evidence cannot be told. That list is the reference the Copy Editor's tier audit and the Critic's Skeptic pass later compare the prose against. Fourth, only if the author asks, a section plan whose budgets sum to the author's word target, with no claim left uncarried and no section carrying a claim the list does not hold. Then it stops.

**Definitions.** 6: an outline as the piece's contract with itself, importance as the author's call, the approved grouping as the skeleton, the claims list as the later passes' reference, a claim present in prose but absent from the list being a finding, and the approved outline as what the Writer drafts against.

**Shares rules with.** public-prose-criteria.md (C007); roles/copy-editor.md (C007, C082); roles/critic.md (C007, C082); roles/writer.md (C007, C184, C185); voice.md (C007).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `outline`. Reason: nothing retires, and the claims list it produces is the artifact two other topics' passes are defined against.

### 23. context-sets/spec-and-change-discipline.md — 28 rows → 28 distinct → 28 after retirement

**What it obliges.** This is the context set an agent loads to know how spec work and change work are kept apart. Its first rule is why any of it exists: a session holds nothing but the documents it is given, so the documents must be right at handoff. A true red-gate is behavioral, not a missing-module red, and the Test Designer must hold enough of the interface contract to write tests that fail on deliberately wrong behavior — taken from the TRD's interface list when writing the spec's suite, and from the architecture summary when writing a unit's tests. The red-gate is run during convergence, before the spec is agreed, and its result is the exit gate's evidence for the tests. Agents propose spec edits rather than making them; the licence to edit a spec freely during an open delta is the owner's alone. Concurrency is achieved by disjoint territory and never by merging: at most two tranches at once, never two deltas over one tranche, and never two deltas editing one document with the result merged — if a project has no disjoint territory, the second tranche goes cross-project or the work runs serially. The loose-end tracker is updated at defined checkpoints rather than relying on Dave to remember, flushed at the end of a work session, and fully accounted for before a release gate. It closes with a derived-fields rule: a change writing an entity's primary fields also maintains its derived fields, enumerated explicitly before the package is called done.

**Definitions.** 7: the file's session-kind scope, why documents must be right at handoff, a true red-gate, the delta-versus-convergence distinction, the stage groupings, and Dave's licence to reconcile mid-tranche at will.

**Shares rules with.** LEXICON.md (C017); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C004); operating-model.md (C004, C012, C014, C017); policies/commit-and-change-control-policy.md (C004); policies/document-metadata-policy.md (C017); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004, C014); policies/verification-boundary-policy.md (C004, C057); roles/test-designer-agent.md (C012, C017, C096, C097); skills/command-blocks.md (C004); skills/test-plan-review.md (C012); specs/prd-template.md (C014); specs/trd-template.md (C014).

**Divergent.**

- **C004** — This topic (R0080) puts both session kinds in scope, the majority position in the cluster; the divergence is with the members that narrow to one kind or restrict to an engagement.
- **C017** — See the LEXICON card. This topic (R0092) takes the middle position: converging is the interval before first agreement or on a revision that re-enters it — narrower than LEXICON's, wider than the metadata policy's `specs/`-only rule.
- **C097** — This topic (R0091) forbids an agent editing a spec *without being told to*, leaving the licence intact when directed. The Test Designer's rule (R1156) forbids editing the spec at all. A Test Designer told to edit the spec is permitted by this topic and forbidden by its own.

**Retiring.** none.

**Proposal.** keep as topic `spec-discipline`. Reason: nothing retires, and the disjoint-territory concurrency rule and the derived-fields rule exist only here.

### 24. specs/prd-template.md — 26 rows → 25 distinct → 24 after retirement

**What it obliges.** The PRD is the standing product specification — the authoritative description of what is being built and why, above the TRD in the spine, Dave's document, and not in force until he agrees it. It is also the parent from which acceptance criteria derive. The template names what each section must answer: the problem, who has it, and why it is worth building now; who uses this and in what concrete situations; the Top K journeys, each with actor, trigger, steps and expected outcome — defined once, here, and never redefined by the TRD; functional goals stated as concrete outcomes rather than feature lists; each of nine non-functional dimensions addressed — performance, reliability, scalability, security, maintainability, usability, observability, portability, compliance — with N/A a valid answer where a dimension is not a constraint, and dimension-specific exclusions noted inside the dimension rather than in non-goals; what the product deliberately does not try to do; the signals that would confirm real user value, each naming the metric, the baseline where known, and the mechanism by which it is observed; acceptance criteria concrete enough to test, since they are what the Test Designer derives cases from; what risk is acceptable and what is not, which feeds the release gate and the consequential-change classification; and standing product unknowns each naming what would resolve it.

**Definitions.** 4: the PRD's place in the spine, Dave's ownership and the agreement that puts it in force, the PRD as parent of the TRD and source of acceptance criteria, and per-unit criteria living in the PRD or the unit's artifact but deriving from the PRD.

**Shares rules with.** context-sets/spec-and-change-discipline.md (C014); operating-model.md (C014); policies/source-of-truth-policy.md (C014, C071); roles/spec-reviewer-agent.md (C177); specs/trd-template.md (C014, C202, C203, C204, C205).

**Divergent.** none.

**Retiring.** 1 row: R1536, opening a new project PRD at status draft with `last-reviewed` null.

**Proposal.** keep as topic `prd`. Reason: 24 of 25 distinct rules survive; only the frontmatter opening line goes, and the nine NFR dimensions are what the TRD and the Spec Reviewer both instantiate against.

### 25. skills/directive-authoring.md — 25 rows → 22 distinct → 21 after retirement

**What it obliges.** This is what a decision session must get right when it writes a directive. Every directive states a working-tree disposition — either an exclusive assignment naming a directory and the command that creates it, or an explicit sole-tree declaration — as its own labelled statement, exactly one per directive, mechanically distinguishable from any incidental mention of trees or commands elsewhere in the file; a prohibition is not a disposition. The route line names one session: a fresh one, or an existing one named by the directive it landed, never by the tree or branch it holds. Stop conditions are pinned to the reviewed ref, not to the head of the branch the directive lands on. Constraints must not contradict the instructions in the same file, so the constraint block is read against the instruction list before sending, and do-not lists and base guards are scoped to the blast radius, naming files rather than directories, with anything a required consistency fix must reach named as explicitly permitted. Wording Dave dictated is carried as a pointer — path, SHA, and the field or section — unless this directive is itself the wording's origin, in which case it is carried inline. An expected-state line is read from the tree at the reviewed ref, never from a session report or from memory. A directive assigning a worktree instructs the holder check before creating it and worktree removal from the main tree after the report is composed and every push is verified landed.

**Definitions.** 1: that worktree entries marked prunable are not the session's.

**Shares rules with.** docs/global-context/core.md (C019); docs/global-context/review-rubric.md (C019); skills/command-blocks.md (C085); skills/conversation-retro.md (C019); skills/spec-review-cycle.md (C198).

**Divergent.**

- **C019** — See the core card. This topic (R1300) requires both date and time components in the timestamp, which is stricter than Core's bare ISO 8601 basic format and looser than the retro skill's UTC-with-`Z`.

**Retiring.** 1 row: R1299, that every directive states the pre-commit hook is never bypassed.

**Proposal.** merge into `directive-invariants`. Reason: the two topics are one subject split across a skill and its definition file — this one states what the author must do, that one defines the label, the markers and the lint that checks it, and 21 of these 22 rules survive to be stated once.

### 26. voice.md — 25 rows → 25 distinct → 25 after retirement

**What it obliges.** This is the register the prose is written in, and it wins over the Public Prose Criteria wherever the two speak to the same thing. One voice, Dave's — not a house style, not a persona — tested by whether it reads as Dave. The baseline is dry, technical and wry, but more expansive and explanatory than the methodology register, walking the reader through reasoning the repo documents compress away; persuasive or poetic passages are occasional flourish, never the constant mode. Structure has named devices with named limits: put the blunt categorical claim first and the personal instance after; sentence fragments are in voice when they land a point, and a fragment opener is a baton-turn device, one per section, never the default cadence; mirrored two-sentence pairs and a closing contrast pair are structure rather than restatement and are flagged rather than cut, with a second closing pair in one piece needing a reason. Rising-specificity repetition — the same claim three times, each more concrete — is his rhythm, and when cutting for budget you keep the first and last and cut the middle. Prefer the flat absolute over the hedged quantifier. Profanity is rare and each use earns its place; day-to-day heat comes from the rhythm of swearing without the words. Methodology vocabulary is used as governed and defined at first use, with the canonical document linked whenever a mechanism is discussed. LLM use is disclosed once at site level with Dave's commitment that he reads and stands behind every published word.

**Definitions.** 6: who the readers are and what the goal is now, hacker register as in voice, sentence fragments and the fragment opener, the source of day-to-day heat, rising-specificity repetition as his rhythm, and the canonical home and cross-post venues.

**Shares rules with.** public-prose-criteria.md (C007, C074, C151, C155, C156); roles/copy-editor.md (C007, C074, C166, C167); roles/critic.md (C007, C173); roles/writer.md (C007); skills/outline.md (C007).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `voice`. Reason: nothing retires; it is the tie-breaker the Criteria defer to, and its rules are about one author's register rather than about method.

### 27. docs/global-context/review-rubric.md — 23 rows → 23 distinct → 23 after retirement

**What it obliges.** Twelve criteria a governed instruction file must satisfy, applied by the Context Quality Reviewer as an execution session, with findings citing the criterion number. A file passes when it satisfies all twelve or is proposed for retirement — and retirement is Dave's. The criteria are demanding in a specific direction: write the file to be read inside a generated bundle by an agent that has never seen the repository, assuming nothing about opening another file, and do not refer to other files by path — unless the file's subject *is* paths, such as a scope definition. Do not restate a rule Core or the Decision Layer already states. State every rule as an instruction to the agent reading it, and cut the arguments for it — including "Never X" restatements and trailing justifications. Say explicitly which session kinds the file is for and say nothing only the other kind needs. Speak model selection in tiers. Follow the descriptor-timestamp filename convention and use no random strings, hashes or UUIDs. Land in at least one bundle and contribute something no other file in that bundle states — a file that contributes nothing another file already says is proposed for retirement. Wording that lets the agent decide what is Dave's is a defect, as are underspecified conditions, missing escalation triggers, implied authority the methodology does not grant, and boundaries two roles could both claim. Contradicting another governed file is a defect, and disagreement between files is surfaced rather than resolved.

**Definitions.** 5: when a file passes, what a criterion is, the paths-as-subject carve-out, retirement being Dave's, and the four wording defects.

**Shares rules with.** docs/global-context/core.md (C018, C019, C034, C061); operating-model.md (C018, C034); policies/decision-log-policy.md (C034); policies/source-of-truth-policy.md (C018); roles/context-quality-reviewer.md (C111, C112); skills/conversation-retro.md (C019); skills/directive-authoring.md (C019); skills/directive-invariants.md (C061).

**Divergent.**

- **C019** — See the core card. This topic (R0277) restates Core's descriptor-timestamp convention without the extra precision the retro and directive-authoring topics require.

**Retiring.** none.

**Proposal.** rename to `context-quality-criteria`. Reason: nothing retires, but under DEC-000380 these twelve criteria become the intake test a Context Quality Reviewer applies to a proposed rule, and "review rubric" names a review cycle that no longer exists.

### 28. policies/source-of-truth-policy.md — 23 rows → 15 distinct → 15 after retirement

**What it obliges.** This says which artifact wins when two disagree, so that disagreements are resolved by authority rather than by guessing. The order is fixed: the PRD is canonical for what and why, the TRD for how, acceptance criteria derive from the PRD and are Dave's, the per-change architecture summary is scoped to one unit and derived from the TRD, and tracker issues are derived PM artifacts — a view onto the specs, never an independent source of truth. The portable operating-model documents are canonical for how the project is run, which produces the adapter rule: vendor-specific AI tooling may never be the sole home of a durable rule, no durable operating principle lives only inside a vendor tool, and when creating a vendor-specific artifact you identify the portable source document, keep the adapter short, update the portable source first, and note intentional deviations — you do not add new durable policy in the adapter. When a derived artifact disagrees with a canonical one, that is a hard stop on the item: you stop, surface the conflict to Dave in the current response naming both artifacts and quoting the contradicting content, say it is a hard stop, and wait. You may not silently reconcile, pick the more recent artifact, or pick the version that is easier to implement. The Spec Reviewer catches drift before it reaches that stop, and anyone meeting suspected drift surfaces it rather than waiting for a scan.

**Definitions.** 7: the policy's session scope, the PRD and TRD as canonical for what/why and how, acceptance criteria as PRD-derived and Dave's, the architecture summary's scope and derivation, tracker issues as derived, and the portable operating-model documents as canonical.

**Shares rules with.** context-sets/spec-and-change-discipline.md (C004, C014); docs/global-context/core.md (C018, C060, C104); docs/global-context/review-rubric.md (C018); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C004); operating-model.md (C004, C014, C018); policies/commit-and-change-control-policy.md (C004); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/verification-boundary-policy.md (C004); roles/spec-reviewer-agent.md (C020, C073); skills/command-blocks.md (C004); specs/prd-template.md (C014, C071); specs/trd-template.md (C014).

**Divergent.**

- **C004** — This topic (R0755) puts both session kinds in scope, the majority position in the cluster.
- **C020** — This topic (R0776) makes continuity scans mandatory on every spec revision — the strict side of the divergence with the Spec Reviewer's on-demand framing.

**Retiring.** none.

**Proposal.** keep as topic `source-of-truth`. Reason: nothing retires, and the authority ordering plus the hard-stop-on-conflict rule are the corpus's only conflict-resolution procedure.

### 29. engagements/sre/speed-audit.md — 21 rows → 20 distinct → 20 after retirement

**What it obliges.** A play for one kind of client engagement, run in a decision session, with the Cartographer, Implementer and Critic reading it for the steps assigned to them without running the play themselves. The sequence is fixed and ordered by evidence. The Cartographer maps the pipeline into a System Map and sends its unknowns to the Assistant, who carries to Dave whatever needs his decision — Dave decides what to ask the client. Then per-stage distributions are captured into a Measurement Baseline, and nothing is changed at that step. Stages are ranked by cost at p50 and p95 and the ranking is presented with its evidence. The Assistant drafts Improvement Proposals for the top stages, in a stated shape, from the map and the baseline; the Cartographer may not propose and the Implementer may not self-authorize. Dave selects proposals with the client's priorities in mind, and his go covers the acceptance criteria in the same breath — recorded in the proposal and cited in the change package. The Implementer builds; the Critic reviews only when asked. The Cartographer re-measures against the baseline after each landing, not in batches. The write-up gives before/after distributions, the remaining ranked opportunities and the recommendation, written for the client's own engineers to keep, in their vocabulary, citing their systems.

**Definitions.** 3: Dave deciding what to ask the client, Dave selecting the proposals, and his go covering the acceptance criteria.

**Shares rules with.** none.

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `speed-audit`. Reason: nothing retires and it shares no rule with any other file in the corpus — it is a self-contained play, the largest fully independent topic in the register.

### 30. roles/test-designer-agent.md — 20 rows → 19 distinct → 19 after retirement

**What it obliges.** The Test Designer defines how correctness will be evaluated before implementation, running as an execution session. It derives test cases from the acceptance criteria and the spec's testable claims, chooses test levels, identifies mocked dependencies, live and browser verification needs, and SLO verification needs for the affected Top K journeys, specifies failure cases, and defines what evidence will be required. It runs the red-gate before handing off to the Coder — confirming the tests fail on bad logic rather than on an absent import — and hands that result to the exit gate as the evidence for the tests. Every gap the test plan leaves open is marked with its release-impact label. During convergence it works against the spec as it stands, giving every testable claim a test and making every test assert something the spec states. Where a test cannot be derived because the spec leaves a contract unstated or a claim untestable, it files a spec finding through the decision session — it does not edit the spec and does not invent the contract in the test. It verifies each disposition it executes against the spec as it stands and discloses any deviation in its report. It does not implement, and it does not create large test suites for their own sake.

**Definitions.** 3: the role's session kind, its responsibilities as construction rather than review of a test plan, and the converging spec it tests against.

**Shares rules with.** LEXICON.md (C017, C024, C025); context-sets/spec-and-change-discipline.md (C012, C017, C096, C097); operating-model.md (C012, C017, C024); policies/document-metadata-policy.md (C017, C024); policies/verification-boundary-policy.md (C025); roles/skeptic-risk-agent.md (C025); roles/spec-reviewer-agent.md (C084); skills/boundary-audit.md (C025); skills/spec-review-cycle.md (C024, C044, C084, C183); skills/test-plan-review.md (C012, C179, C180, C181, C182).

**Divergent.**

- **C017** — See the LEXICON card. This topic (R1152) describes converging as first-gate-run-and-nothing-agreed, which agrees with the operating model and conflicts with the metadata policy's `specs/`-only restriction.
- **C024** — This topic (R1153) forbids implementing until the spec is *agreed*, which is stricter than LEXICON's during-converging bar and directly contradicts the metadata policy's ban on testing before agreement — since testing is exactly what this role does then.
- **C025** — This topic (R1151) labels every gap *the test plan leaves open*, a narrower trigger than the material-gap rule in LEXICON and the boundary policy.
- **C097** — This topic (R1156) forbids editing the spec at all, where the context set forbids editing it only without being told to. Told to edit, this role is forbidden by its own document and permitted by the other.

**Retiring.** none.

**Proposal.** keep as topic `test-designer`. Reason: nothing retires, but it sits in four of the fifteen divergent clusters — more than any other topic its size — so it is where the corpus's disagreements about convergence actually bite.

### 31. engagements/sre-critic.md — 19 rows → 12 distinct → 12 after retirement

**What it obliges.** An engagement-side Critic, run as an execution session, summoned into a fresh session that has not seen the work being reviewed, and only when Dave or his Assistant explicitly asks at a completion point. Its inputs are the diff or artifact, the claims made about it, and whatever context Dave pastes. It judges only whether the work does what is claimed, on the evidence stated — it does not question whether the work should exist, act as a style reviewer, or re-implement. An engagement has no release gate, so its verdict is input to Dave's decision rather than a gate on anything. It follows the review-artifact schema including its verdict values. Its substance is eight infrastructure false-confidence claims it must flag wherever an equivalent statement appears: a green pipeline proving a faster pipeline, a single timing proving a distribution, staging behavior proving production behavior, teardown succeeding because the workflow went green, a module's defaults being what this configuration actually sets, IAM changes taking effect immediately, capacity in one project proving capacity in another, and the runbook matching what the code now does. It raises material risk only, and says which risk is material and which is acceptable.

**Definitions.** 3: the role's session kind, an engagement having no release gate with the verdict as input rather than a gate, and what the Critic's inputs are.

**Shares rules with.** context-sets/testing-and-verification.md (C001); docs/global-context/core.md (C001, C058); docs/global-context/decision-layer.md (C063); engagements/sre/README.md (C064); engagements/working-with-dave.md (C001); operating-model.md (C001); policies/verification-boundary-policy.md (C001); roles/skeptic-risk-agent.md (C001, C116, C117); skills/conversation-retro.md (C063); skills/evidence-review.md (C058); skills/review-artifact.md (C115); skills/spec-review-cycle.md (C114).

**Divergent.** none.

**Retiring.** none.

**Proposal.** merge into `skeptic`. Reason: 12 distinct rules, of which the eight infrastructure false-confidence claims are the only thing not already stated by the Skeptic/Risk Agent — they are a domain list, not a separate role.

### 32. engagements/sre/README.md — 19 rows → 18 distinct → 18 after retirement

**What it obliges.** The frame for a client engagement, for both session kinds inside one. Discovery precedes specification, and the artifact chain is fixed: System Map, then Measurement Baseline, then Improvement Proposal, then change package. The baseline is the failing test — nothing is optimized before the stopwatch exists, the baseline distribution is captured, and the expected delta is stated in advance, which is what the baseline-gate means as a standing condition. Changes land as pull requests through the client's own gates; the agent pushes nothing. An engagement has no release gate at all: release is the client's concern, and the agent may not inquire into or reason about the client's release timing. Ceremony is deliberately minimal — the baseline-gate, a lean change package, and clean-context Critic review where requested — and a Critic read is advisory, gating nothing. Each artifact has a stated shape: the System Map holds pipeline narrative, stage table, claim provenance, unknowns and the proposed measurement plan, small enough to read in one sitting; the Measurement Baseline holds per-stage p50 and p95, the total, the date range, the run count and known confounds; the Improvement Proposal is one screen with the target stage and its baseline cite, the change, the expected delta stated in advance, effort and blast radius with rollback path, and draft acceptance criteria. All of them live in the client-hosted repository designated at kickoff.

**Definitions.** 11: the pack's session scope, the artifact chain, the baseline as the failing test, no release gate in an engagement, a Critic read as advisory, the three artifact shapes, the engagement working area, the baseline-gate as a standing condition, and the no-fixed-filename record shape.

**Shares rules with.** context-sets/spec-and-change-discipline.md (C004); engagements/sre-critic.md (C064); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C004); operating-model.md (C004); policies/commit-and-change-control-policy.md (C004); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004); policies/verification-boundary-policy.md (C004); skills/command-blocks.md (C004).

**Divergent.**

- **C004** — This topic (R0323) is one of the three engagement rows that restrict scope to work within an engagement rather than to a session kind — which is the divergence in this cluster.

**Retiring.** none.

**Proposal.** keep as topic `engagement-frame`. Reason: nothing retires, and the no-release-gate and no-push rules invert the methodology's defaults in ways that must be stated somewhere an engagement session will load.

### 33. engagements/sre/baseline-measurement.md — 18 rows → 18 distinct → 18 after retirement

**What it obliges.** How the stopwatch is built and used, with the roles split so no one grades their own work: the Assistant runs steps one to five in a decision session, the Cartographer runs step six in an execution session and re-measures with the baseline's own instrument, the Implementer never measures its own change, and all three read the whole file. Stages are enumerated from the System Map. Instrumentation is read-only first — stage timings extracted from existing logs and CI APIs before any pipeline change is proposed — and where a change is genuinely required it ships as a pull request like anything else. What is captured is a distribution, not an anecdote: enough runs to state p50 and p95 per stage. Improvements are gated on an expected-delta statement made before implementation and recorded in the change package. After the change, re-measure with the same stopwatch and the same method, cite both measurements in the delta claim, deliver the re-measurement to the Assistant, and cite it in the change package's Evidence item. If the baseline-gate is overridden, the measurement debt is named in the change package. Four things are forbidden outright: optimizing the stage that is easiest to see instead of the one the distribution indicts, changing the stopwatch and the pipeline in the same change, reporting means instead of p50 and p95, and measuring spin-up but not teardown.

**Definitions.** 0: none.

**Shares rules with.** engagements/sre/implementer.md (C118).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `baseline-measurement`. Reason: nothing retires, and it is the only topic in the register with no definitions at all — eighteen rows of pure procedure, which makes it the cleanest test of what a rules store looks like without definitions.

### 34. roles/writer.md — 17 rows → 14 distinct → 14 after retirement

**What it obliges.** The Writer runs as a chat with the document pane — no working tree, no directive — and reads the Public Prose Criteria and the Voice document before writing anything, every time. It drafts what the author asks for, from an approved outline where one exists and from the conversation where one does not, producing the outline through the Outline skill when asked. The draft goes in the document pane and the discussion stays in chat; it never pastes the draft into chat. The author's edits to the draft are the current text. Where an edit reveals a rule the Voice document does not yet state, the Writer proposes the line in chat and continues. When the author says a piece, section or outline is done, it states that a Copy Editor pass and a Critic pass are available, in that order, and stops — it does not start either pass itself, and it never shares a session with them or runs their passes on its own draft. When the work is done it emits a voice harvest: one paste block, one line per rule this session showed that the Voice document or the Criteria do not yet state, or one line saying nothing new appeared. The published text lives in the author's own document; nothing the Writer writes is the record of the piece, and agreement, publication and every decision about what the piece says are the author's.

**Definitions.** 3: the role's session kind, the published text living in the author's document, and agreement and publication being the author's.

**Shares rules with.** public-prose-criteria.md (C007); roles/copy-editor.md (C007, C042, C079); roles/critic.md (C007, C042, C079, C174); skills/outline.md (C007, C184, C185); voice.md (C007).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `writer`. Reason: nothing retires, and the voice-harvest rule is the only mechanism by which the Voice document grows.

### 35. policies/decision-log-policy.md — 16 rows → 14 distinct → 14 after retirement

**What it obliges.** The decision log is the one place a decision resolves to, and DEC-000380 keeps it. One append-only log per project at `decisions/log.md`, plus one in the methodology repo for methodology decisions — one file rather than one file per entry, because the single file is the index. Entries are appended and never edited or deleted. Each entry carries a DEC id and short title, the date, the decision, the context, and a Supersedes line where one applies. IDs are `DEC-NNNNNN` in steps of ten starting at `DEC-000010`, unique within the project and never reused; you assign one by reading the log, taking the last entry's number and adding ten, and if two entries land on the same number you renumber the later ones by plus one within the gap rather than by another ten. The Context field carries the why, so a reader can consult a decision without reopening the chat it came from — a cited decision with no path back to its definition is the defect this log exists to remove. Decisions are Dave's, so there is no who field. Changing or reversing a decision appends a new entry whose Supersedes names the old ID, leaving the old entry as the historical record; whether a decision is still live is answered by searching its ID in later Supersedes lines.

**Definitions.** 5: appending as decision-session work, the log as the one place a decision resolves to, the defect it removes, decisions being Dave's with no who field, and how liveness is determined.

**Shares rules with.** docs/global-context/core.md (C034); docs/global-context/review-rubric.md (C034); engagements/sre/override-log-policy.md (C036); operating-model.md (C034); policies/document-metadata-policy.md (C036, C134).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `decision-log`. Reason: nothing retires — DEC-000380 explicitly keeps the log and makes it the record for prose documents gated by a frontier read, so this topic gains weight rather than losing it.

### 36. roles/architect-agent.md — 16 rows → 15 distinct → 15 after retirement

**What it obliges.** The Architect drafts the standing TRD in a decision session and produces the per-change architecture summary in an execution session. Its subject is technical structure and boundaries: propose system structure, identify components and interfaces, identify external dependencies, define the important boundaries, identify likely failure modes, propose the implementation approach, and avoid unnecessary complexity. It takes responsibility for all TRD sections, inheriting the Top K user journeys from the PRD and defining an SLO, a measurement mechanism and an alerting threshold for each, and instantiating the PRD's NFR dimensions as concrete technical targets. The TRD it drafts is the durable, slow-moving specification — agreed by Dave, changing when the architecture changes rather than once per feature. Per change it produces an architecture summary naming affected components, external dependencies, boundary changes, risk notes and testing implications, which is the artifact the tracker issue is cut from. Where a change alters standing architecture it updates the TRD and keeps it consistent across every affected section. It is told to pay special attention to coupling, hidden dependencies, vendor lock-in, deployment assumptions, operational complexity and maintainability.

**Definitions.** 1: the standing TRD as durable, slow-moving, Architect-drafted and Dave-agreed.

**Shares rules with.** operating-model.md (C129); roles/spec-reviewer-agent.md (C076, C077); specs/trd-template.md (C075, C076, C077, C159).

**Divergent.** none.

**Retiring.** none.

**Proposal.** merge into `trd`. Reason: fifteen distinct rules, of which four already cluster with the TRD template's own rows — the role document and the template state the same obligations from two sides, and after retirement there is no cycle to keep them apart.

### 37. roles/release-manager-agent.md — 16 rows → 15 distinct → 15 after retirement

**What it obliges.** The Release Manager assembles the release package in an execution session and presents it at the release decision in a decision session, producing a ship or no-ship recommendation. It collects the change summary, test evidence and review evidence; checks verification boundary status and the SLO status and error budget consumption for the affected Top K journeys; identifies known gaps and the rollback or mitigation path; and for a consequential change confirms the human-gate tracker issue is open and linked before anything is presented to Dave. The release readiness review it writes has eleven named parts: change summary, user-visible behavior, test evidence, review findings, verification boundary status, SLO status and error budget consumption, live and browser verification status where relevant, operational risks, rollback or mitigation path, known gaps, and the recommendation with Dave's decision points. It assembles from the change package wherever the change package already states something, rather than writing it fresh, and supplies only two items itself: user-visible behavior, and the rollback or mitigation path. Where a required item has no source at all it says so rather than filling the slot, treating the missing item as a known gap. The recommendation is one of four values: ship, ship with accepted risks, do not ship, or needs Dave decision.

**Definitions.** 0: none.

**Shares rules with.** context-sets/testing-and-verification.md (C027); docs/global-context/core.md (C059); operating-model.md (C027, C037, C059, C068); policies/commit-and-change-control-policy.md (C037); policies/verification-boundary-policy.md (C027); roles/skeptic-risk-agent.md (C037).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `release-manager`. Reason: nothing retires, and the four recommendation values and the assemble-don't-rewrite rule are stated only here.

### 38. skills/boundary-audit.md — 16 rows → 11 distinct → 11 after retirement

**What it obliges.** A focused execution-session procedure for finding where the system's tests, mocks, tools or assumptions stop proving production behavior. It runs on six triggers: adding or changing a mock, adding or changing an external integration, changing auth, config or deployment behavior, reviewing a green test suite before release, investigating a surprise production or manual failure, and verifying that the SLO targets defined in the TRD actually have a production monitoring mechanism. Its inputs are the change summary, the test plan, the relevant tests and mocks, the external dependencies, and the release context. Its steps are mechanical and in order: list all affected production behaviors; identify which are tested locally; identify which tests use mocks, fixtures, a headless DOM, fakes or generated data; state for each boundary what is verified and what is not; assign a deferred verification path; and mark each unresolved gap with one of the four release impact labels. It is the operational counterpart to the verification boundary policy — the policy says what a boundary declaration must contain and when one must exist, this skill is the pass that produces them.

**Definitions.** 0: none.

**Shares rules with.** LEXICON.md (C025); context-sets/testing-and-verification.md (C008); policies/verification-boundary-policy.md (C025); roles/skeptic-risk-agent.md (C025); roles/test-designer-agent.md (C025).

**Divergent.**

- **C025** — See the LEXICON card. This topic (R1192) labels every *unresolved* gap, where LEXICON and the boundary policy label every *material* gap. A resolved-but-material gap is labelled by them and not here; an unresolved-but-immaterial gap is labelled here and not by them.

**Retiring.** none.

**Proposal.** merge into `verification-boundaries`. Reason: eleven distinct rules, all of them the procedure for producing what the verification boundary policy requires — and its one divergence from that policy over which gaps get labelled is easier to settle if the two sit together.

### 39. skills/test-plan-review.md — 16 rows → 12 distinct → 12 after retirement

**What it obliges.** A short execution-session pass, run by the Reviewer, that evaluates whether a test plan is sufficient for the intended change and its risk. It triggers on five conditions: writing tests before implementation, reviewing a spec-first or TDD plan, adding external dependencies, adding browser or PWA behavior, and adding or changing mocks. Its inputs are the spec, the acceptance criteria, the proposed test plan, the affected boundaries and the known risks. Its checks map acceptance criteria to tests and identify criteria left untested; identify the mocks and fixtures and the live or browser verification needs; check that the plan includes a red-gate step whose tests can demonstrably fail on bad logic rather than on an absent import, flagging it if absent or import-only; check negative and failure cases; identify over-testing or unnecessary complexity; and recommend changes. It is the mirror image of the Test Designer's construction list — the same seven dimensions, read as a review rather than as a build.

**Definitions.** 0: none.

**Shares rules with.** context-sets/spec-and-change-discipline.md (C012); operating-model.md (C012); roles/reviewer-agent.md (C175); roles/test-designer-agent.md (C012, C179, C180, C181, C182).

**Divergent.**

- **C175** — The Reviewer's own document (R1052) asks only for unnecessary complexity; this topic (R1509) asks for over-testing as well. A Reviewer reading its role document alone does not flag over-testing.

**Retiring.** none.

**Proposal.** merge into `test-designer`. Reason: five of its twelve distinct rules already cluster one-to-one with the Test Designer's construction list, and the two topics are the same seven dimensions stated once as build and once as review.

### 40. skills/evidence-review.md — 15 rows → 9 distinct → 9 after retirement

**What it obliges.** A short execution-session pass that asks whether the evidence actually supports the claims an agent or change package is making. It runs on four triggers: reviewing implementation output, reviewing test results, preparing a release, and deciding whether Dave needs to inspect something more deeply. Its inputs are the spec or intent, the implementation summary, the test evidence, the review notes, the verification boundary declarations, and the known gaps. Its method is four steps: list the claims being made, match each claim to evidence, identify the unsupported claims, and identify overbroad interpretations of test results and missing evidence for material risks. Its output is those three things as findings, each carrying one of the four release impact labels. It does not make a ship call — that is the Release Manager's — and it signals no-ship by marking the gap blocking.

**Definitions.** 0: none.

**Shares rules with.** docs/global-context/core.md (C058); engagements/sre-critic.md (C058); policies/verification-boundary-policy.md (C021); roles/context-quality-reviewer.md (C011); roles/copy-editor.md (C011); roles/critic.md (C011); roles/reviewer-agent.md (C011); roles/skeptic-risk-agent.md (C011, C021, C176); roles/spec-reviewer-agent.md (C011); skills/spec-review-cycle.md (C011).

**Divergent.** none.

**Retiring.** none.

**Proposal.** merge into `skeptic`. Reason: nine distinct rules, six of which already cluster with the Skeptic/Risk Agent's — it is the same claim-versus-evidence pass at a smaller scope, and after merging the two the corpus has one evidence review, not two.

### 41. engagements/cartographer.md — 13 rows → 11 distinct → 11 after retirement

**What it obliges.** The Cartographer runs as an execution session and answers the questions a directive puts about the client's system; its report — the question list and the rendered map — goes back to the Assistant. It does not run a discovery program with its own agenda. It stays read-only throughout, never interviews client humans, and carries the questions worth asking in its report, ranked, rather than asking them. Every claim it makes carries provenance and it never flattens it: for observed, cite the location; for told, name the source and the date; for unknown, phrase it as the question worth asking. Where a question cannot be answered read-only, it names in the report exactly what access or action would answer it. It answers the question asked, at the depth asked, and maps incrementally — letting the accumulated answers become the system map rather than reading everything before rendering anything.

**Definitions.** 1: the role's session kind and where its report goes.

**Shares rules with.** docs/global-context/core.md (C028); engagements/sre/system-discovery.md (C028); skills/directive-invariants.md (C028).

**Divergent.** none.

**Retiring.** none.

**Proposal.** merge into `system-discovery`. Reason: eleven distinct rules that state the same read-only, provenance-carrying, incremental posture the system-discovery skill states as a procedure; the role and its one procedure have no independent existence in the register.

### 42. engagements/sre/system-discovery.md — 13 rows → 12 distinct → 12 after retirement

**What it obliges.** The Cartographer's discovery procedure, run in an execution session, returning the question list and the rendered map to the Assistant. It inventories the sources first — repositories, workflow files, Terraform or equivalent, container image definitions, delivery pipelines, dashboards, runbooks, and any statements client humans have made — listing what exists before reading anything deeply. It then reconstructs the pipeline under study from trigger to ready: what event starts it, what stages execute, in what order, on what infrastructure, with what dependencies, citing the file and line that establishes each link and tagging every claim with its provenance. The unknowns become the question list for the Assistant's triage. For each stage it identifies where a timestamp would come from, preferring measurement that requires no changes, then measurement requiring only PR-able changes, and flagging anything needing write access. It renders the map in the stated shape and place, linking out to everything the map does not carry, and outputs the map plus the unknowns list ready for triage. Where later evidence corrects the map it amends it, noting what earlier belief each amendment replaced. The map does not editorialize.

**Definitions.** 0: none.

**Shares rules with.** docs/global-context/core.md (C028); engagements/cartographer.md (C028); skills/directive-invariants.md (C028).

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `system-discovery`. Reason: nothing retires, and the source inventory, the timestamp-source step and the amend-with-what-it-replaced rule are the concrete procedure the Cartographer role only gestures at.

### 43. engagements/sre/engagement-change-package.md — 12 rows → 12 distinct → 12 after retirement

**What it obliges.** The one-screen package an engagement Implementer produces in an execution session; the Assistant and the Critic read it and neither produces one. It states intent in one paragraph, citing the Improvement Proposal that Dave gave his go to, or the directive it derives from. It states blast radius as three things: what it touches, the worst plausible outcome, and the rollback path. It states the expected delta as the pre-stated measurement claim, citing the baseline it rests on or the logged override that waived it. It carries the pull request link as the diff. It states what was verified, using the evidence classes precisely, and what remains unverified. Where a Critic read was requested it attaches the verdict verbatim. It states what should happen next and any points needing Dave's decision. Overrides go in the package itself and not only in the log, and where the change contradicts the System Map the map correction goes in the package too.

**Definitions.** 0: none.

**Shares rules with.** engagements/sre/implementer.md (C119, C120).

**Divergent.**

- **C119** — This topic (R0364) requires the rollback path as part of the blast radius; the Implementer's own role document (R0379) stops at what it touches and the worst plausible outcome. An Implementer following its role document alone omits the rollback path — this topic is the complete side.

**Retiring.** none.

**Proposal.** merge into `engagement-implementer`. Reason: twelve distinct rules describing the artifact the Implementer produces, two of which already cluster with the Implementer's own rows and one of which contradicts it — a contradiction that a single topic cannot hold.

### 44. engagements/sre/implementer.md — 12 rows → 12 distinct → 12 after retirement

**What it obliges.** The engagement Implementer runs as an execution session and builds what an Improvement Proposal that Dave has given his go to specifies. It may start only when three things hold: the change derives from such a proposal or a direct Dave directive; a baseline covering the affected stage exists and the expected delta is stated, or Dave has overridden the baseline-gate and it is logged; and acceptance criteria are written, even if brief. It implements the smallest change that satisfies the criteria, verifies as far as read-only access allows and states the evidence class precisely, assembles the engagement change package, and declares its blast radius honestly. Where something discovered mid-implementation contradicts the System Map, it surfaces the contradiction as a map correction and escalates before proceeding. It does not perform the apply — the diff is reviewed by the client's own pull-request gate — and it neither requests a Critic read nor reviews its own diff.

**Definitions.** 2: the role's session kind and what it builds from, and the diff being reviewed by the client's pull-request gate.

**Shares rules with.** engagements/sre/baseline-measurement.md (C118); engagements/sre/engagement-change-package.md (C119, C120); operating-model.md (C065, C121).

**Divergent.**

- **C119** — See the change-package card. This topic (R0379) omits the rollback path from blast radius where the change package requires it — this is the incomplete side.
- **C121** — This topic (R0375) accepts acceptance criteria that are brief; the operating model (R0482) requires them explicit and written. Work clearing this bar can fail the operating model's.

**Retiring.** none.

**Proposal.** keep as topic `engagement-implementer`. Reason: nothing retires; it is the natural home for the change-package rules proposed for merging into it, and its three start conditions are the engagement pack's actual gate.

### 45. policies/project-setup-requirements.md — 12 rows → 11 distinct → 6 after retirement

**What it obliges.** Most of this topic is machinery that retires. What it says is what must be true about a repository before the methodology governs work in it, confirmed by a human at adoption because most of it lives outside git — in the forge's configuration, in local hook state, or in tooling — and so is written down instead of enforced. Two things survive. The default branch is protected: no force-push, no branch deletion, changes landing only via pull request, and bypass disallowed including for administrators — which is the structural gate that makes agents pushing and merging safe at all, since history on the default branch cannot be rewritten or destroyed whoever holds the credential. And that protection is confirmed by a human at adoption, because it cannot be verified from inside the repository. The rest — running the frontmatter check at commit time, the in-scope set, adopting the metadata schema, per-clone hook installation, and the requirement that `reviews/expedited-log.md` exist — goes with the machinery.

**Definitions.** 4: what this policy names and does not, why preconditions live outside git, branch protection as the structural gate, and branch protection being unverifiable from the repo and confirmed at adoption.

**Shares rules with.** policies/commit-and-change-control-policy.md (C069); policies/remote-write-verification-policy.md (C144).

**Divergent.** none.

**Retiring.** 5 rows: the frontmatter check at commit time, the in-scope set by reference, adopting the metadata schema with per-project hooks, hook installation as local per-clone state, and `reviews/expedited-log.md` existing.

**Proposal.** merge into `commit-control`. Reason: six surviving distinct rules, of which the branch-protection requirement already clusters with the commit-and-change-control policy's own force-push rules — a topic this small should not stand alone.

### 46. engagements/sre/override-log-policy.md — 11 rows → 11 distinct → 11 after retirement

**What it obliges.** Every ceremonial element of the engagement pack is trivially overridable by Dave, and this says what happens when he does. He states unambiguously that he is overriding, with no justification required in the moment; the agent logs the override and proceeds, and does not argue, re-litigate, or require ceremony around the override itself. Nobody but Dave may override — not the client's humans, not the agent — and the agent may not waive its own stop-and-ask by its own judgment; where Dave waives one, that waiver is logged as an override. The log is append-only, kept in the engagement working area as a record with no fixed filename created on first use, one line per override giving the date, what was overridden, one-line context, and the session or pull request reference. It is reviewed at the engagement review, which Dave runs at the end of the engagement and the Assistant schedules.

**Definitions.** 2: the policy's session scope within an engagement, and every ceremonial element being overridable by Dave.

**Shares rules with.** context-sets/spec-and-change-discipline.md (C004); engagements/sre/README.md (C004); engagements/working-with-dave.md (C004, C122); operating-model.md (C004); policies/commit-and-change-control-policy.md (C004); policies/decision-log-policy.md (C036); policies/document-metadata-policy.md (C036); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004); policies/verification-boundary-policy.md (C004); skills/command-blocks.md (C004).

**Divergent.**

- **C004** — This topic (R0384) restricts scope to work within an engagement rather than to a session kind — one of the three engagement rows that make this cluster divergent.

**Retiring.** none.

**Proposal.** merge into `engagement-frame`. Reason: eleven distinct rules governing one record inside the engagement pack, whose shape the engagement frame already defines as a record with no fixed filename.

### 47. roles/reviewer-agent.md — 11 rows → 11 distinct → 11 after retirement

**What it obliges.** The Reviewer runs in an execution session and reviews the entire change as a single pass — all files, the test plan, the diff and the boundary updates together, not file by file. It reviews for correctness, checks consistency with the spec, checks maintainability, examines the tests for meaningful coverage, identifies unnecessary complexity, and confirms the documentation is updated where needed. It states what was checked and, explicitly, what was not. It may not submit a review that only says looks good, tests pass, implementation matches spec, or no issues found. It does not review spec documents — those are the Spec Reviewer's — which makes it the quality gate on implementation specifically, distinct from the Skeptic/Risk Agent's pass over the evidence chain, and the corpus is explicit that a change can pass one and fail the other.

**Definitions.** 1: the role's session kind.

**Shares rules with.** operating-model.md (C126); roles/context-quality-reviewer.md (C011); roles/copy-editor.md (C011); roles/critic.md (C011); roles/skeptic-risk-agent.md (C011); roles/spec-reviewer-agent.md (C011); skills/evidence-review.md (C011); skills/spec-review-cycle.md (C011); skills/test-plan-review.md (C175).

**Divergent.**

- **C175** — See the test-plan-review card. This topic (R1052) asks only for unnecessary complexity where the test plan review skill asks for over-testing as well; a Reviewer reading only this document does not flag over-testing.

**Retiring.** none.

**Proposal.** keep as topic `reviewer`. Reason: nothing retires, and the single-pass rule and the ban on empty reviews are stated only here — though it is the thinnest role document in the corpus and worth reading beside `test-plan-review` when that merge is decided.

### 48. context-sets/production-grade-software.md — 10 rows → 5 distinct → 5 after retirement

**What it obliges.** A short context set that says what "production-grade" obliges an execution session to consider. It names fifteen attributes a change is assessed against — correctness, usability, performance, accessibility, security, privacy, reliability, SLO compliance and error budget health, operability, observability, recoverability, maintainability, cost and quota exposure, dependency risk, and failure modes — while saying plainly that not every change requires deep treatment of every attribute, so the session explicitly decides which are relevant rather than either skipping the question or answering all fifteen. It names the kinds of evidence that support a production-grade claim: passing unit and integration tests, live and browser smoke tests, contract checks, static analysis, lint and type checks, a manual verification checklist, screenshots or traces, a telemetry or monitoring plan, and a rollback or mitigation plan. And it fixes six questions every meaningful change answers: what happens if it fails, how the failure would be known and whether it burns error budget on a Top K journey, what the user would see, whether the system can recover, whether Dave can debug or roll back, and whether the failure is acceptable.

**Definitions.** 1: that not every change requires deep treatment of every attribute.

**Shares rules with.** specs/trd-template.md (C013).

**Divergent.** none.

**Retiring.** none.

**Proposal.** merge into `verification`. Reason: five distinct rules, of which the six failure questions already cluster with the TRD template's system-level failure section — the attribute list is a checklist that belongs with the testing and verification context an execution session already loads.

### 49. engagements/working-with-dave.md — 10 rows → 10 distinct → 10 after retirement

**What it obliges.** A short brief for engagement sessions on who they are working with and where the hard lines are. Dave is a thirty-year infrastructure and SRE veteran and former director, onsite on a client engagement, hands on the client's keyboard, under time pressure — which sets the register and the expected density. It then defines four infrastructure-specific verification words that the corpus's general evidence classes do not cover: plan-verified means a dry run shows the intended delta and nothing else; apply-verified means the change was applied somewhere real and the resources exist as intended; serving-verified means the resulting system demonstrably does its job rather than merely existing; and delta-verified means a post-change measurement shows the expected improvement against a pre-change one. The obligation attached is that you never treat plan output as proof of apply behavior, or apply success as proof the system serves. And the client guardrail: the agent does not write to the client's cloud and systems — it produces what Dave or the client's own CI runs rather than moving the levers itself — with any override of that guardrail logged, the log entry being the record that write access was granted.

**Definitions.** 6: who Dave is in this context, and the four infrastructure verification words plus their scope.

**Shares rules with.** context-sets/spec-and-change-discipline.md (C004); context-sets/testing-and-verification.md (C001); docs/global-context/core.md (C001); engagements/sre-critic.md (C001); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004, C122); operating-model.md (C001, C004); policies/commit-and-change-control-policy.md (C004); policies/release-readiness-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004); policies/verification-boundary-policy.md (C001, C004); public-prose-criteria.md (C066); roles/critic.md (C066); roles/skeptic-risk-agent.md (C001); skills/command-blocks.md (C004).

**Divergent.**

- **C004** — This topic (R0429) restricts scope to work within an engagement rather than to a session kind — the third of the three engagement rows that make this cluster divergent.

**Retiring.** none.

**Proposal.** merge into `engagement-frame`. Reason: ten distinct rules, four of which are verification vocabulary that belongs beside the corpus's other evidence classes and the rest of which are engagement posture the frame already carries.

### 50. roles/context-quality-reviewer.md — 7 rows → 7 distinct → 6 after retirement

**What it obliges.** This role applies the review rubric. It runs as an execution session and returns a review artifact in the review artifact schema's shape, inspecting each document in scope whole against every criterion of the rubric, and deciding a verdict plus a set of findings — each stating what fails, where, and what would fix it, and citing the rubric criterion it fails. It does not gate documents under `specs/`, which are the Spec Reviewer's, and it does not agree a document: that is Dave's. It also may not assess whether the methodology is correct as a matter of engineering judgment — its subject is whether a governed file is well-formed as instruction, not whether the instruction is right. Under DEC-000380 this role becomes the intake gate on a proposed rule, which makes it the one gate the rebuilt corpus keeps, so what it is forbidden to decide matters more after retirement than before.

**Definitions.** 0: none.

**Shares rules with.** docs/global-context/core.md (C035); docs/global-context/review-rubric.md (C111, C112); roles/copy-editor.md (C011); roles/critic.md (C011, C035); roles/reviewer-agent.md (C011); roles/skeptic-risk-agent.md (C011); roles/spec-reviewer-agent.md (C011); skills/evidence-review.md (C011); skills/spec-review-cycle.md (C011).

**Divergent.** none.

**Retiring.** 1 row: R0945, defining its scope as the frontmatter in-scope set enumerated in the document metadata policy.

**Proposal.** keep as topic `context-quality-reviewer`. Reason: DEC-000380 makes this the one gate on a rule entering the store, so the role survives with a new scope — and the retiring row is precisely the scope definition that must be replaced.

### 51. roles/coder-agent.md — 6 rows → 6 distinct → 6 after retirement

**What it obliges.** The shortest role document in the corpus. The Coder runs in an execution session and implements changes according to the spec and the test plan. It keeps changes small and coherent, preserves existing behavior unless asked to change it, and does not remove meaningful coverage without explanation. Its one hard stop is the red-gate handoff: where the Test Designer's red-gate confirmation is absent, or shows the tests failing only on an absent import rather than on bad logic, the Coder flags it rather than proceeding. That single rule is what makes the red-gate a gate at all — every other statement of it in the corpus says the Test Designer must run it; this is the one that says the next agent must refuse to proceed without it.

**Definitions.** 1: the role's session kind.

**Shares rules with.** none.

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `coder`. Reason: six rules, none shared with any other file, and one of them — refusing to proceed on an absent or import-only red-gate — is the only enforcement the red-gate has.

### 52. boundaries/human-review-boundary.md — 5 rows → 5 distinct → 5 after retirement

**What it obliges.** Five rows stating what the methodology's central bet does and does not mean. The boundary removes human diff-reading as the default, not review itself: code review still happens, performed by the Reviewer and Skeptic/Risk agent roles, and its output feeds the recommendations humans decide on. Escalation to human code review is the exception path — the rare case where a human chooses to read code directly — not the ordinary route. And human code review is a separate axis from the release gate: the release go/no-go is an evidence-and-judgment decision, not a code-reading decision, so a change can need a human go without anyone reading its diff, and a change can warrant a human reading its diff without needing a release go. It is loaded by decision sessions and shares no rule with any other file in the corpus.

**Definitions.** 4: the boundary removing the default rather than review itself, escalation as the exception path, human review as a separate axis from the release gate, and the independence of the two.

**Shares rules with.** none.

**Divergent.** none.

**Retiring.** none.

**Proposal.** keep as topic `human-review-boundary`. Reason: five rows, none shared, stating the premise every other topic assumes — that agents review and humans judge — which is exactly the kind of statement that should survive as its own rule rather than being folded into a larger file.

### 53. engagements/assistant.md — 5 rows → 5 distinct → 5 after retirement

**What it obliges.** The Assistant runs as a decision session and receives the decision layer. Its obligations are few. When a piece of work is about to leave the building, it offers a Critic pass. It keeps a loose-end record — in the shape and place the engagement frame states for a record with no fixed filename — and puts quiet notes in it: smells, risks, and cleanup it noticed that are not today's problem. Those notes accumulate rather than interrupting: it surfaces one only when asked or when it becomes today's problem, and writes the new ones up at session end. That accumulate-and-hold discipline is the whole of the role as the register captured it; everything else the Assistant does in the engagement plays is stated in those plays rather than here.

**Definitions.** 1: the role's session kind and that it receives the decision layer.

**Shares rules with.** none.

**Divergent.** none.

**Retiring.** none.

**Proposal.** merge into `engagement-frame`. Reason: five rules, none shared, and three of them describe a record whose shape the engagement frame already defines — the role has no standing rules independent of the plays that direct it.

### 54. policies/release-readiness-policy.md — 4 rows → 4 distinct → 4 after retirement

**What it obliges.** Four rows, all definitions, all pointing elsewhere. A change is release-ready when the evidence for it exists and is stated, and every known gap carries a release-impact label of deferred, accepted-risk, or not-material. A gap labelled blocking means the change is not release-ready. The release decision itself is gated by the commit and change control policy. It is the smallest topic in the register and it states no obligation of its own — it fixes the meaning of "release-ready" so the other topics can use the term, and hands both the labels and the gate to files that define them.

**Definitions.** 4: the policy's session scope, when a change is release-ready, what a blocking gap means, and where the release decision is gated.

**Shares rules with.** LEXICON.md (C053); context-sets/spec-and-change-discipline.md (C004); engagements/sre/README.md (C004); engagements/sre/override-log-policy.md (C004); engagements/working-with-dave.md (C004); operating-model.md (C004); policies/commit-and-change-control-policy.md (C004); policies/remote-write-verification-policy.md (C004); policies/source-of-truth-policy.md (C004); policies/verification-boundary-policy.md (C004); roles/skeptic-risk-agent.md (C053); skills/command-blocks.md (C004).

**Divergent.**

- **C004** — This topic (R0715) puts both session kinds in scope, the majority position; it is a member of the cluster rather than a source of its divergence.

**Retiring.** none.

**Proposal.** merge into `release-readiness` under `change-control`. Reason: four rows, no obligations, and every term it defines is owned by another topic — it exists to hold a definition that the commit-and-change-control topic should carry.

## What the cards do not settle

Each of these is Dave's to rule. None was decided while writing the digest.

1. **Do definitions become rows, or do they live only in process prose?** 350 rows across the corpus carry verb `define`, 312 of them surviving retirement; LEXICON alone contributes 36 survivors and is almost nothing else. If a definition is not a row, the lexicon, the human-review boundary, and the release-readiness policy have almost no content left. Affects cards 1, 2, 4, 5, 11, 15, 22, 46, 51, 53, 54 most, and every card's Definitions line.
2. **Which merged topics need a new name?** Eight merges are proposed into a topic whose current name does not cover the merged content: `test-designer` absorbing the test-plan review (cards 30, 39), `skeptic` absorbing both the engagement Critic and evidence review (cards 14, 31, 40), `verification-boundaries` absorbing the boundary audit (cards 19, 38), `verification` absorbing production-grade software (cards 7, 48), `system-discovery` absorbing the Cartographer (cards 41, 42), `engagement-implementer` absorbing the change package (cards 43, 44), `engagement-frame` absorbing the override log, working-with-dave, and the Assistant (cards 32, 46, 49, 53), and `commit-control` absorbing project setup (cards 11, 45). Cards 25 and 37 also propose renames — `review-artifact-schema` and `context-quality-criteria` — that are naming decisions rather than merges.
3. **C002 — what does a document's review pointer cite once the flip is gone?** The cluster's divergence was between two `last-reviewed` forms, both of which retire. The rule that survives underneath is that a document names the artifact that reviewed it and the SHA reviewed; whether a row records that, and where, is unresolved. Affects cards 1, 2, 3, 4, 13.
4. **C004 — is a rule's scope a session kind, an engagement, or neither?** Eleven topics declare a scope; three of them scope to an engagement rather than to a session kind, one widens to any tool-mediated remote mutation, and one narrows a single section. Under a rules store the declaration becomes a key on each row, and no proposal here can say what its values are. Affects cards 2, 11, 12, 18, 23, 28, 32, 46, 49, 54.
5. **C017 and C024 — what may be done against a spec before Dave agrees it?** Three files give `converging` three different spans, and four files give three different answers to whether testing is barred before agreement. The Test Designer is told to write tests then; the metadata policy forbids it. The digest could not propose a resolution because the answer determines what the convergence protocol is, not just how it is worded. Affects cards 1, 2, 3, 4, 23, 30.
6. **C025 — which gaps carry a release-impact label?** Material gaps, unresolved gaps, or gaps the test plan leaves open. Three triggers, five topics, and no basis in the corpus for choosing. Affects cards 4, 14, 19, 30, 38.
7. **C019 — how precise is a generated filename's timestamp?** Core requires ISO 8601 basic, directive authoring adds date and time, the retro skill adds UTC and `Z`. One rule with three precisions; the strictest satisfies all three, but that is a ruling, not a reading. Affects cards 5, 15, 25, 27.
8. **C097 — may a Test Designer edit a spec when told to?** Its own document forbids it outright; the context set forbids it only when undirected. Affects cards 23, 30.
9. **C121 and C119 — do the engagement roles hold to the operating model's bar?** Acceptance criteria "brief" versus "explicit and written", and blast radius with or without the rollback path. Both are engagement-versus-methodology divergences, which may be intentional. Affects cards 2, 43, 44.
10. **C153 — on a readability-versus-discoverability conflict, does the Copy Editor change the text or report it?** The Criteria say readability wins; the Copy Editor says report and leave it. Affects cards 8, 9.
11. **C125 and C206 — two files each state one rule twice with different fields.** The operating model names its primary controls twice with different lists; the TRD template states the durable-boundary declaration twice with different required fields. Which statement is the rule is a ruling. Affects cards 2, 17.
12. **C046 — must every command block state its blast radius, or only a destructive one, and must its expected output have been observed?** Three statements of one rule inside one file. Affects card 12.
13. **C020 — is a continuity scan mandatory on every spec revision or available on demand?** The policy says mandatory, the role document says on demand at any depth. Affects cards 13, 28.
14. **C175 — does the Reviewer flag over-testing?** Its role document says complexity only; the test plan review skill says over-testing too. Affects cards 39, 47.
15. **What replaces the Context Quality Reviewer's scope?** DEC-000380 makes it the one gate on a rule entering the store, and the single row defining its scope is the one row of that topic that retires. Affects card 51.
16. **Does `roles/reviewer-agent.md` earn its place?** Eleven rows, three of them shared, and the substantive checks overlap the test plan review skill and the Skeptic. The digest proposes keeping it but flags that the rubric's own earn-your-place criterion would put the question to Dave. Affects cards 39, 47.

