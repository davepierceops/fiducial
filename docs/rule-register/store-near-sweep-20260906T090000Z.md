# Cross-topic sweep — store fix pass 5 item 11

Derived artifact. Directive: `docs/cycles/store-fix-5-20260906T090000Z.md`, branch `store-fix-5`. The mechanical
input is `docs/rule-register/store-near-candidates-20260906T090000Z.md`, generated by
`docs/rule-register/store-near-20260906T070000Z.py` — the pass-4 script, reused unchanged, with only its output
path redirected — over the 456 rows the store held after item 10.

The test applied to each near-pair is the one store fix pass 4 item 9 applied, the intake criterion R0264
carries: *contributes something no rule already in its bundles does*. A pair retires one row only where both
hold — the two rows state one obligation, **and** some role's query loads both. Where the two rows sit in
different bundles, no bundle sees the repetition and both stay; where they state different obligations, both
stay.

**Pairs emitted: 31. Rows retired: 0. Pairs kept both: 31.**

Nothing retires. Pass 4 ran this test over the store's 513 rows and retired the sixteen duplicates it found;
this pass's own items removed a further fifty-seven rows by merge and ruling. What the second run over 456 rows
finds is the residue pass 4 already adjudicated — thirty of its thirty-three kept pairs — plus one pair that did
not exist until item 8 wrote R1603. No pair fails the criterion.

## Retired, with the survivor

None.

## Kept both

**Loaded together, but different obligations (9 pairs).**

| pair | why both stay |
|---|---|
| R1029 / R1176 | each names its own role's tier: the Critic's and the Writer's |
| R0110 / R0456 | the boundary of a claim is not the class of a claim |
| R0819 / R0985 | the Criteria state what the prose must be; the Copy Editor's row states the pass that finds where it is not |
| R0245 / R1603 | when a decision is appended is not how many decisions an entry carries — the only pair new to this run, R1603 being written by item 8 |
| R0818 / R0971 | the same, for speech introduced by a bare colon: the criterion against the correction pass |
| R0846 / R0977 | the same, for searchable titles: the criterion against the discoverability pass |
| R0849 / R0970 | the same, for venue-independent prose against the formatting correction |
| R0181 / R0872 | a false positive of the normalizer — reading the repository before asserting its state has nothing to do with owning a TRD section |
| R0499 / R0573 | flags and canaries against where the go/no-go attaches |

**No role's query loads both (22 pairs).** The Copy Editor, the Critic and the Writer carry parallel rows with
near-identical text, and each role loads only its own: R0954/R1002, R0954/R1162, R1002/R1162, R0957/R1006,
R0955/R1004, R0991/R1023, R0975/R1019, R1001/R1161, R0953/R1001, R0953/R1161, R0997/R1027, R0997/R1174,
R1027/R1174, R0998/R1028, R0956/R1005, R0990/R1021, R0988/R1022. Also R0505/R0555, R0505/R0551 and
R0551/R1039 — `release-manager-agent` against `change-control`, disjoint role lists; R0091/R1156, the C097
mirror pair, deliberately two rows for the spec-editing session and the Test Designer; and R0017/R1153, whose
role lists no longer intersect.

## What moved since pass 4's run

Pass 4 emitted 55 pairs; this run emits 31. Twenty-two of pass 4's pairs had one row in its retired column and
cannot recur. Two more fall below the thresholds because pass 4's own item 9 edited a body after generating its
shortlist: R1101/R1468 (jaccard 0.364 then, 0.343 now — R1468 took R0015's "whatever its verdict" clause) and
R1152/R1468 (R1152 retired into R1468). Pairs involving rows this pass retired — R0482/R1533, R0017/R0471,
R0110/R0787 among them — are gone with those rows. One pair is new: R0245/R1603.
