"""RED-GATE STUB leftover. Deliberately wrong: a module the contract does not name.

The interface contract names exactly `store.py`, `query.py`, `terms.py`,
`keys.py`, `near.py` and `render.py`. This file exists so that
`bin/tests/test_rulestore_boundary.py`'s package-contents assertion has
something to red on. The Coder deletes it.
"""

from __future__ import annotations


def carried_over():
    """Nothing calls this."""
    return None
