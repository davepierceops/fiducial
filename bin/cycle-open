#!/usr/bin/env python3
"""Open a spec-review cycle: directive skeleton + reviewed-revision bundle.

Contract: `specs/directive-tooling-trd.md` §3.9 step 4. A forwarding
executable: argv passed through unchanged to `bin/directive`'s entry point,
`aimeta.directive.main`. No parsing and no skeleton emission of its own.
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from aimeta import cli, directive  # noqa: E402

if __name__ == "__main__":
    sys.exit(cli.run(directive.main))
