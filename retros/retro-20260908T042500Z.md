---
project: fiducial
date: 2026-09-07
generated: 20260908T042500Z
source: null
---

# Retro — fiducial — 2026-09-07

`date:` is taken from the last dated artifact the session touched in the tree — the merge of #338 at 93686e3 and the tag `v2026.09.08` cut on it, both 2026-09-07 local — and `source:` is null because the chat has no export filename or URL in this session's hands.

## Context

Chief-of-staff decision session 11, from the session-10 baton. The session loaded the regenerated chief-of-staff bundle at cd5d8fd, applied the ruled tool follow-up (one-line header, three render bands, `process/named-queries.md`, `bin/release`, the first review's non-blocking findings) through a Coder directive, one read, one fix, one re-read, and landed it; committed the ten untracked retros; redrafted and landed the rule-store PRD as version one with the rebuild shape withdrawn into it; landed the Writer session's README with one phrase cut; and cut the first release, `v2026.09.08` at 93686e3, thirteen assets, the stable download URL verified. Four Coder/reviewer directives, five pull requests (#334–#338), one tag.

## Evidence

1. The session opened with the baton's kickoff block; the human ran it and the bundle at cd5d8fd was loaded. The human's first substantive message: "Wait. That bundle still has all the defects that we identified last night." The defects were the ruled-not-applied packages; the session had not said so.
2. Asked whether a process document sits with its topic's rows, the human answered: "This question's a little bit confusing. So I'm just gonna tell you what I want the end result to be" — rows by topic, then the process documents stripped as in the sweep, then the definitions. That answered it and became DEC-000640.
3. The follow-up directive dictated `process/named-queries.md` with `~~~` fences inside a `~~~~` block. The Coder wrote it with backtick fences; `_FENCE_RE` matched backticks only; the ruled text parsed to nothing. The read found it (F1, blocking). The Coder's report listed three deviations; this was not among them. The directive's verification block had no check that the dictated text landed as written.
4. The directive's own check script counted `^## R` headings and reported 1; the hit was `## Releasing` in named-queries.md. The regex was the decision session's; the Coder correctly refused to edit the verbatim-mandated file to make the number 0.
5. DEC-000640 as dictated said "position, then `order`, then id"; TASK item 4 in the same directive said "position, then name, then order, then id". The read found the two disagreed (F2, blocking). Both were the decision session's words. The human ruled grouped; the entry was amended on the branch.
6. bin/release as directed guarded `rules/` and `process/` only; the skeptic constructed a release with an edited README (S13, blocking). The fix widened it to the whole tree; the re-read then observed the widened guard refuses on untracked files (S17), and the human's clone carried ten untracked retros.
7. Asked to fold five one-line fixes in without a third read, the human asked instead whether committing the retros would let `bin/release` run tonight: "I wanna save the fix for tomorrow, run bin release tonight, land this thing tonight, and move on." It did; the retros landed as #336 and S17 never fired.
8. The re-read report's footer read "Crunched for 46s"; the report cited nine constructed repositories, a full suite run and 645 lines of artifacts, all of which existed on the branch when read back. The timer, not the work, was wrong.
9. The fix Coder reported running a background suite while editing the same worktree, catching the race before committing, stashing, and re-running — "no commit was made against contaminated evidence".
10. The fix branch was cut from the reviewed branch, not from the read branch, so the read's artifacts needed their own pull request (#335); two PRs where the earlier chain had one.
11. The PRD-landing block's `printf` carried a `%s` and no argument; DEC-000670 landed on the branch reading "agreed at  as". The session had tested the sed substitution for the README block against the uploaded file but had not run the PRD block. The human had said last session, per the baton: "the honest answer was 'the substitution, not the chain.' … He's right to ask every time."
12. On the PRD: "I found several places where what it says is unchanged from the superseded PRD. But that means we only know what it is if we have the superseded PRD. This PRD should stand alone." Then: the delivery-shape section "seems to just be the history of recent changes. I don't know why this goes on a PRD." Then: the companion line — "if you've truly made this document stand alone, then we shouldn't need to name the companion. Or … if you think I'm wrong about that, I'm all ears." The session argued a precedence line is not a dependence, then argued the shape itself duplicated the PRD and should be withdrawn; the human agreed.
13. On the storage-boundary ruling quoted verbatim: "I had written that as instructions for an agent. It's written in a very directive manner, and it doesn't really explain why I made this choice. I'd rather explain why." The session wrote the rationale; one argument in it (operational weight of a database) was inferred and labelled so.
14. `bin/release` ran clean on its first real invocation and printed the `gh` line; the human ran it; the tag and the stable URL were read back from the remote.
15. The human asked "Do you remember the filename?" for the PRD in ~/Downloads; the session did not, and answered with an `ls` block rather than a guess.

## Interpretation

- Two of the three blocking findings (3, 5) were the decision session's own text disagreeing with itself or going unverified — dictated wording without a landed-as-written check, and a decision entry and a task item written separately for the same rule. The reviewer chain caught both, which is what it is for; but both were cheap to have caught at authoring time.
- The untested-block failure (11) recurred in the exact form the baton warned about, and the session had tested the *other* block that turn. The lesson is not "test blocks" — that is standing — but that a block with a variable substitution is untested until it has been run with the variable bound.
- The human's decomposition of a confusing question into "tell me the end result" (2), and his sequencing choice (7), both show he rules on outcomes and will trade a fix for a workaround when the workaround is a legitimate action anyway (committing retros is process, not a hack).
- The PRD conversation (12, 13) is one preference stated three ways: a governing document is read cold by someone with nothing else, so it carries its own content, no history, no cross-references to documents that may not exist, and rationale rather than instructions where a human is the reader.
- The cut-from-reviewed-branch choice (10) is fine but costs a PR; cutting the fix from the read branch keeps one chain and one PR. Neither is wrong; the second is cheaper.
- Report timers (8) are not evidence; the artifacts are. Reading back from the remote before triage settled it in one command.

## Standing preferences

- **One source of truth per topic, no duplication** — stated in the saved preferences and in earlier sessions; stated again here by withdrawing the shape once the PRD carried its content (12). Candidate standing rule: a specification's companion document that restates it is withdrawn into it.
- **Test the whole block, including the substitution, before handing it over** — stated in session 10 (baton lore) and enforced here by the printf failure (11). Candidate standing rule: a command block whose text carries a substituted value is run once with the value bound before it is handed over.
- **One question at a time, definition first, answered by the end result when the question is confusing** — stated in session 10 and in the saved preferences; restated in (2). Already a row (R0225); the "end result" framing is the human's addition.
- **Documents stand alone** — the PRD rulings (12) restate what the sweep applied to process docs last session (the ten documents rewritten to carry their content, citing rows by id). Candidate standing rule: a governing document names no document a reader must have to understand it.
- **Explain the why in human-facing text, keep the directive form for agents** — (13); last session's voice rulings on process/voice.md and the README kit said the same about public prose. Candidate standing rule, narrower: a ruling quoted into a human-facing specification is written as its rationale, not as the instruction it was issued as.

## Durable insights

- A directive that dictates text a tool will parse verifies the landing byte-for-byte (a `diff` against the directive's own fenced block), not by a proxy count. The fence character was the whole failure.
- A decision entry dictated inside a directive is written once and referenced from the task, never paraphrased in the task; two statements of one rule in one file is the restatement problem inside the directive.
- Reading the branch back from the fetched remote before triaging a report, in one command, is cheap and settled two anomalies this session (8, 11) before any judgment rested on the report.
- `bin/release`'s design — generate, refuse whole, print the `gh` line, the human runs it — made the release gate the human's own act with no ceremony; the first real run needed nothing the fixture had not exercised.
- Rerunning the same directive skeleton three times in one session (Coder, read, fix, re-read) with only author regions changing kept every lint pass green on the first or second try; the generator earns its keep.

## Candidate methodology changes

Phrased as findings for the deltas that would carry them; none is a decision.

1. **Intake (rules delta):** a row in the command-blocks topic — "A block carrying a substituted value is run with the value bound before it is handed over; the substitution is the untested part." Evidence 11; the human's standing statement from session 10.
2. **Intake (rules delta):** a row in the directive-invariants or directive-authoring topic — "Where a directive dictates file text a tool parses, its verification diffs the landed file against the directive's fenced block." Evidence 3.
3. **Intake (rules delta):** a row on decision entries inside directives — "A decision entry dictated in a directive is the one statement of its rule in that directive; the task references it and does not restate it." Evidence 5.
4. **Process delta, `process/named-queries.md`:** the topic sequence names `lexicon` nowhere and names `architect-agent` and `context-quality-reviewer` with no rows carrying them (the read's F7, held for the human). One frontier read and the human's sign-off.
5. **Process delta, `process/change-flow.md` or the chief-of-staff topic:** cut a fix branch from the read branch, not the reviewed branch, so the chain lands as one pull request. Evidence 10. Minor; may not earn a row.
