---
project: fiducial
date: 2026-09-10
generated: 20260910T223000Z
source: claude.ai Chief of Staff session, fiducial project, opened 2026-09-10 from the baton stamped 2026-09-10T03:20Z; no export filename
---

# Retro — fiducial — 2026-09-10

## Context

A successor decision session took the 2026-09-10T03:20Z baton and worked its three
owed items to completion: the flush package (eleven loose-end tracker entries and
DEC-000700–DEC-000720, #348 at `c1bb437`); the process delta retiring the Public
Prose Criteria as a document from `process/outline.md` and `process/voice.md`,
its frontier read, and the human's sign-off as DEC-000730 (#349 at `351e597`);
and the landing of the prior session's untracked retro (#350 at `1c0299c`). Three
directives were issued, all to fresh sessions; two command blocks ran on the
human's clone; the GitHub connector's merge tool failed every time it was tried.

## Evidence

1. On invocation the session assessed state through the connector's read tools
   (commits, open pull requests) but could not load an issue-list tool, and
   said so: `human-gate` issues were reported as not checked.
2. The session cloned the public repo into its own sandbox and ran
   `bin/directive` and `bin/check-directive` there. The first lint run failed
   M2 with every companion cited at main's SHA; citing each companion at its own
   last-touch SHA (`git log -1 --format=%H -- <path>`) cleared it. All three
   directives passed M1–M8 before emission.
3. The flush directive dictated rulings only — the three decision-log entries
   as agreed prose, the tracker items by intent — and said the executor
   derives IDs, forms and positions. The executor derived DEC-000700–000720
   from the log's rule, read the #343 merge SHA off `git log` when the
   directive said "read that SHA from the log", and flagged that four of the
   eleven tracker items had no review-artifact source, writing that into the
   entries rather than inventing a citation.
4. The flush directive told the executor to leave verification logs
   uncommitted in the worktree and to remove the worktree without force. The
   removal refused over the untracked logs; the executor stopped and reported
   without forcing. The human ran a cleanup block. Every later directive put
   its log outside the worktree, and both later removals succeeded.
5. `github:merge_pull_request` hung to the four-minute Desktop timeout three
   times on #348, with `origin/main` read back unchanged after each; the same
   connector's reads and `create_pull_request` worked throughout. The human
   merged #348, #349 and #350 in the browser. The human asked whether the
   Google and GitHub connectors share one connector; the session's answer —
   separate servers, one Desktop broker for local-MCP calls and approval
   prompts — was labelled inferred, not verified.
6. The session proposed replacing the retired document title in
   `process/outline.md` with a bracketed row citation. The human corrected it:
   bundles carry no row numbers, so the citation is useless to the reader.
   Plain wording was used instead. The session noted that `change-flow.md`,
   `decomposition.md`, `spec-test-suite.md` and `prd-template.md` already
   carry the same defect at scale.
7. The baton named three lines for the process delta; a sweep of both files
   found five (`voice.md:12` and `voice.md:55` in addition). The delta carried
   all five as four edits.
8. Two of the four target strings spanned wrap boundaries in the files. The
   directive said so in advance and told the executor to match a line break
   as a space; the executor landed 4/4 without a stop.
9. The frontier read confirmed co-rendering of R1604, R0821 and R1594 by
   generating the writer, copy-editor and critic bundles at the reviewed ref,
   bypassing `bin/bundle --where`'s sync gate to do it. Its F2 found that
   dropping "from the Criteria's claims taxonomy" left the four tier names
   in all three bundles with nothing stating what earns which — the retired
   wording had been a signpost to a definition, and removing it made the gap
   silent.
10. The read directive dictated an artifact filename for a two-document delta;
    `process/review-artifact.md` has no filename form for that case, and the
    executor reported that the directive's explicit path won.
11. The sign-off entry DEC-000730 and the landing of the prior session's retro
    were done by command blocks on the human's clone (scratch worktree, one
    commit, push, cleanup) rather than by directives; both ran clean first
    time and each was followed by a read-back from origin before the pull
    request was opened.
12. The prior session's retro, `retro-20260910T032500Z.md`, was found
    untracked among the human's `retros/` files, stamped five minutes after
    the baton; its frontmatter `date:` is 2026-09-10 (the session's last
    interaction, UTC) while the human and the commit message call it the
    2026-09-09 retro. The human said: "We should check in everything that
    isn't there also."
13. Every question the session asked was one question, and the human answered
    each with one word ("y", "yes", "merged") or a paste.

## Interpretation

- Dictating rulings and intent, not tree facts, produced short directives
  that executed cleanly and left the executor room to report the right
  deviations (3, 8). The one directive defect of the day was a tree-fact
  instruction — where a log file lives — contradicting another (4).
- The decision session running the generator and lint in its own sandbox
  removed a round trip per directive and caught M2 before the human saw the
  block (2). The per-file SHA convention is a fact about the lint, learned
  once.
- The connector is reliable for reads and for opening pull requests and
  unreliable for merges; three identical timeouts with no state change is a
  clean signal, and stopping after two would have saved eight minutes (5).
- A retired document's title was doing work as a pointer even where the rows
  had absorbed the obligations; removing the pointer without supplying the
  referent is a change the frontier read was right to flag and this session
  did not anticipate (9). The same class — pointers the bundle reader cannot
  follow — is what the human's correction in (6) named.
- Small, fully-dictated, record-only writes (a decision-log entry, landing an
  existing file) are cheaper as a command block on the human's clone than as
  a directive, and the read-back from origin gives the same evidence (11).
- The retro procedure's `date:` rule and the way the human refers to a retro
  disagree for a session that crosses midnight UTC; both are correct and both
  were stated (12).

## Standing preferences

Stated this session and stated before:

- Land every artifact left untracked on the clone as its own pull request
  ("check in everything that isn't there") — stated today (12) and acted on
  yesterday in #347. Candidate standing rule.

Carried by the baton as candidate rows and applied throughout this session,
not restated aloud by the human today (recorded so the count is honest, not
as a second statement):

- The decision session dictates intent and agreed prose, never tree facts.
- A precondition resolvable from the tree without judgment is resolved and
  reported, not stopped on.
- Route every directive to a fresh session.

## Durable insights

- Run `bin/directive` and `bin/check-directive` in the decision session's own
  sandbox clone; cite companions at their own last-touch SHA.
- A directive's verification capture path lives outside the worktree; the
  removal instruction then holds without force.
- After two consecutive merge-tool timeouts, read `origin/main` back and hand
  the merge to the human in the browser; do not spend a third.
- Before dropping a reference to a retired document, find what the reference
  pointed the reader at and confirm something in the same bundle still
  supplies it.
- Bracketed row IDs in process documents do not resolve for a bundle reader.

## Candidate methodology changes

Phrased as findings for the deltas that would carry them:

- For the directive invariants (a process delta, one frontier read): a
  directive names its verification capture path outside the worktree it
  assigns, and a worktree-removal instruction assumes nothing untracked is
  left behind.
- For `process/review-artifact.md` (a process delta): a filename form for a
  delta over more than one reviewed document — the descriptor alone, or the
  branch slug.
- For the human's ruling, then intake or a process delta: process documents
  cite rows by `[R####]` that bundle readers cannot resolve — either
  `bin/bundle` renders row IDs, or process documents stop citing by ID.
- For intake, before the next release cut (from the read's F1, F3, F4): R1015
  and R1171 still instruct against "the Criteria"; `README.md`'s role table
  names it.
- For the human's ruling (from the read's F2): the four claim tiers reach all
  three writing roles without semantics; the reviewer's cheaper fix is
  "carrying its claim strength" in `outline.md` and R0821 so R0038 pulls by
  term, with F5 and F6 riding along in the same process delta.
- For intake, one row each: the three candidate rows the baton carried, and
  the standing preference above.
- For `process/retro.md` (a process delta): state which of `date:` or the
  session's opening day names a retro in conversation, or accept that both
  do.
