---
project: fiducial
date: 2026-09-02
generated: 20260902T185726Z
source: Chief of Staff decision session opened from the 2026-08-31 retrospective/topic-walk baton
---

# Retro — fiducial — 2026-09-02

## Context

A Chief of Staff decision session spanning 2026-08-31 to 2026-09-02, opened
from the retrospective/topic-walk baton with a seven-item queue. It closed
the retro-skill cycle (five ruled changes, two gates, agreed), ran the first
end-to-end human reviews in the artifact pane on two documents (the
bundle-system PRD and the Decision Layer) and landed both through their
reviewer gates to agreed, ran the bundle-system PRD's Spec Reviewer cycle to
agreement, and flushed every ruling to the decision log and OPEN-ITEMS
twice. Fifteen pull requests, #270–#284. Along the way the directive tooling
met real use for the first time and the session produced two instances of
the same authoring defect.

## Evidence

1. The first directive after the tooling was canonised named an artifact path
   that already existed (`reviews/conversation-retro-cycle-1.md`); the
   executor stopped on the review-artifact skill's no-retrofit rule and
   preserved the finished review outside the tree. Dave: "extremely
   disappointing … the very first directive after all of that work."
2. The same defect recurred on the Decision Layer gate: the author ran
   `ls reviews/ | grep | sort | tail`, which lexically hid cycles 10–13
   behind 2/7/9, and dictated cycle 12. This time the executor's commit
   overwrote a 2026-08-23 record on its branch before stopping. Recovery
   restored the record byte-identical and re-landed as cycle 14.
3. `bin/check-directive` M2 rejected an invented companion SHA three times
   across the session — each time before handover.
4. A connector `create_or_update_file` write of a 500-word document, retyped
   from context, landed a one-word regression in a rule the write was not
   meant to touch; a precomputed target blob caught it, and a second write
   corrected it before merge. Two later flips were done by running
   `bin/flip-agreed` on Dave's clone or in the sandbox with blob comparison;
   both landed byte-exact.
5. Dave asked whether the PRD should describe the current product's defects
   or the desired end state. The repo's own texts answered: spec-first means
   "true at handoff and at rest," and the template calls the PRD "standing."
   §1 was rewritten to the end state; the dated measurements moved to a
   baseline block in §5.
6. Dave asked, of AC-BS-14 (dual-corpus invariant), "I don't remember
   deciding this." Search found no ruling; it was drafting inference from G2.
   He ruled a narrowed version after a pros/cons exchange.
7. Dave, on rule 3 (warn once): "the result of it has been that you warn me
   about something every time, even if it's ridiculously trivial." The rule
   was inverted — silence default, landmine exception with the ruled test,
   once as the ceiling.
8. Dave, on compression: "I bet … you could lose zero semantic value while
   still reducing word count by at least 30%. Do you think I'm wrong?" The
   pass reached 16% at the author's zero-loss bar; Dave then cut rule 12 to
   on-request and rule 13 to a third of its length, and the author found
   Core restatements in 13–17 the pass had missed. Final: 569 → 510 words
   carrying seven more rulings.
9. Dave, on forty hours of wall-clock for a change round: the author located
   the cost in serialization and relay, not per-step work, and pointed at
   three levers already in the record (batching, confirmation-scoped
   re-gates, DEC-000300's standing door).
10. Dave pasted the same executor report three times, then: "Shit I dunno
    I've lost track." The author checked `git ls-remote` for the expected
    branch rather than the clipboard, and the missing state resolved.
11. Dave's local main was 82 commits behind origin when he looked for
    `specs/bundle-system.md`; every executor report all day had shown
    `40a8914 [main]` without anyone reading it as a signal.
12. Cycle 14's DL-3 filed "baton-before-retro ordering survives nowhere" as
    an orphan; it was Dave's explicit cut an hour earlier, unlogged at review
    time. Cycle 1 of the PRD confirmed R-1 by running the tool against two
    trees rather than reciting counts.
13. The retro skill was agreed with a "chat closes → run the retro" trigger
    and rule-12 deference at 04:00; Dave cut rule 12's standing obligation
    at 21:00 the same day.
14. The M5 lint fails on any directive whose scope lists a slashless
    root-level path (`OPEN-ITEMS.md`); confirmed in both orderings;
    classified and riddered rather than worked around.
15. Dave, closing: "you don't have any state that can't be computed by a
    new CoS instantiated, do you?" The check found two riders living only in
    a pull-request body; the baton became three lines.

## Interpretation

- The stop machinery held under every failure this session produced (1, 2,
  3, 4): three classes of defect — wrong dictated path, invented SHA,
  retyped-content drift — and none reached a record. The methodology's
  value showed as caught defects, not absent ones.
- Human sweeps are the residual defect source (1, 2). Both path collisions
  came from the author's own listing; the second from a listing that looked
  like a sweep and wasn't. The fix class is mechanical: a path-exists check
  in the lint, and executors confirming path-absence before writing (now
  in every directive since).
- Retyping governed content through a connector is a drift channel (4).
  The reliable pattern is: produce the bytes with a governed tool, compare
  blobs after landing, and prefer running the tool where the clone is.
- Reading the repo's own texts settles methodology questions faster than
  reasoning from principle (5, 6). Both open questions resolved by citation
  to text already agreed; one turned out to be undecided drafting.
- Rules drift toward their title (7). "Warn once" made warning the default
  act; the fix was to make the title carry the intended default.
- Owner compression beat author compression (8). The author's zero-loss
  bar protected Core restatements the owner cut without loss; the
  overlap-with-Core lens found more once applied deliberately.
- Rulings not yet logged produce reviewer false-orphans (12, 13): the
  same-session logging rule (now Decision Layer rule 10) is the fix, and the
  retro-skill/rule-12 whiplash (13) is the cost of reviewing two coupled
  documents a day apart.
- Executor reports carry state nobody reads (11): the main-tree SHA appeared
  in every report; a stale clone hid for a day.
- The end-of-session check (15) was worth doing: the baton was not empty,
  and the two items would have been invisible to a successor.

## Standing preferences

- Ask one question at a time, y/n where possible — stated again today
  ("do you think I'm wrong?" pattern, agreement asks); previously ruled into
  Decision Layer rule 1 and the CoS role.
- Never warn about trivia; name only what changes his action — stated today
  (7); previously the 08-29 sharpening (T15) and DEC-era rule 3 history.
- Documents for reading arrive rendered in the pane — followed today for
  both reviews; previously ruled 08-31 (T26), now Decision Layer rule 11.
- Cut words: he prefers the terser text every time it is offered (8);
  previously the 08-29 "pithy" preference recorded in cycle-11 findings.
- Own decisions get logged the session they are made — asked for implicitly
  by 6 and 12; previously T24, now rule 10.

## Durable insights

- A dictated path is a claim; confirm absence at the base before dictating,
  and make the executor confirm again. Lexical sort is not a sweep.
- Precompute the target blob before any connector write of governed text;
  a landed blob that differs from the precomputed one is a defect, whatever
  the tool response says.
- Human review in the pane before the reviewer gate is a cost lever: the
  Decision Layer went human-review → gate → one blocking fix → confirmation
  re-gate → agreed inside one session; the PRD followed the same shape.
- When the owner cannot remember deciding a rule, search the log before
  defending the rule.
- Confirmation-scoped re-gates worked three times without a miss; the
  spec-review-cycle skill (queue item 3) should state the shape.

## Candidate methodology changes

- `bin/check-directive`: an M-rule that a dictated artifact path must not
  exist at the base ref (riddered to the TRD queue at the flush).
- `bin/check-directive` M5: exempt slashless root-level paths in Documents
  in scope (riddered).
- `skills/directive-authoring.md`: cycle-number derivation by full
  version-sorted listing plus executor path-absence confirmation (riddered).
- `policies/remote-write-verification-policy.md` (queue item 4): close the
  stated known gap with the precomputed-blob comparison as the content
  check; state that governed text is never retyped through a connector.
- `skills/conversation-retro.md`: conforming revision for rule 12
  on-request (queued, DEC-000310); consider the "routes" term (Lexicon
  reuse) in the same cycle.
- `roles/chief-of-staff.md` (queue item 7): read the main-tree SHA in every
  executor report and surface a stale clone once, by the landmine test.
- Retro trigger coupling: when two documents defer to each other (retro
  skill ↔ rule 12), review them in one session or sequence their cycles
  explicitly.
